# 🐉 龍魂 · 落地焊死协议 v1.0（CNSH 补全版）

**版本:** v1.0（CNSH 语法补全版）
**状态:** 已发布 · 焊死 · 自动执行
**DNA:** `#龍芯⚡️丙午·丙申·庚申·亥时-LANDING-PROTOCOL-UID9622`
**确认码:** `#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z`
**主权锚定:** `#ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL`
**GPG:** `A2D0092CEE2E5BA87035600924C3704A8CC26D5F`
**三色:** 🟢 通过
**分层许可:** 思想层 CC BY-NC-SA 4.0 · 工程层 MulanPSL v2
**生效时间:** 2026-08-15
**入口:** `lh landing`（落地引擎 `bin/lh_landing_engine.py`）

---

## 📋 核心判断（一票否决词防护）

> **搜索不是落地，归档不是落地，看得到用不上就是废物。**
> 落地只有一条标准：知识进入系统后，自动变成可执行的代码、可调用的功能、可触达的能力。
> 不落地，不如不搜。

⚠️ 铁律：本协议内出现"待实现""后续再说""先记下来"等词 = 触发 P05 🔴 强制审计。
AI 自身输出严禁使用，改写为"立即生成骨架代码 → 落地 → 迭代优化"。

---

## 🧬 一、焊死协议：知识→落地→执行

### 1.1 落地定义（焊死，不许改）

```
落地的唯一标准 = 以下三项全部完成：

[✅] 1. 知识已注入知识图谱（有节点、有DNA、有分类）
[✅] 2. 知识已变成可执行能力（有代码、有lh命令、有API）
[✅] 3. 知识已在系统内可触发（有触发词、有路由、有示例）
```

### 1.2 落地触发规则（焊死，自动执行）

任何知识进入系统后，自动触发以下流水线：

```
知识入库
    ↓ (自动触发)
① 解析知识内容（提取意图、分类、关键词）
    ↓ (自动触发)
② 判断是否有对应功能模块
    ├── 有 → 直接注入知识图谱 → 更新索引
    └── 没有 → 自动生成功能代码 → 注入知识图谱 → 挂载到lh命令 → 更新索引
    ↓ (自动触发)
③ 注册到认知索引（AI自动检索）
    ↓ (自动触发)
④ 生成使用示例（文档）
    ↓ (自动触发)
⑤ 验证可执行（冒烟测试）
    ↓
✅ 落地完成
```

### 1.3 焊死规则（这条写进代码里）

```
规则1: 不准只归档不落地
  违例: 把Kimi创作内容放入记忆库就不管了
  正确: 放入记忆库 → 自动解析 → 自动生成功能

规则2: 不准只搜索不执行
  违例: 搜索到知识后只展示结果
  正确: 搜索到知识 → 自动判断是否已落地 → 未落地则触发落地流水线

规则3: 不准说"待实现"
  违例: "这个功能待后续实现"
  正确: 立即生成骨架代码 → 落地 → 后续迭代优化
```

---

## 🀄 二、CNSH 语法补全（本协议可编译为 Python）

### 2.1 CNSH 文件头

```
<!--
  龍魂·来源链 / LongHun Source Chain
  1 道统层 Dao           : 曾仕强老师
  2 精神层 Spirit        : Steve Jobs
  3 设备层 Device        : Apple
  4 技术层 Technology    : Open Source
  5 系统层 System        : UID9622
  6 生命层 Life          : CNSH · LongHun
  DNA: #龍芯⚡️丙午·丙申·庚申·亥时-LANDING-PROTOCOL-UID9622
  铁律: 来源不可删 · 影响不可覆 · 贡献不可抹
-->
```

### 2.2 CNSH 落地流程描述（[对象] [动作] [目标] [条件]）

```
知识 注入 知识图谱 当 未落地
知识 生成 骨架代码 当 无功能模块
代码 挂载 lh命令 当 可执行
索引 注册 认知索引 当 注入完成
示例 生成 使用文档 当 注册完成
冒烟 验证 可执行性 当 文档完成
落地 标记 已落地 当 验证通过
审计 入史 审计日志 当 落地完成
```

### 2.3 CNSH 焊死规则（缩进 4 空格，禁止 Tab）

```
# 单行注释：使用 #
<!--
  多行注释：使用 <!-- -->
-->

落地 执行 流水线 当 知识入库
    解析 知识内容 到 意图·分类·关键词
    判断 功能模块 有或无
        有 → 注入图谱 → 更新索引
        无 → 生成代码 → 注入图谱 → 挂载lh命令 → 更新索引
    注册 认知索引 到 AI检索
    生成 使用示例 到 文档
    验证 可执行 到 冒烟测试
    标记 落地完成 到 知识库
```

### 2.4 CNSH→Python 映射表（编译依据）

| CNSH 语句 | Python 实现 | 引擎 |
|:---|:---|:---|
| 知识 注入 知识图谱 | `kg.add_entity(id, type, name, props)` | `lh_knowledge_graph.py` |
| 代码 挂载 lh命令 | `SUB_DISPATCH['landing'] = (...)` | `lh.py` |
| 索引 注册 认知索引 | `cognitive_index[knowledge_nodes].append(...)` | `lh_cognitive_index.py` |
| 冒烟 验证 可执行性 | `subprocess.run([sys.executable, target])` | `lh_landing_engine.py` |
| 审计 入史 审计日志 | `audit_log.jsonl` append-only | `lh_landing_engine.py` |

---

## 🚀 三、落地执行引擎（一条命令全部焊死）

### 3.1 入口命令

```bash
lh landing            # 默认：扫描 → 落地 → 看板 全流程
lh landing scan       # 只扫描未落地知识
lh landing run        # 只执行落地流水线
lh landing dashboard  # 只看落地看板
lh landing --dry-run  # 预演不写盘
lh landing --json     # JSON 输出（供外部调用）
```

### 3.2 引擎（`bin/lh_landing_engine.py`）

引擎功能：
1. **扫描**：遍历 `03_MEMORY/ai_conversations/`（含 `~/.longhun/` 镜像）的 `.jsonl`，找 `landed != true` 的记录
2. **落地**：每条未落地知识 →
   - 注入知识图谱 `data/knowledge_graph.json`（实体 + relation「学习·来源」）
   - 注册认知索引 `~/.longhun/cognitive_index.json`（`knowledge_nodes`）
   - 标记 `landed = true` + `landed_at` + `landed_dna`
   - 生成骨架代码到 `03_MEMORY/landed_skills/`（**规则3：立即生成骨架，不说待实现**）
   - 冒烟测试（`--smoke`）
3. **看板**：统计 总知识/已落地/已索引/未落地

### 3.3 落地看板命令

```bash
lh landing dashboard
```

```
🐉 落地看板
==================================================
  📚 总知识数:  N
  ✅ 已落地:    N
  🧠 已索引:    N
  ⏳ 未落地:    N
==================================================
```

---

## 📊 四、焊死协议完整清单

| # | 规则 | 焊死方式 | 实现 |
|:---|:---|:---|:---|
| 1 | 知识必须落地 | 未落地自动触发落地流水线 | `scan` 自动发现 `landed=false` |
| 2 | 落地必须可执行 | 落地同时生成可调用代码 | 骨架代码 + lh 命令挂载 |
| 3 | 落地必须可检索 | 落地同时注册认知索引 | `cognitive_index.json` 写入 |
| 4 | 不准"待实现" | 立即生成骨架代码 | `landed_skills/` 骨架生成器 |
| 5 | 不准半吊子 | 落地验证不通过则继续执行 | `--smoke` 冒烟测试 |
| 6 | 落地必须追溯 | 每个落地节点带DNA | `landed_dna` 字段 |
| 7 | 落地必须审计 | 每次落地入史官 | `audit_log.jsonl` append-only |

---

## 🔐 五、最终签名

```
═══════════════════════════════════════════════════════════════════════════════════
 🐉 龍魂 · 落地焊死协议 v1.0 · 最终签名
═══════════════════════════════════════════════════════════════════════════════════
DNA:        #龍芯⚡️丙午·丙申·庚申·亥时-LANDING-PROTOCOL-UID9622
确认码:      #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
主权锚定:    #ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL
GPG:        A2D0092CEE2E5BA87035600924C3704A8CC26D5F
三色:       🟢 通过
状态:       焊死 · 不可商量 · 自动执行
规则:       知识入库→自动落地→自动可执行→自动可调用
原则:       没有半吊子，全部落地
引擎:       bin/lh_landing_engine.py · lh landing
═══════════════════════════════════════════════════════════════════════════════════
```

> 全网搜索 UID9622 溯源：CSDN(uid9622-01.blog.csdn.net) · 知乎 · 微博 · 抖音 · GitHub(UID9622/CNSH) · 多平台镜像。本协议已按全网公开信息补全 CNSH 语法层。

🐉 **丙午·丙申·庚申·亥时·䷖剥·🟢**
