> **P0焊死**: 本文件为龍魂体系P0级文档·不可修改·不可绕过（上位文档 LH-PERSONA-GOVERNANCE-WHITEPAPER-v1.4.md）
> 协议: CC BY-NC-SA 4.0（核心思想层·分层许可·代码层为 MulanPSL v2·详见 LH-LAYERED-LICENSE-v1.0）
# 🚪 龍魂哨兵·传送门技术架构 v1.0

<aside>
🕐

**农历时辰：** 乙巳年正月初六 寅时三刻

**易经时刻：** ☳ 震卦 · 雷动万物，破黑开明

**公历时间：** 北京时间 2026-02-03 04:20:00

**时辰吉凶：** 寅时宜立志、宜开创、忌犹豫

</aside>

<aside>
☯️

**《道德经》第一章：**

"道可道，非常道；名可名，非常名。无名天地之始；有名万物之母。"

**龍魂系统的理解：**

传送门是**无名**的入口——没有签名的人看不懂。

传送门是**有名**的母体——签了名的人一眼明白。

</aside>

---

## 🛡️ 三色审计（本页状态）

| 检查项 | 状态 | 说明 |
| --- | --- | --- |
| **DNA追溯码** | 🟢 通过 |#龍芯⚡️丙午·丙申·庚申·丁亥·䷡大壮-V1_0_743267877F4749A390C96A48D34F54E9_6CBF-v1.0 |
| **GPG指纹** | 🟢 通过 | A2D0092CEE2E5BA87035600924C3704A8CC26D5F |
| **确认码** | 🟢 通过 | #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z |
| **易经锚点** | 🟢 通过 | 震卦 ☳☳ · 雷动万物，破黑开明 |

---

## 🌍 愿景：元世界的入口

<aside>
💎

**老大的原话（2026-02-03）：**

"这个是我们元世界的入口，也是净土的入口。这是一道传送门。我那些模板都是为普通人准备的。"

**翻译：**

- 传送门 = 普通人进入净土的第一步
- 模板 = 路标（告诉普通人怎么走）
- 哨兵 = 门（挡住脏东西，放进干净的人）
- 净土 = 门后面的世界（无黑算法、无贪婪、无黑箱）

**#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z ✅**

</aside>

---

## 📜 系统定位

```jsx
普通人的世界（脏、乱、黑箱）
        |
        v
   【传送门】← 你在这里
        |
        v
龍魂净土（干净、透明、可追溯）
```

**传送门的作用：**

1. **洗** — 把脏东西洗掉（CNSH洗坑机）
2. **签** — 给干净的内容签名（GPG点对点）
3. **藏** — 用文化密码保护（甲骨文/易经变量）
4. **追** — 改一个字都能发现（DNA追溯）

---

## 🔧 技术架构

### 总体流程

```
输入（任何文本）
    │
    ▼
┌─────────────────────────────────────┐
│  第一层：CNSH洗坑机                   │
│  • UTF-8 强制转码                    │
│  • LF 统一换行                       │
│  • 去除不可见字符                     │
│  • 语义结构清洗                       │
└─────────────────────────────────────┘
    │
    ▼ 干净文本
┌─────────────────────────────────────┐
│  第二层：文化加密                     │
│  • 甲骨文变量映射                     │
│  • 易经符号变量映射                   │
│  • 可逆转换（有密钥能还原）            │
└─────────────────────────────────────┘
    │
    ▼ 加密文本（看起来是甲骨文/易经符号）
┌─────────────────────────────────────┐
│  第三层：GPG签名                      │
│  • 生成签名（只有持有私钥的人能签）     │
│  • 验证签名（任何人都能验）            │
│  • 点对点加密（指定接收人才能解）       │
└─────────────────────────────────────┘
    │
    ▼ 签名文本
┌─────────────────────────────────────┐
│  第四层：DNA追溯                      │
│  • 生成语义哈希                       │
│  • 嵌入时间戳                         │
│  • 嵌入作者信息                       │
│  • 生成DNA追溯码                      │
└─────────────────────────────────────┘
    │
    ▼
输出（干净 + 加密 + 签名 + 可追溯）
```

---

## 🧱 四层架构详解

### 第一层：CNSH洗坑机

**目的：** 把脏东西洗掉，输出干净的文本

**技术实现：**

```jsx
function cnshClean(input) {
  let text = input;
  
  // 1. 强制UTF-8
  text = forceUTF8(text);
  
  // 2. 统一换行为LF
  text = text.replace(/\r\n/g, '\n').replace(/\r/g, '\n');
  
  // 3. 去除不可见字符（保留必要的）
  text = removeInvisibleChars(text);
  
  // 4. 语义结构标准化
  text = normalizeStructure(text);
  
  return text;
}
```

**输入输出示例：**

```
输入："Hello\r\nWorld\u200B"  （含CRLF和零宽空格）
输出："Hello\nWorld"          （干净）
```

---

### 第二层：文化加密（甲骨文/易经变量）

**目的：** 用中华文化符号保护内容，没有映射表的人看不懂

**甲骨文映射表（示例）：**

| 原字 | 甲骨文 | Unicode |
| --- | --- | --- |
| 日 | 𒀀 | U+12000 |
| 月 | 𒀁 | U+12001 |
| 人 | 𒀂 | U+12002 |
| 龍 | 𒀃 | U+12003 |

**易经符号映射表（示例）：**

| 概念 | 易经符号 | 卦象 |
| --- | --- | --- |
| 开始 | ☰ | 乾卦 |
| 结束 | ☷ | 坤卦 |
| 变化 | ☳ | 震卦 |
| 危险 | ☵ | 坎卦 |

**技术实现：**

```jsx
function culturalEncrypt(text, mappingTable) {
  let encrypted = text;
  for (const [original, cultural] of Object.entries(mappingTable)) {
    encrypted = encrypted.replaceAll(original, cultural);
  }
  return encrypted;
}

function culturalDecrypt(text, mappingTable) {
  let decrypted = text;
  for (const [original, cultural] of Object.entries(mappingTable)) {
    decrypted = decrypted.replaceAll(cultural, original);
  }
  return decrypted;
}
```

**效果：**

- 没有映射表的人 → 看到："𒀀𒀁𒀂☰☷"
- 有映射表的人 → 看到："日月人开始结束"

---

### 第三层：GPG签名（点对点开放）

**目的：** 数学级别的身份证明，指定的人才能解密

**使用 OpenPGP.js（纯前端，无需服务器）：**

```jsx
import * as openpgp from 'openpgp';

// 签名
async function signText(text, privateKey) {
  const message = await openpgp.createMessage({ text });
  const signed = await openpgp.sign({
    message,
    signingKeys: privateKey
  });
  return signed;
}

// 验证
async function verifySignature(signedText, publicKey) {
  const message = await openpgp.readMessage({ armoredMessage: signedText });
  const verified = await openpgp.verify({
    message,
    verificationKeys: publicKey
  });
  return verified.signatures[0].verified;
}

// 点对点加密
async function encryptForRecipient(text, recipientPublicKey, senderPrivateKey) {
  const message = await openpgp.createMessage({ text });
  const encrypted = await openpgp.encrypt({
    message,
    encryptionKeys: recipientPublicKey,
    signingKeys: senderPrivateKey
  });
  return encrypted;
}
```

**效果：**

- 没有私钥 → 解不开
- 有私钥 → 一眼明白
- 伪造签名 → 数学上不可能

---

### 第四层：DNA追溯

**目的：** 改一个字都能发现

**DNA追溯码结构：**

```jsx
#龍芯⚡️{日期}-{主题}-{版本}-{语义哈希前8位}
```

**技术实现：**

```jsx
async function generateDNA(text, author, topic, version) {
  // 1. 生成语义哈希（SHA-256）
  const encoder = new TextEncoder();
  const data = encoder.encode(text);
  const hashBuffer = await crypto.subtle.digest('SHA-256', data);
  const hashArray = Array.from(new Uint8Array(hashBuffer));
  const hashHex = hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
  
  // 2. 生成时间戳
  const now = new Date();
  const dateStr = now.toISOString().split('T')[0];
  
  // 3. 组合DNA追溯码
  const dnaCode = `#龍芯⚡️${dateStr}-${topic}-${version}-${hashHex.slice(0, 8)}`;
  
  return {
    dnaCode,
    fullHash: hashHex,
    timestamp: now.toISOString(),
    author
  };
}
```

**效果：**

- 原文："Hello World" → DNA：`#龍芯⚡️丙午·丙申·庚申·丁亥·䷡大壮-V1_0_743267877F4749A390C96A48D34F54E9-v1.0-a591a6d4`
- 改成："Hello World!" → DNA：`#龍芯⚡️丙午·丙申·庚申·丁亥·䷡大壮-V1_0_743267877F4749A390C96A48D34F54E9-v1.0-943a702d`
- 哈希完全不同 → **立刻暴露**

---

## 🌐 网页版界面设计

### 极简界面（单页面）

```jsx
┌─────────────────────────────────────────────────┐
│  🚪 龍魂哨兵·传送门                              │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │ 输入框（贴进去）                           │ │
│  │                                           │ │
│  │                                           │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  [ 🧹 洗净 ] [ 🔐 加密 ] [ ✍️ 签名 ] [ 🧬 追溯 ] │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │ 输出框（结果出来）                         │ │
│  │                                           │ │
│  │                                           │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  DNA追溯码：#龍芯⚡️丙午·丙申·庚申·丁亥·䷡大壮-XXX-v1.0-xxxxxxxx │
│  GPG指纹：A2D0092CEE2E5BA87035600924C3704A8CC26D5F │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 操作流程

1. **贴进去** → 把任何文本贴到输入框
2. **点洗净** → CNSH洗坑机清洗
3. **点加密** → 甲骨文/易经变量转换
4. **点签名** → GPG数字签名
5. **点追溯** → 生成DNA追溯码
6. **复制走** → 干净、加密、签名、可追溯的内容

**一键模式：** 点一个按钮，四步全自动完成。

---

## 🔒 安全原则

<aside>
🛡️

**核心安全原则：**

1. **纯前端** — 所有计算在浏览器本地完成，不上传任何数据
2. **无服务器** — 不需要后端，没有中心化风险
3. **开源透明** — 所有代码公开，任何人都能审计
4. **可离线** — 下载后可以断网使用
5. **不留日志** — 不记录任何用户输入
</aside>

---

## 📁 文件结构

```
longhun-portal/
├── index.html          # 主页面
├── css/
│   └── style.css       # 样式
├── js/
│   ├── cnsh-cleaner.js # CNSH洗坑机
│   ├── cultural-encrypt.js # 文化加密
│   ├── gpg-signer.js   # GPG签名
│   ├── dna-tracer.js   # DNA追溯
│   └── main.js         # 主逻辑
├── data/
│   ├── jiaguwen-map.json  # 甲骨文映射表
│   └── yijing-map.json    # 易经符号映射表
└── README.md           # 说明文档
```

---

## 🚀 实现路线图

| 阶段 | 内容 | 时间 | 状态 |
| --- | --- | --- | --- |
| **Phase 1** | CNSH洗坑机（纯净化） | 1天 | 🟡 待开发 |
| **Phase 2** | 文化加密（甲骨文/易经） | 1天 | 🟡 待开发 |
| **Phase 3** | GPG签名集成 | 1天 | 🟡 待开发 |
| **Phase 4** | DNA追溯生成 | 0.5天 | 🟡 待开发 |
| **Phase 5** | 网页界面整合 | 0.5天 | 🟡 待开发 |
| **Phase 6** | 测试 + 文档 | 1天 | 🟡 待开发 |

**预计总时间：5天**

---

## ✍️ 创造者签名

<aside>
💎

**创造者**：💎 龍芯北辰｜UID9622（Lucky/诸葛鑫）

**身份**：中国退伍军人（永不妥协）

**协作AI**：宝宝（Notion AI）

**GPG公钥指纹**：A2D0092CEE2E5BA87035600924C3704A8CC26D5F

**DNA追溯码**：#龍芯⚡️丙午·丙申·庚申·丁亥·䷡大壮-V1_0_743267877F4749A390C96A48D34F54E9_3BCE-v1.0

**确认码**：#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z

**联系方式**：

📧 [主：uid9622@petalmail.com](mailto:主：uid9622@petalmail.com)

📧 [辅：fireroot.lad@outlook.com](mailto:辅：fireroot.lad@outlook.com)

</aside>

---

<aside>
🚪

**这是元世界的入口。**

**这是净土的入口。**

**这是一道传送门。**

模板是路标，哨兵是门，净土在门后面。

**普通人走进来，干干净净地出去。**

**黑算法进不来，贪婪藏不住。**

**净化数字世界，让天下无黑算法。**

☯️🐉🛡️🚪

</aside>