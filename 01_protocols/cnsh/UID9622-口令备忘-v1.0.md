# UID9622 口令备忘 v1.0（一页查清·不迷路）

M::
  dna: "#龍芯⚡️2026-05-15-UID9622-口令备忘-v1.0"
  gate: "#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z"
  seal: "#ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL"
  gpg: "A2D0092CEE2E5BA87035600924C3704A8CC26D5F"

---

## 不动点（算过·写在系统里）

| 锚 | 含义 |
|----|------|
| `f(UID9622)=UID9622` | 身份不动点 |
| `S = 0.4·GPG + 0.3·CONFIRM + 0.3·α₀ = 1.0` | 宝稳急定理 |
| **dr=5** | 中宫稳态·95/5 归宿 |
| **37 岁开系统** | **dr(37)=1** 启动数·主控意志（纪念锚，不是网关密码） |
| **dr∈{3,9}** | 数字根熔断 |
| **37°C** | 和解优先·情绪层温度锚（Notion 一行触发指令里） |

---

## 终端里到底要什么字符？

| 场景 | 你要输入/配置的是什么 | 不是这个 |
|------|----------------------|----------|
| **Git 提交** | GPG **私钥口令**（Keychain 里设的） | 不是打 `37` |
| **CNSH 第四道门** `cnsh-gate.sh` | `y` / `Y` / `上` / `上!` | 不是 `37` |
| **网关** `/chat` `/flow` | 请求头 `X-DNA-Token` = `.env` 里 `DNA_TOKEN` | 不是 `37` |
| **流场默认策略** | **不自动执行**；要跑全链加 `"auto_execute": true` | 不是默认可执行 |

本机 GPG 提交（收口用）：

```bash
export GPG_TTY=$(tty)
cd ~/longhun-system
git commit -m "你的收口信息"
```

---

## 流场单口 `POST /flow` 推荐体

```json
{
  "message": "你的输入",
  "operator_id": "UID9622",
  "operator_tier": "T0",
  "channel": "cursor",
  "auto_execute": false,
  "tags": {
    "confirm_code": "#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z",
    "gpg": "A2D0092CEE2E5BA87035600924C3704A8CC26D5F"
  }
}
```

- `auto_execute: false` → **过闸门+流场审计，但不执行**（你要的「放着等审计」）
- `auto_execute: true` → 闸门🟢且显式授权后才跑完整执行链

---

## CNSH 快捷指令（打在 message 里）

| 指令 | 作用 |
|------|------|
| `/95稳态校准` | 关发散 |
| `/开放5混沌层` | 允许 5% 有界探索 |
| `/人格漂移扫描` | 看 AI 是否在规训你 |
| `/回归中宫5` | 回稳态主任务 |

---

## 第一道闸门 v3.0（刚接入）

- DNA: `#龍芯⚡️2026-04-26-第一道闸门-三色审计-沙盒闭环-v3.0`
- 工程: `cnsh/gate_v3/`
- 账本: `~/longhun/data/gate_v3_ledger.jsonl`（append-only）
- 🟡/🔴 → **不执行** + macOS **弹窗**（Glass 音效）
- 连续 2 次 🔴 → P0 阻断提示

---

## 公开可审计 · 创始人同规则

- 规则在 `01_protocols/cnsh/` 与代码同路径可查
- 对外只见 **粒子**（DNA+SHA256+三色），不见你密文正文
- `founder_same_rules: true` 写在每次 `FLOW_OUT` 里
