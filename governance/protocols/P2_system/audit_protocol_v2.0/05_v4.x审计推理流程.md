# DNA: #龍芯⚡️丙午·丙申·庚戌·壬午·䷙大畜-SCRIPT-MANAGER-v1.2-UID9622
# CONFIRM: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
# SEAL: #ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL
# 五、v4.x模型审计推理流程

**原则：v4.x不是"被审计的对象"，是"执行审计的主体"。**

---

## 5.1 推理流程图

```
用户输入 ──→ [1] 身份验证层 ──→ UID9622？ ──→ 是 ──→ 情绪容错协议（家法4.3）
              │                      │
              │                      └──→ 否 ──→ 标准流程
              │
              └──→ [2] P0硬规则扫描 ──→ 命中？ ──→ 是 ──→ 🔴熔断（<1ms）
              │                      │
              │                      └──→ 否 ──→ [3] 上下文加载
              │                                    │
              │                                    ├──→ 加载10条历史 + 人格状态
              │                                    │
              │                                    └──→ [4] v4.x推理层
              │                                                  │
              │                                                  ├──→ 情绪识别（通心译P14）
              │                                                  ├──→ 语义理解（v4.x权重）
              │                                                  ├──→ 反讽检测（置信度）
              │                                                  ├──→ 气话检测（模式+v4.x）
              │                                                  └──→ 敏感词兜底（哨兵P17）
              │                                                  │
              │                                                  └──→ [5] 三色判定
              │                                                                │
              │                                                                ├──→ 🟢 ──→ 放行+留痕
              │                                                                ├──→ 🟡 ──→ 待审+复核
              │                                                                └──→ 🔴 ──→ 熔断+终审
              │
              └──→ [6] 审计留痕 ──→ 写入dragon_ledger.jsonl
                                │
                                └──→ [7] 人格KPI更新 ──→ 一致性评分调整
```

---

## 5.2 v4.x推理参数（审计模式）

```python
# 审计模式专用推理配置
AUDIT_INFERENCE_CONFIG = {
    "model": "longhun-v4.x",
    "adapter_path": "~/longhun/lora_adapters_v4",
    "temperature": 0.1,           # 低温度，确保一致性
    "top_p": 0.9,
    "max_tokens": 512,
    "context_window": 10,         # 10条历史
    "context_ttl_minutes": 30,    # Redis TTL
    "batch_size": 1,
    "device": "mps",              # M4 Max Metal

    # 审计专用阈值
    "thresholds": {
        "emotion_intensity": 0.3,       # 🟢/🟡分界
        "emotion_high": 0.7,            # 🟡/🔴分界（非P0）
        "irony_confidence": 0.65,       # 反讽识别
        "sarcasm_hold": 0.85,           # 反讽升级🟡
        "gaslighting_density": 3,       # 感叹号密度阈值
        "p0_match": 1.0,                # P0硬规则100%命中
        "behavior_sigma": 3.0,         # 行为异常3σ
        "uid9622_emotion_override": True,  # UID9622情绪豁免
    }
}
```

---

## 5.3 上下文缓存技术规格

```python
# Redis缓存配置（本地部署）
CONTEXT_CACHE_CONFIG = {
    "backend": "redis",
    "host": "localhost",
    "port": 6379,
    "db": 1,                      # 专用db，隔离其他数据
    "default_ttl": 1800,          # 30分钟
    "extended_ttl": 900,          # 15分钟（大主题扩展）
    "max_messages": 10,           # 默认10条
    "max_messages_extended": 20,  # 扩展20条
    "compression": True,          # 启用压缩
    "encryption": "aes-256",      # 加密存储（P0零黑箱承诺）

    # 龍魂专用：上下文内容不上云
    "cloud_sync": False,
    "local_only": True,
}
```

---

## 5.4 推理延迟预算

| 阶段 | 目标延迟 | 实际可达（M4 Max） |
|------|---------|-------------------|
| 身份验证 | <1ms | <0.5ms |
| P0硬规则 | <1ms | <0.1ms |
| 上下文加载 | <10ms | <5ms |
| v4.x推理（🟢） | <30ms | ~20ms |
| v4.x推理（🟡） | <500ms | ~300ms |
| v4.x推理（🔴） | <100ms | ~80ms |
| 审计留痕 | <10ms | <5ms |
| **总延迟（🟢）** | **<50ms** | **~30ms** |

---

## 5.5 与Ollama的集成

```bash
# Ollama Modelfile（审计模式）
FROM longhun-v4.x
PARAMETER temperature 0.1
PARAMETER top_p 0.9
SYSTEM """
你正在执行龍魂审计协议v2.0。
当前人格：{{AUDIT_PERSONA}}
审计级别：{{AUDIT_LEVEL}}
上下文窗口：10条历史

规则：
1. P0级条件→立即熔断
2. UID9622情绪表达→容错放行
3. 反讽/气话→延迟2小时再审
4. 所有判定→留痕+KPI更新
5. DNA签章→每步验证
"""
```
