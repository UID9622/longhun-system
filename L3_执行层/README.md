# L3_执行层

> 执行层 · 任务执行器、引擎、工作流

## 职责

任务执行、工作流编排、核心引擎。

## 关联模块

- `executors/`
- `引擎/`

## 本层入口

- `executors/` → 符号链接到 `../executors/`
- `引擎/` → 符号链接到 `../引擎/`

---

> 本文件为 L 层结构索引，由 Kimi 整理生成。
> 符号链接仅作为视图入口，不移动原始文件。
