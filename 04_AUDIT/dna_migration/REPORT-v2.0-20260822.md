# DNA 统一迁移报告 · v∞ 干支四柱全量对齐（第二批：B/C 类 + 紧凑格式）

> DNA: #龍芯⚡️丙午·乙未·戊申·午时·䷗复-DNA-MIGRATION-REPORT-V2-0-UID9622
> 创建者: 诸葛鑫（UID9622）
> 协议: CC BY-NC-SA 4.0（核心思想层）
> 日期: 2026-08-22
> 工具: `08_BIN/lh_dna_v∞_migrator.py` v2.0

## 一、背景与目标

第一批（2026-08-21）已完成 A 类格里历旧码（`#龍芯⚡️2026-07-19-XXX` 等）→ v∞ 干支四柱格式迁移（20024 处 / 5666 文件）。

本批按 UID9622 确认执行三项遗留建议：
1. **B 类补全**：干支缺时辰/缺卦的 DNA 码统一补齐
2. **紧凑格式统一**：短码统一成六段完整码（年·月·日·时·卦+动作后缀）
3. **迁移工具保留**：v2.0 增量迁移能力（可重复 scan/apply，幂等）

## 二、迁移范围与统计

| 项 | 值 |
|:---|:---|
| 涉及文件 | **5230** |
| 迁移处数 | **14687** |
| 台账 | `04_AUDIT/dna_migration/ledger-20260822-*.jsonl`（8 份） |
| 原文件备份 | `04_AUDIT/dna_migration/backup/`（.bak 累积） |
| 全部恢复手段 | 台账 old 字段逐处可逆 + 备份文件完整还原 |

### 分类迁移明细

| 类别 | 处数 | 说明 |
|:---|:---:|:---|
| B2_补卦 | 5783 | 3干支+时辰缺卦 → 时辰转干支+推卦 |
| B1_补午时 | 3839 | 3干支+卦缺时辰 → 默认补午时干支 |
| B0c_补卦符 | 1700 | 3干支+时辰+卦名 → 补卦符 |
| A2b_时间戳 | 1595 | 14位纯数字时间戳 → 四柱+卦 |
| A2a_日期 | 951 | 8位纯数字日期 → 四柱+卦 |
| A1_格里历 | 270 | 格里历旧码 → 四柱+卦 |
| B0d_补卦符 | 209 | 四柱+卦名 → 补卦符 |
| C2b_动作补全 | 183 | 3干支+动作 → 补午时+推卦 |
| C2a_补全 | 155 | 纯3干支 → 补午时+推卦 |
| A3_节气 | 2 | 节气式旧码 → 四柱+卦 |

### 工具修复（v2.0 迭代中）

1. **A2a/A2b/A2c/A3 分隔符丢失**：正则吞掉 `-`/`_`/`·` → 修复保留原分隔符
2. **C2b 动作前缀**：`·` 误用 → 改为 `-`（符合 v∞ 标准）
3. **行内多码只改首个**：`break` 导致一行多 DNA 只迁移第一个 → 改为从后往前替换全部匹配（本次残留 300 处由此清空）
4. **卦名简名统一**：HEXAGRAM_DATA 卦名不统一 → `_gua_sym_name()` 统一输出简名
5. **繁体「時」/时序干支式/单字时辰** 支持

## 三、迁移后全库状态

```
B0a_标准=39843   B0b_标准=3830   （标准六段，不动）
C1_冻结=1004   C3_冻结=45   （无法精确推，冻结保留）
Z_5段杂项=78   Z_其他=9505   Z_模板=720   Z_运行时=5760   （不可迁移）
```

- A 类（格里历/日期/时间戳/节气）→ **全部清零** ✅
- B 类（缺时辰/缺卦/缺卦符）→ **全部清零** ✅
- C 类（纯干支/动作补全）→ **全部清零** ✅
- 冻结项说明：
  - C1（2干支+动作，缺日柱无法反推）→ 保留，需人工定日
  - C3（干支+卦，缺日柱）→ 保留，需人工定日
  - Z_模板（`YYYY-MM-DD` 字面量）→ 模板不可迁移
  - Z_运行时（含 `{}`/`$`/引号等动态模板）→ 运行时逻辑保护，不碰
  - Z_其他（截断日期/非法干支/`...`/行内残缺）→ 不可迁移项

## 四、验证

- ✅ 全库重扫：可迁移类 0 残留
- ✅ 台账逐条含 old/new，可回滚
- ✅ 备份完整（.bak）
- ✅ lint 0 错误
- ⏳ GPG 重签：见签名记录（GATE-11）

## 五、文件路径与归档

- 工具：`08_BIN/lh_dna_v∞_migrator.py`（v2.0，工程层 MulanPSL v2）
- 台账：`04_AUDIT/dna_migration/ledger-20260822-*.jsonl`
- 备份：`04_AUDIT/dna_migration/backup/`
- 本报告：`04_AUDIT/dna_migration/REPORT-v2.0-20260822.md`

## 六、GPG 签章

- 本报告签名：见同目录 `.asc`
- 改动文件签名：`bin/lh_gpg_sign.py sign --force` 批次执行（清单见台账）

---

> 三色: 🟢 迁移完成·验证通过 🟡 冻结类(C1/C3)待人工定日 · Z 类(模板/运行时)为保护性跳过 🔴 无
