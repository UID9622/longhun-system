#!/bin/bash
# 🐉 龍魂指挥台入口
# DNA: #龍芯⚡️2026-06-24-LONGHUN-TERMINAL-v1.0

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LH_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

exec python3 "$LH_ROOT/bin/longhun-terminal.py" "$@"
