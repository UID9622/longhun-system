#!/bin/bash
# lh — 龍魂生态系统命令行入口
# 放在 PATH 中即可全局使用
# 用法: lh        → 打开交互菜单
#       lh 1      → 直接执行菜单项1
#       lh ask "问题"

LH_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec python3 "$LH_ROOT/bin/lh.py" "$@"
