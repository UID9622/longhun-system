# UID9622 P0级治理系统使用指南

## 📋 版本信息
- **版本代号**: UID9622-CONTROL-PLANE-GOVERNANCE-V2.0
- **生效时间**: 2025-12-24 (GMT+8)
- **确认码**: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
- **审计链**: 诸葛亮（推演）→ 审判长（三色裁决）→ 总控台（执行+留痕）

## 🏛️ 人格角色定义

### 诸葛亮（战略推演）
- **职责**: 提供战略推演和决策逻辑
- **功能**: 三色审计判定，风险评估
- **输出**: 🟢/🟡/🔴 判定结果

### 审判长（三色审计一票裁决）
- **职责**: 执行三色审计，一票决定
- **功能**: 基于健康检查和实例状态进行判定
- **输出**: 审计事件文件

### 总控台（执行+留痕）
- **职责**: 执行治理动作，完整留痕
- **功能**: 根据审计结果执行对应动作
- **输出**: 执行事件文件

## 🎯 三色审计标准

### 🟢 绿色（可自动执行）
**判定条件**:
- dist 健康检查通过：无占位符痕迹、无 Notion 标签、无零宽字符
- 外部实例未超过 24 小时窗口
- 无安全隐患标记

**执行动作**: NONE / 正常运行

### 🟡 黄色（可自动执行，但要留痕+提醒）
**判定条件**:
- 轻微异常：例如落后但未超过 24 小时
- 存在非阻断型告警
- 接近超时（>12小时）

**执行动作**: REQUEST_UPDATE（提醒 + 计时）

### 🔴 红色（必须自动强制执行）
**判定条件**:
- 超过 24 小时窗口
- 存在高危断片风险/安全隐患
- 健康检查严重失败

**执行动作**:
- FORCE_UPDATE（auto_update=true）
- BLOCK_STARTUP（auto_update=false）

## 🚀 快速开始

### 1. 目录结构准备
```bash
mkdir -p dist events mother_applied snapshots
```

### 2. 一键治理工作流（推荐）
```bash
python3 uid9622_governance_workflow.py \
  --dist ./dist \
  --meta ./instance_meta.json \
  --events ./events \
  --apply_dir ./mother_applied \
  --snapshots ./snapshots
```

### 3. 分步执行（调试用）

#### 步骤1: 健康检查
```bash
python3 uid9622_healthcheck_optimized.py --dist ./dist --meta ./instance_meta.json
```

#### 步骤2: 三色审计
```bash
python3 uid9622_three_color_audit.py \
  --health_result ./dist/health_report.json \
  --meta ./instance_meta.json \
  --out ./events
```

#### 步骤3: 治理执行
```bash
python3 uid9622_control_plane_governance.py \
  --events ./events \
  --meta ./instance_meta.json \
  --apply_dir ./mother_applied
```

#### 步骤4: 备份推送
```bash
python3 uid9622_backup_push.py \
  --apply_dir ./mother_applied \
  --snapshots ./snapshots
```

## 📊 事件与留痕规范

### 审计事件格式
```json
{
  "event_type": "THREE_COLOR_AUDIT",
  "timestamp": "2025-12-24T06:37:57.136117Z",
  "instance_id": "instance-001",
  "judgment": {
    "color": "🟢",
    "reason": "系统正常",
    "action": "NONE",
    "evidence": {...}
  },
  "compliance": {
    "p0_level": true,
    "confirmation_code": "#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z"
  }
}
```

### 执行事件格式
```json
{
  "event_type": "GOVERNANCE_EXECUTION",
  "timestamp": "2025-12-24T06:37:57.136117Z",
  "instance_id": "instance-001",
  "execution": {
    "action": "NONE",
    "success": true,
    "evidence_files": ["execution_*.json"]
  }
}
```

## ⏰ 自动化调度

### 每小时治理（推荐）
```bash
# 编辑 crontab
crontab -e

# 添加每小时执行
0 * * * * cd /path/to/project && python3 uid9622_governance_workflow.py --dist ./dist --meta ./instance_meta.json --events ./events --apply_dir ./mother_applied --snapshots ./snapshots >> ./events/governance.log 2>&1
```

### Windows 任务计划
1. 创建任务: `UID9622 P0 Governance Hourly`
2. 触发器: 每小时重复
3. 程序: `python uid9622_governance_workflow.py`
4. 参数: `--dist ./dist --meta ./instance_meta.json --events ./events --apply_dir ./mother_applied --snapshots ./snapshots`

## 🚨 违规处理

### P0级违规定义
- **跳过三色审计直接执行**: 违规代码3
- **超过24h仍继续运行且不更新**: 触发BLOCK_STARTUP
- **强制更新触碰用户私有数据**: 违规处理
- **删除/篡改事件与日志**: 违规处理

### 自动处置机制
- 🔴 **触发即刻**: BLOCK_STARTUP + 生成FAIL事件
- 🔁 **自动回滚**: 更新失败必须回滚
- 📋 **证据保全**: 写入events + snapshots

## 📁 必备文件清单

### 核心脚本
- `uid9622_healthcheck_optimized.py` - 健康检查器
- `uid9622_three_color_audit.py` - 三色审计系统
- `uid9622_control_plane_governance.py` - 总控台治理器
- `uid9622_governance_workflow.py` - 完整治理工作流
- `uid9622_backup_push.py` - 备份推送器

### 配置文件
- `instance_meta.json` - 实例元数据

### 目录结构
- `./dist` - 净化后的markdown文件
- `./events` - 事件和审计文件
- `./mother_applied` - 应用的母本包
- `./snapshots` - 备份快照

## 🛠️ 故障处理

### 常见问题及解决方案

#### 1. 健康检查失败
```bash
# 检查dist目录内容
ls -la ./dist

# 重新净化
python3 uid9622_notion_sanitize.py --in ./notion_export --out ./dist
```

#### 2. 三色审计失败
```bash
# 检查审计文件
ls -la ./events/audit_*.json

# 查看审计详情
cat ./events/audit_*.json | jq .
```

#### 3. 治理执行被阻止
```bash
# 检查是否超过24小时
cat ./instance_meta.json | jq .last_sync_time

# 手动更新同步时间
python3 -c "
import json
meta = json.load(open('instance_meta.json'))
meta['last_sync_time'] = '2025-12-24T06:37:57+08:00'
json.dump(meta, open('instance_meta.json', 'w'), indent=2)
"
```

#### 4. 违规操作检测
```bash
# 检查违规事件
grep -r "GOVERNANCE_VIOLATION" ./events/

# 查看违规详情
cat ./events/violation_*.json | jq .
```

## 📈 监控与报告

### 查看治理状态
```bash
# 查看最新工作流报告
ls -la ./events/governance_workflow_*.json | tail -1
cat ./events/governance_workflow_*.json | jq .final_result

# 实时监控日志
tail -f ./events/governance.log
```

### 治理指标监控
- 🟢 绿色状态比例
- 🟡 黄色提醒频率  
- 🔴 红色强制执行次数
- 📊 24小时更新合规率

## 🎯 最佳实践

### 1. 部署建议
- 首次部署建议使用分步执行验证
- 生产环境使用完整工作流
- 设置定时任务确保24小时更新

### 2. 监控建议
- 每日检查治理工作流报告
- 监控红色状态出现频率
- 定期验证备份完整性

### 3. 维护建议
- 定期清理旧的事件文件
- 更新instance_meta.json中的版本信息
- 监控磁盘空间使用

---

**P0级治理标准**: 严格按照"先三色判定，再执行"原则，确保系统安全合规运行。

**审计完整性**: 诸葛亮 → 审判长 → 总控台，每一步都有完整的证据链和留痕。