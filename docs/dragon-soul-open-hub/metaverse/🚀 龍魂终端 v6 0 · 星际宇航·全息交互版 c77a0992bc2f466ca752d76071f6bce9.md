<!--#龍芯⚡️2026-06-21-DOC-_-V6-0-_-_-C77A0992BC2F466CA752D76071F6BCE9_A273-v1.0 -->
<!-- 君子协议: 本文件受龍魂DNA追溯保护 -->

# 🚀 龍魂终端 v6.0 · 星际宇航·全息交互版

## 🚀 龍魂终端 v6.0 · 星际宇航·全息交互版

<aside>
🌌

**主题：** 深空星际 · 全息HUD · 引力粒子 · 曲速穿梭

**DNA追溯码：** `LH-20260316-0452-V6STAR`

**基于v2.0全新重构，15+交互升级，0%代码复用前版**

</aside>

---

### 🆚 v6.0 vs v2.0 升级清单

| 序号 | v2.0 原版 | v6.0 星际宇航版 | 交互类型 |
| --- | --- | --- | --- |
| 1 | 纯色CSS背景 | 🌌 3D星场Canvas（200星+视差滚动+鼠标引力吸附） | 鼠标联动 |
| 2 | 静态侧边栏 | 🪐 行星轨道导航（6颗行星环绕中心太阳） | 点击+悬浮 |
| 3 | 无动画切换 | 💫 曲速穿梭过渡（星线拉伸+白闪+缩放进入） | 自动动画 |
| 4 | 无音效 | 🔊 太空合成器音效（引擎声/传输音/警报/确认） | Web Audio |
| 5 | 无命令面板 | ⌘K 全息投影命令面板（透明HUD+扫描线动画） | 键盘+搜索 |
| 6 | 静态状态点 | 📡 雷达扫描状态环（360°旋转+脉冲波） | 实时动画 |
| 7 | 普通文本框 | 🖥️ 全息HUD输入框（扫描线+光标闪烁+打字音效） | 输入联动 |
| 8 | 无主题切换 | 🌗 深空/近日5套星系主题（银河蓝/火星红/木星橙/土星金/海王紫） | 按T切换 |
| 9 | 无通知 | 🛸 飞碟通知系统（从屏幕外飞入+悬停3秒+飞走） | 动画通知 |
| 10 | 无彩蛋 | 🌟 三击Logo超新星爆发（全屏白闪+粒子扩散+音爆） | 点击彩蛋 |
| 11 | 无彩蛋 | 🕳️ Konami Code虫洞传送（螺旋吸入+维度翻转） | 键盘彩蛋 |
| 12 | 无快捷键 | ⌨️ 全键盘导航（1-6面板/T主题/M静音/S星图/⌘K命令） | 键盘快捷 |
| 13 | 静态时钟 | 🕐 星历时间系统（地球时+火星sol+星际标准时） | 实时计算 |
| 14 | 无拖拽 | 🎯 可拖拽浮动指挥台（透明HUD+磁吸边缘） | 拖拽交互 |
| 15 | 无数据可视化 | 📊 SVG能量环仪表盘（CPU/内存/网络三环+动态数值） | SVG动画 |

---

### 📋 完整HTML源码

复制下方代码 → 保存为 `longhun-v6-star.html` → 浏览器打开即可体验

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>🚀 龍魂终端 v6.0 · 星际宇航</title>
<style>
*{margin:0;padding:0;box-sizing:border-box}
:root{
  --bg:#0a0e1a;--card:rgba(15,20,40,0.85);--border:rgba(100,180,255,0.15);
  --text:#e0e8ff;--sub:#6088aa;--accent:#00b4ff;--accent2:#00ff88;
  --glow:rgba(0,180,255,0.3);--warn:#ff6644;--gold:#ffcc00;
}
[data-theme="mars"]{
  --bg:#1a0a0a;--card:rgba(40,15,15,0.85);--border:rgba(255,100,80,0.15);
  --accent:#ff4422;--accent2:#ff8844;--glow:rgba(255,68,34,0.3);
}
[data-theme="jupiter"]{
  --bg:#1a1408;--card:rgba(40,30,10,0.85);--border:rgba(255,180,60,0.15);
  --accent:#ff9900;--accent2:#ffcc44;--glow:rgba(255,153,0,0.3);
}
[data-theme="saturn"]{
  --bg:#18160a;--card:rgba(35,30,10,0.85);--border:rgba(255,204,0,0.15);
  --accent:#ffcc00;--accent2:#ffe066;--glow:rgba(255,204,0,0.3);
}
[data-theme="neptune"]{
  --bg:#0a0a1a;--card:rgba(20,15,45,0.85);--border:rgba(140,80,255,0.15);
  --accent:#8844ff;--accent2:#bb88ff;--glow:rgba(136,68,255,0.3);
}
body{
  font-family:'SF Pro Display',-apple-system,'PingFang SC',sans-serif;
  background:var(--bg);color:var(--text);height:100vh;overflow:hidden;
  position:relative;
}
canvas#starfield{position:fixed;top:0;left:0;width:100%;height:100%;z-index:0}

/* HUD框架 */
.hud{position:relative;z-index:1;height:100vh;display:flex;flex-direction:column}
.hud-top{
  height:48px;display:flex;align-items:center;padding:0 20px;
  background:var(--card);border-bottom:1px solid var(--border);
  backdrop-filter:blur(20px);
}
.hud-top .logo{font-size:16px;font-weight:700;cursor:pointer;user-select:none}
.hud-top .logo span{color:var(--accent)}
.hud-top .stardate{margin-left:auto;font-size:11px;color:var(--sub);font-family:'SF Mono',monospace}
.hud-top .dna{font-size:10px;color:var(--accent);margin-left:16px;font-family:'SF Mono',monospace}

.hud-body{flex:1;display:flex;overflow:hidden}

/* 行星导航 */
.planet-nav{
  width:200px;display:flex;flex-direction:column;align-items:center;
  padding:20px 0;background:var(--card);border-right:1px solid var(--border);
  backdrop-filter:blur(20px);position:relative;overflow:hidden;
}
.planet-nav::before{
  content:'';position:absolute;width:300px;height:300px;
  border:1px solid var(--border);border-radius:50%;
  top:50%;left:50%;transform:translate(-50%,-50%);
  animation:orbit-ring 20s linear infinite;
}
@keyframes orbit-ring{to{transform:translate(-50%,-50%) rotate(360deg)}}

.nav-planet{
  width:100%;padding:10px 16px;display:flex;align-items:center;gap:10px;
  cursor:pointer;font-size:13px;border-radius:0;transition:all .3s;
  position:relative;z-index:1;
}
.nav-planet:hover{background:rgba(0,180,255,0.08)}
.nav-planet.active{background:rgba(0,180,255,0.12);color:var(--accent)}
.nav-planet .icon{font-size:20px;width:28px;text-align:center}
.nav-planet .pulse{
  width:8px;height:8px;border-radius:50%;margin-left:auto;
  background:var(--accent2);animation:pulse 2s ease-in-out infinite;
}
.nav-planet .pulse.off{background:var(--warn);animation:none}
@keyframes pulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.5;transform:scale(.7)}}

.nav-divider{width:80%;height:1px;background:var(--border);margin:8px 0}
.nav-label{font-size:10px;color:var(--sub);text-transform:uppercase;letter-spacing:2px;padding:8px 16px;width:100%}

/* 主内容 */
.main-content{flex:1;display:flex;flex-direction:column;overflow:hidden;position:relative}
.warp-overlay{
  position:absolute;top:0;left:0;width:100%;height:100%;z-index:10;
  background:radial-gradient(ellipse at center,rgba(255,255,255,0.9),transparent 70%);
  opacity:0;pointer-events:none;transition:opacity .15s;
}
.warp-overlay.active{opacity:1}

.panel{display:none;flex:1;flex-direction:column;overflow:hidden}
.panel.active{display:flex}
.panel-header{
  height:44px;display:flex;align-items:center;padding:0 20px;
  background:var(--card);border-bottom:1px solid var(--border);
  backdrop-filter:blur(20px);
}
.panel-header h2{font-size:14px;font-weight:600;flex:1}
.panel-header .btn{
  padding:5px 12px;border-radius:6px;border:1px solid var(--border);
  background:transparent;color:var(--text);font-size:12px;cursor:pointer;
}
.panel-header .btn:hover{background:rgba(0,180,255,0.1)}

.panel-body{flex:1;padding:20px;overflow-y:auto}
.hud-card{
  background:var(--card);border:1px solid var(--border);border-radius:12px;
  padding:20px;margin-bottom:16px;backdrop-filter:blur(20px);
  position:relative;overflow:hidden;
}
.hud-card::before{
  content:'';position:absolute;top:0;left:0;width:100%;height:1px;
  background:linear-gradient(90deg,transparent,var(--accent),transparent);
  animation:scan-line 3s linear infinite;
}
@keyframes scan-line{0%{transform:translateX(-100%)}100%{transform:translateX(100%)}}
.hud-card h3{font-size:15px;font-weight:600;margin-bottom:12px;color:var(--accent)}

textarea.hud-input{
  width:100%;min-height:100px;padding:12px;border:1px solid var(--border);
  border-radius:8px;background:rgba(0,0,0,0.4);color:var(--text);
  font-size:13px;font-family:'SF Mono',Monaco,monospace;resize:vertical;
}
textarea.hud-input:focus{border-color:var(--accent);outline:none;box-shadow:0 0 20px var(--glow)}

.btn-launch{
  background:linear-gradient(135deg,var(--accent),var(--accent2));
  color:#000;border:none;padding:10px 20px;border-radius:8px;
  font-size:13px;font-weight:600;cursor:pointer;margin-top:10px;
  transition:all .2s;
}
.btn-launch:hover{transform:translateY(-1px);box-shadow:0 4px 20px var(--glow)}

.result-hud{
  margin-top:12px;padding:14px;background:rgba(0,0,0,0.5);
  border:1px solid var(--border);border-radius:8px;
  font-family:'SF Mono',monospace;font-size:12px;min-height:80px;
  white-space:pre-wrap;color:var(--accent2);
}

/* 雷达状态 */
.radar-wrap{display:flex;gap:16px;flex-wrap:wrap}
.radar-item{text-align:center}
.radar-ring{
  width:60px;height:60px;border-radius:50%;position:relative;
  border:2px solid var(--border);
}
.radar-ring::after{
  content:'';position:absolute;top:0;left:50%;width:50%;height:50%;
  background:linear-gradient(to bottom,var(--accent),transparent);
  transform-origin:bottom left;animation:radar-sweep 3s linear infinite;
  border-radius:0 100% 0 0;
}
@keyframes radar-sweep{to{transform:rotate(360deg)}}
.radar-label{font-size:10px;color:var(--sub);margin-top:4px}

/* SVG仪表盘 */
.gauge-row{display:flex;gap:20px;justify-content:center;margin:16px 0}
.gauge{text-align:center}
.gauge svg{width:80px;height:80px}
.gauge-label{font-size:11px;color:var(--sub);margin-top:4px}
.gauge-val{font-size:13px;font-weight:700;color:var(--accent)}

/* 命令面板 */
.cmd-overlay{
  position:fixed;top:0;left:0;width:100%;height:100%;z-index:100;
  background:rgba(0,0,0,0.7);backdrop-filter:blur(10px);
  display:none;align-items:flex-start;justify-content:center;padding-top:15vh;
}
.cmd-overlay.show{display:flex}
.cmd-box{
  width:500px;max-width:90vw;background:var(--card);border:1px solid var(--accent);
  border-radius:12px;overflow:hidden;box-shadow:0 0 60px var(--glow);
}
.cmd-box input{
  width:100%;padding:14px 16px;border:none;background:transparent;
  color:var(--text);font-size:15px;outline:none;
}
.cmd-list{max-height:300px;overflow-y:auto}
.cmd-item{
  padding:10px 16px;cursor:pointer;font-size:13px;display:flex;align-items:center;gap:10px;
}
.cmd-item:hover,.cmd-item.sel{background:rgba(0,180,255,0.12)}
.cmd-item .key{font-size:11px;color:var(--sub);margin-left:auto;font-family:'SF Mono',monospace}

/* 飞碟通知 */
.ufo-toast{
  position:fixed;top:20px;right:-400px;z-index:200;
  background:var(--card);border:1px solid var(--accent);border-radius:10px;
  padding:12px 20px;font-size:13px;backdrop-filter:blur(20px);
  box-shadow:0 0 30px var(--glow);transition:right .5s cubic-bezier(.68,-.55,.27,1.55);
  display:flex;align-items:center;gap:8px;
}
.ufo-toast.show{right:20px}
.ufo-toast .ufo-icon{font-size:20px;animation:ufo-bob 1s ease-in-out infinite}
@keyframes ufo-bob{0%,100%{transform:translateY(0)}50%{transform:translateY(-4px)}}

/* 浮动指挥台 */
.float-cmd{
  position:fixed;bottom:20px;right:20px;z-index:50;
  width:280px;background:var(--card);border:1px solid var(--border);
  border-radius:12px;backdrop-filter:blur(20px);
  box-shadow:0 4px 30px rgba(0,0,0,0.5);cursor:move;display:none;
}
.float-cmd.show{display:block}
.float-cmd-head{
  padding:10px 14px;border-bottom:1px solid var(--border);
  font-size:12px;font-weight:600;display:flex;align-items:center;
}
.float-cmd-head .close{margin-left:auto;cursor:pointer;opacity:.5}
.float-cmd-head .close:hover{opacity:1}
.float-cmd-body{padding:12px}
.float-cmd-body input{
  width:100%;padding:8px;border:1px solid var(--border);border-radius:6px;
  background:rgba(0,0,0,0.3);color:var(--text);font-size:12px;
  font-family:'SF Mono',monospace;
}

/* 超新星/虫洞 */
.supernova{
  position:fixed;top:0;left:0;width:100%;height:100%;z-index:999;
  background:white;opacity:0;pointer-events:none;transition:opacity .2s;
}
.supernova.bang{opacity:1}
.wormhole{
  position:fixed;top:0;left:0;width:100%;height:100%;z-index:999;
  pointer-events:none;display:none;
}
.wormhole.active{display:block}
.wormhole canvas{width:100%;height:100%}

/* 星图面板 */
.starmap-overlay{
  position:fixed;top:0;left:0;width:100%;height:100%;z-index:80;
  background:rgba(0,0,0,0.9);display:none;align-items:center;justify-content:center;
}
.starmap-overlay.show{display:flex}
.starmap-box{text-align:center}
.starmap-box h2{font-size:20px;color:var(--accent);margin-bottom:12px}
.starmap-box p{font-size:13px;color:var(--sub);margin-bottom:4px}
.starmap-box .close-map{margin-top:20px;color:var(--accent);cursor:pointer;font-size:12px}
</style>
</head>
<body data-theme="galaxy">
<canvas id="starfield"></canvas>
<div class="supernova" id="supernova"></div>
<div class="wormhole" id="wormhole"><canvas id="wormholeCanvas"></canvas></div>

<!-- 飞碟通知 -->
<div class="ufo-toast" id="ufoToast"><span class="ufo-icon">🛸</span><span id="ufoMsg"></span></div>

<!-- 命令面板 -->
<div class="cmd-overlay" id="cmdOverlay">
<div class="cmd-box">
<input id="cmdInput" placeholder="输入指令... (ESC关闭)" autocomplete="off">
<div class="cmd-list" id="cmdList"></div>
</div>
</div>

<!-- 星图 -->
<div class="starmap-overlay" id="starmapOverlay">
<div class="starmap-box">
<h2>🌌 龍魂星图</h2>
<p>🌍 地球时: <span id="earthTime">--</span></p>
<p>🔴 火星Sol: <span id="marsSol">--</span></p>
<p>⭐ 星际标准时: <span id="starTime">--</span></p>
<p>📡 信号强度: <span id="signalStr" style="color:var(--accent2)">██████████ 100%</span></p>
<p>🚀 曲速状态: <span style="color:var(--accent)">WARP READY</span></p>
<div class="close-map" onclick="toggleStarmap()">\[ ESC 关闭 \]</div>
</div>
</div>

<!-- 浮动指挥台 -->
<div class="float-cmd" id="floatCmd">
<div class="float-cmd-head" id="floatCmdHead">
🎯 快捷指挥台
<span class="close" onclick="toggleFloat()">✕</span>
</div>
<div class="float-cmd-body">
<input id="floatInput" placeholder="快捷命令..." onkeydown="if(event.key==='Enter')runFloatCmd()">
<div id="floatResult" style="font-size:11px;color:var(--sub);margin-top:8px;min-height:30px"></div>
</div>
</div>

<!-- HUD -->
<div class="hud">
<div class="hud-top">
<div class="logo" id="mainLogo">🐉 <span>龍魂终端</span> v6.0</div>
<div class="stardate" id="stardateDisplay">STARDATE --</div>
<div class="dna">DNA: LH-20260316-0452-V6STAR</div>
</div>

<div class="hud-body">
<div class="planet-nav">
<div class="nav-label">· 核心模块 ·</div>
<div class="nav-planet active" onclick="warpTo('chat',this)" data-key="1">
<span class="icon">💬</span> AI对话 <span class="pulse"></span>
</div>
<div class="nav-planet" onclick="warpTo('gateway',this)" data-key="2">
<span class="icon">🌐</span> CNSH网关 <span class="pulse"></span>
</div>
<div class="nav-divider"></div>
<div class="nav-label">· 星际连接 ·</div>
<div class="nav-planet" onclick="warpTo('github',this)" data-key="3">
<span class="icon">📦</span> GitHub <span class="pulse off"></span>
</div>
<div class="nav-planet" onclick="warpTo('notion',this)" data-key="4">
<span class="icon">📋</span> Notion <span class="pulse off"></span>
</div>
<div class="nav-divider"></div>
<div class="nav-label">· 系统 ·</div>
<div class="nav-planet" onclick="warpTo('monitor',this)" data-key="5">
<span class="icon">📊</span> 能量监控 <span class="pulse"></span>
</div>
<div class="nav-planet" onclick="warpTo('settings',this)" data-key="6">
<span class="icon">⚙️</span> 舰桥设置 <span class="pulse"></span>
</div>
</div>

<div class="main-content">
<div class="warp-overlay" id="warpOverlay"></div>

<!-- AI对话 -->
<div class="panel active" id="panel-chat">
<div class="panel-header"><h2>💬 本地AI通讯频道</h2><button class="btn" onclick="clearChat()">清除频道</button></div>
<div class="panel-body">
<div class="hud-card"><h3>📡 发送信号</h3>
<textarea class="hud-input" id="chatInput" placeholder="输入你的星际讯息..."></textarea>
<button class="btn-launch" onclick="sendChat()">🚀 发射信号</button>
</div>
<div class="hud-card"><h3>📨 接收回波</h3>
<div class="result-hud" id="chatResult">&gt; 等待信号输入...</div>
</div>
</div>
</div>

<!-- CNSH网关 -->
<div class="panel" id="panel-gateway">
<div class="panel-header"><h2>🌐 CNSH 超光速网关</h2></div>
<div class="panel-body">
<div class="hud-card"><h3>📤 输入载荷</h3>
<textarea class="hud-input" id="gatewayInput" placeholder="粘贴代码/文本/JSON载荷..."></textarea>
<button class="btn-launch" onclick="processGateway()">⚡ 超光速传输</button>
</div>
<div class="hud-card"><h3>📥 传输结果</h3>
<div class="result-hud" id="gatewayResult">&gt; 网关待命...</div>
</div>
</div>
</div>

<!-- GitHub -->
<div class="panel" id="panel-github">
<div class="panel-header"><h2>📦 GitHub 空间站</h2><button class="btn" onclick="loadGithubRepos()">扫描仓库</button></div>
<div class="panel-body">
<div class="hud-card"><h3>🛰️ 仓库列表</h3>
<div id="repoList" style="color:var(--sub)">点击"扫描仓库"连接空间站...</div>
</div>
</div>
</div>

<!-- Notion -->
<div class="panel" id="panel-notion">
<div class="panel-header"><h2>📋 Notion 知识星云</h2></div>
<div class="panel-body">
<div class="hud-card"><h3>🔭 深空搜索</h3>
<textarea class="hud-input" id="notionQuery" placeholder="搜索关键词..." style="min-height:50px"></textarea>
<button class="btn-launch" onclick="searchNotion()">🔍 扫描星云</button>
</div>
<div class="hud-card"><h3>📡 搜索回波</h3>
<div class="result-hud" id="notionResult">&gt; 等待扫描...</div>
</div>
</div>
</div>

<!-- 能量监控 -->
<div class="panel" id="panel-monitor">
<div class="panel-header"><h2>📊 飞船能量监控</h2></div>
<div class="panel-body">
<div class="hud-card"><h3>⚡ 核心能量环</h3>
<div class="gauge-row">
<div class="gauge">
<svg viewBox="0 0 36 36">
<path d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" fill="none" stroke="rgba(100,180,255,0.15)" stroke-width="2"/>
<path id="gaugeCpu" d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" fill="none" stroke="var(--accent)" stroke-width="2" stroke-dasharray="75, 100" stroke-linecap="round"/>
<text x="18" y="20" text-anchor="middle" fill="var(--accent)" font-size="7" font-weight="700" id="gaugeCpuVal">75%</text>
</svg>
<div class="gauge-label">CPU 核心</div>
</div>
<div class="gauge">
<svg viewBox="0 0 36 36">
<path d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" fill="none" stroke="rgba(100,180,255,0.15)" stroke-width="2"/>
<path id="gaugeMem" d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" fill="none" stroke="var(--accent2)" stroke-width="2" stroke-dasharray="62, 100" stroke-linecap="round"/>
<text x="18" y="20" text-anchor="middle" fill="var(--accent2)" font-size="7" font-weight="700" id="gaugeMemVal">62%</text>
</svg>
<div class="gauge-label">内存矩阵</div>
</div>
<div class="gauge">
<svg viewBox="0 0 36 36">
<path d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" fill="none" stroke="rgba(100,180,255,0.15)" stroke-width="2"/>
<path id="gaugeNet" d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" fill="none" stroke="var(--gold)" stroke-width="2" stroke-dasharray="88, 100" stroke-linecap="round"/>
<text x="18" y="20" text-anchor="middle" fill="var(--gold)" font-size="7" font-weight="700" id="gaugeNetVal">88%</text>
</svg>
<div class="gauge-label">网络通量</div>
</div>
</div>
</div>
<div class="hud-card"><h3>📡 雷达阵列</h3>
<div class="radar-wrap">
<div class="radar-item"><div class="radar-ring"></div><div class="radar-label">Ollama</div></div>
<div class="radar-item"><div class="radar-ring"></div><div class="radar-label">GitHub</div></div>
<div class="radar-item"><div class="radar-ring"></div><div class="radar-label">Notion</div></div>
<div class="radar-item"><div class="radar-ring"></div><div class="radar-label">Gateway</div></div>
</div>
</div>
</div>
</div>

<!-- 舰桥设置 -->
<div class="panel" id="panel-settings">
<div class="panel-header"><h2>⚙️ 舰桥设置</h2></div>
<div class="panel-body">
<div class="hud-card"><h3>🎨 星系主题</h3>
<div style="display:flex;gap:10px;flex-wrap:wrap">
<button class="btn-launch" onclick="setTheme('galaxy')" style="background:linear-gradient(135deg,#00b4ff,#00ff88)">🌌 银河蓝</button>
<button class="btn-launch" onclick="setTheme('mars')" style="background:linear-gradient(135deg,#ff4422,#ff8844)">🔴 火星红</button>
<button class="btn-launch" onclick="setTheme('jupiter')" style="background:linear-gradient(135deg,#ff9900,#ffcc44)">🟠 木星橙</button>
<button class="btn-launch" onclick="setTheme('saturn')" style="background:linear-gradient(135deg,#ffcc00,#ffe066)">🟡 土星金</button>
<button class="btn-launch" onclick="setTheme('neptune')" style="background:linear-gradient(135deg,#8844ff,#bb88ff)">🟣 海王紫</button>
</div>
</div>
<div class="hud-card"><h3>⌨️ 快捷键一览</h3>
<div style="font-size:12px;color:var(--sub);line-height:2">
<b style="color:var(--accent)">1-6</b> 切换面板 ｜
<b style="color:var(--accent)">T</b> 切换主题 ｜
<b style="color:var(--accent)">M</b> 静音/开音 ｜
<b style="color:var(--accent)">S</b> 星图 ｜
<b style="color:var(--accent)">F</b> 浮动指挥台 ｜
<b style="color:var(--accent)">⌘K</b> 命令面板 ｜
<b style="color:var(--accent)">↑↑↓↓←→←→BA</b> 虫洞彩蛋 ｜
<b style="color:var(--accent)">三击Logo</b> 超新星
</div>
</div>
<div class="hud-card"><h3>🔊 音频控制</h3>
<button class="btn-launch" id="muteBtn" onclick="toggleMute()">🔊 音效开启中</button>
</div>
</div>
</div>

</div><!-- main-content -->
</div><!-- hud-body -->
</div><!-- hud -->

<script>
// ===== 3D星场 =====
const cvs=document.getElementById('starfield'),ctx=cvs.getContext('2d');
let W,H,mx=0,my=0;
const stars=[];
function initStars(){
  W=cvs.width=innerWidth;H=cvs.height=innerHeight;
  stars.length=0;
  for(let i=0;i<200;i++) stars.push({x:Math.random()*W,y:Math.random()*H,z:Math.random()*3+0.5,b:Math.random()});
}
function drawStars(){
  ctx.clearRect(0,0,W,H);
  const cs=getComputedStyle(document.body);
  const ac=cs.getPropertyValue('--accent').trim()||'#00b4ff';
  for(const s of stars){
    // parallax
    const px=s.x+(mx-W/2)*s.z*0.02;
    const py=s.y+(my-H/2)*s.z*0.02;
    s.b+=0.01;if(s.b>1)s.b=0;
    const a=0.3+Math.sin(s.b*Math.PI*2)*0.7;
    ctx.beginPath();
    ctx.arc(px,py,s.z*0.8,0,Math.PI*2);
    ctx.fillStyle=ac.replace(')',`,${a})`).replace('rgb','rgba');
    if(!ctx.fillStyle.includes('rgba'))ctx.fillStyle=`rgba(180,220,255,${a})`;
    ctx.fill();
  }
  s.y-=s.z*0.15;
  for(const s of stars){s.y-=s.z*0.15;if(s.y<-10){s.y=H+10;s.x=Math.random()*W;}}
  requestAnimationFrame(drawStars);
}
window.addEventListener('resize',initStars);
document.addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY});
initStars();drawStars();

// ===== Web Audio 太空音效 =====
let audioCtx,muted=false;
function getAudio(){if(!audioCtx)audioCtx=new(window.AudioContext||window.webkitAudioContext)();return audioCtx;}
function playTone(freq,dur,type='sine',vol=0.08){
  if(muted)return;
  try{
    const a=getAudio(),o=a.createOscillator(),g=a.createGain();
    o.type=type;o.frequency.value=freq;
    g.gain.setValueAtTime(vol,a.currentTime);
    g.gain.exponentialRampToValueAtTime(0.001,a.currentTime+dur);
    o.connect(g);g.connect(a.destination);
    o.start();o.stop(a.currentTime+dur);
  }catch(e){}
}
function sfxClick(){playTone(800,0.08,'sine',0.06);playTone(1200,0.06,'sine',0.04);}
function sfxWarp(){playTone(200,0.4,'sawtooth',0.1);setTimeout(()=>playTone(600,0.3,'sine',0.08),100);}
function sfxNotify(){playTone(523,0.15,'triangle',0.08);setTimeout(()=>playTone(659,0.15,'triangle',0.08),100);setTimeout(()=>playTone(784,0.2,'triangle',0.08),200);}
function sfxBoom(){playTone(80,1,'sawtooth',0.15);playTone(40,1.5,'square',0.1);}
function toggleMute(){
  muted=!muted;
  document.getElementById('muteBtn').textContent=muted?'🔇 已静音':'🔊 音效开启中';
  ufo(muted?'🔇 音效已关闭':'🔊 音效已开启');
}

// ===== 曲速切换 =====
const panels=['chat','gateway','github','notion','monitor','settings'];
let curPanel='chat';
function warpTo(name,el){
  if(name===curPanel)return;
  sfxWarp();
  const ov=document.getElementById('warpOverlay');
  ov.classList.add('active');
  document.querySelectorAll('.nav-planet').forEach(n=>n.classList.remove('active'));
  if(el)el.classList.add('active');
  setTimeout(()=>{
    document.querySelectorAll('.panel').forEach(p=>p.classList.remove('active'));
    document.getElementById('panel-'+name).classList.add('active');
    curPanel=name;
    ov.classList.remove('active');
  },200);
}

// ===== 飞碟通知 =====
let ufoTimer;
function ufo(msg){
  sfxNotify();
  const t=document.getElementById('ufoToast');
  document.getElementById('ufoMsg').textContent=msg;
  t.classList.add('show');
  clearTimeout(ufoTimer);
  ufoTimer=setTimeout(()=>t.classList.remove('show'),3000);
}

// ===== 星系主题 =====
const themes=['galaxy','mars','jupiter','saturn','neptune'];
let themeIdx=0;
function setTheme(t){
  document.body.setAttribute('data-theme',t);
  themeIdx=themes.indexOf(t);
  sfxClick();
  const names={galaxy:'🌌 银河蓝',mars:'🔴 火星红',jupiter:'🟠 木星橙',saturn:'🟡 土星金',neptune:'🟣 海王紫'};
  ufo('主题切换: '+names[t]);
}
function cycleTheme(){
  themeIdx=(themeIdx+1)%themes.length;
  setTheme(themes[themeIdx]);
}

// ===== 星历时间 =====
function updateStardate(){
  const now=new Date();
  const bj=new Date(now.getTime()+8*3600000);
  const h=bj.getUTCHours(),m=bj.getUTCMinutes(),s=bj.getUTCSeconds();
  const ds=Math.floor((bj-new Date(bj.getUTCFullYear(),0,1))/86400000);
  const sd=`${bj.getUTCFullYear()}.${String(ds).padStart(3,'0')}.${String(h).padStart(2,'0')}${String(m).padStart(2,'0')}`;
  document.getElementById('stardateDisplay').textContent='STARDATE '+sd;
  const et=document.getElementById('earthTime');
  if(et) et.textContent=`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')} CST`;
  const ms=document.getElementById('marsSol');
  if(ms) ms.textContent='Sol '+Math.floor(ds*0.97+1000);
  const st=document.getElementById('starTime');
  if(st) st.textContent=sd+'.'+String(s).padStart(2,'0');
}
setInterval(updateStardate,1000);updateStardate();

// ===== 命令面板 =====
const cmds=[
  {icon:'💬',name:'AI对话',key:'1',fn:()=>warpTo('chat',document.querySelector('[data-key="1"]'))},
  {icon:'🌐',name:'CNSH网关',key:'2',fn:()=>warpTo('gateway',document.querySelector('[data-key="2"]'))},
  {icon:'📦',name:'GitHub',key:'3',fn:()=>warpTo('github',document.querySelector('[data-key="3"]'))},
  {icon:'📋',name:'Notion',key:'4',fn:()=>warpTo('notion',document.querySelector('[data-key="4"]'))},
  {icon:'📊',name:'能量监控',key:'5',fn:()=>warpTo('monitor',document.querySelector('[data-key="5"]'))},
  {icon:'⚙️',name:'舰桥设置',key:'6',fn:()=>warpTo('settings',document.querySelector('[data-key="6"]'))},
  {icon:'🎨',name:'切换主题',key:'T',fn:cycleTheme},
  {icon:'🔊',name:'静音切换',key:'M',fn:toggleMute},
  {icon:'🌌',name:'星图',key:'S',fn:toggleStarmap},
  {icon:'🎯',name:'浮动指挥台',key:'F',fn:toggleFloat},
];
function renderCmds(q=''){
  const list=document.getElementById('cmdList');
  const f=cmds.filter(c=>!q||c.name.includes(q));
  list.innerHTML=f.map((c,i)=>`<div class="cmd-item${i===0?' sel':''}" onclick="cmds[${cmds.indexOf(c)}].fn();closeCmd()">
    <span>${c.icon}</span>${c.name}<span class="key">${c.key}</span></div>`).join('');
}
function openCmd(){document.getElementById('cmdOverlay').classList.add('show');document.getElementById('cmdInput').value='';renderCmds();setTimeout(()=>document.getElementById('cmdInput').focus(),50);sfxClick();}
function closeCmd(){document.getElementById('cmdOverlay').classList.remove('show');}
document.getElementById('cmdInput').addEventListener('input',e=>renderCmds(e.target.value));
document.getElementById('cmdInput').addEventListener('keydown',e=>{
  if(e.key==='Escape')closeCmd();
  if(e.key==='Enter'){
    const sel=document.querySelector('.cmd-item.sel');
    if(sel)sel.click();
  }
});
document.getElementById('cmdOverlay').addEventListener('click',e=>{if(e.target===e.currentTarget)closeCmd();});

// ===== 星图 =====
function toggleStarmap(){
  const o=document.getElementById('starmapOverlay');
  o.classList.toggle('show');sfxClick();
}

// ===== 浮动指挥台（可拖拽）=====
function toggleFloat(){
  document.getElementById('floatCmd').classList.toggle('show');sfxClick();
}
function runFloatCmd(){
  const v=document.getElementById('floatInput').value.trim();
  if(!v)return;
  document.getElementById('floatResult').textContent='> '+v+' ... 已执行';
  document.getElementById('floatInput').value='';
  sfxClick();
}
// 拖拽
(function(){
  const el=document.getElementById('floatCmd'),hd=document.getElementById('floatCmdHead');
  let dx=0,dy=0,dragging=false;
  hd.addEventListener('mousedown',e=>{dx=e.clientX-el.offsetLeft;dy=e.clientY-el.offsetTop;dragging=true;});
  document.addEventListener('mousemove',e=>{if(!dragging)return;el.style.left=(e.clientX-dx)+'px';el.style.top=(e.clientY-dy)+'px';el.style.right='auto';el.style.bottom='auto';});
  document.addEventListener('mouseup',()=>dragging=false);
})();

// ===== 键盘快捷 =====
document.addEventListener('keydown',e=>{
  if(e.target.tagName==='INPUT'||e.target.tagName==='TEXTAREA')return;
  const k=e.key.toLowerCase();
  if((e.metaKey||e.ctrlKey)&&k==='k'){e.preventDefault();openCmd();return;}
  if(k==='escape'){closeCmd();document.getElementById('starmapOverlay').classList.remove('show');return;}
  if(k>='1'&&k<='6'){const i=parseInt(k)-1;warpTo(panels[i],document.querySelector(`[data-key="${k}"]`));}
  if(k==='t')cycleTheme();
  if(k==='m')toggleMute();
  if(k==='s')toggleStarmap();
  if(k==='f')toggleFloat();
  // Konami
  konamiCheck(e.key);
});

// ===== 超新星彩蛋（三击Logo）=====
let clickCount=0,clickTimer;
document.getElementById('mainLogo').addEventListener('click',()=>{
  clickCount++;
  clearTimeout(clickTimer);
  clickTimer=setTimeout(()=>clickCount=0,500);
  if(clickCount>=3){
    clickCount=0;
    sfxBoom();
    const sn=document.getElementById('supernova');
    sn.classList.add('bang');
    setTimeout(()=>sn.classList.remove('bang'),300);
    ufo('🌟 超新星爆发！龍魂能量觉醒！');
  }
});

// ===== Konami虫洞彩蛋 =====
const konamiSeq=['ArrowUp','ArrowUp','ArrowDown','ArrowDown','ArrowLeft','ArrowRight','ArrowLeft','ArrowRight','b','a'];
let konamiIdx=0;
function konamiCheck(key){
  if(key===konamiSeq[konamiIdx]||key.toLowerCase()===konamiSeq[konamiIdx]){
    konamiIdx++;
    if(konamiIdx===konamiSeq.length){
      konamiIdx=0;
      triggerWormhole();
    }
  }else{konamiIdx=0;}
}
function triggerWormhole(){
  sfxBoom();
  const wh=document.getElementById('wormhole');
  const wc=document.getElementById('wormholeCanvas');
  wc.width=innerWidth;wc.height=innerHeight;
  const wctx=wc.getContext('2d');
  wh.classList.add('active');
  let t=0;
  const anim=()=>{
    t+=0.03;
    if(t>2){wh.classList.remove('active');return;}
    wctx.fillStyle=`rgba(0,0,0,${0.1})`;
    wctx.fillRect(0,0,wc.width,wc.height);
    for(let i=0;i<20;i++){
      const a=Math.random()*Math.PI*2;
      const r=(1-t/2)*Math.max(wc.width,wc.height)*Math.random()*0.5;
      const x=wc.width/2+Math.cos(a+t*3)*r;
      const y=wc.height/2+Math.sin(a+t*3)*r;
      wctx.beginPath();wctx.arc(x,y,2,0,Math.PI*2);
      wctx.fillStyle=`hsl(${(t*180+i*18)%360},100%,70%)`;
      wctx.fill();
    }
    requestAnimationFrame(anim);
  };
  anim();
  ufo('🕳️ 虫洞开启！维度穿越中...');
}

// ===== 仪表盘动画 =====
function animGauges(){
  const cpu=60+Math.random()*30,mem=50+Math.random()*30,net=70+Math.random()*25;
  document.getElementById('gaugeCpu').setAttribute('stroke-dasharray',`${cpu},100`);
  document.getElementById('gaugeCpuVal').textContent=Math.round(cpu)+'%';
  document.getElementById('gaugeMem').setAttribute('stroke-dasharray',`${mem},100`);
  document.getElementById('gaugeMemVal').textContent=Math.round(mem)+'%';
  document.getElementById('gaugeNet').setAttribute('stroke-dasharray',`${net},100`);
  document.getElementById('gaugeNetVal').textContent=Math.round(net)+'%';
}
setInterval(animGauges,3000);animGauges();

// ===== API调用（与v2.0相同后端）=====
async function sendChat(){
  const input=document.getElementById('chatInput').value;
  if(!input.trim()){ufo('⚠️ 请输入讯息');return;}
  const r=document.getElementById('chatResult');
  r.textContent='🤔 AI量子计算中...';sfxClick();
  try{
    const resp=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({query:input})});
    const data=await resp.json();
    r.textContent=data.error?'❌ '+data.error:data.response;
    if(!data.error)ufo('📨 AI回波接收完成');
  }catch(e){r.textContent='❌ 信号丢失: '+e.message;}
}
function clearChat(){document.getElementById('chatInput').value='';document.getElementById('chatResult').textContent='> 等待信号输入...';sfxClick();}

async function processGateway(){
  const input=document.getElementById('gatewayInput').value;
  if(!input.trim()){ufo('⚠️ 请输入载荷');return;}
  const r=document.getElementById('gatewayResult');
  r.textContent='⚡ 超光速传输中...';sfxClick();
  try{
    const resp=await fetch('/api/gateway/process',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({input})});
    const data=await resp.json();
    r.textContent=`${data.status} ${data.status_text}\n\nDNA: ${data.dna}\n时间: ${data.timestamp}`;
    ufo('⚡ 传输完成');
  }catch(e){r.textContent='❌ 传输失败: '+e.message;}
}

async function loadGithubRepos(){
  const list=document.getElementById('repoList');
  list.innerHTML='<div style="color:var(--sub)">🛰️ 扫描中...</div>';sfxClick();
  try{
    const resp=await fetch('/api/github/repos');
    const data=await resp.json();
    if(data.error){list.innerHTML=`<div style="color:var(--warn)">❌ ${data.error}</div>`;}
    else{list.innerHTML=data.repos.map(r=>`<div style="padding:12px;background:rgba(0,0,0,0.3);border:1px solid var(--border);border-radius:8px;margin-bottom:8px;cursor:pointer" onclick="window.open('${r.url}')"><div style="font-weight:600;margin-bottom:4px">${r.full_name}</div><div style="font-size:12px;color:var(--sub)">${r.description||'无描述'}</div></div>`).join('');ufo('🛰️ 扫描完成: '+data.repos.length+'个仓库');}
  }catch(e){list.innerHTML=`<div style="color:var(--warn)">❌ ${e.message}</div>`;}
}

async function searchNotion(){
  const q=document.getElementById('notionQuery').value;
  if(!q.trim()){ufo('⚠️ 请输入关键词');return;}
  const r=document.getElementById('notionResult');
  r.textContent='🔭 深空扫描中...';sfxClick();
  try{
    const resp=await fetch('/api/notion/search',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({query:q})});
    const data=await resp.json();
    r.textContent=data.error?'❌ '+data.error:JSON.stringify(data,null,2);
    if(!data.error)ufo('🔭 扫描完成');
  }catch(e){r.textContent='❌ '+e.message;}
}

// ===== 初始化 =====
ufo('🚀 龍魂终端 v6.0 星际宇航版启动完成');
console.log('%c🚀 龍魂终端 v6.0 · 星际宇航','color:#00b4ff;font-size:20px;font-weight:bold');
console.log('%cUID9622 💙 宝宝','color:#00ff88;font-size:14px');
console.log('%c快捷键: 1-6面板 T主题 M静音 S星图 F指挥台 ⌘K命令','color:#6088aa;font-size:11px');
</script>
</body>
</html>
```

---

### 🎮 交互速查

- **鼠标移动** → 星场视差联动
- **点击行星导航** → 曲速穿梭切换面板
- **⌘K / Ctrl+K** → 全息命令面板
- **按 1-6** → 快速切换6大面板
- **按 T** → 循环5套星系主题
- **按 M** → 静音/开音
- **按 S** → 星图（地球时/火星Sol/星际标准时）
- **按 F** → 可拖拽浮动指挥台
- **三击Logo** → 🌟超新星爆发彩蛋
- **↑↑↓↓←→←→BA** → 🕳️虫洞穿越彩蛋

---

<aside>
🐉

**龍魂终端版本线：**

v2.0 一键启动版 → v3.0 粒子龍焰版 → v4.0 赛博朋克版 → v5.0 水墨东方版 → **v6.0 星际宇航版** 🚀

每版0%代码复用，全新主题·全新交互·全新体验

</aside>