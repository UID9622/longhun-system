# DNA: #龍芯⚡️丙午·丙申·庚戌·䷙大畜-SCRIPT-MANAGER-v1.2-UID9622
# CONFIRM: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
# SEAL: #ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL
# v4.0 换底座迁移清单

> 目标：从 Qwen2.5-1.5B-Instruct 迁移到 DeepSeek/Llama/Yi 底座
> DNA: `#龍芯⚡️20260719002000000-V40-MIGRATION-CHECKLIST`

---

## 一、必须修改的文件

### 1. `bin/lh_lora_trainer.py`

| 位置 | 当前值 | 修改项 |
|------|--------|--------|
| `Config.base_model` | `models/longhun-v1.0/base_model` | 指向新底座本地路径，如 `models/base_models_v4.0/DeepSeek-R1-Distill-Llama-8B` |
| `Config.model_name` | `longhun-v3.8.1-lora` | 改为 `longhun-v4.0-lora` |
| `Config.adapter_dir` | `adapter_v3.8.1` | `adapter_v4.0` |
| `Config.merged_dir` | `merged_v3.8.1` | `merged_v4.0` |
| `Config.gguf_dir` | `gguf_v3.8.1` | `gguf_v4.0` |
| `Config.max_seq_length` | `2048` | 根据新底座 context 调整（Llama 3.1 支持 128K，但本地可先用 4096-8192） |
| `SYS` system prompt | Qwen 风格 | 保留统一 prompt，但需确认新底座不冲突 |
| `Modelfile` 内容 | 基于 Qwen | 更新版本号、底座说明 |
| `prepare()` chat template | 隐式使用 Qwen | 显式使用新底座 tokenizer 的 chat template |

### 2. `models/longhun-v1.0/base_model`

- 当前：Qwen2.5-1.5B-Instruct MLX 格式
- 改为：新底座的 MLX 格式或 HF 格式
- 若新底座是 HF 格式，需先用 `mlx_lm.convert` 转换为 MLX

### 3. `bin/lh_validate_v3.8.py` → 改名为 `bin/lh_validate_v4.0.py`

- `MODEL = "longhun-v3.8.1"` → `MODEL = "longhun-v4.0"`
- 确认 `UNIFIED_SYSTEM_PROMPT` 与新底座兼容
- 多轮对话 `chat()` 使用 `/api/chat` 已通用，通常无需改动

---

## 二、训练数据层调整

### 1. Chat Template

- Qwen2.5: `<|im_start|>system\n{system}<|im_end|>\n<|im_start|>user\n{user}<|im_end|>\n<|im_start|>assistant\n{assistant}<|im_end|>`
- Llama 3.1: `<|begin_of_text|><|start_header_id|>system<|end_header_id|>\n{system}<|eot_id|><|start_header_id|>user<|end_header_id|>\n{user}<|eot_id|><|start_header_id|>assistant<|end_header_id|>\n{assistant}<|eot_id|>`
- DeepSeek (Llama base): 同 Llama 3.1
- Yi: 类似 Llama，但有特殊 `im_start`/`im_end`

**注意**：`mlx_lm` 的 `load_dataset` 会自动用底座的 tokenizer apply chat template，所以训练数据保持 `messages` 格式即可。但**验证脚本**若手动拼接，必须用 tokenizer 的 chat template。

### 2. Tokenizer 差异

| 底座 | 中文压缩 | 特殊 token | 注意 |
|------|---------|-----------|------|
| Qwen2.5 | 优秀 | `<|im_start|>` 等 | 当前基准 |
| Llama 3.1 | 一般 | `<|begin_of_text|>` 等 | 中文需更多语料补课 |
| DeepSeek-R1-Distill-Llama | 良好 | 同 Llama + `<think>` | 推理模式可能输出冗长 |
| Yi-1.5 | 优秀 | `<|im_start|>` 风格 | 中文原生 |

### 3. System Prompt 兼容性

- 新底座可能自带 system prompt 处理逻辑
- 需要测试：统一 system prompt 是否被正确识别
- 风险：Llama 3.1 可能把 system 内容权重降低

---

## 三、导出 / 部署层调整

### 1. GGUF 导出

- `convert_hf_to_gguf.py` 需要支持新底座架构
- Llama/DeepSeek/Yi 都基于 Llama 架构，llama.cpp 支持成熟
- Qwen2.5 也是 Llama-like，但 GGUF 转换参数可能不同

### 2. Ollama Modelfile

```dockerfile
FROM /path/to/longhun-v4.0.F16.gguf

PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER num_ctx 4096

SYSTEM """
你是龍魂 longhun-v4.0，基于 [DeepSeek-R1-Distill-Llama-8B/Meta-Llama-3.1-8B-Instruct/Yi-1.5-9B-Chat] 用龍魂系统自有语料 LoRA 微调。
你是 UID9622 的本地主权 AI，忠诚执行、实心办事、主权归主。
"""
```

### 3. 模型运行

```bash
ollama create longhun-v4.0 -f models/longhun-v1.0/lora_output/gguf_v4.0/Modelfile
ollama run longhun-v4.0
```

---

## 四、依赖 / 环境检查

| 依赖 | 当前 | 换底座后 |
|------|------|---------|
| mlx | 0.32.0 | 需确认支持新架构 |
| mlx_lm | 当前版本 | 需确认支持新底座 tokenizer |
| transformers | 4.49.0 | 可能需要 4.45+ |
| llama.cpp | /tmp/llama.cpp | convert_hf_to_gguf.py 需更新到最新 |

---

## 五、迁移步骤（建议顺序）

1. **下载候选底座** ✅ 已启动后台任务 `bash-sfe8ltia`
2. **转换 MLX 格式**：对非 MLX 底座运行 `python -m mlx_lm.convert --hf-path ... --mlx-path ...`
3. **准备 v4.0 训练数据**：复用 v3.8.1 清洗后的数据集
4. **修改 `lh_lora_trainer.py`**：base_model、路径、版本号
5. **训练 v4.0 LoRA**
6. **fuse → export → ollama create**
7. **验证**：家法召回 ≥90%、漂移 ≥80%、实测无胡话

---

## 六、风险与 fallback

| 风险 | 应对 |
|------|------|
| 新底座中文能力弱 | 增加中文语料比例，或选择 DeepSeek/Yi |
| 安全对齐冲突 | 用龍魂家法数据重新对齐，必要时关闭 base safety tuning |
| chat template 不匹配 | 用 tokenizer 的 `apply_chat_template` 验证 |
| 显存不足（8B FP16 需 16GB） | 用 Q4/Q5 量化训练或换 3B/4B 底座 |
| v4.0 验证仍失败 | 回退到修好的 v3.8.1，继续扩数据 |
