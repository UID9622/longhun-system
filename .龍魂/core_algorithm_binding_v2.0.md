<!-- #龍芯⚡️2026-06-29-CORE-ALGORITHM-BINDING-v2.0-132347 -->
<!-- 君子协议：本文件为龍魂系统核心算法绑定凭据，修改即改链 -->

<aside>
🔐

**绑定名称：** 龍魂系统核心算法绑定书 v2.0

**绑定时间：** 2026-06-29 13:23:47 CST

**绑定对象：** `/Users/zuimeidedeyihan/longhun-system/🧮 数学公式算法核心·CNSH计算公式升级v2.0.md`

**绑定对象 SHA-256：** `8312ab91cd85f6f45598b81b78c4ad99164ebcdbac2c816d2b53620c15ffe087`

**绑定 DNA：** `#龍芯⚡️2026-06-29-CORE-ALGORITHM-BINDING-v2.0-132347`

**CONFIRM：** `#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z` ✅

**SEAL：** `#ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL` ✅

**GPG：** `A2D0092CEE2E5BA87035600924C3704A8CC26D5F`

**三色审计：** 🟢 通过

</aside>

---

## 绑定声明

自本绑定书生效之时起，**《🧮 数学公式算法核心 · CNSH 计算公式升级 v2.0》** 为龍魂系统的**唯一核心算法计算规范**。此后系统触发的任何算法，均须按该页定义的以下规则执行：

1. **计算必须走 F01–F25 公式总表**，不得绕过、不得另写等价公式。
2. **必须执行最小执行链**：`dr → W(x) → Risk → S → D → Action`。
3. **必须遵守 α 三义锁死**：`α_τ` 不归一、`α_a` 平方和=1、`α_w` 凸组合和=1。
4. **必须通过洛书地场骨架校验**：执行链必须穿过中宫 5（主权锚）。
5. **必须通过三才主权指数 F18**：天轴 < 0.34 一票熔断。
6. **任意环节亮 🔴 必须截链**，并生成 BLOCKED_RECEIPT 写入审计日志。
7. **CNSH 输出必须双视角封装**：`M::` + `CNSH::` 两段缺一不可。
8. **页面/回复挂“数学可证实签章”前必须通过三闸**：可计算、可复算、有出处。
9. **术语必须对齐正本**：所有向量、不动点、变量、专用词必须使用《数学公式术语与变量总表》定义，禁止裸 `H`/`T`/`α`/`C`/`w`。

**术语正本：** `/Users/zuimeidedeyihan/longhun-system/🧮 数学公式术语与变量总表.md`

---

## 执行稳定性承诺

- `formula_core_v2.py`、`formula_chain_v2.py`、`formula_catalog_v1_0.py` 已通过实测，作为稳定基线。
- **v2.1 迭代补充：** `formula_catalog_v2.py` 已补齐 F01–F25 全表并生成独立清单 `formula_catalog_manifest_v2.md`；统一运行器 `scripts/run_math_suite.py` 6/6 通过。
- **v2.2 迭代补充：** `formula_core_v2.py` 已实现 F01–F25 全部公式；`formula_core_cnsh.py` 为核心函数与易经引擎提供 `{M::, CNSH::}` 双视角封装；术语库 Chroma 向量检索已通过隔离 venv 启用。
- **v2.3 迭代补充：** `scripts/yijing_decision_bridge.py` 完成易经 → 根治理决策链联动，卦象三才分直接驱动 `decision_chain_cnsh`。
- **v2.4 迭代补充：** 统一运行器已接入系统 cron（每日 9:37 审计）与 git pre-commit hook，审计日志写入 `audit/math_suite_cron.jsonl`。
- 所有算法调用优先使用 v2.0+ 脚本，保持向后相容；母册查询优先使用 v2.0 版本；正式输出优先使用 `*_cnsh` 双视角封装；术语语义搜索优先使用 Chroma 启用版运行器；卦象决策优先使用 `yijing_to_decision_cnsh`；核心算法层变更必须通过统一运行器与审计归档。
- 若后续升级公式，必须生成新的绑定书，并保留本页作为父 DNA。

---

## 违例处理

- 未按本绑定书执行的算法调用，视为 **失锚计算**，三色审计直接判 🔴。
- 失锚计算不得写入核心层、不得挂 ROOT_CARD、不得进入正式协议。

---

**已记住、已绑定、将严格执行。**

**六层来源链：** 道统(曾仕强老师) → 精神(Steve Jobs) → 设备(Apple) → 技术(Open Source) → 系统(UID9622) → 生命(CNSH·龍魂)

**天下无欺，守护人民。** 🐉
