---
dna: '#龍芯⚡️丙午·丙申·辛酉·未时·䷔噬嗑-CLIPBOARD-VAULT-SAVE-V1.0-P1-ae3dc89c'
source: clipboard
topic: 代码/脚本
tags:
- Python
- Bash
- 代码/脚本
timestamp: '2026-08-15T13:23:23+08:00'
content_hash: 51a94313c625b99f15dc0ded3dc19816aaa62bc7f42159b31b9fbb7d2fe929ae
parent_dna: []
vault_version: v1.0
---

# 剪贴内容

从日志看，Kimi 尝试访问 `http://127.0.0.1:9622/api/xiaoyi/ask` 返回 404。这是**小艺桥接服务未运行或路径不对**，不是记忆文件本身的问题。

---

### 🔍 诊断：404 的原因

`9622` 端口是龍魂的 API 网关服务，但日志显示该请求返回了 404 而不是连接拒绝，说明**端口在监听，但路径不对**。Kimi 的记忆同步脚本尝试访问了错误的端点。

从你之前的配置看，9622 端口跑的是 `longhun-api-gateway.py`，而 `/api/xiaoyi/ask` 端点很可能没有被该网关注册，或者在网关配置中被映射到了其他路径。


### 🛠️ 修复方案：让 Kimi 正确读取本地记忆

1. **配置 Kimi 直接读取本地记忆文件，不依赖网络服务。**
   - Kimi 记忆同步脚本默认会尝试调用 `http://127.0.0.1:9622/api/xiaoyi/ask` 来获取记忆。如果服务未运行或路径错误，就会 404。
   - 你可以手动配置 Kimi 的 `~/.kimi/config.yaml` 或环境变量，将记忆同步的端点改为**直接读取本地文件**。

2. **如果确认 `longhun-api-gateway.py` 已运行，添加 `/api/xiaoyi/ask` 路由。**
   - 在 `longhun-api-gateway.py` 中增加对应路由，返回 `~/.longhun/memory/latest_digest.json` 的内容。


### ✅ 最快修复：直接让 Kimi 读取本地文件

```bash
# 1. 确认本地记忆文件存在
ls -la ~/.longhun/memory/latest_digest.json

# 2. 如果文件不存在，先生成
python3 08_BIN/lh_memory_bootstrap.py

# 3. 配置 Kimi 跳过远程同步，直接读取本地文件
# 编辑 ~/.kimi/config.yaml，添加：
memory:
  source: local
  path: ~/.longhun/memory/latest_digest.json
```


### 🔁 如果坚持用 9622 服务，在网关中补全该端点

在 `longhun-api-gateway.py` 中添加一个简单路由，返回记忆文件内容，然后重启服务，让 `kimi` 的请求不再 404。你现在先确认 `~/.longhun/memory/latest_digest.json` 文件是否存在，如果存在，Kimi 就可以直接读本地，无需依赖端口服务。这样更快也更稳定。🔥🐉
</think>好的，Kimi 读记忆出现 404 是因为 `9622` 端口上的服务在运行，但记忆同步脚本在请求一个不存在的路径 `/api/xiaoyi/ask`。解决思路是让 Kimi 直接读取本地的记忆文件，而不是依赖一个可能挂掉的服务。你只需要确认 `~/.longhun/memory/latest_digest.json` 这个文件存在，然后配置 Kimi 读本地就行了。如果文件不存在，先执行 `lh_memory_bootstrap.py` 生成。这样就绕开了 404 问题，而且更稳定，不需要依赖网络服务。🔥🐉

---

*归档于 2026-08-15T13:23:23+08:00 · DNA `#龍芯⚡️丙午·丙申·辛酉·未时·䷔噬嗑-CLIPBOARD-VAULT-SAVE-V1.0-P1-ae3dc89c`*
