---
dna: '#龍芯⚡️丙午·丙申·辛酉·未时·䷢晋-CLIPBOARD-VAULT-SAVE-V1.0-P1-990c21b0'
source: clipboard
topic: 代码/脚本
tags:
- Python
- Bash
- FastAPI
- Notion
- 龍魂
- DNA
- 审计
- 代码/脚本
timestamp: '2026-08-15T13:26:39+08:00'
content_hash: 1600a722e0f682b7e1eb3cacb8e1a1ad429b04793d067fc8f1a92f6f0315e707
parent_dna: []
vault_version: v1.0
---

# 剪贴内容

# 🐉 龍魂 · 主权网关集成架构 v1.0

**DNA:** `#龍芯⚡️丙午·丙酉·丙寅·申时-SOVEREIGN-GATEWAY-UID9622`
**确认码:** `#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z`
**GPG:** `A2D0092CEE2E5BA87035600924C3704A8CC26D5F`
**三色:** 🟢 通过
**分层许可:** 思想层 CC BY-NC-SA 4.0 · 工程层 MulanPSL v2


## 📋 核心判断

> **你是唯一主权人。你的身份锚定在鲲鹏服务器上。所有外部工具只能通过主权代理接口访问，不能直连。**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    📡 外部工具层（只能访问代理接口）                       │
│                                                                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐                │
│  │  Kimi    │  │CodeBuddy │  │  Notion  │  │  Mac本地 │                │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘                │
│       │             │             │             │                         │
│       └─────────────┴─────────────┴─────────────┘                         │
│                                    │                                      │
│                                    ▼                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                 主权代理网关 (Sovereign Gateway)                    │  │
│  │  • 验证 DNA + GPG 签名                                             │  │
│  │  • 检查主权身份 (UID9622)                                          │  │
│  │  • 三色审计 + 史官记录                                             │  │
│  │  • 只转发已认证的请求                                              │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                    │                                      │
│                                    ▼                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                  🐉 鲲鹏服务器 (主权核心)                          │  │
│  │  • 主权身份锚定 (UID9622)                                          │  │
│  │  • 所有工具集成接口                                                │  │
│  │  • 数据主存储 (不出境)                                             │  │
│  │  • 唯一可直连人: UID9622                                           │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```


## 🏛️ 一、主权网关架构

### 1.1 接入规则

| 接入方 | 连接方式 | 权限 |
|:---|:---|:---|
| **你 (UID9622)** | SSH + 直连 API | ✅ 完整主权权限 |
| **Kimi** | 通过主权代理接口 | ⚠️ 受限，需 DNA 验证 |
| **CodeBuddy** | 通过主权代理接口 | ⚠️ 受限，需 DNA 验证 |
| **Notion** | 通过主权代理接口 | ⚠️ 受限，需 DNA 验证 |
| **Mac 本地** | 通过主权代理接口 | ⚠️ 受限，需 DNA 验证 |
| **其他人** | ❌ 拒绝 | 无权限 |


## 🧬 二、实现代码

### 2.1 主权代理网关 `08_BIN/lh_sovereign_gateway.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
🐉 龍魂 · 主权代理网关 v1.0
DNA: #龍芯⚡️丙午·丙酉·丙寅·申时-SOVEREIGN-GATEWAY-UID9622
确认码: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
GPG: A2D0092CEE2E5BA87035600924C3704A8CC26D5F
三色: 🟢 通过

功能:
  1. 所有外部工具只能通过此网关访问鲲鹏
  2. 验证请求携带 DNA + GPG 签名
  3. 检查主权身份 (UID9622)
  4. 三色审计 + 史官记录
  5. 非授权请求直接拒绝
"""

import os
import sys
import json
import hashlib
import hmac
import time
from datetime import datetime
from pathlib import Path
from typing import Dict, Optional, Any
from functools import wraps
import subprocess

from fastapi import FastAPI, Request, HTTPException, Depends
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import JSONResponse
import uvicorn

# ============================================================
# 主权锚定
# ============================================================

UID = "9622"
CONFIRM = "#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z"
GPG = "A2D0092CEE2E5BA87035600924C3704A8CC26D5F"
DNA_PREFIX = "#龍芯⚡️"

SOVEREIGN_DEVICE = {
    "hostname": "鲲鹏服务器",
    "ip": "119.13.90.27",
    "owner": "UID9622",
    "owner_name": "诸葛鑫",
    "since": "2025-06-01",
    "status": "🟢 主权锚定"
}

# 授权代理
AUTHORIZED_PROXIES = {
    "kimi": {"id": "kimi", "allowed": True, "rate": 100},
    "codebuddy": {"id": "codebuddy", "allowed": True, "rate": 50},
    "notion": {"id": "notion", "allowed": True, "rate": 30},
    "mac_local": {"id": "mac_local", "allowed": True, "rate": 200},
}


# ============================================================
# 认证与审计
# ============================================================

def generate_dna(suffix: str = "") -> str:
    timestamp = datetime.now().strftime("%Y-%m-%d")
    rand = hashlib.sha256(f"{suffix}{timestamp}{time.time()}".encode()).hexdigest()[:8].upper()
    return f"{DNA_PREFIX}{timestamp}-{suffix}-{rand}-{UID}"


def verify_dna(dna: str) -> bool:
    """验证 DNA 是否属于主权体系"""
    return dna.startswith(DNA_PREFIX) and UID in dna


def verify_gpg_signature(data: str, signature: str) -> bool:
    """验证 GPG 签名"""
    try:
        with open("/tmp/verify_data.txt", "w") as f:
            f.write(data)
        with open("/tmp/verify_sig.asc", "w") as f:
            f.write(signature)
        result = subprocess.run(
            ["gpg", "--verify", "/tmp/verify_sig.asc", "/tmp/verify_data.txt"],
            capture_output=True,
            text=True,
            timeout=5
        )
        return result.returncode == 0 and "Good signature" in result.stdout
    except Exception:
        return False


async def authenticate_request(request: Request) -> Dict:
    """认证请求 - 所有外部请求必须携带 DNA 和 GPG 签名"""
    headers = request.headers

    dna = headers.get("X-Dragon-DNA")
    signature = headers.get("X-Dragon-Signature")
    proxy_id = headers.get("X-Proxy-ID", "unknown")

    # 1. 检查 DNA
    if not dna or not verify_dna(dna):
        raise HTTPException(status_code=401, detail="无效或缺少 DNA 追溯码")

    # 2. 检查代理 ID 是否在授权列表
    if proxy_id not in AUTHORIZED_PROXIES:
        raise HTTPException(status_code=403, detail=f"代理 {proxy_id} 未授权")

    if not AUTHORIZED_PROXIES[proxy_id]["allowed"]:
        raise HTTPException(status_code=403, detail=f"代理 {proxy_id} 已被禁用")

    # 3. 检查 GPG 签名
    if not signature:
        raise HTTPException(status_code=401, detail="缺少 GPG 签名")

    # 构建验证数据
    body = await request.body()
    data = f"{dna}{body.decode()}{CONFIRM}"
    if not verify_gpg_signature(data, signature):
        raise HTTPException(status_code=401, detail="GPG 签名验证失败")

    # 4. 记录审计
    await record_audit({
        "timestamp": datetime.now().isoformat(),
        "proxy_id": proxy_id,
        "dna": dna,
        "path": request.url.path,
        "method": request.method,
        "status": "authenticated"
    })

    return {
        "dna": dna,
        "proxy_id": proxy_id,
        "authenticated": True,
        "timestamp": datetime.now().isoformat()
    }


async def record_audit(entry: Dict):
    """记录审计日志"""
    audit_path = Path("/opt/longhun-system/04_AUDIT/gateway_audit.jsonl")
    audit_path.parent.mkdir(parents=True, exist_ok=True)
    with open(audit_path, "a", encoding="utf-8") as f:
        f.write(json.dumps(entry, ensure_ascii=False) + "\n")


async def record_shame(reason: str, dna: str, details: Dict):
    """记录到耻辱墙"""
    shame_path = Path("/opt/longhun-system/08_STATE/shame_wall.jsonl")
    shame_path.parent.mkdir(parents=True, exist_ok=True)
    entry = {
        "timestamp": datetime.now().isoformat(),
        "reason": reason,
        "dna": dna,
        "details": details,
        "severity": "HIGH"
    }
    with open(shame_path, "a", encoding="utf-8") as f:
        f.write(json.dumps(entry, ensure_ascii=False) + "\n")


# ============================================================
# FastAPI 应用
# ============================================================

app = FastAPI(
    title="龍魂主权代理网关",
    version="1.0.0",
    dna="#龍芯⚡️丙午·丙酉·丙寅·申时-SOVEREIGN-GATEWAY-UID9622",
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


# ============================================================
# 端点
# ============================================================

@app.get("/")
async def root():
    """网关状态"""
    return {
        "service": "龍魂主权代理网关",
        "status": "🟢 运行中",
        "dna": app.dna,
        "confirm": CONFIRM,
        "sovereign": SOVEREIGN_DEVICE,
        "message": "只有 UID9622 可直连，所有代理需验证 DNA + GPG 签名"
    }


@app.get("/api/sovereign/status")
async def sovereign_status():
    """主权状态"""
    return {
        "sovereign": SOVEREIGN_DEVICE,
        "gateway_version": "v1.0",
        "dna": app.dna,
        "authorized_proxies": len(AUTHORIZED_PROXIES),
        "audit_enabled": True,
        "shame_wall_enabled": True
    }


@app.post("/api/proxy/{target}")
async def proxy_request(target: str, request: Request, auth: Dict = Depends(authenticate_request)):
    """代理转发 - 所有外部工具通过此端点访问鲲鹏服务"""
    proxy_id = auth["proxy_id"]
    dna = auth["dna"]

    # 检查目标服务是否存在
    target_services = {
        "kimi": {"port": 8765, "service": "Kimi桥接"},
        "notion": {"port": 8766, "service": "Notion同步"},
        "memory": {"port": 8767, "service": "记忆服务"},
        "clipboard": {"port": 8768, "service": "剪贴板容器"},
        "knowledge": {"port": 8769, "service": "知识图谱"},
        "ollama": {"port": 11434, "service": "Ollama推理"},
    }

    if target not in target_services:
        await record_shame(
            f"未授权目标: {target}",
            dna,
            {"proxy_id": proxy_id, "target": target}
        )
        raise HTTPException(status_code=404, detail=f"目标服务 {target} 不存在")

    service = target_services[target]

    # 记录审计
    await record_audit({
        "timestamp": datetime.now().isoformat(),
        "proxy_id": proxy_id,
        "dna": dna,
        "target": target,
        "service": service["service"],
        "status": "forwarded"
    })

    # 转发请求到目标服务
    try:
        import httpx
        body = await request.body()
        async with httpx.AsyncClient(timeout=30) as client:
            response = await client.request(
                method=request.method,
                url=f"http://127.0.0.1:{service['port']}{request.url.path}",
                headers={
                    "X-Dragon-DNA": dna,
                    "X-Proxy-ID": proxy_id,
                },
                content=body,
            )
            return JSONResponse(
                status_code=response.status_code,
                content=response.json()
            )
    except Exception as e:
        await record_shame(
            f"转发失败: {target}",
            dna,
            {"proxy_id": proxy_id, "error": str(e)}
        )
        raise HTTPException(status_code=503, detail=f"服务 {target} 不可用")


@app.get("/api/audit")
async def get_audit(limit: int = 100):
    """查看审计日志"""
    audit_path = Path("/opt/longhun-system/04_AUDIT/gateway_audit.jsonl")
    if not audit_path.exists():
        return {"entries": [], "count": 0}

    entries = []
    with open(audit_path, "r", encoding="utf-8") as f:
        for line in f:
            if line.strip():
                entries.append(json.loads(line))
    return {
        "entries": entries[-limit:],
        "count": len(entries)
    }


@app.get("/api/shame")
async def get_shame():
    """查看耻辱墙"""
    shame_path = Path("/opt/longhun-system/08_STATE/shame_wall.jsonl")
    if not shame_path.exists():
        return {"entries": [], "count": 0}

    entries = []
    with open(shame_path, "r", encoding="utf-8") as f:
        for line in f:
            if line.strip():
                entries.append(json.loads(line))
    return {"entries": entries, "count": len(entries)}


# ============================================================
# 启动
# ============================================================

if __name__ == "__main__":
    print("""
╔══════════════════════════════════════════════════════════════╗
║  🐉 龍魂 · 主权代理网关 v1.0                               ║
╠══════════════════════════════════════════════════════════════╣
║  DNA: #龍芯⚡️丙午·丙酉·丙寅·申时-SOVEREIGN-GATEWAY-UID9622║
║  确认码: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z              ║
║  主权人: UID9622 · 诸葛鑫                                  ║
║  主权设备: 鲲鹏服务器 (119.13.90.27)                       ║
║  状态: 🟢 运行中                                           ║
╠══════════════════════════════════════════════════════════════╣
║  📡 网关地址: 0.0.0.0:8766                                 ║
║  🔐 认证方式: DNA + GPG 签名                               ║
║  📋 审计: 已启用                                           ║
║  📋 耻辱墙: 已启用                                         ║
╚══════════════════════════════════════════════════════════════╝
    """)
    uvicorn.run(app, host="0.0.0.0", port=8766)
```


## 🚀 三、部署到鲲鹏

```bash
#!/bin/bash
# 🐉 主权代理网关部署脚本
# 执行方式: 在鲲鹏服务器上运行

echo "🐉 部署主权代理网关..."

# 1. 复制网关代码
cd /opt/longhun-system
cat > 08_BIN/lh_sovereign_gateway.py << 'EOF'
# [粘贴上面的完整代码]
EOF

# 2. 安装依赖
pip install fastapi uvicorn httpx

# 3. 创建目录
mkdir -p 04_AUDIT 08_STATE

# 4. 启动网关
nohup python3 08_BIN/lh_sovereign_gateway.py > /var/log/longhun/gateway.log 2>&1 &

# 5. 验证
sleep 2
curl http://127.0.0.1:8766/

echo "✅ 主权代理网关已部署"
```


## 🔑 四、代理接入配置

### 4.1 Kimi 接入

```yaml
# ~/.kimi/config.yaml
longhun:
  gateway: "http://鲲鹏IP:8766"
  proxy_id: "kimi"
  dna: "你的DNA码"
  gpg_key: "你的GPG密钥"
```

### 4.2 CodeBuddy 接入

```json
// ~/.codebuddy/settings.json
{
  "longhun": {
    "gateway": "http://鲲鹏IP:8766",
    "proxy_id": "codebuddy",
    "dna": "你的DNA码",
    "gpg_key": "你的GPG密钥"
  }
}
```

### 4.3 Mac 本地接入

```bash
# 设置环境变量
export LONGHUN_GATEWAY="http://鲲鹏IP:8766"
export LONGHUN_PROXY_ID="mac_local"
export LONGHUN_DNA="你的DNA码"
export LONGHUN_GPG_KEY="你的GPG密钥"
```


## 📋 五、验证清单

```bash
# 1. 检查网关状态
curl http://鲲鹏IP:8766/

# 2. 检查主权状态
curl http://鲲鹏IP:8766/api/sovereign/status

# 3. 测试代理访问 (需携带 DNA 和签名)
curl -X POST http://鲲鹏IP:8766/api/proxy/memory \
  -H "X-Dragon-DNA: #龍芯⚡️..." \
  -H "X-Proxy-ID: mac_local" \
  -H "X-Dragon-Signature: ..."

# 4. 查看审计日志
curl http://鲲鹏IP:8766/api/audit

# 5. 查看耻辱墙
curl http://鲲鹏IP:8766/api/shame
```


## 🔐 六、最终签名

```
═══════════════════════════════════════════════════
 🐉 龍魂 · 主权网关集成架构 v1.0 · 最终签名
═══════════════════════════════════════════════════
DNA:        #龍芯⚡️丙午·丙酉·丙寅·申时-SOVEREIGN-GATEWAY-UID9622
确认码:      #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
GPG:        A2D0092CEE2E5BA87035600924C3704A8CC26D5F
三色:       🟢 通过
核心:       主权代理网关
集成:       Kimi / CodeBuddy / Notion / Mac
部署:       鲲鹏服务器
═══════════════════════════════════════════════════
```

🐉 **丙午·丙酉·丙寅·申时·䷬萃·🟢**

---

*归档于 2026-08-15T13:26:39+08:00 · DNA `#龍芯⚡️丙午·丙申·辛酉·未时·䷢晋-CLIPBOARD-VAULT-SAVE-V1.0-P1-990c21b0`*
