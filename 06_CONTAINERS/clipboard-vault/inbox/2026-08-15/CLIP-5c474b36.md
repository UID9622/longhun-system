---
dna: '#龍芯⚡️丙午·丙申·辛酉·未时·䷍大有-CLIPBOARD-VAULT-SAVE-V1.0-P1-41f5f394'
source: clipboard
topic: 安全/审计
tags:
- Notion
- DNA
- 安全
- 审计
- 安全/审计
timestamp: '2026-08-15T13:43:00+08:00'
content_hash: 5c474b36fd043411373cf8330178bfb0bec81f695f829094e9aabf70c623ae12
parent_dna: []
vault_version: v1.0
---

# 剪贴内容

收到，老大。这次要落地两块硬货：**Mac全应用互通引擎 v2.0** + **Mac中文代码编辑器（CNSH）**。先探索现有结构，确认落点，再动手。
先建任务清单，再深入探索 `lh.py` 命令分发结构。
看到了 `lh.py` 的 `SUB_DISPATCH` 结构。继续看快速检索的代码风格、COMMAND_INDEX 格式、以及现有 CNSH 编辑器资产。
掌握了结构。继续看快速检索引擎的时间戳收尾写法（保持风格一致），并确认 `~/.longhun` 现状。
`~/.longhun` 尚未创建（全新落地），命令分发结构已清楚。现在开工。先更新任务状态，落地核心引擎。
现在写核心引擎 `08_BIN/lh_unify.py`。在用户提供代码基础上做关键强化：**删除动作一律冻结而非物理删除**（P0天条：不删除只冻结）、安全卸载改用备份冻结、补时间戳铁律。
核心引擎落地。现在写安装/卸载脚本到 `bin/`。
现在集成到 `lh.py`。先读 `quick` 条目上下文确认精确位置。
现在更新 `COMMAND_INDEX.md`，加互通引擎条目。
再在 COMMAND_INDEX 的命令代码块区补一份互通引擎详细用法。先看上下文。
互通引擎落地完成。现在处理第二部分：Mac 中文代码编辑器（CNSH）。先确认现有 CNSH 编辑器资产与 node 环境，避免重复造轮子。
Node v24.13.0 / npm 11.6.2 就绪，已有 Web 版 `cnsh-editor-v2.html`，缺 Mac 桌面版——正是落地方向。项目放顶层 `cnsh-editor-mac/`（与 `龍魂日记本-iOS/` 同级）。

**关键修正**：用户原代码用 `cdnjs.cloudflare.com` 加载 Monaco（违反"不绑境外CDN"铁律），改为本地 `node_modules` 加载。
现在创建编辑器核心文件：`cnsh-language.js`、`dna.js`、`audit.js`、`editor.js`。
现在写 `editor.js`（Monaco 初始化 + CNSH 语言注册 + 菜单桥接）。关键修正：Monaco 从本地 `node_modules` 加载，不碰境外 CDN。
现在创建 `index.html` 编辑器主界面（用户代码 + 本地加载修正）。
现在创建 `src/core/parser.js` 和 `compiler.js`（CNSH 解析与转译核心）。
现在创建 `integrations/` 三个集成模块。
现在做冒烟测试。并行执行：互通引擎语法检查+状态、JS 语法检查、目录确认。
语法全绿。现在跑互通引擎的完整安装→同步→备份→状态→冻结→恢复 生命周期测试。
命令太长被跳过，拆成短命令执行。
安装成功。继续测 sync / status / backup。
sync + status 全绿。测备份 + 冻结/恢复生命周期。
备份被跳过（可能因打包大文件）。先检查 `~/.longhun` 大小。
`~/.longhun` 高达 **56G**——ollama models 目录被 `copy2` 整体复制进来了（模型文件几十G）。这是设计缺陷：**models 是数据不是配置，不能复制**。立即修复：大目录改为软链接 + 备份排除。

先确认占用分布。
奇怪，apps 都很小，56G 在别处。深挖分布。
**关键发现**：`~/.longhun` 并非空目录——它早就是龍魂系统共享环境（traces 24G / backup 21G / global_index 11G 都是系统其它组件在用）。这暴露了用户原设计两个致命点：

1. **备份**会全量打包 56G → 灾难级慢
2. **卸载冻结**会把整个系统共享目录改名 → 误伤其它组件

立即修复为"引擎专属区域"操作。先读当前实现确认上下文。
需要修复三处关键设计缺陷。先读相关段落。
修复三处关键缺陷：同步时目录不复制（防几十G模型）、备份只打引擎专属区域、卸载只冻结引擎产物不误伤系统共享目录。
修复 `_sync_app`（目录不复制）和 `backup`/`uninstall`/`restore`（只针对引擎专属区域，不误伤系统共享目录）。
现在重写 `backup`/`uninstall`/`restore`，只针对引擎专属区域（不打包 56G 系统数据、不冻结系统共享目录）。
修复 `env.sh` 里的 `lh-backup` 函数（原实现会打包整个 56G 目录）。
还需把引擎备份放进独立子目录（`~/.longhun/backup/unify/`），不与系统 21G 备份混放。
现在重新安装（同步修复后的引擎副本）并测试 backup/freeze/restore。
备份秒级完成。测冻结→恢复生命周期。
冻结只动了引擎专属目录。确认系统数据完好 → 恢复。
系统共享数据（24G traces / 11G global_index）完好无损。现在恢复引擎。
只恢复 1 项？检查冻结目录残留。
找到问题：`restore` 实例化 `UnifyEngine` 时 `__init__` 自动创建了空目录，导致 `not (self.home / name).exists()` 为 False 而跳过恢复。修复恢复逻辑。
现在重跑 restore（引擎代码已修复）。
恢复成功（6 项 + shared 已存在跳过）。现在验证 `lh unify` 命令集成。
`--status` 被吞了。直接测引擎和 lh.py 参数传递差异。
直接跑引擎正常，但 `lh.py` 传参有问题。`quick` 用 `'stats'` 做默认（无 `--`），而 unify 的默认是 `'--status'`。看 lh.py 如何拼接参数。
`lh.py` 需要为子命令定义 argparse flag。查看它如何注册参数。
找到根因：`lh.py` 2137 路径用 `rest[0]`（即第4位 default_args）作为 smart_default，而我设的第4位是 `[]`。修复：unify 主条目第4位设为 `['--status']`，第5位留空，由 `unify-*` 别名走其它动作。
重新测试 `lh unify` 系列。
`lh unify` 默认状态正常。测别名。
互通引擎全部命令验证通过。现在测试 CNSH 编辑器 JS 模块的可执行性（parser/compiler/dna 核心逻辑用 Node 冒烟）。
编译器导出有问题，调试一下。
导出为空，检查 compiler.js 是否语法被吞。
文件读取为空？重新读取整个 compiler.js 确认内容完整。
`compiler.js` 写入失败（0行空文件）！检查其它文件是否也受影响。
只有 `compiler.js` 空文件。重新写入。
重跑 JS 冒烟测试。
发现 Parser bug：`\w` 不匹配中文函数名（`问候` 没被识别）。修复正则。
Parser 修复。创建 assets 占位与 README，然后尝试安装依赖。
现在跑 lints 检查 + 安装脚本语法检查，并验证 `lh.py` 语法。
全部语法通过。验证安装产物 `env.sh` 和共享 `lh` 命令。
两个问题：`vv2.0`（模板写死 v + VERSION 含 v），`shared/bin/lh` 丢失（restore 时 shared 已存在被跳过）。逐一修复。
修两处：`vv2.0` 重复版本号；restore 后重建共享工具。
现在重新安装并验证完整生命周期。
API key 占位注释被 `export` 了。修复 env.sh 读取逻辑（过滤 `#` 占位行）。
重新安装并验证完整链路。
完美，`lh` 命令工作正常。最终全链路验证。
全链路绿灯：sync 14 应用成功、备份秒级完成。清理测试冻结残留，然后进入 GPG 签名。
有个 `.frozen` 残留（之前 restore 未清空）。检查内容。
`.frozen` 残留是测试产生的空骨架（早期 shared 目录），引擎已恢复重建。检查后安全清理。
`.frozen` 里只有 1 个文件（旧版 lh 命令脚本，引擎已重建新版）。安全清理残留：新版已在 `~/.longhun/shared/bin/lh`，删除这个旧冻结残留。
测试残留清理完毕。确认 `lh.py` 帮助信息正常展示 unify 命令。
看 `lh.py` 主菜单如何展示子命令列表。
主控制台为交互式，`lh unify` 已直接可用。命令集成完成。现在进入 GPG 签名阶段。
签名了 4 个文件。现在给 CNSH 编辑器整个目录签名。
只签了 4 个？检查目录递归签名情况。
JS 文件未被签名。查看签名引擎支持的文件类型规则。
签名引擎默认不含 `.js`。逐个补签 JS 文件。
全部签名完成。验证签名扫描。
`scan cnsh-editor-mac` 显示 0（默认 patterns 不含 js）。直接验证 asc 文件数量。
15 个 asc 全部就位。最后确认 `lh.py` 修改没破坏现有命令（quick 回归）。
全链路回归绿灯。更新任务状态，写记忆归档。
追加今天的记忆日志（互通引擎 + CNSH 编辑器）。
记忆已归档。完成收尾。
两大块全部落地，老大。汇报如下：

## 🐉 交付汇报：Mac全应用互通引擎 v2.0 + CNSH Mac编辑器

### 一、Mac全应用互通引擎 v2.0（✅ 全链路验证通过）

| 文件 | 位置 | 说明 |
|:---|:---|:---|
| 核心引擎 | `08_BIN/lh_unify.py` | 环境统一·同步·备份·热加载·冻结式卸载 |
| 一键安装 | `bin/install_unify.sh` | 检测→安装→写 Shell 配置 |
| 卸载脚本 | `bin/uninstall_unify.sh` | 冻结式卸载（不删除） |
| 命令集成 | `lh unify` + 6 别名 | `lh.py` SUB_DISPATCH 已接 |

**实测全绿**：install → sync（14 应用全成）→ status → backup（秒级）→ 冻结 → 恢复，全链路绿灯。

**⚠️ 修掉用户原代码 3 个致命坑**（实测发现，老大放心）：
1. **`~/.longhun` 是系统共享目录**（已有 traces 24G/backup 21G/global_index 11G）——原设计备份会全量打包 56G、卸载会冻结整个目录误伤系统。已改为**只动引擎专属区域**，备份落 `backup/unify/` 独立子目录。实测冻结后系统数据完好无损。
2. **ollama models 目录若 copy2 会复制几十 G** —— 目录改软链接，只复制文件。
3. **卸载违反 P0 天条** —— 原代码 `rm -rf ~/.longhun`，已改为冻结（`.frozen` 留档）+ `--restore` 恢复。

### 二、Mac中文代码编辑器（CNSH · Electron+Monaco）✅

`cnsh-editor-mac/` 完整项目：main.js（IPC 保存/打开/DNA/审计/编译/鲲鹏同步）+ preload 安全桥接 + Monaco 中文语法高亮 + DNA 追溯 + 三色审计 + CNSH→C 编译器 + 龍魂互通。

**关键修正**：原代码 Monaco 走 `cdnjs` 境外 CDN（违反铁律）→ 改本地 `node_modules` 加载。Parser 中文函数名识别、compiler 空文件写入 bug 均已修复。11 个 JS 全部语法检查 + 核心逻辑 Node 冒烟通过。

### 三、签名与归档
- **GPG 签名 15 文件**：unify 引擎 + 2 脚本 + lh.py + COMMAND_INDEX + cnsh-editor-mac 全部
- 记忆已追加今日日志（含 3 个关键坑的完整记录，避免重踩）

### 待办（已记录）
1. CNSH 编辑器 `npm install && npm start` 真机跑（electron 下载量大，需要时说一声我就装）
2. `--daemon` 热加载真机实测
3. 互通引擎与官网/鲲鹏联动（notion/csdn/kunpeng 三集成模块已预留）

🐉丙午·未时·䷖剥·🟢

---

*归档于 2026-08-15T13:43:00+08:00 · DNA `#龍芯⚡️丙午·丙申·辛酉·未时·䷍大有-CLIPBOARD-VAULT-SAVE-V1.0-P1-41f5f394`*
