name: ohos_audit
description: 🐉 龙魂·鸿蒙安全审计插件 - 网络/存储/权限三方扫描
version: 2.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      openharmony:
        pluginClass: OhosAuditPlugin
        fileName: OhosAuditPlugin.ets
