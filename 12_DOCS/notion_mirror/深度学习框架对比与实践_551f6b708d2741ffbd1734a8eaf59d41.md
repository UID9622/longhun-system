# 🧠 深度学习框架对比与实践

> Notion URL: https://app.notion.com/p/551f6b708d2741ffbd1734a8eaf59d41
> Created: 2025-09-09T11:06:00.000Z
> Last edited: 2025-09-24T00:21:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
# 🧠 深度学习框架完全对比指南
## 🔥 PyTorch vs TensorFlow 对比
### 📊 框架对比表
## 🔥 PyTorch 实战指南
### 基础张量操作
```python
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import DataLoader

# 创建张量
x = torch.tensor([[1, 2], [3, 4]], dtype=torch.float32)
y = torch.randn(2, 2)

# 基本运算
z = torch.matmul(x, y)  # 矩阵乘法
z = x + y               # 元素相加
z = x.view(-1)          # 变形

# GPU加速
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
x = x.to(device)
```
### 构建神经网络
```python
class SimpleNet(nn.Module):
    def __init__(self, input_size, hidden_size, num_classes):
        super(SimpleNet, self).__init__()
        self.fc1 = nn.Linear(input_size, hidden_size)
        self.relu = nn.ReLU()
        self.fc2 = nn.Linear(hidden_size, num_classes)
        self.dropout = nn.Dropout(0.5)
    
    def forward(self, x):
        x = self.fc1(x)
        x = self.relu(x)
        x = self.dropout(x)
        x = self.fc2(x)
        return x

# 实例化模型
model = SimpleNet(input_size=784, hidden_size=128, num_classes=10)
model = model.to(device)
```
## 🌟 TensorFlow/Keras 实战
### Keras Sequential模型
```python
import tensorflow as tf
from tensorflow import keras
from tensorflow.keras import layers

# 构建模型
model = keras.Sequential([
    layers.Dense(128, activation='relu', input_shape=(784,)),
    layers.Dropout(0.5),
    layers.Dense(64, activation='relu'),
    layers.Dropout(0.5),
    layers.Dense(10, activation='softmax')
])

# 编译模型
model.compile(
    optimizer='adam',
    loss='sparse_categorical_crossentropy',
    metrics=['accuracy']
)
```
## 🎯 CNN卷积神经网络实践
### PyTorch CNN
```python
class CNN(nn.Module):
    def __init__(self):
        super(CNN, self).__init__()
        # 卷积层
        self.conv1 = nn.Conv2d(1, 32, kernel_size=3, padding=1)
        self.conv2 = nn.Conv2d(32, 64, kernel_size=3, padding=1)
        
        # 池化层
        self.pool = nn.MaxPool2d(kernel_size=2, stride=2)
        
        # 全连接层
        self.fc1 = nn.Linear(64 * 7 * 7, 128)
        self.fc2 = nn.Linear(128, 10)
        
        # Dropout
        self.dropout = nn.Dropout(0.5)
    
    def forward(self, x):
        # 第一个卷积块
        x = self.pool(torch.relu(self.conv1(x)))
        # 第二个卷积块
        x = self.pool(torch.relu(self.conv2(x)))
        
        # 扁平化
        x = x.view(-1, 64 * 7 * 7)
        
        # 全连接层
        x = torch.relu(self.fc1(x))
        x = self.dropout(x)
        x = self.fc2(x)
        
        return x
```
## 🎯 框架选择建议
### 选择PyTorch的场景
- 🔬 科研和实验：快速原型开发
- 🐛 调试密集：需要频繁调试模型
- 📚 学习阶段：更直观的编程体验
- 🔄 动态模型：模型结构需要动态变化
### 选择TensorFlow的场景
- 🏭 生产部署：工业级应用
- 📱 移动端：手机、嵌入式设备
- ☁️ 云服务：与Google Cloud集成
- 👥 大团队：标准化开发流程
> 💡 学习建议：先掌握一个框架的核心概念，再学习另一个框架会事半功倍！
