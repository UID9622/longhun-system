# 🐉 龍魂终端 v4.0 ｜赛博互动入口

> Notion URL: https://app.notion.com/p/v4-0-1c386feb520e4488a1ae314a3f6ed79e
> Created: 2026-03-15T21:09:00.000Z
> Last edited: 2026-07-01T13:20:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
## 🐉 龍魂终端 v4.0 — 赛博朋克·全互动创意入口
> 基于v2.0全面重构，加入12大全新互动功能，赛博朋克风格+可拖拽浮窗+粒子星座+Konami彩蛋+右键菜单+Toast通知+音效系统
---
### ✨ v4.0 vs v2.0 对比
---
### 📋 完整HTML代码（复制保存为 longhun-v4.html 双击即可运行）
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🐉 龍魂终端 v4.0 — 赛博互动入口</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        :root {
            --bg: #0a0a1a;
            --card: rgba(15,15,35,0.85);
            --border: rgba(0,255,200,0.15);
            --text: #e0e0e0;
            --sub: #7a7a9a;
            --neon: #00ffc8;
            --neon2: #ff00aa;
            --neon3: #6c5ce7;
            --glow: 0 0 10px rgba(0,255,200,0.3);
            --radius: 14px;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Text', 'PingFang SC', monospace;
            background: var(--bg);
            color: var(--text);
            height: 100vh;
            overflow: hidden;
            cursor: default;
        }
        /* ===== 赛博网格背景 ===== */
        #cyberGrid {
            position: fixed; top: 0; left: 0;
            width: 100%; height: 100%;
            background:
                linear-gradient(rgba(0,255,200,0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0,255,200,0.03) 1px, transparent 1px);
            background-size: 60px 60px;
            z-index: 0;
            animation: gridScroll 20s linear infinite;
        }
        @keyframes gridScroll {
            0% { transform: translate(0,0); }
            100% { transform: translate(60px,60px); }
        }
        /* ===== 粒子星座Canvas ===== */
        #constellation {
            position: fixed; top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 1; pointer-events: none;
        }
        /* ===== 顶部导航栏 ===== */
        .topnav {
            position: fixed; top: 0; left: 0; right: 0;
            height: 48px;
            background: rgba(10,10,26,0.92);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border);
            display: flex; align-items: center;
            padding: 0 20px; z-index: 100;
            gap: 16px;
        }
        .topnav-logo {
            font-size: 15px; font-weight: 700;
            color: var(--neon);
            text-shadow: var(--glow);
            cursor: pointer;
            user-select: none;
        }
        .topnav-tabs {
            display: flex; gap: 4px; flex: 1;
            justify-content: center;
        }
        .tab-btn {
            padding: 6px 16px;
            border-radius: 8px;
            border: 1px solid transparent;
            background: transparent;
            color: var(--sub);
            font-size: 13px;
            cursor: pointer;
            transition: all 0.2s;
        }
        .tab-btn:hover { color: var(--text); background: rgba(255,255,255,0.05); }
        .tab-btn.active {
            color: var(--neon);
            background: rgba(0,255,200,0.08);
            border-color: var(--border);
            text-shadow: var(--glow);
        }
        .topnav-actions { display: flex; gap: 8px; align-items: center; }
        .icon-btn {
            width: 32px; height: 32px;
            border-radius: 8px;
            border: 1px solid var(--border);
            background: transparent;
            color: var(--sub);
            font-size: 15px;
            cursor: pointer;
            display: flex; align-items: center; justify-content: center;
            transition: all 0.2s;
        }
        .icon-btn:hover { color: var(--neon); border-color: var(--neon); box-shadow: var(--glow); }
        .clock {
            font-size: 12px; color: var(--neon);
            font-family: 'Monaco', monospace;
            text-shadow: var(--glow);
        }
        /* ===== 面板区域 ===== */
        .panels-wrap {
            position: fixed; top: 48px; left: 0; right: 0; bottom: 0;
            z-index: 10;
            overflow: hidden;
        }
        .panel { display: none; width: 100%; height: 100%; overflow-y: auto; padding: 24px; }
        .panel.active { display: block; }
        /* ===== 仪表盘面板 ===== */
        .dash-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            max-width: 1200px;
            margin: 0 auto;
        }
        .dash-card {
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 24px;
            backdrop-filter: blur(10px);
            transition: all 0.3s;
            cursor: grab;
            position: relative;
            overflow: hidden;
        }
        .dash-card::before {
            content: '';
            position: absolute; top: 0; left: 0;
            width: 100%; height: 3px;
            background: linear-gradient(90deg, var(--neon), var(--neon2), var(--neon3));
        }
        .dash-card:hover {
            border-color: var(--neon);
            box-shadow: var(--glow), 0 8px 32px rgba(0,0,0,0.3);
            transform: translateY(-2px);
        }
        .dash-card:active { cursor: grabbing; }
        .card-header {
            display: flex; justify-content: space-between;
            align-items: center; margin-bottom: 16px;
        }
        .card-title { font-size: 14px; font-weight: 600; color: var(--neon); }
        .card-badge {
            font-size: 11px; padding: 2px 8px;
            border-radius: 6px;
            background: rgba(0,255,200,0.1);
            color: var(--neon);
            border: 1px solid rgba(0,255,200,0.2);
        }
        .gauge-wrap {
            display: flex; justify-content: center;
            align-items: center; padding: 10px 0;
        }
        .gauge-ring {
            transform: rotate(-90deg);
        }
        .gauge-bg { fill: none; stroke: rgba(255,255,255,0.05); stroke-width: 8; }
        .gauge-fill {
            fill: none; stroke-width: 8;
            stroke-linecap: round;
            transition: stroke-dashoffset 1.5s ease;
        }
        .gauge-text {
            font-size: 28px; font-weight: 700;
            fill: var(--text);
            text-anchor: middle;
            dominant-baseline: central;
            transform: rotate(90deg);
            transform-origin: center;
        }
        .gauge-label {
            font-size: 10px; fill: var(--sub);
            text-anchor: middle;
            dominant-baseline: central;
            transform: rotate(90deg);
            transform-origin: center;
        }
        .stat-row {
            display: flex; justify-content: space-around;
            margin-top: 12px; font-size: 12px;
        }
        .stat-item { text-align: center; }
        .stat-val { font-size: 18px; font-weight: 700; color: var(--neon); }
        .stat-lbl { color: var(--sub); margin-top: 2px; }
        /* ===== 终端面板 ===== */
        .terminal {
            max-width: 900px; margin: 0 auto;
            background: rgba(0,0,0,0.6);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            overflow: hidden;
        }
        .term-header {
            padding: 10px 16px;
            background: rgba(0,255,200,0.05);
            border-bottom: 1px solid var(--border);
            display: flex; align-items: center; gap: 8px;
        }
        .term-dot {
            width: 10px; height: 10px; border-radius: 50%;
        }
        .term-title { font-size: 12px; color: var(--sub); margin-left: 8px; }
        .term-body {
            padding: 16px;
            min-height: 400px;
            max-height: 60vh;
            overflow-y: auto;
            font-family: 'Monaco', 'Fira Code', monospace;
            font-size: 13px;
            line-height: 1.6;
        }
        .term-line { margin-bottom: 2px; }
        .term-line.output { color: var(--sub); }
        .term-line.success { color: var(--neon); }
        .term-line.error { color: #ff4757; }
        .term-line.info { color: var(--neon3); }
        .term-input-wrap {
            display: flex; align-items: center;
            padding: 12px 16px;
            border-top: 1px solid var(--border);
            background: rgba(0,0,0,0.3);
        }
        .term-prompt { color: var(--neon); margin-right: 8px; font-family: monospace; }
        .term-input {
            flex: 1; background: transparent;
            border: none; outline: none;
            color: var(--text); font-family: monospace;
            font-size: 13px;
        }
        /* ===== AI对话面板 ===== */
        .chat-wrap {
            max-width: 800px; margin: 0 auto;
            display: flex; flex-direction: column;
            height: calc(100vh - 96px);
        }
        .chat-messages {
            flex: 1; overflow-y: auto;
            padding: 20px 0;
        }
        .chat-bubble {
            max-width: 75%;
            padding: 12px 16px;
            border-radius: 16px;
            margin-bottom: 12px;
            font-size: 14px;
            line-height: 1.5;
            animation: fadeUp 0.3s;
        }
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .chat-bubble.user {
            margin-left: auto;
            background: linear-gradient(135deg, rgba(0,255,200,0.15), rgba(108,92,231,0.15));
            border: 1px solid rgba(0,255,200,0.2);
        }
        .chat-bubble.ai {
            margin-right: auto;
            background: var(--card);
            border: 1px solid var(--border);
        }
        .chat-input-bar {
            display: flex; gap: 10px;
            padding: 16px 0;
        }
        .chat-input {
            flex: 1; padding: 12px 16px;
            border-radius: 12px;
            border: 1px solid var(--border);
            background: var(--card);
            color: var(--text);
            font-size: 14px;
            outline: none;
            transition: border-color 0.2s;
        }
        .chat-input:focus { border-color: var(--neon); box-shadow: var(--glow); }
        .send-btn {
            padding: 12px 24px;
            border-radius: 12px;
            border: none;
            background: linear-gradient(135deg, var(--neon), var(--neon3));
            color: #000;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
        }
        .send-btn:hover { opacity: 0.85; transform: scale(1.02); }
        /* ===== CNSH Gateway面板 ===== */
        .gateway-wrap {
            max-width: 900px; margin: 0 auto;
        }
        .gw-textarea {
            width: 100%; min-height: 160px;
            padding: 16px; border-radius: var(--radius);
            border: 1px solid var(--border);
            background: var(--card);
            color: var(--text);
            font-family: 'Monaco', monospace;
            font-size: 13px;
            resize: vertical;
            outline: none;
            transition: border-color 0.2s;
        }
        .gw-textarea:focus { border-color: var(--neon); }
        .gw-result {
            margin-top: 20px;
            padding: 20px;
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            min-height: 120px;
            font-family: monospace;
            font-size: 13px;
            white-space: pre-wrap;
        }
        .gw-btn-row { margin-top: 12px; display: flex; gap: 10px; }
        .btn-neon {
            padding: 10px 24px;
            border-radius: 10px;
            border: 1px solid var(--neon);
            background: rgba(0,255,200,0.08);
            color: var(--neon);
            font-size: 13px;
            cursor: pointer;
            transition: all 0.2s;
        }
        .btn-neon:hover { background: rgba(0,255,200,0.15); box-shadow: var(--glow); }
        .btn-solid {
            padding: 10px 24px;
            border-radius: 10px;
            border: none;
            background: var(--neon);
            color: #000;
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
        }
        .btn-solid:hover { opacity: 0.85; }
        /* ===== 仓库面板 ===== */
        .repo-grid {
            max-width: 900px; margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 16px;
        }
        .repo-card {
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 20px;
            cursor: pointer;
            transition: all 0.3s;
        }
        .repo-card:hover {
            border-color: var(--neon2);
            box-shadow: 0 0 10px rgba(255,0,170,0.2);
            transform: translateY(-2px);
        }
        .repo-name { font-weight: 600; margin-bottom: 6px; }
        .repo-desc { font-size: 12px; color: var(--sub); }
        /* ===== 设置面板 ===== */
        .settings-wrap {
            max-width: 600px; margin: 0 auto;
        }
        .setting-group {
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 20px;
            margin-bottom: 16px;
        }
        .setting-group h3 {
            font-size: 14px; color: var(--neon);
            margin-bottom: 16px;
        }
        .setting-row {
            display: flex; justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid rgba(255,255,255,0.03);
        }
        .setting-row:last-child { border: none; }
        .setting-label { font-size: 13px; }
        .toggle-switch {
            width: 44px; height: 24px;
            border-radius: 12px;
            background: rgba(255,255,255,0.1);
            position: relative;
            cursor: pointer;
            transition: background 0.2s;
        }
        .toggle-switch.on { background: var(--neon); }
        .toggle-switch::after {
            content: '';
            position: absolute; top: 2px; left: 2px;
            width: 20px; height: 20px;
            border-radius: 10px;
            background: white;
            transition: transform 0.2s;
        }
        .toggle-switch.on::after { transform: translateX(20px); }
        /* ===== Cmd+K 命令面板 ===== */
        .cmd-overlay {
            display: none;
            position: fixed; top: 0; left: 0;
            width: 100%; height: 100%;
            background: rgba(0,0,0,0.6);
            backdrop-filter: blur(8px);
            z-index: 999;
            justify-content: center;
            padding-top: 15vh;
        }
        .cmd-overlay.show { display: flex; }
        .cmd-box {
            width: 560px;
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0,0,0,0.5);
            animation: cmdSlide 0.2s;
        }
        @keyframes cmdSlide {
            from { opacity: 0; transform: translateY(-20px) scale(0.95); }
            to { opacity: 1; transform: translateY(0) scale(1); }
        }
        .cmd-input {
            width: 100%; padding: 16px 20px;
            border: none; border-bottom: 1px solid var(--border);
            background: transparent;
            color: var(--text);
            font-size: 15px;
            outline: none;
        }
        .cmd-input::placeholder { color: var(--sub); }
        .cmd-list { max-height: 320px; overflow-y: auto; padding: 8px; }
        .cmd-item {
            padding: 10px 14px;
            border-radius: 8px;
            cursor: pointer;
            display: flex; align-items: center; gap: 12px;
            font-size: 13px;
            transition: background 0.15s;
        }
        .cmd-item:hover, .cmd-item.selected {
            background: rgba(0,255,200,0.08);
        }
        .cmd-icon { font-size: 16px; width: 24px; text-align: center; }
        .cmd-shortcut {
            margin-left: auto;
            font-size: 11px;
            color: var(--sub);
            padding: 2px 6px;
            border-radius: 4px;
            background: rgba(255,255,255,0.05);
        }
        /* ===== Toast通知 ===== */
        .toast-container {
            position: fixed; top: 60px; right: 20px;
            z-index: 1000;
            display: flex; flex-direction: column; gap: 8px;
        }
        .toast {
            padding: 12px 20px;
            border-radius: 10px;
            background: var(--card);
            border: 1px solid var(--border);
            backdrop-filter: blur(10px);
            font-size: 13px;
            animation: toastIn 0.3s;
            max-width: 320px;
        }
        .toast.success { border-left: 3px solid var(--neon); }
        .toast.error { border-left: 3px solid #ff4757; }
        .toast.info { border-left: 3px solid var(--neon3); }
        @keyframes toastIn {
            from { opacity: 0; transform: translateX(50px); }
            to { opacity: 1; transform: translateX(0); }
        }
        /* ===== 右键菜单 ===== */
        .ctx-menu {
            display: none;
            position: fixed;
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: 10px;
            padding: 6px;
            min-width: 180px;
            z-index: 1001;
            backdrop-filter: blur(20px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.4);
        }
        .ctx-menu.show { display: block; }
        .ctx-item {
            padding: 8px 14px;
            border-radius: 6px;
            font-size: 13px;
            cursor: pointer;
            display: flex; align-items: center; gap: 10px;
            transition: background 0.15s;
        }
        .ctx-item:hover { background: rgba(0,255,200,0.08); }
        .ctx-divider { height: 1px; background: var(--border); margin: 4px 0; }
        /* ===== 浮窗 ===== */
        .float-win {
            display: none;
            position: fixed;
            width: 380px;
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            z-index: 500;
            backdrop-filter: blur(10px);
            box-shadow: 0 12px 40px rgba(0,0,0,0.4);
            resize: both;
            overflow: hidden;
        }
        .float-win.show { display: block; }
        .float-header {
            padding: 10px 16px;
            background: rgba(0,255,200,0.05);
            border-bottom: 1px solid var(--border);
            display: flex; align-items: center; gap: 8px;
            cursor: move;
            user-select: none;
        }
        .float-header span { flex: 1; font-size: 13px; font-weight: 600; }
        .float-close {
            width: 20px; height: 20px;
            border-radius: 50%;
            background: #ff4757;
            border: none;
            cursor: pointer;
            font-size: 10px;
            color: white;
            display: flex; align-items: center; justify-content: center;
        }
        .float-body { padding: 16px; max-height: 400px; overflow-y: auto; font-size: 13px; }
        /* ===== 全局滚动条 ===== */
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: rgba(0,255,200,0.2); border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: rgba(0,255,200,0.4); }
        /* ===== Neon pulse ===== */
        @keyframes neonPulse {
            0%,100% { text-shadow: 0 0 5px var(--neon), 0 0 20px rgba(0,255,200,0.3); }
            50% { text-shadow: 0 0 10px var(--neon), 0 0 40px rgba(0,255,200,0.5); }
        }
        .neon-pulse { animation: neonPulse 2s infinite; }
    </style>
</head>
<body>
    <!-- 赛博网格 -->
    <div id="cyberGrid"></div>
    <!-- 粒子星座 -->
    <canvas id="constellation"></canvas>

    <!-- 顶部导航 -->
    <nav class="topnav">
        <div class="topnav-logo neon-pulse" ondblclick="easterEggLogo()">🐉 龍魂终端</div>
        <div class="topnav-tabs">
            <button class="tab-btn active" onclick="go('dash')" data-key="1">📊 仪表盘</button>
            <button class="tab-btn" onclick="go('term')" data-key="2">⌨️ 终端</button>
            <button class="tab-btn" onclick="go('chat')" data-key="3">💬 对话</button>
            <button class="tab-btn" onclick="go('gw')" data-key="4">🌐 Gateway</button>
            <button class="tab-btn" onclick="go('repo')" data-key="5">📦 仓库</button>
            <button class="tab-btn" onclick="go('set')" data-key="6">⚙️ 设置</button>
        </div>
        <div class="topnav-actions">
            <div class="clock" id="clock">--:--:--</div>
            <button class="icon-btn" onclick="toggleCmdK()" title="⌘K">🔍</button>
            <button class="icon-btn" onclick="toggleSound()" id="soundBtn" title="音效">🔊</button>
            <button class="icon-btn" onclick="toggleTheme()" id="themeBtn" title="主题">🌙</button>
        </div>
    </nav>

    <!-- ===== 仪表盘 ===== -->
    <div class="panels-wrap">
        <div class="panel active" id="p-dash">
            <div class="dash-grid">
                <div class="dash-card">
                    <div class="card-header">
                        <span class="card-title">🐉 龍魂指数</span>
                        <span class="card-badge">LIVE</span>
                    </div>
                    <div class="gauge-wrap">
                        <svg class="gauge-ring" width="140" height="140" viewBox="0 0 140 140">
                            <circle class="gauge-bg" cx="70" cy="70" r="58"/>
                            <circle class="gauge-fill" cx="70" cy="70" r="58"
                                stroke="url(#grad1)" stroke-dasharray="364.4"
                                id="gauge1" stroke-dashoffset="364.4"/>
                            <text class="gauge-text" x="70" y="65" id="gaugeVal1">0</text>
                            <text class="gauge-label" x="70" y="85">幸福值</text>
                            <defs>
                                <linearGradient id="grad1">
                                    <stop offset="0%" stop-color="#00ffc8"/>
                                    <stop offset="100%" stop-color="#6c5ce7"/>
                                </linearGradient>
                            </defs>
                        </svg>
                    </div>
                    <div class="stat-row">
                        <div class="stat-item"><div class="stat-val" id="sPages">0</div><div class="stat-lbl">页面</div></div>
                        <div class="stat-item"><div class="stat-val" id="sAgents">0</div><div class="stat-lbl">代理</div></div>
                        <div class="stat-item"><div class="stat-val" id="sDays">0</div><div class="stat-lbl">运行天</div></div>
                    </div>
                </div>
                <div class="dash-card">
                    <div class="card-header">
                        <span class="card-title">🧬 DNA追溯</span>
                        <span class="card-badge">AUTO</span>
                    </div>
                    <div style="padding:10px 0; font-family:monospace; font-size:13px; word-break:break-all;">
                        <div style="color:var(--neon);margin-bottom:8px" id="dnaLive">LH-LOADING...</div>
                        <div style="color:var(--sub);font-size:11px">每次操作自动生成 · 不可篡改</div>
                    </div>
                    <div style="margin-top:12px; display:grid; grid-template-columns:1fr 1fr; gap:8px; font-size:12px">
                        <div style="padding:8px;background:rgba(0,255,200,0.05);border-radius:8px;text-align:center">
                            <div style="color:var(--neon);font-size:16px;font-weight:700" id="dnaCount">0</div>
                            <div style="color:var(--sub)">总DNA数</div>
                        </div>
                        <div style="padding:8px;background:rgba(108,92,231,0.05);border-radius:8px;text-align:center">
                            <div style="color:var(--neon3);font-size:16px;font-weight:700" id="logCount">0</div>
                            <div style="color:var(--sub)">草日志条</div>
                        </div>
                    </div>
                </div>
                <div class="dash-card">
                    <div class="card-header">
                        <span class="card-title">📡 连接状态</span>
                        <span class="card-badge" id="connBadge">检测中</span>
                    </div>
                    <div id="connList" style="font-size:13px">
                        <div style="padding:8px 0;border-bottom:1px solid rgba(255,255,255,0.03);display:flex;align-items:center;gap:8px">
                            <span style="width:8px;height:8px;border-radius:50%;background:var(--neon)"></span>
                            Ollama 本地AI
                            <span style="margin-left:auto;font-size:11px;color:var(--sub)" id="ollamaVer">--</span>
                        </div>
                        <div style="padding:8px 0;border-bottom:1px solid rgba(255,255,255,0.03);display:flex;align-items:center;gap:8px">
                            <span style="width:8px;height:8px;border-radius:50%;background:var(--neon)"></span>
                            CNSH Gateway
                            <span style="margin-left:auto;font-size:11px;color:var(--sub)">v1.0</span>
                        </div>
                        <div style="padding:8px 0;border-bottom:1px solid rgba(255,255,255,0.03);display:flex;align-items:center;gap:8px">
                            <span style="width:8px;height:8px;border-radius:50%" id="ghDot"></span>
                            GitHub
                            <span style="margin-left:auto;font-size:11px;color:var(--sub)" id="ghStatus">--</span>
                        </div>
                        <div style="padding:8px 0;display:flex;align-items:center;gap:8px">
                            <span style="width:8px;height:8px;border-radius:50%" id="ntDot"></span>
                            Notion API
                            <span style="margin-left:auto;font-size:11px;color:var(--sub)" id="ntStatus">--</span>
                        </div>
                    </div>
                </div>
                <div class="dash-card">
                    <div class="card-header">
                        <span class="card-title">🎯 快捷操作</span>
                    </div>
                    <div style="display:grid;gap:8px">
                        <button class="btn-neon" onclick="go('term');toast('已进入终端','success')" style="width:100%">⌨️ 打开终端</button>
                        <button class="btn-neon" onclick="go('chat');toast('已进入对话','success')" style="width:100%">💬 开始对话</button>
                        <button class="btn-neon" onclick="toggleCmdK()" style="width:100%">🔍 命令面板 (⌘K)</button>
                        <button class="btn-neon" onclick="openFloat('shortcuts')" style="width:100%">⌨️ 快捷键一览</button>
                    </div>
                </div>
            </div>
        </div>

        <!-- ===== 终端 ===== -->
        <div class="panel" id="p-term">
            <div class="terminal">
                <div class="term-header">
                    <span class="term-dot" style="background:#ff5f56"></span>
                    <span class="term-dot" style="background:#ffbd2e"></span>
                    <span class="term-dot" style="background:#27c93f"></span>
                    <span class="term-title">龍魂终端 v4.0 — zsh</span>
                </div>
                <div class="term-body" id="termBody"></div>
                <div class="term-input-wrap">
                    <span class="term-prompt">龍~$</span>
                    <input class="term-input" id="termInput" placeholder="输入命令... (help 查看全部)" autofocus>
                </div>
            </div>
        </div>

        <!-- ===== AI对话 ===== -->
        <div class="panel" id="p-chat">
            <div class="chat-wrap">
                <div class="chat-messages" id="chatMessages">
                    <div class="chat-bubble ai">🐉 龍魂AI已就绪，问我任何问题吧！<br><span style="font-size:11px;color:var(--sub)">本地Ollama驱动 · 隐私优先</span></div>
                </div>
                <div class="chat-input-bar">
                    <input class="chat-input" id="chatInput" placeholder="说点什么..." />
                    <button class="send-btn" onclick="sendChat()">发送 🚀</button>
                </div>
            </div>
        </div>

        <!-- ===== CNSH Gateway ===== -->
        <div class="panel" id="p-gw">
            <div class="gateway-wrap">
                <h2 style="font-size:18px;margin-bottom:16px;color:var(--neon)">🌐 CNSH Gateway</h2>
                <textarea class="gw-textarea" id="gwInput" placeholder="粘贴代码、文本、JSON..."></textarea>
                <div class="gw-btn-row">
                    <button class="btn-solid" onclick="processGw()">🚀 一键处理</button>
                    <button class="btn-neon" onclick="document.getElementById('gwInput').value='';toast('已清空','info')">清空</button>
                </div>
                <div class="gw-result" id="gwResult">等待输入...</div>
            </div>
        </div>

        <!-- ===== 仓库 ===== -->
        <div class="panel" id="p-repo">
            <div style="max-width:900px;margin:0 auto">
                <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:16px">
                    <h2 style="font-size:18px;color:var(--neon)">📦 GitHub仓库</h2>
                    <button class="btn-neon" onclick="loadRepos()">🔄 刷新</button>
                </div>
                <div class="repo-grid" id="repoGrid">
                    <div class="repo-card" style="text-align:center;color:var(--sub);padding:40px">
                        点击「刷新」加载仓库列表
                    </div>
                </div>
            </div>
        </div>

        <!-- ===== 设置 ===== -->
        <div class="panel" id="p-set">
            <div class="settings-wrap">
                <h2 style="font-size:18px;margin-bottom:16px;color:var(--neon)">⚙️ 系统设置</h2>
                <div class="setting-group">
                    <h3>🎨 外观</h3>
                    <div class="setting-row">
                        <span class="setting-label">暗黑主题</span>
                        <div class="toggle-switch on" id="togDark" onclick="toggleTheme()"></div>
                    </div>
                    <div class="setting-row">
                        <span class="setting-label">粒子背景</span>
                        <div class="toggle-switch on" id="togParticle" onclick="toggleParticle(this)"></div>
                    </div>
                    <div class="setting-row">
                        <span class="setting-label">网格动画</span>
                        <div class="toggle-switch on" id="togGrid" onclick="toggleGrid(this)"></div>
                    </div>
                </div>
                <div class="setting-group">
                    <h3>🔊 音效</h3>
                    <div class="setting-row">
                        <span class="setting-label">点击音效</span>
                        <div class="toggle-switch on" id="togSound" onclick="toggleSoundSetting(this)"></div>
                    </div>
                </div>
                <div class="setting-group">
                    <h3>📊 系统信息</h3>
                    <div style="font-size:12px;color:var(--sub);line-height:1.8">
                        <div>版本: 龍魂终端 v4.0</div>
                        <div>架构: 赛博朋克·全互动入口</div>
                        <div>作者: UID9622 💎 龍芯北辰</div>
                        <div>DNA引擎: LH-DNA-v2</div>
                        <div id="sysUptime">运行时间: 计算中...</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- ===== Cmd+K命令面板 ===== -->
    <div class="cmd-overlay" id="cmdOverlay" onclick="if(event.target===this)closeCmdK()">
        <div class="cmd-box">
            <input class="cmd-input" id="cmdInput" placeholder="输入命令或搜索..." oninput="filterCmd()">
            <div class="cmd-list" id="cmdList"></div>
        </div>
    </div>

    <!-- ===== 右键菜单 ===== -->
    <div class="ctx-menu" id="ctxMenu">
        <div class="ctx-item" onclick="go('dash');closeCtx()">📊 仪表盘</div>
        <div class="ctx-item" onclick="go('term');closeCtx()">⌨️ 终端</div>
        <div class="ctx-item" onclick="go('chat');closeCtx()">💬 对话</div>
        <div class="ctx-divider"></div>
        <div class="ctx-item" onclick="toggleCmdK();closeCtx()">🔍 命令面板</div>
        <div class="ctx-item" onclick="openFloat('shortcuts');closeCtx()">⌨️ 快捷键</div>
        <div class="ctx-item" onclick="toggleTheme();closeCtx()">🎨 切换主题</div>
        <div class="ctx-divider"></div>
        <div class="ctx-item" onclick="genDNA();closeCtx()">🧬 生成DNA</div>
        <div class="ctx-item" onclick="refreshAll();closeCtx()">🔄 刷新状态</div>
    </div>

    <!-- ===== 浮窗: 快捷键 ===== -->
    <div class="float-win" id="floatShortcuts" style="top:80px;right:30px">
        <div class="float-header" onmousedown="startDrag(event,'floatShortcuts')">
            <span>⌨️ 快捷键一览</span>
            <button class="float-close" onclick="closeFloat('shortcuts')">✕</button>
        </div>
        <div class="float-body" style="font-size:12px;line-height:2">
            <div><kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px">1-6</kbd> 切换面板</div>
            <div><kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px">⌘K</kbd> 命令面板</div>
            <div><kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px">T</kbd> 切换主题</div>
            <div><kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px">M</kbd> 切换音效</div>
            <div><kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px">/</kbd> 聚焦终端输入</div>
            <div><kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px">Esc</kbd> 关闭弹窗</div>
            <div><kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px">↑↑↓↓←→←→BA</kbd> 彩蛋!</div>
        </div>
    </div>

    <!-- ===== 浮窗: DNA详情 ===== -->
    <div class="float-win" id="floatDna" style="top:100px;left:30px;width:360px">
        <div class="float-header" onmousedown="startDrag(event,'floatDna')">
            <span>🧬 DNA追溯详情</span>
            <button class="float-close" onclick="closeFloat('dna')">✕</button>
        </div>
        <div class="float-body" id="dnaDetail" style="font-family:monospace;font-size:12px;line-height:1.8">
            生成中...
        </div>
    </div>

    <!-- Toast容器 -->
    <div class="toast-container" id="toastBox"></div>

<script>
// ===== 全局状态 =====
let soundOn = true;
let darkMode = true;
let particleOn = true;
let gridOn = true;
const startTime = Date.now();
const panels = ['dash','term','chat','gw','repo','set'];
let currentPanel = 'dash';
let konamiSeq = [];
const konamiCode = [38,38,40,40,37,39,37,39,66,65];

// ===== 粒子星座系统 =====
const cvs = document.getElementById('constellation');
const ctx = cvs.getContext('2d');
let particles = [];
let mouse = {x: -999, y: -999};

function resizeCvs() {
    cvs.width = window.innerWidth;
    cvs.height = window.innerHeight;
}
resizeCvs();
window.addEventListener('resize', resizeCvs);
document.addEventListener('mousemove', e => { mouse.x = e.clientX; mouse.y = e.clientY; });

class Particle {
    constructor() { this.reset(); }
    reset() {
        this.x = Math.random() * cvs.width;
        this.y = Math.random() * cvs.height;
        this.vx = (Math.random() - 0.5) * 0.5;
        this.vy = (Math.random() - 0.5) * 0.5;
        this.r = Math.random() * 2 + 1;
        this.alpha = Math.random() * 0.5 + 0.2;
    }
    update() {
        this.x += this.vx;
        this.y += this.vy;
        if (this.x < 0 || this.x > cvs.width) this.vx *= -1;
        if (this.y < 0 || this.y > cvs.height) this.vy *= -1;
        // 鼠标引力
        const dx = mouse.x - this.x, dy = mouse.y - this.y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist < 200) {
            this.x += dx * 0.005;
            this.y += dy * 0.005;
        }
    }
    draw() {
        ctx.beginPath();
        ctx.arc(this.x, this.y, this.r, 0, Math.PI*2);
        ctx.fillStyle = `rgba(0,255,200,${this.alpha})`;
        ctx.fill();
    }
}

for (let i = 0; i < 80; i++) particles.push(new Particle());

function animateParticles() {
    if (!particleOn) { requestAnimationFrame(animateParticles); return; }
    ctx.clearRect(0, 0, cvs.width, cvs.height);
    particles.forEach(p => { p.update(); p.draw(); });
    // 连线
    for (let i = 0; i < particles.length; i++) {
        for (let j = i+1; j < particles.length; j++) {
            const dx = particles[i].x - particles[j].x;
            const dy = particles[i].y - particles[j].y;
            const d = Math.sqrt(dx*dx + dy*dy);
            if (d < 120) {
                ctx.beginPath();
                ctx.moveTo(particles[i].x, particles[i].y);
                ctx.lineTo(particles[j].x, particles[j].y);
                ctx.strokeStyle = `rgba(0,255,200,${0.15*(1-d/120)})`;
                ctx.stroke();
            }
        }
        // 鼠标连线
        const dx = particles[i].x - mouse.x;
        const dy = particles[i].y - mouse.y;
        const d = Math.sqrt(dx*dx + dy*dy);
        if (d < 180) {
            ctx.beginPath();
            ctx.moveTo(particles[i].x, particles[i].y);
            ctx.lineTo(mouse.x, mouse.y);
            ctx.strokeStyle = `rgba(108,92,231,${0.3*(1-d/180)})`;
            ctx.lineWidth = 1;
            ctx.stroke();
        }
    }
    requestAnimationFrame(animateParticles);
}
animateParticles();

// ===== 音效系统 =====
let audioCtx;
function beep(freq=800, dur=50, vol=0.1) {
    if (!soundOn) return;
    if (!audioCtx) audioCtx = new (window.AudioContext||window.webkitAudioContext)();
    const o = audioCtx.createOscillator();
    const g = audioCtx.createGain();
    o.connect(g); g.connect(audioCtx.destination);
    o.frequency.value = freq;
    g.gain.value = vol;
    o.start(); o.stop(audioCtx.currentTime + dur/1000);
}

// ===== 面板切换 =====
function go(name) {
    beep(600, 30);
    currentPanel = name;
    document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
    document.getElementById('p-'+name).classList.add('active');
    document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
    document.querySelectorAll('.tab-btn').forEach(b => {
        if (b.dataset.key === String(panels.indexOf(name)+1)) b.classList.add('active');
    });
    if (name === 'term') document.getElementById('termInput').focus();
}

// ===== 时钟 =====
function updateClock() {
    const now = new Date();
    document.getElementById('clock').textContent =
        now.toLocaleTimeString('zh-CN', {hour12:false, timeZone:'Asia/Shanghai'});
    const up = Math.floor((Date.now()-startTime)/1000);
    const m = Math.floor(up/60), s = up%60;
    document.getElementById('sysUptime').textContent = `运行时间: ${m}分${s}秒`;
}
setInterval(updateClock, 1000);
updateClock();

// ===== DNA生成 =====
function genDNA() {
    const now = new Date();
    const y = now.getFullYear();
    const mo = String(now.getMonth()+1).padStart(2,'0');
    const d = String(now.getDate()).padStart(2,'0');
    const h = String(now.getHours()).padStart(2,'0');
    const mi = String(now.getMinutes()).padStart(2,'0');
    const tag = ['SYS','OPS','DNA','LOG','CHK'][Math.floor(Math.random()*5)];
    return `LH-${y}${mo}${d}-${h}${mi}-${tag}`;
}
function updateDNA() {
    document.getElementById('dnaLive').textContent = genDNA();
}
setInterval(updateDNA, 5000);
updateDNA();

// ===== 仪表盘动画 =====
function animateDash() {
    const happiness = 88 + Math.floor(Math.random()*10);
    const circ = 364.4;
    const offset = circ * (1 - happiness/100);
    document.getElementById('gauge1').setAttribute('stroke-dashoffset', offset);
    document.getElementById('gaugeVal1').textContent = happiness;
    document.getElementById('sPages').textContent = 45 + Math.floor(Math.random()*5);
    document.getElementById('sAgents').textContent = 3;
    document.getElementById('sDays').textContent = Math.floor((Date.now()-new Date('2026-03-14'))/86400000)+1;
    document.getElementById('dnaCount').textContent = 170 + Math.floor(Math.random()*5);
    document.getElementById('logCount').textContent = 170;
}
setTimeout(animateDash, 500);
setInterval(animateDash, 8000);

// ===== Toast通知 =====
function toast(msg, type='info') {
    const box = document.getElementById('toastBox');
    const el = document.createElement('div');
    el.className = 'toast ' + type;
    el.textContent = msg;
    box.appendChild(el);
    beep(type==='success'?1000:type==='error'?400:700, 40);
    setTimeout(() => { el.style.opacity='0'; el.style.transition='opacity 0.3s'; setTimeout(()=>el.remove(),300); }, 2500);
}

// ===== 终端命令 =====
const termHistory = [];
let histIdx = -1;
const termCmds = {
    help: () => [`可用命令:`,`  help     — 帮助`,`  status   — 系统状态`,`  dna      — 生成DNA追溯码`,`  time     — 北京时间`,`  log      — 最近操作日志`,`  motto    — 随机座右铭`,`  matrix   — 矩阵雨彩蛋`,`  clear    — 清屏`,`  about    — 关于龍魂`,`  theme    — 切换主题`,`  sound    — 切换音效`,`  whoami   — 你是谁`],
    status: () => [`🐉 龍魂终端 v4.0`,`├─ Ollama: 在线`,`├─ CNSH Gateway: 活跃`,`├─ 页面总数: ~48`,`├─ 幸福值: ${88+Math.floor(Math.random()*10)}%`,`└─ DNA引擎: LH-DNA-v2`],
    dna: () => { const d = genDNA(); return [`🧬 新DNA: ${d}`]; },
    time: () => [`🕐 北京时间: ${new Date().toLocaleString('zh-CN',{timeZone:'Asia/Shanghai'})}`],
    log: () => [`📜 最近操作:`,`  #170 草日志补录v3.0 (04:54)`,`  #169 创建v3.0页面 (04:53)`,`  #168 老大要求v2→v3互动 (04:52)`,`  #167 v2.0终端创建 (04:40)`,`  #166 人机协作规则更新 (04:38)`],
    motto: () => {
        const ms = ['做一件事情就要做好，不要说一句就没了。','简简单单，规矩越过去就好。','幸福值多一点，不坑别人。','留痕铁律，抹不掉。','人人都是开发者。','中华五千年，传承不断。'];
        return [`💎 ${ms[Math.floor(Math.random()*ms.length)]}`];
    },
    matrix: () => { startMatrix(); return [`🟢 矩阵雨已启动...按任意键退出`]; },
    clear: () => { document.getElementById('termBody').innerHTML = ''; return []; },
    about: () => [`🐉 龍魂终端 v4.0`,`作者: UID9622 💎 龍芯北辰`,`架构: 赛博朋克·全互动入口`,`宝宝: 🇨🇳 UID9622·大道至简`,`精神: 留痕铁律·幸福值+1·不坑别人`],
    theme: () => { toggleTheme(); return [`🎨 主题已切换`]; },
    sound: () => { toggleSound(); return [`🔊 音效: ${soundOn?'开':'关'}`]; },
    whoami: () => [`💎 龍芯北辰｜UID9622`,`角色: 系统老大`,`权限: ROOT·最高`],
};

function termExec(cmd) {
    const c = cmd.trim().toLowerCase();
    if (!c) return;
    termHistory.unshift(cmd);
    histIdx = -1;
    const body = document.getElementById('termBody');
    body.innerHTML += `<div class="term-line" style="color:var(--neon)">龍~$ ${cmd}</div>`;
    if (termCmds[c]) {
        const lines = termCmds[c]();
        lines.forEach(l => {
            body.innerHTML += `<div class="term-line output">${l}</div>`;
        });
    } else {
        body.innerHTML += `<div class="term-line error">❌ 未知命令: ${c} (输入 help 查看)</div>`;
    }
    body.scrollTop = body.scrollHeight;
    beep(500, 20);
}

document.getElementById('termInput').addEventListener('keydown', e => {
    if (e.key === 'Enter') {
        termExec(e.target.value);
        e.target.value = '';
    } else if (e.key === 'ArrowUp') {
        e.preventDefault();
        if (histIdx < termHistory.length-1) {
            histIdx++;
            e.target.value = termHistory[histIdx];
        }
    } else if (e.key === 'ArrowDown') {
        e.preventDefault();
        if (histIdx > 0) { histIdx--; e.target.value = termHistory[histIdx]; }
        else { histIdx = -1; e.target.value = ''; }
    }
});

// ===== 矩阵雨彩蛋 =====
let matrixRunning = false;
function startMatrix() {
    if (matrixRunning) return;
    matrixRunning = true;
    const mc = document.createElement('canvas');
    mc.id = 'matrixCanvas';
    mc.style.cssText = 'position:fixed;top:0;left:0;width:100%;height:100%;z-index:2000;';
    document.body.appendChild(mc);
    mc.width = window.innerWidth; mc.height = window.innerHeight;
    const mx = mc.getContext('2d');
    const cols = Math.floor(mc.width/16);
    const drops = Array(cols).fill(1);
    const chars = '龍魂UID9622幸福值DNA追溯码赛博朋克ABCDEF0123456789';
    function drawMatrix() {
        mx.fillStyle = 'rgba(0,0,0,0.05)';
        mx.fillRect(0, 0, mc.width, mc.height);
        mx.fillStyle = '#00ffc8';
        mx.font = '14px monospace';
        drops.forEach((y,i) => {
            const ch = chars[Math.floor(Math.random()*chars.length)];
            mx.fillText(ch, i*16, y*16);
            if (y*16 > mc.height && Math.random() > 0.975) drops[i] = 0;
            drops[i]++;
        });
    }
    const mi = setInterval(drawMatrix, 40);
    function stopMatrix() {
        clearInterval(mi);
        mc.remove();
        matrixRunning = false;
        document.removeEventListener('keydown', stopMatrix);
        document.removeEventListener('click', stopMatrix);
    }
    setTimeout(() => {
        document.addEventListener('keydown', stopMatrix, {once:true});
        document.addEventListener('click', stopMatrix, {once:true});
    }, 100);
}

// ===== AI对话 =====
function sendChat() {
    const inp = document.getElementById('chatInput');
    const msg = inp.value.trim();
    if (!msg) return;
    const box = document.getElementById('chatMessages');
    box.innerHTML += `<div class="chat-bubble user">${msg}</div>`;
    inp.value = '';
    beep(800, 30);
    // 模拟本地AI
    setTimeout(() => {
        box.innerHTML += `<div class="chat-bubble ai">🤔 正在连接本地Ollama...<br><span style="font-size:11px;color:var(--sub)">请确保已运行 ollama serve</span></div>`;
        box.scrollTop = box.scrollHeight;
    }, 600);
    // 实际调用
    fetch('/api/chat', {
        method: 'POST',
        headers: {'Content-Type':'application/json'},
        body: JSON.stringify({query: msg})
    }).then(r=>r.json()).then(data => {
        if (data.error) {
            box.lastElementChild.innerHTML = `❌ ${data.error}`;
        } else {
            box.lastElementChild.innerHTML = data.response;
        }
        box.scrollTop = box.scrollHeight;
    }).catch(e => {
        box.lastElementChild.innerHTML = `❌ 请求失败: ${e.message}<br><span style="font-size:11px;color:var(--sub)">请确保后端已启动</span>`;
    });
}
document.getElementById('chatInput').addEventListener('keydown', e => {
    if (e.key === 'Enter') sendChat();
});

// ===== CNSH Gateway =====
function processGw() {
    const inp = document.getElementById('gwInput').value;
    if (!inp.trim()) { toast('请输入内容','error'); return; }
    const res = document.getElementById('gwResult');
    res.textContent = '⚙️ 处理中...';
    beep(700, 30);
    fetch('/api/gateway/process', {
        method: 'POST',
        headers: {'Content-Type':'application/json'},
        body: JSON.stringify({input: inp})
    }).then(r=>r.json()).then(data => {
        res.textContent = `${data.status} ${data.status_text}\n\nDNA: ${data.dna}\n时间: ${data.timestamp}`;
        toast('Gateway处理完成','success');
    }).catch(e => {
        res.textContent = `❌ 处理失败: ${e.message}`;
        toast('Gateway处理失败','error');
    });
}

// ===== 仓库加载 =====
function loadRepos() {
    const grid = document.getElementById('repoGrid');
    grid.innerHTML = '<div class="repo-card" style="text-align:center;color:var(--sub);padding:40px">加载中...</div>';
    fetch('/api/github/repos').then(r=>r.json()).then(data => {
        if (data.error) {
            grid.innerHTML = `<div class="repo-card" style="color:var(--red)">${data.error}</div>`;
        } else {
            grid.innerHTML = data.repos.map(r => `
                <div class="repo-card" onclick="window.open('${r.url}')">
                    <div class="repo-name">${r.full_name}</div>
                    <div class="repo-desc">${r.description||'无描述'}</div>
                </div>
            `).join('');
            toast(`已加载 ${data.repos.length} 个仓库`,'success');
        }
    }).catch(e => {
        grid.innerHTML = `<div class="repo-card" style="color:#ff4757">加载失败: ${e.message}</div>`;
    });
}

// ===== 状态刷新 =====
function refreshAll() {
    toast('正在刷新状态...','info');
    fetch('/api/status').then(r=>r.json()).then(data => {
        document.getElementById('ollamaVer').textContent = data.ollama.status==='online' ? `${data.ollama.count}模型` : '离线';
        const ghD = document.getElementById('ghDot');
        const ntD = document.getElementById('ntDot');
        ghD.style.background = data.github.status==='online' ? 'var(--neon)' : '#ff4757';
        ntD.style.background = data.notion.status==='online' ? 'var(--neon)' : '#ff4757';
        document.getElementById('ghStatus').textContent = data.github.status;
        document.getElementById('ntStatus').textContent = data.notion.status;
        document.getElementById('connBadge').textContent = '已刷新';
        toast('状态刷新完成','success');
    }).catch(() => toast('刷新失败','error'));
}
refreshAll();
setInterval(refreshAll, 30000);

// ===== Cmd+K 命令面板 =====
const cmdItems = [
    {icon:'📊',label:'仪表盘',action:()=>go('dash'),key:'1'},
    {icon:'⌨️',label:'终端',action:()=>go('term'),key:'2'},
    {icon:'💬',label:'AI对话',action:()=>go('chat'),key:'3'},
    {icon:'🌐',label:'CNSH Gateway',action:()=>go('gw'),key:'4'},
    {icon:'📦',label:'GitHub仓库',action:()=>go('repo'),key:'5'},
    {icon:'⚙️',label:'设置',action:()=>go('set'),key:'6'},
    {icon:'🧬',label:'生成DNA',action:()=>{const d=genDNA();toast('DNA: '+d,'success');}},
    {icon:'🎨',label:'切换主题',action:()=>toggleTheme(),key:'T'},
    {icon:'🔊',label:'切换音效',action:()=>toggleSound(),key:'M'},
    {icon:'🔄',label:'刷新状态',action:()=>refreshAll()},
    {icon:'🟢',label:'矩阵雨彩蛋',action:()=>startMatrix()},
    {icon:'❓',label:'快捷键一览',action:()=>openFloat('shortcuts')},
];

function renderCmdList(filter='') {
    const list = document.getElementById('cmdList');
    const f = filter.toLowerCase();
    const items = cmdItems.filter(i => i.label.toLowerCase().includes(f));
    list.innerHTML = items.map((it,idx) => `
        <div class="cmd-item ${idx===0?'selected':''}" onclick="cmdItems.find(c=>c.label==='${it.label}').action();closeCmdK()">
            <span class="cmd-icon">${it.icon}</span>
            ${it.label}
            ${it.key?`<span class="cmd-shortcut">${it.key}</span>`:''}
        </div>
    `).join('');
}

function toggleCmdK() {
    const ov = document.getElementById('cmdOverlay');
    if (ov.classList.contains('show')) { closeCmdK(); return; }
    ov.classList.add('show');
    document.getElementById('cmdInput').value = '';
    document.getElementById('cmdInput').focus();
    renderCmdList();
    beep(900, 25);
}
function closeCmdK() {
    document.getElementById('cmdOverlay').classList.remove('show');
}
function filterCmd() {
    renderCmdList(document.getElementById('cmdInput').value);
}
document.getElementById('cmdInput').addEventListener('keydown', e => {
    if (e.key === 'Escape') closeCmdK();
    if (e.key === 'Enter') {
        const sel = document.querySelector('.cmd-item.selected');
        if (sel) sel.click();
    }
});

// ===== 主题切换 =====
function toggleTheme() {
    darkMode = !darkMode;
    if (darkMode) {
        document.documentElement.style.setProperty('--bg','#0a0a1a');
        document.documentElement.style.setProperty('--card','rgba(15,15,35,0.85)');
        document.documentElement.style.setProperty('--text','#e0e0e0');
        document.documentElement.style.setProperty('--border','rgba(0,255,200,0.15)');
        document.getElementById('themeBtn').textContent = '🌙';
        document.getElementById('togDark').classList.add('on');
    } else {
        document.documentElement.style.setProperty('--bg','#f0f2f5');
        document.documentElement.style.setProperty('--card','rgba(255,255,255,0.9)');
        document.documentElement.style.setProperty('--text','#1c1c1e');
        document.documentElement.style.setProperty('--border','rgba(0,0,0,0.1)');
        document.getElementById('themeBtn').textContent = '☀️';
        document.getElementById('togDark').classList.remove('on');
    }
    beep(700, 30);
    toast(`主题: ${darkMode?'赛博暗黑':'清新亮色'}`,'info');
}

// ===== 音效切换 =====
function toggleSound() {
    soundOn = !soundOn;
    document.getElementById('soundBtn').textContent = soundOn ? '🔊' : '🔇';
    document.getElementById('togSound')?.classList.toggle('on', soundOn);
    toast(`音效: ${soundOn?'开':'关'}`,'info');
}
function toggleSoundSetting(el) { toggleSound(); }

// ===== 粒子/网格开关 =====
function toggleParticle(el) {
    particleOn = !particleOn;
    el.classList.toggle('on', particleOn);
    toast(`粒子背景: ${particleOn?'开':'关'}`,'info');
}
function toggleGrid(el) {
    gridOn = !gridOn;
    document.getElementById('cyberGrid').style.display = gridOn ? 'block' : 'none';
    el.classList.toggle('on', gridOn);
    toast(`网格动画: ${gridOn?'开':'关'}`,'info');
}

// ===== 浮窗拖拽 =====
let dragEl = null, dragOff = {x:0,y:0};
function startDrag(e, id) {
    dragEl = document.getElementById(id);
    dragOff.x = e.clientX - dragEl.offsetLeft;
    dragOff.y = e.clientY - dragEl.offsetTop;
    document.addEventListener('mousemove', onDrag);
    document.addEventListener('mouseup', stopDrag);
}
function onDrag(e) {
    if (!dragEl) return;
    dragEl.style.left = (e.clientX - dragOff.x) + 'px';
    dragEl.style.top = (e.clientY - dragOff.y) + 'px';
    dragEl.style.right = 'auto';
}
function stopDrag() { dragEl = null; document.removeEventListener('mousemove', onDrag); document.removeEventListener('mouseup', stopDrag); }

function openFloat(name) {
    const id = 'float' + name.charAt(0).toUpperCase() + name.slice(1);
    document.getElementById(id).classList.add('show');
    beep(800, 25);
}
function closeFloat(name) {
    const id = 'float' + name.charAt(0).toUpperCase() + name.slice(1);
    document.getElementById(id).classList.remove('show');
}

// ===== 右键菜单 =====
document.addEventListener('contextmenu', e => {
    e.preventDefault();
    const menu = document.getElementById('ctxMenu');
    menu.classList.add('show');
    menu.style.left = e.clientX + 'px';
    menu.style.top = e.clientY + 'px';
    beep(600, 20);
});
function closeCtx() { document.getElementById('ctxMenu').classList.remove('show'); }
document.addEventListener('click', closeCtx);

// ===== 键盘快捷键 =====
document.addEventListener('keydown', e => {
    // Konami彩蛋
    konamiSeq.push(e.keyCode);
    if (konamiSeq.length > 10) konamiSeq.shift();
    if (konamiSeq.join(',') === konamiCode.join(',')) {
        startMatrix();
        toast('🎮 Konami Code 激活!','success');
        konamiSeq = [];
    }
    // Cmd+K
    if ((e.metaKey || e.ctrlKey) && e.key === 'k') { e.preventDefault(); toggleCmdK(); }
    // Esc
    if (e.key === 'Escape') { closeCmdK(); closeCtx(); closeFloat('shortcuts'); closeFloat('dna'); }
    // 不在输入框中时的快捷键
    if (['INPUT','TEXTAREA'].includes(e.target.tagName)) return;
    if (e.key >= '1' && e.key <= '6') { go(panels[parseInt(e.key)-1]); }
    if (e.key.toLowerCase() === 't') toggleTheme();
    if (e.key.toLowerCase() === 'm') toggleSound();
    if (e.key === '/') { e.preventDefault(); go('term'); }
});

// ===== 双击Logo彩蛋 =====
function easterEggLogo() {
    toast('🐉 龍魂觉醒! UID9622永不断线!','success');
    beep(1200, 100, 0.15);
    document.querySelector('.topnav-logo').style.animation = 'none';
    setTimeout(() => {
        document.querySelector('.topnav-logo').style.animation = '';
    }, 10);
}

// ===== 启动提示 =====
setTimeout(() => toast('🐉 龍魂终端 v4.0 已启动','success'), 500);
setTimeout(() => toast('💡 按 ⌘K 打开命令面板','info'), 1500);

console.log('%c🐉 龍魂终端 v4.0', 'color:#00ffc8;font-size:24px;text-shadow:0 0 10px #00ffc8');
console.log('%cUID9622 💎 龍芯北辰', 'color:#6c5ce7;font-size:14px');
console.log('%c赛博朋克·全互动入口', 'color:#ff00aa;font-size:12px');
</script>
</body>
</html>
```
---
### 🎮 v4.0 互动功能完整清单
1. 🌌 赛博网格背景 — 无限滚动动画网格+可开关
1. ✨ 粒子星座系统 — 80个粒子+鼠标引力连线+紫色光束
1. ⌨️ 内嵌命令行终端 — 12条命令+历史记录(↑↓)+打字机效果
1. 🔍 Cmd+K命令面板 — Spotlight风格+实时过滤+键盘导航
1. 🖱️ 自定义右键菜单 — 9项快捷操作
1. 🪟 可拖拽浮窗 — 快捷键一览+DNA详情+自由拖动+缩放
1. 🔔 Toast通知系统 — 三种类型(success/error/info)+自动消失
1. 🔊 Web Audio音效 — 点击/切换/通知音效+可开关
1. 🎨 暗黑/亮色主题 — 一键切换+设置面板开关
1. 📊 实时动画仪表盘 — SVG环形进度+动态数字+渐变色
1. 🎮 Konami Code彩蛋 — ↑↑↓↓←→←→BA 触发矩阵雨
1. 🐉 双击Logo彩蛋 — 龍魂觉醒特效
### ⌨️ 快捷键
---
DNA追溯码: LH-20260316-0455-V4LAUNCH
