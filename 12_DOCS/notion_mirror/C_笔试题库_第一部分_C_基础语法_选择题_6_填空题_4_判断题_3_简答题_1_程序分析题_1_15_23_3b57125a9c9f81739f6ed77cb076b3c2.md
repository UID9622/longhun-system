# 🐉 C# 笔试题库 | | 第一部分：C#基础语法 | 选择题×6、填空题×4、判断题×3、简答题×1、程序分析题×1 | 15 | 23 |

> Notion URL: https://app.notion.com/p/C-C-6-4-3-1-1-15-23-3b57125a9c9f81739f6ed77cb076b3c2
> Created: 2026-08-07T15:31:00.000Z
> Last edited: 2026-08-07T15:40:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
# 🐉 C# 全方位笔试题库 v1.0
> DNA: #龍芯⚡️丙午·甲申·壬寅·坤卦-LEARNING-CSHARP-EXAM-v1.0-UID9622
> 创建者: 诸葛鑫（UID9622）
> 协议: CC BY-NC-SA 4.0（核心思想层）
> GPG: A2D0092CEE2E5BA87035600924C3704A8CC26D5F
> 确认码: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
> 三色: 🟢 通过
> 分层许可: 思想层 CC BY-NC-SA 4.0 · 工程层 MulanPSL v2
---
## 📋 试卷结构
| 部分 | 题型 | 题量 | 分值 |
|---|---|---|---|
| 第一部分：C#基础语法 | 选择题×6、填空题×4、判断题×3、简答题×1、程序分析题×1 | 15 | 23 |
| 第二部分：数据结构与容器 | 选择题×5、填空题×3、判断题×2、简答题×1、程序分析题×1 | 12 | 19 |
| 第三部分：函数/方法/模块 | 选择题×2、填空题×2、判断题×2、简答题×1、编程题×3 | 11 | 21 |
| 第四部分：面向对象/类型系统 | 选择题×3、填空题×3、判断题×2、简答题×2、程序分析题×2、编程题×3 | 15 | 28 |
| 第五部分：高级特性 | 选择题×3、填空题×2、判断题×2、简答题×3、程序分析题×3、编程题×4 | 17 | 34 |
| 第六部分：标准库与工程实践 | 选择题×6、填空题×1、判断题×1、简答题×2、程序分析题×2、编程题×1 | 13 | 25 |
| 第七部分：智力与逻辑 | 智力与逻辑题×6 | 6 | 12 |
| 第八部分：实战演练 | 综合应用题×5、代码调试题×4、系统设计题×6、编程题×1 | 16 | 63 |
| **合计** | **—** | **105** | **225** |
## 📊 详细题型分布
| 题型 | 题量 | 每题分值 | 小计 |
|---|---|---|---|
| 选择题 | 25 | 2 | 50 |
| 填空题 | 15 | 1 | 15 |
| 判断题 | 12 | 1 | 12 |
| 简答题 | 10 | 2 | 20 |
| 程序分析题 | 10 | 2 | 20 |
| 智力与逻辑题 | 6 | 2 | 12 |
| 编程题 | 12 | 3 | 36 |
| 综合应用题 | 5 | 4 | 20 |
| 代码调试题 | 4 | 4 | 16 |
| 系统设计题 | 6 | 4 | 24 |
| **合计** | **105** | **—** | **225** |
## 🎯 知识点分布与难度标注
| 知识点 | 难度 | 分值 |
|---|---|---|
| 基础语法 | ⭐⭐ | 23 |
| 数据类型 | ⭐⭐ | 19 |
| 数据结构 | ⭐⭐⭐ | 21 |
| 函数与模块 | ⭐⭐⭐ | 21 |
| 面向对象 | ⭐⭐⭐⭐ | 28 |
| 高级特性 | ⭐⭐⭐⭐ | 34 |
| 标准库 | ⭐⭐⭐ | 25 |
| 工程实践 | ⭐⭐⭐⭐ | 25 |
| 逻辑思维 | ⭐⭐⭐ | 12 |
| 实战演练 | ⭐⭐⭐⭐⭐ | 63 |
| **合计** | **—** | **225** |
---
# 第一部分：C#基础语法
**题型分布：** 选择题×6、填空题×4、判断题×3、简答题×1、程序分析题×1
## 1. [难度：⭐⭐] 下列关于 `var` 关键字的说法，正确的是？
A. `var` 声明的变量是动态类型，运行时可以改变类型。
B. `var` 只能用于局部变量，编译器会根据右侧表达式推断类型。
C. `var x = null;` 是合法的 C# 语句。
D. `var` 与 `dynamic` 在语义上完全相同。
**答案：B**
**解析：** `var` 是编译期类型推断，变量类型在编译时确定且不可变。A 错误，它并非动态类型；C 错误，`null` 没有类型信息，编译器无法推断；D 错误，`dynamic` 是运行期类型绑定，绕过编译器检查。
## 2. [难度：⭐⭐] 以下哪个是 C# 中合法的字符串插值写法？
A. `string s = "x = {x}";`
B. `string s = $"x = {x}";`
C. `string s = @"x = {x}";`
D. `string s = #"x = {x}";`
**答案：B**
**解析：** `$"..."` 是字符串插值语法，花括号内可嵌入表达式。A 是普通字符串，花括号原样输出；C 是逐字字符串，不处理转义也不插值；D 不是合法的字符串前缀。
## 3. [难度：⭐⭐] 关于 `switch` 表达式（switch expression）和模式匹配，以下说法正确的是？
A. `switch` 表达式必须使用 `break` 结束每个分支。
B. `switch` 表达式可以使用 `_` 作为默认分支。
C. `switch` 表达式只能返回整数类型。
D. `switch` 表达式不支持元组模式。
**答案：B**
**解析：** `switch` 表达式是 C# 8+ 引入的表达式级 switch，使用 `=>` 连接分支，`_` 表示默认分支。A 错误，不需要 `break`；C 错误，可返回任意类型；D 错误，支持元组、属性、关系等多种模式。
## 4. [难度：⭐⭐] 以下代码的输出是？
A. 10
B. 11
C. 12
D. 13
**答案：C**
**解析：** `a++` 先返回 5，然后 `a` 变为 6；`++a` 先将 `a` 变为 7，再返回 7。因此 `b = 5 + 7 = 12`。注意 `a++` 与 `++a` 的副作用发生时机不同。
## 5. [难度：⭐⭐] 在 C# 中，`const` 与 `readonly` 的主要区别是？
A. `const` 在运行期解析，`readonly` 在编译期解析。
B. `const` 只能修饰值类型，`readonly` 只能修饰引用类型。
C. `const` 是编译期常量，其值直接嵌入调用处；`readonly` 可在构造器中赋值，运行期确定。
D. 两者没有任何区别。
**答案：C**
**解析：** `const` 必须在声明时初始化且为编译期可计算的表达式，值会嵌入 IL。`readonly` 字段可在声明时或实例/静态构造器中赋值，运行期才确定。A、B、D 均错误。
## 6. [难度：⭐⭐] 关于 `default` 关键字，下列代码中 `default(int)` 与 `default(string)` 的值分别是？
A. `0` 与 `""`
B. `null` 与 `null`
C. `0` 与 `null`
D. `false` 与 `null`
**答案：C**
**解析：** `default(T)` 返回类型 `T` 的默认值。值类型 `int` 的默认值为 `0`，引用类型 `string` 的默认值为 `null`。空字符串 `""` 不是默认值。
## 7. [难度：⭐] C# 中用于声明一个不可变局部变量的关键字是 ________。
**答案：** `readonly`
**解析：** 在 C# 7.3+ 中，`readonly` 可用于修饰局部变量（通常与 `ref`/`ref readonly` 一起使用）或字段，表示不可重新赋值。对于局部变量直接不可变，更常见的是使用 `const`（编译期常量）或 `readonly` 字段。
## 8. [难度：⭐] 使用 `?.` 运算符可以在访问成员前检查对象是否为 ________。
**答案：** `null`
**解析：** `?.` 是空条件运算符（null-conditional operator），当左侧对象为 `null` 时整个表达式返回 `null`，不会抛出 `NullReferenceException`。
## 9. [难度：⭐] 在 C# 中，`using` 语句用于确保实现了 ________ 接口的对象被正确释放。
**答案：** `IDisposable`
**解析：** `using` 语句在离开作用域时自动调用对象的 `Dispose()` 方法，适用于实现 `IDisposable` 的资源，如文件流、数据库连接等。
## 10. [难度：⭐] 要将一个字符串转换为整数，可以使用 `int` 结构体的 ________ 方法或 `int.Parse`。
**答案：** `TryParse`
**解析：** `int.TryParse(string, out int result)` 在转换失败时返回 `false` 而不抛出异常，比 `int.Parse` 更安全。
## 11. [难度：⭐] `int? x = null;` 中的 `?` 表示 `x` 是可空值类型。
**答案：** ✅正确
**解析：** `int?` 是 `Nullable<int>` 的简写，允许值类型存储 `null`。该语法正确。
## 12. [难度：⭐] 在 C# 中，所有类型都直接或间接继承自 `System.Type`。
**答案：** ❌错误
**解析：** 所有类型都继承自 `System.Object`（或 `object`），`System.Type` 是反射中用于表示类型元数据的类，不是根基类。
## 13. [难度：⭐] `break` 语句只能用于 `switch` 语句中。
**答案：** ❌错误
**解析：** `break` 可用于 `switch`、`for`、`foreach`、`while`、`do-while` 等循环或分支语句中，用于跳出当前结构。
## 14. [难度：⭐⭐] 简述 C# 中 `is` 与 `as` 运算符的区别及适用场景。
**参考答案：**
- `is` 用于检查对象是否是某个类型，返回 `bool`，C# 7+ 支持模式匹配并赋值，如 `if (obj is string s)`。
- `as` 用于将对象转换为指定引用类型或可空值类型，转换失败时返回 `null`，不会抛出异常。
- 适用场景：需要安全类型判断时用 `is`；需要安全向下转换引用类型时用 `as`，避免 `InvalidCastException`。
**解析：** `is` 重在“判断”，`as` 重在“转换”。对引用类型优先使用 `as`，因为它更高效且安全；对值类型应使用 `(T)obj` 或 `is T` 后强制转换。
## 15. [难度：⭐⭐] 阅读以下代码，写出输出结果。
**答案：** `hello`
**解析：** `string` 在 C# 中是不可变的引用类型。`s2 = s1` 让 `s2` 引用与 `s1` 相同的字符串对象；`s1 += " world"` 会创建新的字符串对象并让 `s1` 指向它，`s2` 仍指向原来的 `"hello"`。因此输出 `hello`。
---
# 第二部分：数据结构与容器
**题型分布：** 选择题×5、填空题×3、判断题×2、简答题×1、程序分析题×1
## 16. [难度：⭐⭐] 关于 `List<T>` 与 `Dictionary<TKey, TValue>`，下列说法正确的是？
A. `List<T>` 基于哈希表实现，查找元素时间复杂度为 O(1)。
B. `Dictionary<TKey, TValue>` 基于动态数组实现，按键查找为 O(n)。
C. `List<T>` 按索引访问为 O(1)，但查找值为 O(n)。
D. `Dictionary<TKey, TValue>` 允许存储重复的键。
**答案：C**
**解析：** `List<T>` 基于动态数组，索引访问 O(1)，值查找 O(n)。A 错误；`Dictionary` 基于哈希表，按键查找平均 O(1)，不允许重复键，B、D 错误。
## 17. [难度：⭐⭐] 在 C# 中，`HashSet<T>` 最适合用于以下哪种场景？
A. 需要按键快速查找对应值。
B. 需要存储有序且不重复的元素。
C. 需要判断元素是否存在的集合，且元素唯一。
D. 需要频繁按索引插入和删除元素。
**答案：C**
**解析：** `HashSet<T>` 是无序、不重复的哈希集合，主要用于去重和存在性判断。A 适合 `Dictionary`；B 适合 `SortedSet`；D 适合 `List` 或链表结构。
## 18. [难度：⭐⭐] 关于 `Queue<T>` 与 `Stack<T>`，下列说法正确的是？
A. `Queue<T>` 是后进先出（LIFO），`Stack<T>` 是先进先出（FIFO）。
B. `Queue<T>` 的 `Enqueue` 用于出队，`Dequeue` 用于入队。
C. `Stack<T>` 的 `Pop` 移除并返回栈顶元素。
D. 两者都继承自 `List<T>`。
**答案：C**
**解析：** `Queue<T>` 是 FIFO，`Stack<T>` 是 LIFO；`Enqueue` 入队，`Dequeue` 出队；两者都独立实现，不继承 `List<T>`。只有 C 正确。
## 19. [难度：⭐⭐⭐] 以下代码运行后，`count` 的值是？
A. 6
B. 3
C. 1
D. 0
**答案：B**
**解析：** `HashSet<int>` 会自动去重。原列表有 1、2、3 三个不同值，因此 `set.Count = 3`。
## 20. [难度：⭐⭐⭐] 关于 `Span<T>` 与 `Memory<T>`，下列说法正确的是？
A. `Span<T>` 只能用于托管堆内存。
B. `Memory<T>` 可以在异步方法中安全使用。
C. `Span<T>` 是引用结构，可存储于类字段中。
D. `Memory<T>` 不支持切片操作。
**答案：B**
**解析：** `Span<T>` 是 `ref struct`，只能存在于栈上，不能作为类字段或异步状态机字段。`Memory<T>` 可跨异步边界使用，也支持 `Slice`/`Span` 转换。因此 B 正确，A、C、D 错误。
## 21. [难度：⭐] `Dictionary<TKey, TValue>` 使用 ________ 结构存储键值对。
**答案：** `KeyValuePair<TKey, TValue>`
**解析：** 遍历 `Dictionary<TKey, TValue>` 时，每个元素是 `KeyValuePair<TKey, TValue>`，包含 `Key` 和 `Value` 两个属性。
## 22. [难度：⭐] `Stack<T>` 中用于查看栈顶元素但不移除的方法是 ________。
**答案：** `Peek`
**解析：** `Peek()` 返回栈顶元素但不移除，`Pop()` 返回并移除栈顶元素，`Push(T)` 将元素压入栈顶。
## 23. [难度：⭐⭐] `SortedDictionary<TKey, TValue>` 内部按键的 ________ 顺序维护元素。
**答案：** 排序（或自然/升序）
**解析：** `SortedDictionary` 基于红黑树实现，按键的默认比较器或自定义 `IComparer<TKey>` 排序，插入、删除、查找均为 O(log n)。
## 24. [难度：⭐] `Array.Sort` 方法默认对数组进行升序排序。
**答案：** ✅正确
**解析：** `Array.Sort` 使用默认比较器（或元素实现的 `IComparable`）按升序排列数组元素。如需降序，可传入自定义比较器或调用 `Reverse()`。
## 25. [难度：⭐] `LinkedList<T>` 不支持在任意节点处进行 O(1) 的插入和删除。
**答案：** ❌错误
**解析：** `LinkedList<T>` 提供 `AddAfter`、`AddBefore`、`Remove` 等方法，只要持有 `LinkedListNode<T>` 引用，插入和删除都是 O(1)。
## 26. [难度：⭐⭐] 简述 `IEnumerable<T>` 与 `IEnumerator<T>` 的区别。
**参考答案：**
- `IEnumerable<T>` 表示一个可枚举集合，提供 `GetEnumerator()` 方法返回迭代器。
- `IEnumerator<T>` 是迭代器本身，负责维护当前遍历状态，包含 `Current`、`MoveNext()`、`Reset()`（通常不推荐）。
- `foreach` 循环会调用 `GetEnumerator()` 获取迭代器，然后通过 `MoveNext()`/`Current` 遍历。
**解析：** 一个集合实现 `IEnumerable<T>` 即可被 `foreach` 遍历；`IEnumerator<T>` 负责实际的遍历状态机。LINQ 的延迟执行也依赖于 `IEnumerable<T>` 的惰性求值。
## 27. [难度：⭐⭐] 阅读以下代码，写出输出结果。
**答案：** `3`
**解析：** 字典初始有两个键。`dict["a"] = 10` 修改已有键的值，不增加数量。`ContainsKey("c")` 为 `false`，因此新增 `"c"`，最终 `Count` 为 3。
---
# 第三部分：函数/方法/模块
**题型分布：** 选择题×2、填空题×2、判断题×2、简答题×1、编程题×3
## 28. [难度：⭐⭐] 关于 C# 方法参数修饰符，下列说法错误的是？
A. `ref` 参数要求调用前必须初始化。
B. `out` 参数在方法返回前必须赋值。
C. `in` 参数允许在方法内部修改参数值。
D. `params` 允许传入可变数量的同类型参数。
**答案：C**
**解析：** `in` 参数按引用传递但只读，方法内部不能修改。A、B、D 描述均正确。
## 29. [难度：⭐⭐⭐] 以下代码的输出是？
A. 5
B. 10
C. 编译错误
D. 运行时异常
**答案：B**
**解析：** `ref` 按引用传递，`Modify` 方法内修改会反映到外部变量。`a` 初始为 5，调用后变为 10。
## 30. [难度：⭐] 在 C# 中，使用 ________ 关键字可以定义一个扩展方法。
**答案：** `this`
**解析：** 扩展方法的第一个参数使用 `this` 修饰，如 `public static void Foo(this string s)`。扩展方法必须定义在静态类中。
## 31. [难度：⭐] 局部函数（local function）只能在 ________ 内部声明。
**答案：** 方法（或属性/构造器/代码块）
**解析：** 局部函数是 C# 7 引入的特性，可以在另一个方法、属性访问器、构造器等成员内部声明，具有闭包捕获能力。
## 32. [难度：⭐] C# 支持方法重载（overload），即同一个类中可以有多个同名但参数列表不同的方法。
**答案：** ✅正确
**解析：** 方法重载通过参数数量、类型或顺序区分，与返回类型无关。
## 33. [难度：⭐] `out` 参数在调用方法之前不需要初始化。
**答案：** ✅正确
**解析：** `out` 参数按引用传递，方法内部必须赋值，调用前可以不初始化。与 `ref` 不同，`ref` 要求调用前已初始化。
## 34. [难度：⭐⭐] 简述 C# 中 `static` 方法与普通实例方法的区别。
**参考答案：**
- `static` 方法属于类型本身，不依赖实例，不能访问实例成员（除非通过对象引用）。
- 实例方法属于对象实例，可以访问该实例的实例字段、属性和其他实例方法。
- `static` 方法在类型加载时即可调用，不需要 `new` 创建对象。
**解析：** 静态成员在内存中只有一份，由所有实例共享；实例成员每个对象独立一份。静态方法常用于工具类、工厂方法等。
## 35. [难度：⭐⭐] 编程题：编写一个扩展方法 `IsPalindrome`，判断一个字符串是否为回文（忽略大小写和非字母数字字符）。
**参考答案：**
**解析：** 扩展方法定义在静态类中，第一个参数加 `this`。本例先过滤掉非字母数字字符，再转小写，最后正序与倒序比较。
## 36. [难度：⭐⭐⭐] 编程题：实现一个泛型方法 `Swap<T>`，交换两个同类型变量的值，并用 `ref` 参数演示。
**参考答案：**
**解析：** `ref` 参数按引用传递，方法内部交换会影响外部变量。泛型方法 `Swap<T>` 可同时用于值类型和引用类型。
## 37. [难度：⭐⭐⭐] 编程题：编写一个递归方法计算第 n 个斐波那契数，并附加记忆化优化。
**参考答案：**
**解析：** 递归直接计算斐波那契会有大量重复子问题，时间复杂度 O(2^n)。使用 `Dictionary` 记忆化后降为 O(n)，空间 O(n)。也可使用迭代法进一步优化空间。
---
# 第四部分：面向对象/类型系统
**题型分布：** 选择题×3、填空题×3、判断题×2、简答题×2、程序分析题×2、编程题×3
## 38. [难度：⭐⭐] 关于 C# 中的 `class` 与 `struct`，下列说法正确的是？
A. `struct` 总是分配在栈上。
B. `class` 是值类型，`struct` 是引用类型。
C. `struct` 默认支持无参构造器，可以声明字段时不初始化。
D. `class` 支持继承，`struct` 隐式密封，不能继承其他类型。
**答案：D**
**解析：** `class` 是引用类型，支持单继承；`struct` 是值类型，隐式密封，只能实现接口不能继承类。A 不完全正确，字段为值类型成员时可能在托管堆上；B 说反了；C 错误，C# 10 之前 `struct` 不能显式声明无参构造器。
## 39. [难度：⭐⭐⭐] 以下代码的输出是？
A. Base
B. Derived
C. 编译错误
D. 运行时异常
**答案：B**
**解析：** `Show` 是虚方法，`Derived` 使用 `override` 重写。即使变量类型是 `Base`，实际对象类型是 `Derived`，调用的是重写后的实现，多态性输出 `Derived`。
## 40. [难度：⭐⭐⭐] 关于抽象类与接口，下列说法错误的是？
A. 抽象类可以包含实现代码，接口在 C# 8 之前不能包含实现。
B. 一个类可以实现多个接口，但只能继承一个抽象类。
C. 接口成员默认是 `public abstract`。
D. 接口可以包含字段。
**答案：D**
**解析：** C# 8 之前接口不能包含字段；C# 8+ 接口可以有静态字段或默认实现，但实例字段仍不允许。A、B、C 描述正确。
## 41. [难度：⭐] 在 C# 中，使用 ________ 关键字声明一个抽象方法。
**答案：** `abstract`
**解析：** `abstract` 方法没有方法体，必须在抽象类中声明，由派生类使用 `override` 实现。
## 42. [难度：⭐] 派生类中使用 ________ 关键字调用基类的构造器。
**答案：** `base`
**解析：** 如 `public Derived(int x) : base(x)` 调用基类构造器。类似地，`this` 用于调用本类其他构造器。
## 43. [难度：⭐⭐] 在 C# 9 中，使用 ________ 关键字可以定义一个不可变的引用类型，具有值语义相等性。
**答案：** `record`
**解析：** `record` 用于创建不可变（默认）数据模型，编译器自动生成 `Equals`、`GetHashCode`、`ToString`、解构等成员，支持 `with` 表达式创建副本。
## 44. [难度：⭐] `sealed` 关键字用于修饰类时，表示该类不能被继承。
**答案：** ✅正确
**解析：** `sealed class` 阻止其他类继承。用于方法时，表示该方法不能再被重写。
## 45. [难度：⭐] 接口中所有成员默认都是 `public`，且不能指定访问修饰符。
**答案：** ✅正确
**解析：** 接口成员默认公共访问。C# 8+ 支持显式接口实现和私有默认实现，但普通接口成员仍为 `public`。
## 46. [难度：⭐⭐] 简述 C# 中 `virtual`、`abstract` 与 `override` 的区别。
**参考答案：**
- `virtual`：在基类中声明可重写的方法，已有默认实现。
- `abstract`：在抽象类中声明没有实现的方法，必须被派生类重写。
- `override`：在派生类中重写基类的 `virtual` 或 `abstract` 方法。
- 区别：`virtual` 可有实现，`abstract` 无实现；`override` 是派生类中的具体实现。
**解析：** 三者的组合构成了 C# 的多态体系。`abstract` 方法必须在抽象类中，且派生类若不是抽象类则必须实现它。
## 47. [难度：⭐⭐⭐] 解释 C# 中的装箱（boxing）与拆箱（unboxing），以及如何避免频繁装箱带来的性能开销。
**参考答案：**
- 装箱：将值类型转换为 `object` 或接口类型的引用对象，会在堆上分配新对象。
- 拆箱：将装箱后的对象转回值类型，需要类型检查。
- 开销：频繁装箱导致堆分配和 GC 压力。
- 避免方法：使用泛型（`List<T>` 代替 `ArrayList`）、避免将值类型赋值给 `object`、使用 `ref struct`/`Span<T>` 等栈上结构。
**解析：** 泛型是避免装箱的最有效手段。例如 `List<int>` 直接存储 `int`，而 `ArrayList` 存储 `object` 会导致每个 `int` 装箱。
## 48. [难度：⭐⭐] 阅读以下代码，写出输出结果。
**答案：** `2`
**解析：** `A` 是类（引用类型），`a2 = a1` 只是复制引用，两个变量指向同一对象。修改 `a2.X` 也就是修改 `a1.X`，输出 2。
## 49. [难度：⭐⭐⭐] 阅读以下代码，指出问题并给出修改建议。
**答案：** `Circle` 中的 `Draw` 应使用 `override` 关键字重写基类方法，否则隐藏基类实现。
**解析：** 当前代码编译器会发出警告：成员 `Circle.Draw` 隐藏了继承成员 `Shape.Draw`，应使用 `new` 关键字显式隐藏或使用 `override` 重写。若希望多态行为，应改为 `public override void Draw()`。
## 50. [难度：⭐⭐] 编程题：定义一个抽象类 `Animal` 和一个派生类 `Dog`，演示多态。
**参考答案：**
**解析：** 抽象类 `Animal` 定义抽象方法 `Speak` 和具体方法 `Move`。`Dog` 必须实现 `Speak`。通过基类引用调用派生类方法体现多态。
## 51. [难度：⭐⭐⭐] 编程题：实现一个 `IDisposable` 模式，正确释放托管与非托管资源。
**参考答案：**
**解析：** 标准 `IDisposable` 模式通过 `Dispose(bool disposing)` 区分用户调用与终结器调用。用户调用时释放托管和非托管资源，并通知 GC 不再调用终结器。
## 52. [难度：⭐⭐⭐] 编程题：定义一个 `record` 类型 `Person`，展示 `with` 表达式创建不可变成员副本。
**参考答案：**
**解析：** `record` 的 `with` 表达式基于现有对象创建新实例，只修改指定属性。`record` 使用值语义比较，`p1 == p2` 比较所有属性值。
---
# 第五部分：高级特性
**题型分布：** 选择题×3、填空题×2、判断题×2、简答题×3、程序分析题×3、编程题×4
## 53. [难度：⭐⭐⭐] 关于 C# 泛型约束，下列写法正确的是？
A. `where T : class, struct`
B. `where T : new()` 要求类型参数必须有公共无参构造器。
C. `where T : IComparable, class` 中接口约束不能同时存在。
D. `where T : unmanaged` 表示 `T` 必须是类类型。
**答案：B**
**解析：** `new()` 约束要求类型参数有公共无参构造器。A 错误，`class` 与 `struct` 互斥；C 错误，接口约束可与 `class` 同时存在；D 错误，`unmanaged` 要求非托管值类型。
## 54. [难度：⭐⭐⭐] 关于 LINQ，下列说法正确的是？
A. LINQ 查询语法与方法语法不能混合使用。
B. `Where`、`Select` 等 LINQ 方法会立即执行并返回结果。
C. `foreach` 枚举 LINQ 查询时会触发延迟执行。
D. `ToList()` 是延迟执行方法。
**答案：C**
**解析：** LINQ 大多数操作符是延迟执行的，只有枚举（`foreach`）或调用 `ToList`、`ToArray`、`Count` 等终结操作时才真正执行。A 错误，可以混合；B 错误，`Where` 不立即执行；D 错误，`ToList` 是立即执行。
## 55. [难度：⭐⭐⭐] 关于 `async`/`await`，下列说法错误的是？
A. `async` 方法可以返回 `void`、`Task` 或 `Task<T>`。
B. `await` 会阻塞当前线程直到任务完成。
C. `async` 方法在 `await` 之前同步运行。
D. `Task.Run` 可将工作排到线程池执行。
**答案：B**
**解析：** `await` 不会阻塞线程，它会挂起当前方法并将控制权返回给调用方，任务完成后在合适的上下文恢复。A、C、D 描述正确。
## 56. [难度：⭐⭐] 委托（delegate）是一种类型安全的 ________，可引用一个或多个方法。
**答案：** 函数指针（或方法引用/回调机制）
**解析：** C# 委托是对方法的类型安全封装，支持单播和多播（`+=`、`-=`），是事件、LINQ、回调的基础。
## 57. [难度：⭐⭐] 在 C# 8 中，启用可空引用类型后，使用 ________ 运算符可抑制编译器对可能为 null 的警告。
**答案：** `!`
**解析：** `!` 是空原谅运算符（null-forgiving operator），如 `name!.Length`，告诉编译器此处不会为 null，但运行时若实际为 null 仍会抛异常。
## 58. [难度：⭐] `delegate` 声明的委托类型本身也是引用类型。
**答案：** ✅正确
**解析：** 委托是引用类型，继承自 `System.MulticastDelegate`，其实例引用一个或多个方法。
## 59. [难度：⭐⭐] `record` 类型的 `with` 表达式会修改原对象并返回它。
**答案：** ❌错误
**解析：** `with` 表达式基于原对象创建一个新对象，原对象保持不变。`record` 默认不可变，修改属性通过创建副本实现。
## 60. [难度：⭐⭐⭐] 简述 C# 中事件（event）与委托的区别。
**参考答案：**
- 委托是一种类型，可以存储方法引用，可被外部直接调用或赋值。
- 事件是基于委托的封装，提供 `add`/`remove` 访问器，外部只能订阅（`+=`）和取消订阅（`-=`），不能直接触发或覆盖。
- 事件更安全，遵循发布-订阅模式，常用于 UI、消息总线等场景。
**解析：** 事件本质上是对委托字段的封装，限制外部访问权限，防止调用方随意清空或触发委托链。
