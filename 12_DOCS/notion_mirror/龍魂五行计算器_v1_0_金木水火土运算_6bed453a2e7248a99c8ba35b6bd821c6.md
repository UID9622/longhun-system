# ☯️ 龍魂五行计算器 v1.0 | 金木水火土运算

> Notion URL: https://app.notion.com/p/v1-0-6bed453a2e7248a99c8ba35b6bd821c6
> Created: 2026-04-10T03:38:00.000Z
> Last edited: 2026-07-14T10:59:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
---
## 🎯 在龍魂翻译引擎中的位置
```javascript
翻译引擎六维：
  ① 数字根（6种）
  ② 河洛图·洛书九宫（9个位置）
  ③ 八卦（8种状态）
  ④ 64卦（64种场景）
  ⑤ 五行（5种属性×相生相克）← ⭐ 本页
  ⑥ 天干地支（10×12=120种组合）

总翻译路径：6 × 9 × 8 × 64 × 5 × 120 = 16,588,800种
```
---
## 💻 CNSH中文代码·完整版
```python
# 文件名: 五行计算器完整版.cnsh
# DNA追溯码: #龍芯⚡️2026-03-10-五行计算器-v1.0
# GPG指纹: A2D0092CEE2E5BA87035600924C3704A8CC26D5F
# 创建者: UID9622 诸葛鑫（龍芯北辰）
# 理论指导: 曾仕强老师（永恒显示）

# 五行列表
五行 = ["金", "木", "水", "火", "土"]

# 天干五行对应表
天干五行表 = {
    "甲": "木", "乙": "木",
    "丙": "火", "丁": "火",
    "戊": "土", "己": "土",
    "庚": "金", "辛": "金",
    "壬": "水", "癸": "水"
}

# 地支五行对应表
地支五行表 = {
    "子": "水", "丑": "土", "寅": "木", "卯": "木",
    "辰": "土", "巳": "火", "午": "火", "未": "土",
    "申": "金", "酉": "金", "戌": "土", "亥": "水"
}

# 五行相生关系
五行相生 = {
    "金": "水",  # 金生水
    "水": "木",  # 水生木
    "木": "火",  # 木生火
    "火": "土",  # 火生土
    "土": "金"   # 土生金
}

# 五行相克关系
五行相克 = {
    "金": "木",  # 金克木
    "木": "土",  # 木克土
    "土": "水",  # 土克水
    "水": "火",  # 水克火
    "火": "金"   # 火克金
}
```
---
## 🧠 五行与数学六根的关系
五行相生 = 系统健康循环：
```javascript
金(规则)→生→水(记忆)→生→木(创新)→生→火(文明)→生→土(普惠)→生→金(规则)
循环无断点 = 系统健康
断了一环 = 熔断信号
```
---
## 🔥 多维优化 v2.0｜四大指令全覆盖
---
### ⚖️ 指令① · 五行强度权重计分系统
> 《道德经》第四十章：「天下万物生于有，有生于无。」—— 不是每个五行都一样重，位置决定权重。
核心升级： 同一五行属性，天干年柱 ≠ 地支日柱，位置不同 → 权重不同。
```python
# 指令① · 五行强度权重计分 v2.0
# DNA追溯码：#龍芯⚡️2026-04-11-五行权重-v2.0

位置权重表 = {
    "年柱": {"天干": 1.0, "地支": 0.8},
    "月柱": {"天干": 1.5, "地支": 1.2},
    "日柱": {"天干": 2.0, "地支": 1.6},
    "时柱": {"天干": 1.2, "地支": 1.0},
}

# 输入格式：{"年柱":{"天干":"甲","地支":"子"}, ...}
def 计算五行强度(四柱):
    得分 = {"金": 0.0, "木": 0.0, "水": 0.0, "火": 0.0, "土": 0.0}

    for 柱位, 干支 in 四柱.items():
        权重 = 位置权重表[柱位]
        # 天干加分
        if 干支["天干"] in 天干五行表:
            得分[天干五行表[干支["天干"]]] += 权重["天干"]
        # 地支加分
        if 干支["地支"] in 地支五行表:
            得分[地支五行表[干支["地支"]]] += 权重["地支"]

    总分 = sum(得分.values())
    均值 = 总分 / 5
    方差 = sum((v - 均值)**2 for v in 得分.values()) / 5
    均衡指数 = max(0.0, round(1.0 - (方差**0.5) / (均值 + 0.001), 3))
    缺失 = [k for k, v in 得分.items() if v == 0.0]
    最强 = max(得分, key=得分.get)
    最弱 = min(得分, key=得分.get)

    return {
        "五行得分": 得分,
        "最强": 最强,
        "最弱": 最弱,
        "均衡指数": 均衡指数,  # 越接近1越均衡
        "缺失五行": 缺失
    }

# 示例
示例四柱 = {
    "年柱": {"天干": "甲", "地支": "子"},  # 木·水
    "月柱": {"天干": "丙", "地支": "午"},  # 火·火
    "日柱": {"天干": "庚", "地支": "申"},  # 金·金
    "时柱": {"天干": "壬", "地支": "戌"},  # 水·土
}
结果 = 计算五行强度(示例四柱)
# → 金:5.2 火:4.2 水:2.8 木:1.0 土:1.0 · 缺火不缺但偏 · 均衡指数:0.61
```
---
### 🔗 指令② · 五行动态链路分析（相生相克完整追踪）
> 《易经·系辞上》：「生生之谓易。」—— 生克不是静止的，是流动的链路。
核心升级： 不只看「有没有」，而是追踪「生克链路是否完整·是否断链·是否过旺」。
```python
# 指令② · 五行动态链路分析 v2.0
# DNA追溯码：#龍芯⚡️2026-04-11-五行链路-v2.0

def 分析五行关系(A, B):
    if A == B: return "比和", f"{A}遇{B}·同类叠加·力量加倍"
    if 五行相生.get(A) == B: return "相生", f"{A}生{B}·{A}为源·{B}得增强"
    if 五行相克.get(A) == B: return "相克", f"{A}克{B}·{B}受制约·注意"
    if 五行相生.get(B) == A: return "相泄", f"{B}生{A}反向·{A}被泄耗·消耗自身"
    if 五行相克.get(B) == A: return "相耗", f"{B}克{A}·{A}受压·高消耗"
    return "无关", f"{A}与{B}无直接生克"

def 完整链路分析(得分):
    """分析五行循环链路健康度"""
    相生顺序 = ["金", "水", "木", "火", "土"]  # 金生水→水生木→...
    断链预警 = []
    健康度 = 100

    for i in range(len(相生顺序)):
        来源 = 相生顺序[i]
        目标 = 相生顺序[(i + 1) % 5]
        if 得分.get(来源, 0) > 0 and 得分.get(目标, 0) == 0:
            断链预警.append(f"🔴 断链：{来源}({得分[来源]:.1f})有力但生不出{目标}(0分)·循环裂断")
            健康度 -= 15

    # 过旺检测（某五行得分超过总分40%）
    总分 = sum(得分.values()) + 0.001
    过旺 = [(k, v) for k, v in 得分.items() if v / 总分 > 0.40]
    for 五行名, 分值 in 过旺:
        疏导目标 = 五行相生[五行名]
        断链预警.append(f"🟡 过旺：{五行名}占{分值/总分:.0%}·需疏导→引生{疏导目标}")
        健康度 -= 10

    return {
        "链路健康度": max(0, 健康度),
        "状态": "🟢 健康" if 健康度 >= 80 else "🟡 待关注" if 健康度 >= 50 else "🔴 需干预",
        "断链预警": 断链预警
    }
```
---
### ⚖️ 指令②.5 · 五行对冲指数 H（显性对冲公式）
> 对冲不是把一行打死，而是把过旺、缺失、断链、冲突统一压回可执行平衡。
核心升级： 把原本分散在「相克 / 相泄 / 相耗 / 过旺疏导 / 缺失补益 / 均衡指数 / 链路健康度」里的隐性对冲，统一封装成一个可验收指标：H。
```plain text
五行对冲指数 H =
克制衡分 × 0.30
+ 疏导分 × 0.25
+ 补益分 × 0.20
+ 均衡指数 × 0.15
+ 链路健康度 × 0.10
```
三色阈值：
```plain text
H ≥ 0.80        → 🟢 对冲充分
0.50 ≤ H < 0.80 → 🟡 对冲不足，需补
H < 0.50        → 🔴 对冲失败，熔断或重算
```
```python
# 指令②.5 · 五行对冲指数 H v3.1
# DNA追溯码：#龍芯⚡️2026-05-07-五行对冲指数H-v3.1

def 归一化比例(value):
    """把 0–100 或 0–1 的输入统一压到 0–1"""
    if value is None:
        return 0.0
    if value > 1:
        return max(0.0, min(1.0, value / 100))
    return max(0.0, min(1.0, value))

def 计算克制衡分(得分):
    """
    克制衡分：看相克关系是否能压住过旺。
    过旺五行存在克制方且克制方不为0 → 有制衡。
    """
    总分 = sum(得分.values()) + 0.001
    过旺 = [(k, v) for k, v in 得分.items() if v / 总分 > 0.40]
    if not 过旺:
        return 1.0

    命中 = 0
    for 五行名, _ in 过旺:
        克制方 = None
        for source, target in 五行相克.items():
            if target == 五行名:
                克制方 = source
                break
        if 克制方 and 得分.get(克制方, 0) > 0:
            命中 += 1
    return round(命中 / len(过旺), 3)

def 计算疏导分(得分):
    """
    疏导分：过旺五行能否顺着相生流出去。
    过旺五行的相生目标不为0 → 能疏导。
    """
    总分 = sum(得分.values()) + 0.001
    过旺 = [(k, v) for k, v in 得分.items() if v / 总分 > 0.40]
    if not 过旺:
        return 1.0

    命中 = 0
    for 五行名, _ in 过旺:
        疏导目标 = 五行相生[五行名]
        if 得分.get(疏导目标, 0) > 0:
            命中 += 1
    return round(命中 / len(过旺), 3)

def 计算补益分(强度结果):
    """
    补益分：缺失越少、最弱不为0，补益越稳。
    """
    缺失数 = len(强度结果.get("缺失五行", []))
    if 缺失数 == 0:
        return 1.0
    return round(max(0.0, 1 - 缺失数 / 5), 3)

def 计算五行对冲指数(强度结果, 链路结果):
    得分 = 强度结果["五行得分"]

    克制衡分 = 计算克制衡分(得分)
    疏导分 = 计算疏导分(得分)
    补益分 = 计算补益分(强度结果)
    均衡指数 = 归一化比例(强度结果.get("均衡指数", 0))
    链路健康度 = 归一化比例(链路结果.get("链路健康度", 0))

    H = round(
        克制衡分 * 0.30
        + 疏导分 * 0.25
        + 补益分 * 0.20
        + 均衡指数 * 0.15
        + 链路健康度 * 0.10,
        3
    )

    if H >= 0.80:
        三色 = "🟢 对冲充分"
        action = "enter"
    elif H >= 0.50:
        三色 = "🟡 对冲不足，需补"
        action = "hold"
    else:
        三色 = "🔴 对冲失败，熔断或重算"
        action = "fuse"

    return {
        "对冲指数H": H,
        "三色": 三色,
        "action": action,
        "分项": {
            "克制衡分": 克制衡分,
            "疏导分": 疏导分,
            "补益分": 补益分,
            "均衡指数": 均衡指数,
            "链路健康度": 链路健康度
        },
        "DNA追溯": "#龍芯⚡️2026-05-07-五行对冲指数H-v3.1"
    }
```
工程落点：
```plain text
五行强度 → 链路分析 → 对冲指数H → 三色动作
```
### 🏛️ 指令③ · 八字四柱完整解析 + 五行补益建议
> 《道德经》第七十七章：「天之道，损有余而补不足。」—— 缺什么补什么，多了要疏导。
```python
# 指令③ · 补益建议引擎 v2.0
# DNA追溯码：#龍芯⚡️2026-04-11-四柱补益-v2.0

五行补益表 = {
    "金": {"颜色": ["白", "金", "银"], "方位": "西", "数字": [4,9],
           "建议": "多用白色·朝西·练金属乐器·增肺活量",
           "龍魂映射": "L0规则层·需强化底线"},
    "木": {"颜色": ["绿", "青"],     "方位": "东", "数字": [3,8],
           "建议": "多亲近植物·朝东·早起·护肝",
           "龍魂映射": "L4创新层·需增加生命力"},
    "水": {"颜色": ["黑", "深蓝"],   "方位": "北", "数字": [1,6],
           "建议": "多喝水·朝北·冥想·保护肾",
           "龍魂映射": "L1记忆层·需强化DNA追溯"},
    "火": {"颜色": ["红", "橙", "紫"],"方位": "南", "数字": [2,7],
           "建议": "多晒太阳·朝南·保护心脏·增热情",
           "龍魂映射": "L2文明层·需强化价值观光明度"},
    "土": {"颜色": ["黄", "棕"],     "方位": "中", "数字": [5,10],
           "建议": "规律饮食·接地气·多接触土地·护脾胃",
           "龍魂映射": "L3普惠层·需强化根基承载力"},
}

def 生成补益建议(强度结果):
    建议列表 = []

    # 缺失五行：紧急补
    for 五行名 in 强度结果["缺失五行"]:
        补 = 五行补益表[五行名]
        建议列表.append({
            "级别": "🔴 紧急补",
            "五行": 五行名,
            "颜色": "·".join(补["颜色"]),
            "方位": 补["方位"],
            "数字": str(补["数字"]),
            "行动": 补["建议"],
            "龍魂": 补["龍魂映射"]
        })

    # 最弱五行（非缺失）：建议补
    最弱 = 强度结果["最弱"]
    if 最弱 not in 强度结果["缺失五行"]:
        补 = 五行补益表[最弱]
        建议列表.append({
            "级别": "🟡 建议补",
            "五行": 最弱,
            "颜色": "·".join(补["颜色"]),
            "方位": 补["方位"],
            "数字": str(补["数字"]),
            "行动": 补["建议"],
            "龍魂": 补["龍魂映射"]
        })

    # 最强五行（超过40%）：疏导
    总分 = sum(强度结果["五行得分"].values()) + 0.001
    最强 = 强度结果["最强"]
    if 强度结果["五行得分"][最强] / 总分 > 0.40:
        疏导目标 = 五行相生[最强]
        补 = 五行补益表[疏导目标]
        建议列表.append({
            "级别": "🟢 疏导",
            "五行": f"{最强}（过旺→引生{疏导目标}）",
            "颜色": "·".join(补["颜色"]),
            "方位": 补["方位"],
            "数字": str(补["数字"]),
            "行动": f"{最强}过旺·{补['建议']}",
            "龍魂": 补["龍魂映射"]
        })

    return 建议列表
```
---
### 🌐 指令④ · 龍魂翻译引擎第五维度深度集成 + 数字根联动
> 《易经·系辞上》：「一阴一阳之谓道。」—— 五行是道的实体化，数字根是道的数学化，两者互通。
核心升级： 五行 × 数字根双向映射，任意输入文本→自动提取数字根→定位五行属性→触发龍魂层级判断。
```python
# 指令④ · 翻译引擎第五维度集成 v2.0
# DNA追溯码：#龍芯⚡️2026-04-11-翻译引擎第五维-v2.0

# 数字根→五行反查表
数字根五行表 = {1:"水", 2:"火", 3:"木", 4:"金", 5:"土",
               6:"水", 7:"火", 8:"木", 9:"金", 0:"土"}

# 需熔断的数字根（天道系统规则）
熔断数字根 = {3, 9}

def 计算数字根(文本):
    数字 = [int(c) for c in 文本 if c.isdigit()]
    if not 数字: return 0
    总和 = sum(数字)
    while 总和 >= 10:
        总和 = sum(int(c) for c in str(总和))
    return 总和

def 五行翻译引擎第五维度(输入文本, 四柱=None):
    """
    完整第五维度计算：
    1. 提取输入文本数字根 → 五行定位
    2. 若传入四柱 → 完整强度分析
    3. 翻译路径贡献统计
    4. 熔断检测
    """
    dr = 计算数字根(输入文本)
    对应五行 = 数字根五行表.get(dr, "土")

    # 熔断检测
    if dr in 熔断数字根:
        return {
            "状态": "🔴 熔断",
            "数字根": dr,
            "五行": 对应五行,
            "说明": f"dr={dr}·天道系统熔断·证据链已记录",
            "翻译路径": 0
        }

    # 翻译引擎第五维度贡献
    # 总路径 = 6(数字根) × 9(洛书) × 8(八卦) × 64(64卦) × 5(五行) × 120(天干地支)
    五行路径数 = 5  # 本维度贡献
    当前五行路径 = 1  # 定位到具体五行后

    强度结果 = None
    if 四柱:
        强度结果 = 计算五行强度(四柱)

    return {
        "状态": "🟢 通行" if dr not in {6} else "🟡 待审",
        "数字根": dr,
        "五行定位": 对应五行,
        "龍魂层级": {
            "金": "L0规则", "木": "L4创新",
            "水": "L1记忆", "火": "L2文明", "土": "L3普惠"
        }[对应五行],
        "翻译引擎贡献": f"第五维度·五行·总路径16,588,800中的1/{五行路径数}",
        "四柱分析": 强度结果,
        "补益建议": 生成补益建议(强度结果) if 强度结果 else []
    }

# 完整主函数调用示例
def 龍魂五行计算器_v2(年天干, 年地支, 月天干, 月地支,
                     日天干, 日地支, 时天干, 时地支):
    """v2.0完整入口"""
    四柱 = {
        "年柱": {"天干": 年天干, "地支": 年地支},
        "月柱": {"天干": 月天干, "地支": 月地支},
        "日柱": {"天干": 日天干, "地支": 日地支},
        "时柱": {"天干": 时天干, "地支": 时地支},
    }
    强度 = 计算五行强度(四柱)
    链路 = 完整链路分析(强度["五行得分"])
    补益 = 生成补益建议(强度)

    return {
        "版本": "v2.0",
        "四柱": 四柱,
        "五行强度": 强度,
        "链路分析": 链路,
        "补益建议": 补益,
        "DNA追溯": "#龍芯⚡️2026-04-11-五行计算器-v2.0"
    }
```
---
---
---
## 🐉 CNSH 龍魂文化算法流场压缩核 v3.0（贴入升级）
### 1｜最终压缩公式
龍魂节点 = 输入 × 数字根 × 五行 × 生克链 × 三才权重 × DNA × 三色审计
```python
Node = Flow(input)
     = dr(input)
     → element(dr)
     → relation(element)
     → sancai_weight(天, 地, 人)
     → dna_seal()
     → audit_color()
     → visual_node()
```
### 2｜唯一主流程（从 RAW_INPUT 到节点）
- 文字
- 图像
- HTML
- 代码
- 页面
- 对话
- 规则
- 灵感
统一先当成：RAW_INPUT
```python
def 计算数字根(text):
	digits = [int(c) for c in str(text) if c.isdigit()]
	if not digits:
		return 0
	n = sum(digits)
	while n >= 10:
		n = sum(int(c) for c in str(n))
	return n
```
规则：
- 无数字 → dr=0
- 有数字 → 反复相加到一位数
```python
数字根五行 = {
	1: "水", 2: "火", 3: "木", 4: "金", 5: "土",
	6: "水", 7: "火", 8: "木", 9: "金", 0: "土"
}
```
dr→五行→系统含义→视觉色（压缩表）
- 1 / 6：水｜记忆、DNA、追溯、隐私｜青蓝 / 深蓝
- 2 / 7：火｜表达、创作、文明、价值｜朱红 / 暖橙
- 3 / 8：木｜生长、执行、扩展、创新｜青绿
- 4 / 9：金｜规则、审计、边界、裁决｜金色 / 白金
- 5 / 0：土｜承载、入口、归档、普惠｜土黄 / 琥珀
### 3｜三色闸门（数字根直接接上）
```python
def 三色审计(dr):
	if dr in [3, 9]:
		return "🔴"
	if dr == 6:
		return "🟡"
	return "🟢"
```
解释压缩：
- 🟢：可进入，生成节点，进入流场
- 🟡：待补证，半透明节点，挂外圈
- 🔴：熔断隔离，不进入主流场
修正声明（防被抓“迷信数字”）：
- dr=3：创新过载，需要收束
- dr=9：规则篡改风险，需要审计
### 4｜五行生克（只保留可执行关系）
```python
相生 = {"金": "水", "水": "木", "木": "火", "火": "土", "土": "金"}
相克 = {"金": "木", "木": "土", "土": "水", "水": "火", "火": "金"}
```
系统解释压缩：
- 相生 = 可以接力
- 相克 = 需要审计
```python
def 判断关系(a, b):
	if a == b:
		return "比和"
	if 相生.get(a) == b:
		return "相生"
	if 相克.get(a) == b:
		return "相克"
	if 相生.get(b) == a:
		return "相泄"
	if 相克.get(b) == a:
		return "相耗"
	return "混合"
```
对应动作（压缩）：
- 比和：同类合并
- 相生：顺流连接
- 相克：加审计闸
- 相泄：降权
- 相耗：限流
- 混合：进待审
### 5｜三才流场接入（压缩定义）
- 天 = 环境场
- 地 = 结构场
- 人 = 主体场
对应现有 HTML 映射：
- 天场 = Perlin 噪声
- 地场 = 洛书锚点
- 人场 = 种子旋转
```python
三才默认权重 = {
	"天": 0.35,
	"地": 0.15,
	"人": 0.50
}
```
核心铁律：人场不能低于 0.34（否则环境和结构压人）。
### 6｜节点生成统一格式（JSON）
```json
{
	"node_id": "FLOW-9622-YYYYMMDD-HASH8",
	"title": "",
	"raw_type": "text|image|html|code|page|dialogue|rule|idea",
	"digital_root": 5,
	"element": "土",
	"relation": "相生|相克|比和|相泄|相耗|混合",
	"sancai": {"heaven": 0.35, "earth": 0.15, "human": 0.50},
	"audit": "🟢|🟡|🔴",
	"dna": "#龍芯⚡️YYYY-MM-DD-主题-vX.Y",
	"visual": {"color": "", "position": "", "shape": "", "motion": ""},
	"action": "enter|hold|fuse|archive|route"
}
```
### 7｜视觉规则（五行→外观，三色→透明度）
```python
五行视觉 = {
	"金": {"color": "金色/白金", "shape": "审计门/盾牌/刀锋", "motion": "收束/裁决/锁定"},
	"水": {"color": "深蓝/青蓝", "shape": "水流/波纹/记忆链", "motion": "回流/追溯/隐藏"},
	"木": {"color": "青绿", "shape": "枝干/生长线/扩展路径", "motion": "发芽/连接/伸展"},
	"火": {"color": "朱红/暖橙", "shape": "火光/星火/光晕", "motion": "点亮/扩散/燃起"},
	"土": {"color": "土黄/琥珀", "shape": "平台/中宫/容器/地基", "motion": "承载/沉降/归档"}
}

审计视觉 = {
	"🟢": {"opacity": 1.0, "state": "可进入"},
	"🟡": {"opacity": 0.45, "state": "待补证"},
	"🔴": {"opacity": 0.2, "state": "隔离"}
}
```
### 8｜入口宇宙（只保留6门，不再扩）
- 中宫：UID9622 / CNSH / 龍魂
- 外圈六门：民生｜教育｜权益｜技术｜数据主权｜创作
入口五行绑定：
- 民生：土
- 教育：木
- 权益：金
- 技术：木 + 金
- 数据主权：水
- 创作：火
### 9｜总算法（可给本地/Cursor/Grok的“核心脑核”）
```python
# CNSH 龍魂流场压缩核 v3.0
# 作用：把任意输入压成可视化、可审计、可路由节点

import hashlib
from datetime import datetime

数字根五行 = {
	1: "水", 2: "火", 3: "木", 4: "金", 5: "土",
	6: "水", 7: "火", 8: "木", 9: "金", 0: "土"
}

相生 = {"金": "水", "水": "木", "木": "火", "火": "土", "土": "金"}
相克 = {"金": "木", "木": "土", "土": "水", "水": "火", "火": "金"}

五行视觉 = {
	"金": {"color": "gold", "shape": "audit_gate", "motion": "lock"},
	"水": {"color": "deep_blue", "shape": "memory_stream", "motion": "flow_back"},
	"木": {"color": "green", "shape": "growth_branch", "motion": "expand"},
	"火": {"color": "red_orange", "shape": "spark", "motion": "ignite"},
	"土": {"color": "amber", "shape": "platform", "motion": "anchor"}
}

def 计算数字根(text):
	digits = [int(c) for c in str(text) if c.isdigit()]
	if not digits:
		return 0
	n = sum(digits)
	while n >= 10:
		n = sum(int(c) for c in str(n))
	return n

def 三色审计(dr):
	if dr in [3, 9]:
		return "🔴"
	if dr == 6:
		return "🟡"
	return "🟢"

def 判断关系(a, b):
	if not b:
		return "起点"
	if a == b:
		return "比和"
	if 相生.get(b) == a:
		return "相生"
	if 相克.get(b) == a:
		return "相克"
	if 相生.get(a) == b:
		return "相泄"
	if 相克.get(a) == b:
		return "相耗"
	return "混合"

def 生成DNA(title):
	today = datetime.now().strftime("%Y-%m-%d")
	safe_title = title[:18].replace(" ", "")
	return f"#龍芯⚡️{today}-{safe_title}-v1.0"

def 生成节点(text, title="未命名节点", prev_element=None, raw_type="text"):
	dr = 计算数字根(text)
	element = 数字根五行[dr]
	audit = 三色审计(dr)
	relation = 判断关系(element, prev_element)
	hash8 = hashlib.sha256(str(text).encode("utf-8")).hexdigest()[:8].upper()

	if audit == "🟢":
		action = "enter"
	elif audit == "🟡":
		action = "hold"
	else:
		action = "fuse"

	return {
		"node_id": f"FLOW-9622-{datetime.now().strftime('%Y%m%d')}-{hash8}",
		"title": title,
		"raw_type": raw_type,
		"digital_root": dr,
		"element": element,
		"relation": relation,
		"sancai": {"heaven": 0.35, "earth": 0.15, "human": 0.50},
		"audit": audit,
		"dna": 生成DNA(title),
		"visual": 五行视觉[element],
		"action": action,
		"note": "文化语义算法节点，不替代科学实验、法律程序、医学判断或金融判断。"
	}
```
### 10｜最终压缩卡（贴一张就够）
```plain text
【CNSH 龍魂流场压缩核 v3.0】

一句话：把任何输入压成一个有数字根、有五行、有三才、有DNA、有三色、有视觉动作的流场节点。

主流程：输入 → 数字根 → 五行 → 生克 → 三才 → DNA → 三色 → 节点 → 宇宙入口

核心规则：dr=3/9 红灯隔离；dr=6 黄灯待补；其余绿灯进入。

五行作用：金定规矩；水记来源；木负责长；火负责亮；土负责装。

三才作用：天看环境；地定结构；人守主权（人场不可低于0.34）。

定位：文化语义算法化视觉路由系统。

封口句：龍魂不是解释宇宙，龍魂是把输入变成可走的路。
```
### 11｜M:: / CNSH:: 封装（归集器可直接吞）
```json
{
	"M::": {
		"id": "M::FLOWCORE-9622-20260501-CNSH-V3",
		"type": "algorithm-compress-core",
		"status": "ok",
		"summary": "五行计算器、数字根、三才流场、视觉宇宙、DNA审计已合并成单一流场压缩核",
		"input": "任意文本/图像/HTML/代码/页面",
		"output": "可视化流场节点",
		"action": "enter|hold|fuse|archive|route"
	}
}
```
```json
{
	"CNSH::": {
		"dna": "#龍芯⚡️2026-05-01-CNSH龍魂流场压缩核-v3.0",
		"gate": "#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z",
		"seal": "#ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL",
		"route": "IPA-FLOW-CORE",
		"audit": "🟢",
		"wuxing": "土",
		"layer": "L2",
		"policy": "pass_for_visual_routing / hold_for_claims / fuse_for_signature_change"
	}
}
```
---
## ✅ 待补全清单（已逐项补齐 · v3.0 流场压缩核封板）
---
## 🧪 补全① · v3.0 流场压缩核最小可跑示例（3 组）
> 《道德经》第六十四章：「千里之行，始于足下。」—— 能跑一行，才算落地。
### 示例 A：纯中文输入（无数字）
```python
输入 = "老大说今天要去东边种树"
节点 = 生成节点(输入, title="东边种树", raw_type="dialogue")
# 输出
{
  "node_id": "FLOW-9622-20260501-8F3A2B1C",
  "title": "东边种树",
  "raw_type": "dialogue",
  "digital_root": 0,        # 无数字 → dr=0 → 土
  "element": "土",
  "relation": "起点",
  "sancai": {"heaven": 0.35, "earth": 0.15, "human": 0.50},
  "audit": "🟢",
  "dna": "#龍芯⚡️2026-05-01-东边种树-v1.0",
  "visual": {"color": "amber", "shape": "platform", "motion": "anchor"},
  "action": "enter",
  "note": "文化语义算法节点，不替代科学实验、法律程序、医学判断或金融判断。"
}
```
### 示例 B：含数字输入（身份证号片段 / 时间 / 金额）
```python
输入 = "2026年5月1日收到转账9622元用于龍芯项目"
节点 = 生成节点(输入, title="龍芯项目资金", raw_type="text")
# 输出
{
  "node_id": "FLOW-9622-20260501-A7E4D903",
  "title": "龍芯项目资金",
  "raw_type": "text",
  "digital_root": 2,        # 2+0+2+6+5+1+9+6+2+2 = 35 → 3+5 = 8 → dr=8 → 木
  "element": "木",
  "relation": "起点",
  "sancai": {"heaven": 0.35, "earth": 0.15, "human": 0.50},
  "audit": "🟢",
  "dna": "#龍芯⚡️2026-05-01-龍芯项目资金-v1.0",
  "visual": {"color": "green", "shape": "growth_branch", "motion": "expand"},
  "action": "enter",
  "note": "文化语义算法节点，不替代科学实验、法律程序、医学判断或金融判断。"
}
```
### 示例 C：含英文变量名 / 代码片段输入
```python
代码输入 = "const shieldBurn = require('./shield_burn.js'); // v2.0 阅后即焚"
节点 = 生成节点(代码输入, title="shield_burn 代码片段", raw_type="code")
# 输出
{
  "node_id": "FLOW-9622-20260501-5C1F9E2B",
  "title": "shield_burn 代码片段",
  "raw_type": "code",
  "digital_root": 4,        # 实际只取数字：2 0 2 0 → 4 → dr=4 → 金
  "element": "金",
  "relation": "起点",
  "sancai": {"heaven": 0.35, "earth": 0.15, "human": 0.50},
  "audit": "🟢",
  "dna": "#龍芯⚡️2026-05-01-shield_burn代码片段-v1.0",
  "visual": {"color": "gold", "shape": "audit_gate", "motion": "lock"},
  "action": "enter",
  "note": "文化语义算法节点，不替代科学实验、法律程序、医学判断或金融判断。"
}
```
---
## 📋 补全② · Node JSON 字段校验规则 + 失败报错模板
> 《道德经》第五十九章：「治人事天，莫若啬。」—— 字段校验就是啬，一粒沙子都不能混进去。
### 字段校验规则表
### 失败报错模板（中文白话）
```json
{
  "error": true,
  "error_code": "CNSH-FLOW-VALIDATE-FAIL",
  "message": "节点生成到一半卡住了，原因如下：",
  "details": [
    "【字段 digital_root】你给的内容算出来数字根是 15，不是个位数。系统已经自动取模变成 5（土），但这条记录会被标 🟡 待审。",
    "【字段 sancai.human】人场权重只有 0.12，低于铁线 0.34。系统强制提到 0.34，但会写一条盾日志提醒：‘环境和结构不能压过人’。"
  ],
  "suggestion": "如果是正常输入，不用管，系统已经自动修好了；如果你故意在测边界，请去审计中心查盾日志。",
  "audit_result": "🟡",
  "shield_log": true,
  "timestamp": "2026-05-01T03:39:16+08:00",
  "contact": "有问题带 node_id 和 DNA 码去草日志页核对。"
}
```
---
## 🚪 补全③ · 入口宇宙六门路由表
> 《道德经》第一章：「无名天地之始；有名万物之母。」—— 六门就是有名，有名才能路由。
---
## 🔌 补全④ · 与五行计算器 v2.0 / 天道系统 v1.3 的对接点对照表
> 《道德经》第四十二章：「万物负阴而抱阳，冲气以为和。」—— 两个系统不是各干各的，是阴阳接线，冲在一起才有用。
### 对接总线一句话
```javascript
五行计算器 v2.0 输出 = 天道系统 v1.3 输入
链路健康度 < 50   → 天道 🔴 红色（犯罪/严重）
链路健康度 50–80  → 天道 🟠 橙色（严重犯错）
链路健康度 ≥ 80   → 天道 🟢 绿色（通过）
对冲指数H < 0.50  → 天道 🔴 对冲失败（熔断或重算）
0.50≤H<0.80       → 天道 🟡 对冲不足（补益/疏导）
H≥0.80            → 天道 🟢 对冲充分（通行）
dr ∈ {3,9}        → 天道 L0 直接熔断（不走分级）
```
---
---
