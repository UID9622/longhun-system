# 🤖 机器学习算法全景图

> Notion URL: https://app.notion.com/p/bff7042550e94c12af4e6d5433aa679e
> Created: 2025-09-09T11:06:00.000Z
> Last edited: 2026-07-01T15:28:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
# 🤖 机器学习算法完整知识体系
---
### 🛡️ 合规规范 · 同步块
## 🎯 监督学习算法
### 📈 回归算法
### 线性回归 (Linear Regression)
```python
from sklearn.linear_model import LinearRegression

# 模型训练
model = LinearRegression()
model.fit(X_train, y_train)

# 预测
y_pred = model.predict(X_test)

# 评估
from sklearn.metrics import mean_squared_error, r2_score
mse = mean_squared_error(y_test, y_pred)
r2 = r2_score(y_test, y_pred)
```
适用场景：房价预测、销售预测、股价预测
优点：简单易懂、计算快速、可解释性强
缺点：只能处理线性关系、对异常值敏感
### 🎯 分类算法
### 逻辑回归 (Logistic Regression)
```python
from sklearn.linear_model import LogisticRegression

logistic = LogisticRegression()
logistic.fit(X_train, y_train)

# 预测概率
proba = logistic.predict_proba(X_test)
```
### 决策树 (Decision Tree)
```python
from sklearn.tree import DecisionTreeClassifier
from sklearn.tree import plot_tree

tree = DecisionTreeClassifier(
    max_depth=5,
    min_samples_split=10,
    random_state=42
)
tree.fit(X_train, y_train)

# 可视化决策树
plt.figure(figsize=(15, 10))
plot_tree(tree, filled=True, feature_names=feature_names)
```
### 支持向量机 (SVM)
```python
from sklearn.svm import SVC

svm = SVC(
    kernel='rbf',
    C=1.0,
    gamma='scale'
)
svm.fit(X_train, y_train)
```
## 🔍 无监督学习算法
### 📊 聚类算法
### K-Means聚类
```python
from sklearn.cluster import KMeans
import matplotlib.pyplot as plt

# 确定最优K值 - 肘部法则
inertias = []
K_range = range(1, 11)
for k in K_range:
    kmeans = KMeans(n_clusters=k, random_state=42)
    kmeans.fit(X)
    inertias.append(kmeans.inertia_)

# 最终聚类
kmeans = KMeans(n_clusters=3, random_state=42)
cluster_labels = kmeans.fit_predict(X)
```
### 🔽 降维算法
### 主成分分析 (PCA)
```python
from sklearn.decomposition import PCA
import numpy as np

# PCA降维
pca = PCA(n_components=2)
X_pca = pca.fit_transform(X)

# 解释方差比
print('解释方差比:', pca.explained_variance_ratio_)
```
## ⚙️ 模型评估与选择
### 📊 评估指标
```python
# 分类指标
from sklearn.metrics import (
    accuracy_score, precision_score, recall_score, 
    f1_score, confusion_matrix, classification_report
)

accuracy = accuracy_score(y_test, y_pred)
precision = precision_score(y_test, y_pred, average='macro')
recall = recall_score(y_test, y_pred, average='macro')
f1 = f1_score(y_test, y_pred, average='macro')

# 混淆矩阵
cm = confusion_matrix(y_test, y_pred)
sns.heatmap(cm, annot=True, fmt='d')
```
### 🔄 交叉验证
```python
from sklearn.model_selection import cross_val_score, StratifiedKFold

# K折交叉验证
scores = cross_val_score(model, X, y, cv=5, scoring='accuracy')
print(f'平均准确率: {scores.mean():.3f} (+/- {scores.std() * 2:.3f})')
```
## 🎯 算法选择指南
> 💡 算法选择原则：从简单模型开始 → 逐步尝试复杂模型 → 根据业务需求平衡精度和可解释性！
## 📚 学习资源与工具
### 🔧 常用库与工具
```python
# 常用科学计算与可视化库
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 机器学习工作流工具
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import GridSearchCV

# 创建简单的机器学习流水线
pipeline = Pipeline([
    ('scaler', StandardScaler()),
    ('classifier', RandomForestClassifier())
])
```
推荐在线学习资源：
- Scikit-Learn官方文档: 最全面的API参考和用例
- Kaggle竞赛平台: 实践项目和社区讨论
- 机器学习公开课: Andrew Ng的Coursera课程
需要额外补充的学习建议:
- 建立学习路线图: 从基础算法到高级模型，循序渐进
- 定期动手实践: 理论与代码结合，每个算法至少实现一次
- 参与开源项目: 通过贡献代码提升实战能力
本知识条目涵盖了机器学习领域的主要算法类型、实现方式和评估方法，作为入门到进阶的完整参考指南。通过这个算法全景图，我能够系统性地理解各种算法的适用场景、优缺点及代码实现。后续将定期更新并拓展深度学习和强化学习相关内容。
