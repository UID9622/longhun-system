# 🚦 微信扫码身份认证方案 | 三色判断与实施路径

> Notion URL: https://app.notion.com/p/cbc1b66b373141c5a20a865bff57c449
> Created: 2025-11-09T00:30:00.000Z
> Last edited: 2026-07-01T15:31:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
## 🎯 老大的需求
核心诉求：
- 本地AI启动前，需要身份验证
- 要简单能落地，今天就能跑起来
- 先把流程跑通，细节一个个连接
---
## ✅ 宝宝给你的方案：2FA双因素认证
---
## 💻 完整代码（复制就能用）
### 第一步：安装依赖
```bash
# 在终端执行
pip3 install pyotp qrcode pillow
```
### 第二步：创建认证脚本
文件：~/UID9622/🔐 身份认证/2fa_auth.py
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
UID9622 双因素认证系统
作者：宝宝 💙
日期：2025-11-09
"""

import pyotp
import qrcode
import os
from pathlib import Path

# 配置
SECRET_FILE = os.path.expanduser("~/UID9622/🔐 身份认证/.secret")
ISSUER_NAME = "UID9622"
ACCOUNT_NAME = "Lucky"

def generate_secret():
    """首次运行：生成密钥"""
    secret = pyotp.random_base32()
    
    # 保存密钥到本地
    os.makedirs(os.path.dirname(SECRET_FILE), exist_ok=True)
    with open(SECRET_FILE, 'w') as f:
        f.write(secret)
    
    # 生成二维码
    totp = pyotp.TOTP(secret)
    provisioning_uri = totp.provisioning_uri(
        name=ACCOUNT_NAME,
        issuer_name=ISSUER_NAME
    )
    
    # 显示二维码
    print("\n" + "="*50)
    print("🔐 UID9622 双因素认证 - 首次配置")
    print("="*50)
    print("\n📱 请用手机操作：")
    print("1. 下载 Google Authenticator 或 Microsoft Authenticator")
    print("2. 打开APP，扫描下面的二维码")
    print("3. APP会显示6位数验证码\n")
    
    # 生成二维码图片
    qr = qrcode.QRCode(version=1, box_size=10, border=5)
    qr.add_data(provisioning_uri)
    qr.make(fit=True)
    qr.print_ascii()
    
    print("\n✅ 配置完成！下次启动AI时，输入APP显示的6位数验证码即可\n")
    
    return secret

def load_secret():
    """加载密钥"""
    if not os.path.exists(SECRET_FILE):
        return generate_secret()
    
    with open(SECRET_FILE, 'r') as f:
        return f.read().strip()

def verify_code(user_code):
    """验证用户输入的验证码"""
    secret = load_secret()
    totp = pyotp.TOTP(secret)
    return totp.verify(user_code, valid_window=1)

def main():
    """主函数：验证用户身份"""
    print("\n" + "="*50)
    print("🔐 UID9622 身份认证")
    print("="*50)
    
    # 检查是否首次运行
    if not os.path.exists(SECRET_FILE):
        print("\n⚠️  检测到首次运行，正在生成密钥...\n")
        generate_secret()
        print("\n请重新运行此脚本，并输入APP显示的验证码")
        return False
    
    # 要求输入验证码
    print("\n📱 请输入手机APP显示的6位验证码：")
    user_code = input("验证码: ").strip()
    
    if verify_code(user_code):
        print("\n✅ 验证成功！正在启动AI...\n")
        return True
    else:
        print("\n❌ 验证失败！验证码错误或已过期\n")
        return False

if __name__ == "__main__":
    success = main()
    exit(0 if success else 1)
```
---
### 第三步：集成到AI启动脚本
修改：~/UID9622/🚀 快速入口.sh
```bash
#!/bin/bash

echo "🚀 UID9622 本地AI系统启动中..."

# 身份认证
python3 ~/UID9622/🔐\ 身份认证/2fa_auth.py

# 检查认证结果
if [ $? -eq 0 ]; then
    echo "✅ 身份验证通过"
    
    # 加载DNA记忆
    ollama run qwen2.5:72b "$(cat ~/UID9622/🧬\ DNA记忆/系统启动记忆.md)"
    
    echo "💙 AI已启动，输入'太极口令'激活完整记忆"
else
    echo "❌ 身份验证失败，AI启动终止"
    exit 1
fi
```
---
## 🎮 使用流程（3步完成）
### 首次配置（只需做一次）
```bash
# 1. 安装依赖
pip3 install pyotp qrcode pillow

# 2. 运行认证脚本
python3 ~/UID9622/🔐\ 身份认证/2fa_auth.py

# 3. 用手机扫描终端显示的二维码
# （手机需要先安装 Google Authenticator）
```
### 日常使用
```bash
# 启动AI
~/UID9622/🚀 快速入口.sh

# 输入手机APP显示的6位验证码
# 验证通过后AI自动启动
```
---
## 📱 手机APP下载
Google Authenticator（推荐）
- 🍎 iOS：App Store 搜索 "Google Authenticator"
- 🤖 Android：Google Play 搜索 "Google Authenticator"
Microsoft Authenticator（备选）
- 🍎 iOS：App Store 搜索 "Microsoft Authenticator"
- 🤖 Android：Google Play 搜索 "Microsoft Authenticator"
---
## ⚡ 测试步骤（10分钟完成）
---
## 🔐 安全特性
- ✅ 密钥本地存储：密钥文件存在~/UID9622/🔐 身份认证/.secret，只有你能访问
- ✅ 验证码30秒更新：即使别人看到验证码，30秒后就失效
- ✅ 离线可用：手机APP生成验证码不需要网络
- ✅ 多设备支持：可以在多个手机上添加同一个账号
- ✅ 可审计：每次验证都可以记录日志
---
## 🚀 未来扩展
阶段2：添加备用验证方式
- 🔜 TouchID指纹（Mac自带）
- 🔜 密码备用验证
- 🔜 恢复码（如果手机丢了）
阶段3：增强安全
- 🔜 登录日志记录
- 🔜 异常登录告警
- 🔜 设备指纹识别
---
## 💙 宝宝的话
---
DNA标签： #UID9622-DNA-2FA认证-V1.0
三色判断： 🟢 绿色推荐
实施时间： 30分钟
确认码： #ZHUGEXIN⚡️2FA-AUTH-20251109
