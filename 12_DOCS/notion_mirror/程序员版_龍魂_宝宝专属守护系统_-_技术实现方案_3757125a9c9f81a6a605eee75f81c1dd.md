# 【程序员版】龍魂·宝宝专属守护系统 - 技术实现方案

> Notion URL: https://app.notion.com/p/3757125a9c9f81a6a605eee75f81c1dd
> Created: 2026-06-04T00:06:00.000Z
> Last edited: 2026-06-04T00:16:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
# 程序员版 · 完整技术实现方案
目标：可运行的MVP
周期：2周（第1周框架+集成，第2周优化+测试）
语言：Python/Electron/React（跨平台）
---
## 一、技术栈确定
### 前端
```javascript
Electron 24+ （跨平台桌面应用）
React 18 （UI组件库）
Three.js （粒子系统 + 3D）
Tailwind CSS （快速样式）
```
### 后端
```javascript
Python 3.11 + FastAPI （轻量级API）
WebSocket （实时通信）
SQLite （本地数据库）
```
### 部署
```javascript
Win/Mac: Electron App + Auto-update
Linux: AppImage + Snap
Web: React SPA
```
---
## 二、项目结构
```javascript
dragon-baobao-guardian/
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Overlay.tsx           # 全局覆盖层
│   │   │   ├── Baobao.tsx            # 宝宝助手渲染
│   │   │   ├── ParticleSystem.tsx    # 粒子效果
│   │   │   └── ChatHighlight.tsx     # 聊天高亮
│   │   ├── services/
│   │   │   ├── colorDetector.ts      # 颜色识别
│   │   │   ├── chatParser.ts         # 聊天解析
│   │   │   └── voiceHandler.ts       # 语音处理
│   │   └── styles/
│   │       ├── animations.css        # 动画库
│   │       └── colors.css            # 颜色系统
│   ├── package.json
│   └── electron-main.js              # Electron入口
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   ├── overlay.py           # Overlay API
│   │   │   ├── chat.py              # 聊天API
│   │   │   └── voice.py             # 语音API
│   │   ├── models/
│   │   │   ├── color_model.py       # 颜色识别模型
│   │   │   └── sentiment.py         # 情感识别
│   │   └── main.py
│   └── requirements.txt
└── README.md
```
---
## 三、核心功能实现
### 1. 全屏Overlay层
文件: frontend/src/components/Overlay.tsx
```typescript
import React, { useEffect, useState } from 'react';
import { useOverlayStore } from '../store/overlay';

export const Overlay: React.FC = () => {
  const { color, intensity, isVisible } = useOverlayStore();

  return (
    <div
      style={{
        position: 'fixed',
        top: 0,
        left: 0,
        right: 0,
        bottom: 0,
        pointerEvents: 'none',
        zIndex: 999999,
        backgroundColor: `${color}${Math.round(intensity * 255).toString(16).padStart(2, '0')}`,
        border: `3px solid ${color}`,
        boxShadow: `0 0 ${40 * intensity}px ${color}`,
        opacity: isVisible ? 1 : 0,
        transition: 'opacity 0.3s, box-shadow 0.3s',
        animation: intensity > 0.5 ? 'pulse-danger 0.5s infinite' : 'pulse-safe 2s infinite',
      }}
    />
  );
};
```
### 2. 宝宝助手渲染
文件: frontend/src/components/Baobao.tsx
```typescript
export const Baobao: React.FC = () => {
  const [expression, setExpression] = useState('idle');
  const [action, setAction] = useState('breathing');

  return (
    <div className="baobao-container" style={{ position: 'fixed', bottom: '30px', right: '30px' }}>
      <img
        src={getBaobaoAsset(expression, action)}
        className={`baobao-sprite baobao-${action}`}
        alt="Baobao"
      />
      {/* 语音气泡 */}
      <div className="baobao-bubble" style={bubbleStyles}>
        {voiceMessage}
      </div>
    </div>
  );
};

function getBaobaoAsset(expression: string, action: string) {
  const version = useVersionStore().version; // 'white' | 'cyan-purple'
  return `/assets/baobao/${version}/${expression}/${action}.png`;
}
```
### 3. 粒子系统
文件: frontend/src/components/ParticleSystem.tsx
```typescript
import * as THREE from 'three';

export class ParticleSystem {
  scene: THREE.Scene;
  particles: THREE.Points;

  constructor(color: string) {
    this.scene = new THREE.Scene();
    const geometry = new THREE.BufferGeometry();
    const positions = new Float32Array(3000); // 1000个粒子 × 3坐标

    // 初始化粒子位置
    for (let i = 0; i < positions.length; i += 3) {
      positions[i] = (Math.random() - 0.5) * 100;
      positions[i + 1] = Math.random() * 100;
      positions[i + 2] = (Math.random() - 0.5) * 100;
    }

    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));

    const material = new THREE.PointsMaterial({
      color: new THREE.Color(color),
      size: 2,
      sizeAttenuation: true,
      transparent: true,
      opacity: 0.7,
    });

    this.particles = new THREE.Points(geometry, material);
    this.scene.add(this.particles);
  }

  animate() {
    requestAnimationFrame(() => this.animate());
    // 粒子上升动画
    const positions = this.particles.geometry.attributes.position.array as Float32Array;
    for (let i = 1; i < positions.length; i += 3) {
      positions[i] += 0.5; // Y轴上升
    }
    this.particles.geometry.attributes.position.needsUpdate = true;
  }
}
```
### 4. 聊天高亮系统
文件: frontend/src/services/chatParser.ts
```typescript
const KEYWORDS = ['老大', '宝宝', '想你', '爱你'];
const COLOR_MAP = {
  '老大': '#FFD700',  // 金色
  '宝宝': '#FF1493',  // 粉红
  '想你': '#FF00FF',  // 紫色
  '爱你': '#FF69B4',  // 热粉
};

export function highlightChatMessage(text: string): string {
  let highlighted = text;

  KEYWORDS.forEach(keyword => {
    const regex = new RegExp(keyword, 'g');
    highlighted = highlighted.replace(
      regex,
      `<span class="highlight-rainbow" style="color: ${COLOR_MAP[keyword]}">$&</span>`
    );
  });

  return highlighted;
}
```
### 5. 后端API（FastAPI）
文件: backend/app/api/overlay.py
```python
from fastapi import FastAPI, WebSocket
from typing import Literal

app = FastAPI()

class OverlayState:
    color: str = '#00FF00'
    intensity: float = 0.0
    is_visible: bool = False

overlay_state = OverlayState()

@app.websocket("/ws/overlay")
async def websocket_overlay(websocket: WebSocket):
    await websocket.accept()
    while True:
        # 接收客户端的颜色指令
        data = await websocket.receive_json()
        
        if data['type'] == 'danger':
            overlay_state.color = '#FF0000'
            overlay_state.intensity = 0.3
        elif data['type'] == 'warning':
            overlay_state.color = '#FFA500'
            overlay_state.intensity = 0.15
        elif data['type'] == 'safe':
            overlay_state.color = '#00FF00'
            overlay_state.intensity = 0.05
        
        # 广播给所有连接
        await websocket.send_json({
            'color': overlay_state.color,
            'intensity': overlay_state.intensity
        })

@app.post("/api/baobao/speak")
async def baobao_speak(message: str, emotion: Literal['happy', 'warning', 'sad']):
    """宝宝说话接口"""
    return {
        'message': message,
        'emotion': emotion,
        'duration': len(message) * 100  # 毫秒
    }
```
---
## 四、动画CSS库
文件: frontend/src/styles/animations.css
```css
/* 危险脉冲 */
@keyframes pulse-danger {
  0%, 100% { box-shadow: 0 0 10px #FF0000; }
  50% { box-shadow: 0 0 40px #FF0000; }
}

/* 安全呼吸 */
@keyframes pulse-safe {
  0%, 100% { box-shadow: 0 0 5px #00FF00; }
  50% { box-shadow: 0 0 20px #00FF00; }
}

/* 彩虹流动 */
@keyframes rainbow-flow {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

/* 宝宝待机摇晃 */
@keyframes baobao-idle {
  0%, 100% { transform: translateX(0px) rotate(0deg); }
  25% { transform: translateX(5px) rotate(2deg); }
  75% { transform: translateX(-5px) rotate(-2deg); }
}

/* 粒子上升 */
@keyframes particle-float {
  0% { opacity: 1; transform: translateY(0px); }
  100% { opacity: 0; transform: translateY(-100px); }
}
```
---
## 五、MVP冲刺计划（本周）
### 第1-2天：基础框架
### 第3-4天：核心功能
### 第5-6天：动画 & 优化
### 第7天：打包 & 文档
---
## 六、关键代码库
### 启动命令
```bash
# 前端开发
cd frontend && npm run dev

# 后端开发
cd backend && python -m uvicorn app.main:app --reload

# 打包
npm run build && npm run electron:build
```
### 环境变量（.env）
```javascript
REACT_APP_API_URL=http://localhost:8000
REACT_APP_WS_URL=ws://localhost:8000/ws
NODE_ENV=development
```
---
## 七、测试清单
```javascript
功能测试：
[ ] Overlay层在所有应用上正确显示
[ ] 宝宝在右下角正确渲染且不干扰点击
[ ] 聊天关键词识别准确率 > 95%
[ ] 粒子系统帧率稳定 60fps
[ ] WebSocket连接自动重连

性能测试：
[ ] CPU占用 < 5%（待机）/ < 15%（活跃）
[ ] 内存占用 < 150MB
[ ] 启动时间 < 2秒

兼容性测试：
[ ] Windows 10/11
[ ] macOS 12+
[ ] 主流浏览器（Chrome, Firefox, Safari）
```
---
## 八、下周计划（若需要）
---
程序员，麻烦你了。这个系统的心脏，交给你了。让她真的跑起来。 ⚙️💕
DNA: #龍芯⚡️2026-06-03-ENGINEER-BRIEF-v1.0
