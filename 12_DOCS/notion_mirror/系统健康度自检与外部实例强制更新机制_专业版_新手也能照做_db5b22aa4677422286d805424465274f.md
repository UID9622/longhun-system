# ✅ 系统健康度自检与外部实例强制更新机制（专业版）｜新手也能照做

> Notion URL: https://app.notion.com/p/db5b22aa4677422286d805424465274f
> Created: 2025-12-23T21:51:00.000Z
> Last edited: 2026-07-01T15:34:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
---
## 0) 本页定位（诸葛亮主持：先三色判断，再执行）
- 主持人格：诸葛亮（战略推演）
- 审计人格：审判长（三色审计一票裁决）
- 执行载体：总控台（control plane）→ 更新器（updater）→ 备份推送（backup push）
- 治理目标：自动发现 → 自动决策 → 自动执行 → 自动留痕 → 自动回滚
---
## 1) 名词与边界（必须统一口径）
### 1.1 名词
- 母本（Mother）：Notion 中的唯一权威规则源（治理与规范只在母本更新）。
- 外部实例（Instance）：母本在外部运行的一份系统副本/镜像（本地、CI、服务器、CodeBuddy 工作区、海外协作方环境）。
- 总控台（Control Plane）：统一入口（脚本 + 日志 + 事件输出 + 报警）。
- 事件（Event）：机器可读 JSON，作为审计与执行的唯一触发信号。
- 接入包（dist）：对外协作的唯一交付物（脱敏后的发布产物）。
### 1.2 边界（P0）
- PULL ONLY：外部实例只允许从母本拉取更新，不允许反向覆盖母本。
- 数据不触碰：强制更新只更新“规则/配置/提示词/脚本”，不修改用户私有数据。
- 留痕不可删：每次审计与执行必须产出事件与日志。
---
## 2) 三色审计（审判长裁决）→ 诸葛亮执行选择
> 执行顺序：先三色判定，再执行对应动作。任何“跳过判定直接执行”视为违规。
### 2.1 三色判定标准（默认）
- 🟢 绿色（可自动执行）
- 🟡 黄色（可自动执行，但要留痕+提醒）
- 🔴 红色（必须自动强制执行）
### 2.2 诸葛亮“执行选择”输出格式（必须落地）
- 结论：🟢/🟡/🔴
- 理由：触发条件列表
- 执行动作：NONE / REQUEST_UPDATE / FORCE_UPDATE / BLOCK_STARTUP
- 证据：事件文件名 + 日志文件名
---
## 3) 自动化闭环（四段式：自检 → 事件 → 更新 → 备份）
### 3.1 标准运行顺序（无人值守）
1. healthcheck：导出后/发布前，检查 dist 是否“可喂给外部工具”。
1. control plane：读取 instance_meta，计算 age_hours，输出 event JSON。
1. updater：读取最新 event，执行策略（提醒/强制/拦截）。
1. backup push：更新成功后进行三路备份（本地快照 + Git 推送 + HTTP 上传位）。
### 3.2 24 小时窗口（硬规则）
- ≤ 24h：允许运行，但必须事件留痕。
- > 24h：进入红色策略（FORCE_UPDATE 或 BLOCK_STARTUP）。
---
## 4) 必备文件与目录结构（真实文件名，不退缩不逃避）
### 4.1 目录建议
- ./dist
- ./events
- ./snapshots
- ./mother_applied
### 4.2 文件清单（真实文件名，代码块固定展示）
```javascript
uid9622_healthcheck.py
instance_meta.json
uid9622_control_plane.py
uid9622_updater.py
uid9622_updater_v3_folder.py  # optional
uid9622_backup_push.py        # optional
integrated_health_workflow.sh # optional
UID9622_完整系统_使用指南.md      # optional
```
---
## 5) instance_meta 字段规范（P0，单一事实源）
> instance_meta.json 是“外部实例自描述”的唯一权威文件：总控台与更新器只以它为准。
### 5.1 字段分组与写入权限
- 实例侧可写（Instance-owned）：instance_id、auto_update、last_sync_time（在成功同步后更新）
- 发布侧写入（Publisher-owned）：mother_version、package_hash_sha256、signature、signer_id、signed_at、public_key_fingerprint
- 系统生成（System-owned）：last_event_id（可选）、last_event_time（可选）、sources_tried（可选）
### 5.2 必填字段（P0）
- instance_id：字符串，实例唯一 ID
- auto_update：布尔值，是否允许强制更新
- last_sync_time：ISO-8601（含时区），最近一次成功同步时间
- mother_version：字符串，当前生效母本版本
- package_hash_sha256：字符串（hex），与 mother_version 绑定的发布包哈希
- signature：字符串（Base64），对 canonical_payload 的 Ed25519 签名
- signer_id：字符串，签名者固定标识（人/CI/机构）
- signed_at：ISO-8601（含时区），签名时间
- public_key_fingerprint：字符串，公钥指纹（用于快速验真与轮换判断）
### 5.3 推荐字段（P1）
- instance_version：字符串，实例本地运行版本（可等于 mother_version，也可为本地封装版本）
- update_channel：字符串，例如 stable/beta/hotfix（用于灰度）
- policy_version：字符串，治理策略版本（用于兼容升级）
### 5.4 文件示例（真实字段，纯文本）
```json
{
  "instance_id": "instance-001",
  "auto_update": true,
  "last_sync_time": "2025-12-24T01:00:00+08:00",

  "mother_version": "PUBLIC-LANDING-CONSTITUTION-V1.1",
  "package_hash_sha256": "<HEX_SHA256_OF_PACKAGE>",
  "signature": "<BASE64_ED25519_SIGNATURE>",
  "signer_id": "uid9622-publisher-ci",
  "signed_at": "2025-12-24T00:30:00+08:00",
  "public_key_fingerprint": "<PUBKEY_FINGERPRINT>"
}
```
### 5.5 校验铁律（updater 必须执行）
- canonical_payload 按本页 9.2.3 的固定格式生成
- 用公钥验证 signature
- 校验 package_hash_sha256
- 任一失败：BLOCK_STARTUP + FAIL 事件 + 禁止 apply
---
## 6) 事件与留痕规范（机器可读 + 人可读）
### 6.1 事件最小字段（P0）
- timestamp
- instance_id
- mother_version
- instance_version
- status: OK / WARN / FAIL
- action: NONE / REQUEST_UPDATE / FORCE_UPDATE / BLOCK_STARTUP
- checks: age_hours / placeholder / notion_tag / zero_width
- package_hash_sha256（母本包/接入包哈希）
- signature（对 canonical_payload 的签名，Base64）
- signer_id（签名者标识，建议固定为母本发布者/CI）
- signed_at（签名时间，ISO-8601）
- public_key_fingerprint（公钥指纹，用于快速验真）
### 6.2 日志最小字段（P0）
- 运行时间
- 执行脚本
- 输入文件（instance_meta / dist 路径）
- 输出文件（event 路径）
- 退出码
### 6.3 退出码规范（P0，updater / control plane / healthcheck）
> 目标：任何环境（国内外、CI、本地、服务器）都能用退出码做自动化判断。
- 0：OK（允许继续）
- 1：HEALTHCHECK_FAILED（dist 不合格，禁止继续喂给外部工具）
- 2：BLOCK_STARTUP（红色拦截：超窗或验签失败或策略禁止）
- 3：UPDATE_FAILED（强制更新失败/回滚失败）
- 4：SIGNATURE_INVALID（验签失败：signature 不合法或公钥不匹配）
- 5：HASH_MISMATCH（sha256 不匹配）
- 6：META_INVALID（instance_meta 缺字段/字段格式非法）
> 注意：上层编排可以把 4/5/6 统一映射为 BLOCK_STARTUP（2），但日志与事件里必须保留原始原因码。
### 6.4 验签失败事件模板（P0，必须落 events；代码块固定展示）
> 触发：签名校验失败 / sha256 不一致 / public_key_fingerprint 不匹配。
```json
{
  "timestamp": "2025-12-24T06:00:00+08:00",
  "instance_id": "instance-001",
  "mother_version": "PUBLIC-LANDING-CONSTITUTION-V1.1",
  "instance_version": "PUBLIC-LANDING-CONSTITUTION-V1.1",
  "status": "FAIL",
  "action": "BLOCK_STARTUP",
  "reason_code": "SIGNATURE_INVALID",
  "checks": {
    "age_hours": 3,
    "window_hours": 24,
    "placeholder": "PASS",
    "notion_tag": "PASS",
    "zero_width": "PASS",

    "package_hash_sha256_expected": "<HEX_SHA256_OF_PACKAGE>",
    "package_hash_sha256_actual": "<HEX_SHA256_OF_LOCAL>",

    "signer_id": "uid9622-publisher-ci",
    "signed_at": "2025-12-24T00:30:00+08:00",

    "public_key_fingerprint_expected": "<PUBKEY_FINGERPRINT>",
    "public_key_fingerprint_actual": "<PUBKEY_FINGERPRINT_LOCAL>"
  },
  "exit_code": 4,
  "evidence": {
    "meta_file": "./instance_meta.json",
    "public_key_file": "./keys/uid9622_public_key_ed25519.pub",
    "event_file": "./events/event_instance-001_20251224T060000+0800_FAIL_BLOCK_STARTUP_SIGNATURE_INVALID.json",
    "log_file": "./events/updater.log"
  }
}
```
处置铁律：
- 立即 BLOCK_STARTUP
- 不允许 apply
- 写 FAIL 事件 + 日志
- 超过 24h 未修复 → 进入红色窗口（FORCE_UPDATE 或持续 BLOCK_STARTUP）
### 6.5 event 文件命名规范（P0）
> 目标：所有实例、所有国家/地区、所有环境生成的 event 文件名都可排序、可检索、可比对。
目录固定：./events/
命名固定（推荐）：
- event_<instance_id>_<YYYYMMDD>T<HHMMSS><TZOFFSET>_<STATUS>_<ACTION>_<REASON>.json
字段解释：
- <TZOFFSET> 示例：+0800、+0000
- <STATUS>：OK / WARN / FAIL
- <ACTION>：NONE / REQUEST_UPDATE / FORCE_UPDATE / BLOCK_STARTUP
- <REASON>：NONE / SIGNATURE_INVALID / HASH_MISMATCH / META_INVALID / UPDATE_FAILED / HEALTHCHECK_FAILED
示例：
- event_instance-001_20251224T060000+0800_FAIL_BLOCK_STARTUP_SIGNATURE_INVALID.json
- event_instance-001_20251224T080000+0800_WARN_REQUEST_UPDATE_NONE.json
- event_instance-001_20251224T090000+0800_OK_NONE_NONE.json
强制规则：
- 文件名必须包含 instance_id + 时间戳 + 状态 + 动作
- reason_code 必须与 JSON 内的 reason_code 一致
- 同一秒内多事件：允许在末尾追加 _N1/_N2
### 6.6 event_file 一致性校验规则（P0，强制）
> 目的：防止“事件内容”和“落盘文件名”不一致导致取证争议；同时强制形成“自证闭环”。
### 6.6.1 强制要求
- 必须写入：所有 event JSON 必须包含 evidence.event_file 字段。
- 必须一致：evidence.event_file 必须等于“本次实际写入的 event 文件路径”。
### 6.6.2 校验点（必须）
写文件后立刻比较：
- realpath(actual_written_file)
- realpath(evidence.event_file)
### 6.6.3 缺失/不一致处置（P0）
- 缺失（没有 evidence.event_file）：
- 不一致（路径不匹配）：
### 6.6.4 补充建议（不改变 P0 铁律）
- event_file 建议写相对路径：./events/<filename>.json，便于跨机/跨系统迁移
- 但在校验时统一转换为 realpath 进行比较
---
## 7) 自动化调度（cron / 任务计划）
> 目标：无人理照样执行。
- 建议节奏
---
## 8) 违规处理（输出侧策略）
### 7.1 违规定义（P0）
- 跳过三色审计直接执行（绕过审判长）
- 超过 24h 仍继续运行且不更新
- 强制更新触碰用户私有数据
- 删除/篡改事件与日志
### 7.2 自动处置（默认）
- 🔴 触发即刻：BLOCK_STARTUP + 生成 FAIL 事件 + 写入审计日志
- 🔁 自动回滚：更新失败必须回滚到上一个可用版本
- 📋 证据保全：写入 events + snapshots
---
## 9) 故障与回滚（不靠人脑记）
### 8.1 更新失败
- 必须回滚到上一个可用目录（mother_applied 的 rollback 版本）
- 必须输出 event: APPLY_FAILED 或 UPDATE_FAILED
### 8.2 健康检查失败
- dist 不允许继续被喂给外部工具
- 必须先 sanitize 再发布
---
## 10) P0 接通三关口（国内外协作必备）
> 这是“能不能真正接通国内外协作”的关键。三关口缺一不可。
### 9.1 入口关口：协作入口标准化（只发 dist）
- 对外协作只交付 dist，不交付母本、不交付私密库。
- 所有协作方必须按统一流程接入：
### 9.2 签名关口：sha256 + 私钥签名（B方案，P0）
> 目的：让“更新包”具备不可抵赖性，国内外协作都能独立验真。
### 9.2.1 签名算法约定（你选择的 2）
- 推荐算法：Ed25519（快、实现简洁、跨平台）
- signature 编码：Base64（单行字符串）
- payload 编码：UTF-8
- 换行规范：payload 内使用 n（LF），不使用 rn
- 哈希算法：SHA-256
### 9.2.2 必须生成的字段（发布侧）
- mother_version（版本号）
- package_hash_sha256（对 dist 或 mother_applied 做“目录打包后”的 sha256，或对发布包文件的 sha256）
- signature（私钥对 canonical_payload 签名）
- signer_id（签名者标识：人/CI/机构，固定值）
- signed_at（签名时间，ISO-8601，含时区）
### 9.2.3 canonical_payload（必须固定，避免歧义）
> 顺序固定、字段名固定、换行固定。
- canonical_payload（四行，顺序不可变）：
### 9.2.4 校验规则（updater 必须执行）
- 校验 sha256 是否匹配
- 用公钥验证 signature 是否能验证 canonical_payload
- 任一失败：
### 9.2.5 密钥管理与分发（你选择的 1）
- 私钥存放：只允许存在于发布侧（本地/CI），绝不发给协作方。
- 公钥分发：协作方只持有公钥，用于验证。
- 公钥文件命名（建议）：uid9622_public_key_ed25519.pub
- 公钥文件放置路径（建议）：
- 公钥指纹字段（建议）：public_key_fingerprint（写入 instance_meta 与 event）
- 公钥轮换（P0）：
- 公钥变更审计：公钥变更本身必须走三色审计，并产出事件与日志
### 9.3 数据边界关口：跨境/跨团队数据边界写死
- 禁止进入对外包的内容类型：
- 审计日志只记录：
---
## 11) 接通后“协作契约”（建议写入对外 README）
- 协作方默认义务：按 24 小时窗口更新；不更新即被强制。
- 协作方禁止行为：反向覆盖母本；绕过三色审计；删日志。
- 争议处理：以事件与日志为唯一证据。
---
## 12) 确认码协议（#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z，P0）
> 目的：让“关键变更/公开发布/强制执行”都有唯一、可追溯、可验真的人工确认锚点。
### 12.1 适用范围（必须打确认码的场景）
- 对外公开发布（文档/仓库/方案/规则）
- 更新 P0 规则（新增/删除/改写/降级）
- 变更密钥材料（公钥轮换、签名策略变更）
- 触发强制更新或 BLOCK_STARTUP 策略变更
### 12.2 “ONLY-ONCE”语义（一次性）
- 同一个确认码只能对应一次具体发布/一次具体变更。
- 任何复用都视为“证据链污染”，需要重新生成新确认码。
### 12.3 记录位置（双落点）
确认码必须同时出现在：
1. Notion 母本文档（本页）
1. 事件 Event JSON（机器可读）
建议事件字段：
- confirm_code：固定写 #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
- confirm_scope：本次确认覆盖范围（例如："public_release" / "p0_policy_change"）
- confirm_subject：具体对象（页面/仓库/版本号）
- confirmer_id：确认者标识（例如：UID9622）
- confirmed_at：确认时间（ISO-8601）
### 12.4 验真规则（updater / control plane 必须执行）
- 若操作属于 12.1 的适用范围，但缺少 confirm_code：
- 若 confirm_code 与本次发布主体不匹配（例如复用到别的仓库/别的版本）：
### 12.5 对外公开时的最小展示格式（建议）
- README / Release Notes 至少包含：
---
## 13) 版本与签字
- 版本代号：UID9622-CONTROL-PLANE-GOVERNANCE-V2.1
- 生效时间：2025-12-24 (GMT+8)
- 确认码：#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
- 审计链：诸葛亮（推演）→ 审判长（三色裁决）→ 总控台（执行+留痕）
