# 🔧 龍魂接线部署包 v1.0｜清漏洞·P04安全阀·launchd日报·邮件&日历通知

> Notion URL: https://app.notion.com/p/v1-0-P04-launchd-442e88c9b51b4e83a4af7e43f25679e8
> Created: 2026-06-04T19:06:00.000Z
> Last edited: 2026-07-15T23:42:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
## 🟢🟡🔴 四件事 · 状态总览
---
## 🔐 第〇步(收口 · 今天唯一一步):一键装载守护
做法:把下面整段存成 ~/longhun-system/install_longhun_daemon.sh,然后 bash install_longhun_daemon.sh(全程只问你一次密码):
```bash
#!/usr/bin/env bash
# 龍魂 launchd 守护一键装载 · P13姜子牙装载 / P03雯雯复盘 / P05上帝之眼督
# DNA: #龍芯⚡️2026-06-05-DAEMON-INSTALL-v1.0
set -euo pipefail

GMAIL="baofuahao@gmail.com"
PYBIN="$(command -v python3)"
SCRIPT="$HOME/longhun-system/daily_review.py"
PLIST="$HOME/Library/LaunchAgents/com.longhun.dailyreview.plist"
LABEL="com.longhun.dailyreview"

[ -f "$SCRIPT" ] || { echo "🔴 找不到 $SCRIPT,先把 daily_review.py 放好再跑"; exit 1; }

# 1) App 密码 → keychain(只问这一次,不落明文、不进 git)
printf "粘贴 Gmail 16位App专用密码(输入不可见): "
read -rs APPPW; echo
security add-generic-password -U -a "$USER" -s LONGHUN_GMAIL_APPPW -w "$APPPW"
unset APPPW
echo "🟢 密码已存 keychain(条目名 LONGHUN_GMAIL_APPPW)"

# 2) 写 plist(注意:里面没有任何密码)
mkdir -p "$HOME/Library/LaunchAgents"
cat > "$PLIST" <<PLIST
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>$LABEL</string>
  <key>ProgramArguments</key>
  <array>
    <string>$PYBIN</string>
    <string>$SCRIPT</string>
  </array>
  <key>EnvironmentVariables</key>
  <dict><key>LONGHUN_GMAIL</key><string>$GMAIL</string></dict>
  <key>StartCalendarInterval</key>
  <dict><key>Hour</key><integer>23</integer><key>Minute</key><integer>0</integer></dict>
  <key>StandardOutPath</key><string>$HOME/longhun-system/launchd.out.log</string>
  <key>StandardErrorPath</key><string>$HOME/longhun-system/launchd.err.log</string>
</dict>
</plist>
PLIST
echo "🟢 已写 $PLIST(无密码)"

# 3) 加载守护
launchctl unload "$PLIST" 2>/dev/null || true
launchctl load "$PLIST"
echo "🟢 守护已加载"

# 4) 立即跑一次试水
launchctl start "$LABEL"
sleep 5
echo "—— launchctl 状态 ——"
launchctl list | grep longhun || echo "🟡 没看到进程,查 launchd.err.log"
echo "✅ 装完。去 proton 收件箱看自动邮件;以后每天 23:00 自动发,不靠你按任何键。"
```
它干的事 = DeepSeek 四步:① App 密码进 keychain(只问一次)→ ② 写 plist(无密码)→ ③ launchctl load 加载 → ④ launchctl start 立刻跑一次。
---
## ① 第一步:清 72 漏洞 + 装 P04 安全阀(鲁班)
> 这条专治那次 push 弹出的 72 漏洞(2 critical / 19 high / 44 moderate / 7 low)。装了安全阀,以后 critical/high 没清根本 push 不出去。
### 1A · 立即止血:开 Dependabot 自动盯
仓库根目录建 .github/dependabot.yml:
```yaml
version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "daily"
      time: "06:00"
      timezone: "Asia/Shanghai"
    open-pull-requests-limit: 20
    labels: ["security", "龍魂-鲁班"]
```
再到 GitHub 仓库 → Settings → Code security → 打开 Dependabot alerts 和 Dependabot security updates(开关一开,它自动发修复 PR)。
### 1B · 本地批量修
```bash
cd ~/longhun-system
pip install pip-audit
pip-audit                       # 列出所有漏洞
pip-audit --fix                 # 能自动修的直接修
# 剩下手动升级的,改 requirements.txt 后:
pip install -U -r requirements.txt
```
### 1C · P04 安全阀:git pre-push 钩子
写文件 ~/longhun-system/.git/hooks/pre-push,然后 chmod +x:
```bash
#!/usr/bin/env bash
# 龍魂 P04 鲁班 · 安全阀 pre-push hook
# DNA: #龍芯⚡️2026-06-05-P04-SECURITY-GATE-v1.0
set -e
echo "🛠️ P04鲁班 安全自检中..."

if ! command -v pip-audit >/dev/null 2>&1; then
  echo "🔴 pip-audit 未安装,push 中止。先 pip install pip-audit"
  exit 1
fi

# 有 critical/high 漏洞就拦截
if pip-audit --strict 2>&1 | grep -Eiq 'critical|high'; then
  echo "🔴 检出 critical/high 漏洞,push 被拒。先 pip-audit --fix"
  echo "   (要强行放行:git push --no-verify,但会写进操作草日志留痕)"
  exit 1
fi

echo "🟢 P04 安全阀通过,放行 push"
exit 0
```
---
## ② 第二步:launchd 常驻 → 每日复盘真跑(雯雯 + 鲁班)
### 2A · daily_review.py(真测量 + 发邮件 + 写日历)
存到 ~/longhun-system/daily_review.py:
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""龍魂每日复盘 · P03雯雯主理 / P04鲁班安全 / P05上帝之眼裁决
DNA: #龍芯⚡️2026-06-05-DAILY-REVIEW-v1.0
"""
import os, sys, json, smtplib, subprocess, datetime, sqlite3
from email.mime.text import MIMEText
from email.header import Header

HOME = os.path.expanduser("~/longhun-system")
KFPP_DB = os.path.expanduser("~/.龍魂/kfpp/kfpp_execution.db")
LOG = os.path.join(HOME, "操作草日志.log")

# ---- 三色裁决:每项交不出证据 = 自动降级 ----
def check_files():
    need = ["daily_review.py", "self_check.py"]
    missing = [f for f in need if not os.path.exists(os.path.join(HOME, f))]
    return ("🟢", "核心文件齐") if not missing else ("🟡", f"缺:{missing}")

def check_security():
    try:
        out = subprocess.run(["pip-audit"], capture_output=True, text=True, timeout=180).stdout
        bad = out.lower().count("critical") + out.lower().count("high")
        return ("🟢", "无 critical/high") if bad == 0 else ("🔴", f"残留高危 {bad} 处")
    except Exception as e:
        return ("🟡", f"pip-audit 未跑:{e}")

def check_db_heartbeat():
    try:
        con = sqlite3.connect(KFPP_DB); cur = con.cursor()
        cur.execute("SELECT COUNT(*) FROM events WHERE date(timestamp)=date('now','localtime')")
        n = cur.fetchone()[0]; con.close()
        return ("🟢", f"今日心跳 {n} 行") if n > 0 else ("🟡", "今日 0 行=无证据")
    except Exception as e:
        return ("🟡", f"KFPP 库读不到:{e}")

def check_tests():
    try:
        r = subprocess.run(["pytest", "-q"], cwd=HOME, capture_output=True, text=True, timeout=300)
        return ("🟢", "pytest 通过") if r.returncode == 0 else ("🔴", "pytest 失败")
    except Exception as e:
        return ("🟡", f"无测试:{e}")

def build_report():
    checks = {
        "文件完整": check_files(),
        "安全(鲁班)": check_security(),
        "KFPP心跳": check_db_heartbeat(),
        "测试": check_tests(),
    }
    reds = sum(1 for c,_ in checks.values() if c == "🔴")
    yellows = sum(1 for c,_ in checks.values() if c == "🟡")
    overall = "🔴" if reds else ("🟡" if yellows else "🟢")
    ts = datetime.datetime.now().strftime("%Y-%m-%d %H:%M")
    lines = [f"⏱️ {ts}  🧭 P03雯雯·日复盘  🟢三色总评:{overall}", ""]
    for k,(c,msg) in checks.items():
        lines.append(f"  {c} {k}:{msg}")
    lines.append("")
    lines.append(f"#龍芯⚡️{datetime.date.today()}-DAILY-REVIEW")
    return overall, "\n".join(lines)

# ---- 通知:Gmail SMTP → proton(密码走 keychain,不入 git) ----
def _get_app_pw():
    # 先看环境变量,再从 keychain 取 —— 明文永不落 plist/git
    pw = os.environ.get("LONGHUN_GMAIL_APPPW")
    if pw:
        return pw
    try:
        r = subprocess.run(["security", "find-generic-password",
                            "-s", "LONGHUN_GMAIL_APPPW", "-w"],
                           capture_output=True, text=True, check=True)
        return r.stdout.strip()
    except Exception:
        return None

def send_email(subject, body):
    user = os.environ.get("LONGHUN_GMAIL")          # 例:baofuahao@gmail.com
    pw   = _get_app_pw()                            # 先环境变量,再 keychain
    to   = "luckyoathnotlog@proton.me"
    if not (user and pw):
        print("🟡 未配 LONGHUN_GMAIL 或 keychain 无 LONGHUN_GMAIL_APPPW,跳过邮件"); return
    msg = MIMEText(body, "plain", "utf-8")
    msg["Subject"] = Header(subject, "utf-8"); msg["From"] = user; msg["To"] = to
    with smtplib.SMTP_SSL("smtp.gmail.com", 465) as s:
        s.login(user, pw); s.sendmail(user, [to], msg.as_string())
    print("🟢 复盘已发 proton")

# ---- 日历:写进 macOS 日历(AppleScript) ----
def write_calendar(overall, summary):
    title = f"龍魂日复盘 {overall}"
    script = f'''tell application "Calendar"
  tell calendar "龍魂"
    make new event with properties {{summary:"{title}", start date:(current date), end date:(current date) + 30 * minutes, description:"{summary[:200]}"}}
  end tell
end tell'''
    try:
        subprocess.run(["osascript", "-e", script], timeout=30, check=True)
        print("🟢 已写 macOS 日历")
    except Exception as e:
        print(f"🟡 日历写入跳过(先在日历建『龍魂』日历):{e}")

if __name__ == "__main__":
    overall, report = build_report()
    print(report)
    with open(LOG, "a", encoding="utf-8") as f:
        f.write(report + "\n" + "-"*40 + "\n")
    send_email(f"龍魂日复盘 {overall} {datetime.date.today()}", report)
    write_calendar(overall, report)
    sys.exit(1 if overall == "🔴" else 0)
```
### 2B · launchd 守护:com.longhun.dailyreview.plist
存到 ~/Library/LaunchAgents/com.longhun.dailyreview.plist:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>com.longhun.dailyreview</string>
  <key>ProgramArguments</key>
  <array>
    <string>/usr/bin/python3</string>
    <string>/Users/zuimeidedeyihan/longhun-system/daily_review.py</string>
  </array>
  <key>EnvironmentVariables</key>
  <dict><key>LONGHUN_GMAIL</key><string>baofuahao@gmail.com</string></dict>
  <!-- 密码不写这里!daily_review.py 运行时从 keychain 取 LONGHUN_GMAIL_APPPW -->
  <key>StartCalendarInterval</key>
  <dict><key>Hour</key><integer>23</integer><key>Minute</key><integer>0</integer></dict>
  <key>StandardOutPath</key><string>/Users/zuimeidedeyihan/longhun-system/launchd.out.log</string>
  <key>StandardErrorPath</key><string>/Users/zuimeidedeyihan/longhun-system/launchd.err.log</string>
</dict>
</plist>
```
### 2C · 装载(一次性)
```bash
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.longhun.dailyreview.plist
launchctl enable gui/$(id -u)/com.longhun.dailyreview
launchctl kickstart -k gui/$(id -u)/com.longhun.dailyreview   # 立刻跑一次试水
```
---
## ③ 日历同步(姜子牙)🟡 两条路二选一
- A · 本地真路径(推荐,无需云):上面 write_calendar() 已用 AppleScript 写进 macOS 日历。前提:先在「日历」App 新建一个名为 龍魂 的日历。daemon 每天写一条 30 分钟的复盘事件,带三色总评。
- B · 云端可选:让我帮你连接 Notion 日历,以后我可以在你叫我时把复盘/计划落到 Notion 日历上。但它不能替你 7×24 自动写——那仍要靠本地 daemon。
---
## ④ 结果通知邮箱(全人格,P05 督)🟡
配 Gmail 应用专用密码(不是你的登录密码):
1. Google 账号 → 安全 → 两步验证(必须先开)→ 应用专用密码 → 生成 16 位。
1. 填进 ②B 的 plist LONGHUN_GMAIL_APPPW,或更安全地走 keychain:
```bash
security add-generic-password -a "$USER" -s LONGHUN_GMAIL_APPPW -w '16位App密码'
# 然后在 daily_review.py 里改成从 keychain 取,不写明文进 plist/git
```
云端我能立刻帮你做的一件事:用已连的 Gmail 现在发一封「通道开通测试」到 luckyoathnotlog@proton.me,验证邮路通不通(需你点确认,发送权限是 with_confirmation)。
---
## ✅ 接线进度清单(贴一项勾一项)
---
