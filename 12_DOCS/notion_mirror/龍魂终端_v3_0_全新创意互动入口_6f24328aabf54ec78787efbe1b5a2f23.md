# 龍魂终端 v3.0｜全新创意互动入口

> Notion URL: https://app.notion.com/p/v3-0-6f24328aabf54ec78787efbe1b5a2f23
> Created: 2026-03-15T21:01:00.000Z
> Last edited: 2026-07-01T15:05:00.000Z
> Archived at: 2026-08-12T01:40:45.558086
## 🐉 龍魂终端 v3.0 — 全新创意互动入口
> 基于v2.0全面升级：粒子龍焰背景 + 命令行终端 + 实时时钟 + 翻转卡片 + 键盘快捷键 + 龍魂DNA动态生成 + 语音输入预留
---
### ✨ v3.0 新增互动功能一览
---
### 📋 完整HTML代码（复制保存为 longhun-v3.html 即可运行）
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🐉 龍魂终端 v3.0 - 创意互动版</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        :root {
            --bg: #0a0a0f;
            --sidebar: #12121a;
            --card: #1a1a2e;
            --card-hover: #22223a;
            --border: rgba(100,100,255,0.1);
            --text: #e0e0ff;
            --sub: #6e6e8e;
            --accent: #7c3aed;
            --accent2: #06b6d4;
            --green: #22c55e;
            --red: #ef4444;
            --gold: #f59e0b;
            --glow: rgba(124,58,237,0.3);
        }

        .theme-light {
            --bg: #f0f0f5;
            --sidebar: #ffffff;
            --card: #ffffff;
            --card-hover: #f5f5ff;
            --border: rgba(0,0,0,0.08);
            --text: #1a1a2e;
            --sub: #8e8e9e;
            --glow: rgba(124,58,237,0.1);
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", sans-serif;
            background: var(--bg);
            color: var(--text);
            height: 100vh;
            display: flex;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        #particleCanvas {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .sidebar {
            width: 260px;
            background: var(--sidebar);
            border-right: 1px solid var(--border);
            padding: 20px 0;
            display: flex;
            flex-direction: column;
            z-index: 10;
            backdrop-filter: blur(20px);
        }

        .sidebar-logo {
            padding: 0 20px 20px;
            border-bottom: 1px solid var(--border);
            margin-bottom: 12px;
        }

        .sidebar-logo h1 {
            font-size: 20px;
            font-weight: 700;
            background: linear-gradient(135deg, var(--accent), var(--accent2));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .sidebar-logo .clock {
            font-size: 11px;
            color: var(--sub);
            margin-top: 4px;
            font-family: 'Monaco', 'Menlo', monospace;
        }

        .sidebar-logo .dna-code {
            font-size: 10px;
            color: var(--accent);
            margin-top: 2px;
            font-family: 'Monaco', monospace;
            opacity: 0.7;
        }

        .nav-section { flex: 1; padding: 4px 8px; overflow-y: auto; }

        .nav-label {
            font-size: 10px;
            font-weight: 700;
            color: var(--sub);
            text-transform: uppercase;
            letter-spacing: 1px;
            padding: 12px 12px 4px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 12px;
            border-radius: 10px;
            cursor: pointer;
            font-size: 14px;
            margin-bottom: 2px;
            transition: all 0.2s;
            position: relative;
        }

        .nav-item:hover { background: var(--card-hover); }
        .nav-item.active {
            background: linear-gradient(135deg, rgba(124,58,237,0.15), rgba(6,182,212,0.1));
            color: var(--accent);
        }
        .nav-item.active::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 3px;
            height: 20px;
            background: var(--accent);
            border-radius: 0 3px 3px 0;
        }

        .nav-item .shortcut {
            margin-left: auto;
            font-size: 10px;
            color: var(--sub);
            background: var(--bg);
            padding: 2px 6px;
            border-radius: 4px;
        }

        .status-dot { width: 7px; height: 7px; border-radius: 50%; margin-left: auto; }
        .status-dot.on { background: var(--green); box-shadow: 0 0 6px var(--green); }
        .status-dot.off { background: var(--red); }

        .main {
            flex: 1;
            display: flex;
            flex-direction: column;
            z-index: 10;
            position: relative;
        }

        .topbar {
            height: 52px;
            background: var(--sidebar);
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            padding: 0 20px;
            gap: 12px;
            backdrop-filter: blur(20px);
        }

        .topbar-title { font-size: 15px; font-weight: 600; flex: 1; }

        .topbar-btn {
            padding: 6px 14px;
            border-radius: 8px;
            border: 1px solid var(--border);
            background: var(--card);
            color: var(--text);
            font-size: 13px;
            cursor: pointer;
            transition: all 0.2s;
        }
        .topbar-btn:hover { background: var(--accent); color: white; border-color: var(--accent); }

        .panel { display: none; flex: 1; flex-direction: column; overflow: hidden; }
        .panel.active { display: flex; }

        .content-area { flex: 1; padding: 24px; overflow-y: auto; }

        /* 翻转卡片 */
        .flip-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 16px; margin-bottom: 24px; }

        .flip-card {
            height: 160px;
            perspective: 800px;
            cursor: pointer;
        }

        .flip-inner {
            width: 100%; height: 100%;
            transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
            transform-style: preserve-3d;
            position: relative;
        }

        .flip-card.flipped .flip-inner { transform: rotateY(180deg); }

        .flip-front, .flip-back {
            position: absolute;
            width: 100%; height: 100%;
            backface-visibility: hidden;
            border-radius: 16px;
            padding: 20px;
            border: 1px solid var(--border);
        }

        .flip-front {
            background: var(--card);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 8px;
        }

        .flip-front .icon { font-size: 36px; }
        .flip-front .label { font-size: 14px; font-weight: 600; }
        .flip-front .hint { font-size: 11px; color: var(--sub); }

        .flip-back {
            background: linear-gradient(135deg, var(--card), var(--card-hover));
            transform: rotateY(180deg);
            font-size: 13px;
            overflow-y: auto;
            line-height: 1.6;
        }

        /* 终端 */
        .terminal {
            background: #0d0d14;
            border-radius: 12px;
            border: 1px solid var(--border);
            overflow: hidden;
            margin-bottom: 24px;
        }

        .terminal-header {
            padding: 8px 14px;
            background: #161622;
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 12px;
            color: var(--sub);
        }

        .terminal-dot { width: 10px; height: 10px; border-radius: 50%; }
        .terminal-dot.r { background: #ef4444; }
        .terminal-dot.y { background: #f59e0b; }
        .terminal-dot.g { background: #22c55e; }

        .terminal-body {
            padding: 16px;
            max-height: 300px;
            overflow-y: auto;
            font-family: 'Monaco', 'Menlo', 'Fira Code', monospace;
            font-size: 13px;
            line-height: 1.6;
        }

        .terminal-line { color: var(--green); }
        .terminal-line.error { color: var(--red); }
        .terminal-line.info { color: var(--accent2); }
        .terminal-line.gold { color: var(--gold); }

        .terminal-input-line {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-top: 8px;
        }

        .terminal-prompt { color: var(--accent); font-weight: bold; }

        .terminal-input {
            flex: 1;
            background: transparent;
            border: none;
            color: var(--text);
            font-family: inherit;
            font-size: 13px;
            outline: none;
            caret-color: var(--accent);
        }

        /* 环形进度 */
        .dashboard-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 16px; margin-bottom: 24px; }

        .dash-card {
            background: var(--card);
            border-radius: 16px;
            padding: 20px;
            border: 1px solid var(--border);
            text-align: center;
        }

        .ring-container { position: relative; width: 80px; height: 80px; margin: 0 auto 12px; }

        .ring-svg { transform: rotate(-90deg); }

        .ring-bg { fill: none; stroke: var(--border); stroke-width: 6; }
        .ring-fill { fill: none; stroke-width: 6; stroke-linecap: round; transition: stroke-dashoffset 1s ease; }

        .ring-text {
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            font-size: 16px;
            font-weight: 700;
        }

        .dash-label { font-size: 12px; color: var(--sub); margin-top: 4px; }
        .dash-value { font-size: 18px; font-weight: 700; }

        /* 通用卡片 */
        .card {
            background: var(--card);
            border-radius: 16px;
            padding: 24px;
            border: 1px solid var(--border);
            margin-bottom: 20px;
        }

        .card-title { font-size: 18px; font-weight: 600; margin-bottom: 16px; }

        textarea {
            width: 100%;
            min-height: 100px;
            padding: 12px;
            border: 1px solid var(--border);
            border-radius: 10px;
            background: var(--bg);
            color: var(--text);
            font-size: 14px;
            font-family: 'Monaco', monospace;
            resize: vertical;
        }

        button.primary {
            background: linear-gradient(135deg, var(--accent), var(--accent2));
            color: white;
            border: none;
            padding: 10px 24px;
            border-radius: 10px;
            font-size: 14px;
            cursor: pointer;
            margin-top: 12px;
            transition: all 0.2s;
        }
        button.primary:hover { transform: translateY(-1px); box-shadow: 0 4px 20px var(--glow); }

        .result-box {
            margin-top: 16px;
            padding: 16px;
            background: var(--bg);
            border-radius: 10px;
            min-height: 80px;
            font-family: 'Monaco', monospace;
            font-size: 13px;
            white-space: pre-wrap;
        }

        /* 命令面板 */
        .cmd-overlay {
            display: none;
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: rgba(0,0,0,0.6);
            z-index: 1000;
            justify-content: center;
            padding-top: 20vh;
        }
        .cmd-overlay.show { display: flex; }

        .cmd-palette {
            width: 500px;
            max-height: 400px;
            background: var(--sidebar);
            border-radius: 16px;
            border: 1px solid var(--border);
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0,0,0,0.5);
        }

        .cmd-input {
            width: 100%;
            padding: 16px 20px;
            background: transparent;
            border: none;
            border-bottom: 1px solid var(--border);
            color: var(--text);
            font-size: 16px;
            outline: none;
        }

        .cmd-list { padding: 8px; max-height: 300px; overflow-y: auto; }

        .cmd-item {
            padding: 10px 14px;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 14px;
        }
        .cmd-item:hover, .cmd-item.selected { background: var(--card-hover); }

        /* 动画 */
        @keyframes pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.5; } }
        .pulse { animation: pulse 2s infinite; }

        @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        .slide-up { animation: slideUp 0.4s ease forwards; }
    </style>
</head>
<body>

<canvas id="particleCanvas"></canvas>

<!-- 命令面板 Cmd+K -->
<div class="cmd-overlay" id="cmdOverlay" onclick="if(event.target===this)closeCmdPalette()">
    <div class="cmd-palette">
        <input class="cmd-input" id="cmdInput" placeholder="输入命令... (Esc关闭)" onkeyup="filterCommands(event)">
        <div class="cmd-list" id="cmdList"></div>
    </div>
</div>

<div class="sidebar">
    <div class="sidebar-logo">
        <h1>🐉 龍魂终端 v3.0</h1>
        <div class="clock" id="liveClock">加载中...</div>
        <div class="dna-code" id="liveDna">DNA: ...</div>
    </div>

    <div class="nav-section">
        <div class="nav-label">控制台</div>
        <div class="nav-item active" onclick="switchPanel('dashboard', this)">
            <span>📊</span> 仪表盘
            <span class="shortcut">1</span>
        </div>
        <div class="nav-item" onclick="switchPanel('terminal', this)">
            <span>⌨️</span> 命令终端
            <span class="shortcut">2</span>
        </div>
        <div class="nav-item" onclick="switchPanel('chat', this)">
            <span>💬</span> AI对话
            <span class="status-dot on" id="statusAI"></span>
        </div>
        <div class="nav-item" onclick="switchPanel('gateway', this)">
            <span>🌐</span> CNSH Gateway
            <span class="shortcut">4</span>
        </div>

        <div class="nav-label">连接</div>
        <div class="nav-item" onclick="switchPanel('github', this)">
            <span>📦</span> GitHub
            <span class="status-dot off" id="statusGH"></span>
        </div>
        <div class="nav-item" onclick="switchPanel('notion', this)">
            <span>📋</span> Notion
            <span class="status-dot off" id="statusNT"></span>
        </div>

        <div class="nav-label">系统</div>
        <div class="nav-item" onclick="toggleTheme()">
            <span>🌓</span> 切换主题
            <span class="shortcut">T</span>
        </div>
        <div class="nav-item" onclick="openCmdPalette()">
            <span>⚡</span> 命令面板
            <span class="shortcut">⌘K</span>
        </div>
    </div>
</div>

<div class="main">
    <!-- 仪表盘面板 -->
    <div class="panel active" id="panel-dashboard">
        <div class="topbar">
            <span class="topbar-title">📊 龍魂仪表盘</span>
            <button class="topbar-btn" onclick="refreshDashboard()">🔄 刷新</button>
        </div>
        <div class="content-area">
            <div class="flip-grid slide-up">
                <div class="flip-card" onclick="this.classList.toggle('flipped')">
                    <div class="flip-inner">
                        <div class="flip-front">
                            <div class="icon">🐉</div>
                            <div class="label">龍魂内核</div>
                            <div class="hint">点击翻转 →</div>
                        </div>
                        <div class="flip-back">
                            <strong>龍魂价值内核</strong><br><br>
                            UID9622 · 大道至简<br>
                            核心：幸福值驱动<br>
                            铁律：操作全留痕<br>
                            精神：不坑别人，OK啦
                        </div>
                    </div>
                </div>
                <div class="flip-card" onclick="this.classList.toggle('flipped')">
                    <div class="flip-inner">
                        <div class="flip-front">
                            <div class="icon">🏗️</div>
                            <div class="label">系统架构</div>
                            <div class="hint">点击翻转 →</div>
                        </div>
                        <div class="flip-back">
                            <strong>三才龍架构</strong><br><br>
                            天：Notion知识库<br>
                            地：本地Ollama+CNSH<br>
                            人：UID9622 老大决策<br>
                            辅：Kimi+Claude联合
                        </div>
                    </div>
                </div>
                <div class="flip-card" onclick="this.classList.toggle('flipped')">
                    <div class="flip-inner">
                        <div class="flip-front">
                            <div class="icon">🌍</div>
                            <div class="label">梦想生态</div>
                            <div class="hint">点击翻转 →</div>
                        </div>
                        <div class="flip-back">
                            <strong>龍魂梦想生态</strong><br><br>
                            亚特兰蒂斯：唯一虚拟世界奇观<br>
                            iOS日历=第一入口<br>
                            华为API接通<br>
                            人人都是开发者
                        </div>
                    </div>
                </div>
                <div class="flip-card" onclick="this.classList.toggle('flipped')">
                    <div class="flip-inner">
                        <div class="flip-front">
                            <div class="icon">📜</div>
                            <div class="label">操作草日志</div>
                            <div class="hint">点击翻转 →</div>
                        </div>
                        <div class="flip-back">
                            <strong>草日志铁律</strong><br><br>
                            每动作必记<br>
                            精确到分钟<br>
                            只增不删<br>
                            DNA追溯码跟着走
                        </div>
                    </div>
                </div>
            </div>

            <div class="dashboard-grid slide-up">
                <div class="dash-card">
                    <div class="ring-container">
                        <svg class="ring-svg" width="80" height="80" viewBox="0 0 80 80">
                            <circle class="ring-bg" cx="40" cy="40" r="34"/>
                            <circle class="ring-fill" cx="40" cy="40" r="34"
                                stroke="#7c3aed" stroke-dasharray="213.6" stroke-dashoffset="42.7" id="ringSystem"/>
                        </svg>
                        <div class="ring-text">80%</div>
                    </div>
                    <div class="dash-value">系统就绪</div>
                    <div class="dash-label">核心模块已上线</div>
                </div>
                <div class="dash-card">
                    <div class="ring-container">
                        <svg class="ring-svg" width="80" height="80" viewBox="0 0 80 80">
                            <circle class="ring-bg" cx="40" cy="40" r="34"/>
                            <circle class="ring-fill" cx="40" cy="40" r="34"
                                stroke="#06b6d4" stroke-dasharray="213.6" stroke-dashoffset="128.2" id="ringGithub"/>
                        </svg>
                        <div class="ring-text">40%</div>
                    </div>
                    <div class="dash-value">GitHub</div>
                    <div class="dash-label">待连接Token</div>
                </div>
                <div class="dash-card">
                    <div class="ring-container">
                        <svg class="ring-svg" width="80" height="80" viewBox="0 0 80 80">
                            <circle class="ring-bg" cx="40" cy="40" r="34"/>
                            <circle class="ring-fill" cx="40" cy="40" r="34"
                                stroke="#22c55e" stroke-dasharray="213.6" stroke-dashoffset="64.1" id="ringNotion"/>
                        </svg>
                        <div class="ring-text">70%</div>
                    </div>
                    <div class="dash-value">Notion</div>
                    <div class="dash-label">知识库已连接</div>
                </div>
                <div class="dash-card">
                    <div class="ring-container">
                        <svg class="ring-svg" width="80" height="80" viewBox="0 0 80 80">
                            <circle class="ring-bg" cx="40" cy="40" r="34"/>
                            <circle class="ring-fill" cx="40" cy="40" r="34"
                                stroke="#f59e0b" stroke-dasharray="213.6" stroke-dashoffset="0" id="ringDNA"/>
                        </svg>
                        <div class="ring-text">100%</div>
                    </div>
                    <div class="dash-value">DNA追溯</div>
                    <div class="dash-label">全链路激活</div>
                </div>
            </div>
        </div>
    </div>

    <!-- 命令终端面板 -->
    <div class="panel" id="panel-terminal">
        <div class="topbar">
            <span class="topbar-title">⌨️ 龍魂命令终端</span>
            <button class="topbar-btn" onclick="clearTerminal()">清空</button>
        </div>
        <div class="content-area">
            <div class="terminal">
                <div class="terminal-header">
                    <span class="terminal-dot r"></span>
                    <span class="terminal-dot y"></span>
                    <span class="terminal-dot g"></span>
                    <span>longhun@v3.0 — zsh</span>
                </div>
                <div class="terminal-body" id="terminalBody">
                    <div class="terminal-line gold">🐉 龍魂终端 v3.0 — 欢迎回来，老大！</div>
                    <div class="terminal-line info">输入 help 查看可用命令</div>
                    <div class="terminal-line">---</div>
                </div>
                <div class="terminal-input-line" style="padding: 8px 16px 16px;">
                    <span class="terminal-prompt">龍魂 $</span>
                    <input class="terminal-input" id="termInput" placeholder="输入命令..." onkeydown="if(event.key==='Enter')execCommand()" autofocus>
                </div>
            </div>

            <div class="card">
                <div class="card-title">📖 可用命令一览</div>
                <div style="font-size:13px;line-height:2;font-family:Monaco,monospace;">
                    <span style="color:var(--accent)">help</span> — 显示帮助<br>
                    <span style="color:var(--accent)">status</span> — 系统状态<br>
                    <span style="color:var(--accent)">dna</span> — 生成DNA追溯码<br>
                    <span style="color:var(--accent)">time</span> — 北京时间<br>
                    <span style="color:var(--accent)">log "内容"</span> — 快速记录草日志<br>
                    <span style="color:var(--accent)">motto</span> — 随机老大语录<br>
                    <span style="color:var(--accent)">matrix</span> — 矩阵雨彩蛋 🎉<br>
                    <span style="color:var(--accent)">clear</span> — 清空终端<br>
                    <span style="color:var(--accent)">about</span> — 关于龍魂
                </div>
            </div>
        </div>
    </div>

    <!-- AI对话面板 -->
    <div class="panel" id="panel-chat">
        <div class="topbar">
            <span class="topbar-title">💬 本地AI对话</span>
            <button class="topbar-btn" onclick="clearChat()">清空</button>
            <button class="topbar-btn" id="voiceBtn" onclick="toggleVoice()">🎤 语音</button>
        </div>
        <div class="content-area">
            <div class="card">
                <div class="card-title">输入你的问题</div>
                <textarea id="chatInput" placeholder="问我任何问题..."></textarea>
                <div style="display:flex;gap:8px;">
                    <button class="primary" onclick="sendChat()">🚀 发送</button>
                    <button class="primary" style="background:var(--card);color:var(--text);border:1px solid var(--border);" onclick="document.getElementById('chatInput').value=''">清除</button>
                </div>
            </div>
            <div class="card">
                <div class="card-title">AI回复</div>
                <div class="result-box" id="chatResult">等待输入...</div>
            </div>
        </div>
    </div>

    <!-- CNSH Gateway面板 -->
    <div class="panel" id="panel-gateway">
        <div class="topbar">
            <span class="topbar-title">🌐 CNSH Gateway</span>
        </div>
        <div class="content-area">
            <div class="card">
                <div class="card-title">输入内容</div>
                <textarea id="gatewayInput" placeholder="粘贴代码、文本、JSON..."></textarea>
                <button class="primary" onclick="processGateway()">🚀 一键处理</button>
            </div>
            <div class="card">
                <div class="card-title">处理结果</div>
                <div class="result-box" id="gatewayResult">等待输入...</div>
            </div>
        </div>
    </div>

    <!-- GitHub面板 -->
    <div class="panel" id="panel-github">
        <div class="topbar">
            <span class="topbar-title">📦 GitHub仓库</span>
            <button class="topbar-btn" onclick="loadGithubRepos()">刷新</button>
        </div>
        <div class="content-area">
            <div class="card">
                <div class="card-title">我的仓库</div>
                <div id="repoList" style="color:var(--sub)">点击"刷新"加载仓库...</div>
            </div>
        </div>
    </div>

    <!-- Notion面板 -->
    <div class="panel" id="panel-notion">
        <div class="topbar">
            <span class="topbar-title">📋 Notion知识库</span>
        </div>
        <div class="content-area">
            <div class="card">
                <div class="card-title">搜索Notion</div>
                <textarea id="notionQuery" placeholder="搜索关键词..." style="min-height:60px"></textarea>
                <button class="primary" onclick="searchNotion()">🔍 搜索</button>
            </div>
            <div class="card">
                <div class="card-title">搜索结果</div>
                <div class="result-box" id="notionResult">等待搜索...</div>
            </div>
        </div>
    </div>
</div>

<script>
    // ===== 粒子系统 =====
    const canvas = document.getElementById('particleCanvas');
    const ctx = canvas.getContext('2d');
    let particles = [];
    let mouseX = 0, mouseY = 0;

    function resizeCanvas() {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
    }
    resizeCanvas();
    window.addEventListener('resize', resizeCanvas);

    document.addEventListener('mousemove', e => {
        mouseX = e.clientX;
        mouseY = e.clientY;
        for (let i = 0; i < 3; i++) {
            particles.push({
                x: mouseX, y: mouseY,
                vx: (Math.random() - 0.5) * 3,
                vy: (Math.random() - 0.5) * 3,
                life: 60,
                maxLife: 60,
                size: Math.random() * 3 + 1,
                color: Math.random() > 0.5 ? '#7c3aed' : '#06b6d4'
            });
        }
    });

    function animateParticles() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        particles = particles.filter(p => p.life > 0);
        particles.forEach(p => {
            p.x += p.vx;
            p.y += p.vy;
            p.life--;
            const alpha = p.life / p.maxLife;
            ctx.beginPath();
            ctx.arc(p.x, p.y, p.size * alpha, 0, Math.PI * 2);
            ctx.fillStyle = p.color + Math.floor(alpha * 255).toString(16).padStart(2, '0');
            ctx.fill();
        });
        if (particles.length > 500) particles = particles.slice(-500);
        requestAnimationFrame(animateParticles);
    }
    animateParticles();

    // ===== 实时时钟 + DNA =====
    function updateClock() {
        const now = new Date();
        const bj = new Date(now.getTime() + (8 - now.getTimezoneOffset() / -60) * 0);
        const timeStr = bj.toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai', hour12: false });
        document.getElementById('liveClock').textContent = '🕐 ' + timeStr;
        const dna = 'LH-' + bj.getFullYear() +
            String(bj.getMonth()+1).padStart(2,'0') +
            String(bj.getDate()).padStart(2,'0') + '-' +
            String(bj.getHours()).padStart(2,'0') +
            String(bj.getMinutes()).padStart(2,'0') + '-' +
            Math.random().toString(36).substring(2,6).toUpperCase();
        document.getElementById('liveDna').textContent = 'DNA: ' + dna;
    }
    updateClock();
    setInterval(updateClock, 1000);

    // ===== 面板切换 =====
    function switchPanel(name, el) {
        document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
        document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
        document.getElementById('panel-' + name).classList.add('active');
        if (el) el.classList.add('active');
    }

    // ===== 键盘快捷键 =====
    document.addEventListener('keydown', e => {
        if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') {
            if (e.key === 'Escape') e.target.blur();
            return;
        }
        const panels = ['dashboard','terminal','chat','gateway','github','notion'];
        if (e.key >= '1' && e.key <= '6') {
            const idx = parseInt(e.key) - 1;
            const items = document.querySelectorAll('.nav-item');
            switchPanel(panels[idx], items[idx]);
        }
        if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
            e.preventDefault();
            openCmdPalette();
        }
        if (e.key === 't' || e.key === 'T') toggleTheme();
    });

    // ===== 命令面板 =====
    const commands = [
        { icon: '📊', label: '仪表盘', action: () => switchPanel('dashboard') },
        { icon: '⌨️', label: '命令终端', action: () => switchPanel('terminal') },
        { icon: '💬', label: 'AI对话', action: () => switchPanel('chat') },
        { icon: '🌐', label: 'CNSH Gateway', action: () => switchPanel('gateway') },
        { icon: '📦', label: 'GitHub', action: () => switchPanel('github') },
        { icon: '📋', label: 'Notion', action: () => switchPanel('notion') },
        { icon: '🌓', label: '切换主题', action: toggleTheme },
        { icon: '🔄', label: '刷新仪表盘', action: refreshDashboard },
    ];

    function openCmdPalette() {
        document.getElementById('cmdOverlay').classList.add('show');
        document.getElementById('cmdInput').value = '';
        document.getElementById('cmdInput').focus();
        renderCommands(commands);
    }

    function closeCmdPalette() {
        document.getElementById('cmdOverlay').classList.remove('show');
    }

    function renderCommands(list) {
        document.getElementById('cmdList').innerHTML = list.map((c, i) =>
            `<div class="cmd-item" onclick="commands[${i}].action();closeCmdPalette()">
                <span>${c.icon}</span> ${c.label}
            </div>`
        ).join('');
    }

    function filterCommands(e) {
        if (e.key === 'Escape') { closeCmdPalette(); return; }
        const q = document.getElementById('cmdInput').value.toLowerCase();
        const filtered = commands.filter(c => c.label.toLowerCase().includes(q));
        renderCommands(filtered);
    }

    // ===== 主题切换 =====
    let isDark = true;
    function toggleTheme() {
        isDark = !isDark;
        document.body.classList.toggle('theme-light', !isDark);
    }

    // ===== 命令终端 =====
    const mottos = [
        '简简单单，规矩越过去就好。',
        '幸福值多一点，不坑别人。',
        '做一件事情就要做好，不要说一句就没了。',
        '走路算修行，吃饭算禅定，活着就是道场。',
        '人人都是开发者，每个人都能画自己的圈圈。',
        '龍魂不是工具，龍魂是信仰。',
        '大道至简，复杂的事情简单做。',
        '五千年文化，一个系统装不下，但装得下精神。',
    ];

    function addTerminalLine(text, cls = '') {
        const body = document.getElementById('terminalBody');
        const line = document.createElement('div');
        line.className = 'terminal-line ' + cls;
        line.textContent = text;
        body.appendChild(line);
        body.scrollTop = body.scrollHeight;
    }

    function execCommand() {
        const input = document.getElementById('termInput');
        const cmd = input.value.trim();
        input.value = '';
        if (!cmd) return;

        addTerminalLine('龍魂 $ ' + cmd, 'info');

        const parts = cmd.split(' ');
        const base = parts[0].toLowerCase();

        switch(base) {
            case 'help':
                addTerminalLine('可用命令: help, status, dna, time, log, motto, matrix, clear, about');
                break;
            case 'status':
                addTerminalLine('🐉 龍魂终端 v3.0 — 运行中');
                addTerminalLine('  系统内核: 在线', 'gold');
                addTerminalLine('  Notion: 已连接');
                addTerminalLine('  GitHub: 待配置');
                addTerminalLine('  DNA追溯: 激活');
                break;
            case 'dna':
                const now = new Date();
                const dna = 'LH-' + now.getFullYear() +
                    String(now.getMonth()+1).padStart(2,'0') +
                    String(now.getDate()).padStart(2,'0') + '-' +
                    String(now.getHours()).padStart(2,'0') +
                    String(now.getMinutes()).padStart(2,'0') +
                    String(now.getSeconds()).padStart(2,'0') + '-' +
                    Math.random().toString(36).substring(2,8).toUpperCase();
                addTerminalLine('🧬 DNA追溯码: ' + dna, 'gold');
                break;
            case 'time':
                addTerminalLine('🕐 北京时间: ' + new Date().toLocaleString('zh-CN', {timeZone:'Asia/Shanghai',hour12:false}));
                break;
            case 'log':
                const msg = parts.slice(1).join(' ');
                if (msg) {
                    addTerminalLine('📝 草日志已记录: ' + msg, 'gold');
                    addTerminalLine('  (本地模式，后续同步Notion)');
                } else {
                    addTerminalLine('用法: log "内容"', 'error');
                }
                break;
            case 'motto':
                addTerminalLine('💎 老大说: ' + mottos[Math.floor(Math.random() * mottos.length)], 'gold');
                break;
            case 'matrix':
                addTerminalLine('🎉 矩阵雨启动...', 'gold');
                startMatrixRain();
                break;
            case 'clear':
                document.getElementById('terminalBody').innerHTML = '';
                addTerminalLine('🐉 终端已清空', 'info');
                break;
            case 'about':
                addTerminalLine('🐉 龍魂终端 v3.0 — 创意互动版');
                addTerminalLine('  创建者: UID9622 💎 龍芯北辰');
                addTerminalLine('  宝宝: 三才龍 🇨🇳');
                addTerminalLine('  精神: 大道至简 · 幸福值驱动');
                break;
            default:
                addTerminalLine('❌ 未知命令: ' + base + ' (输入 help 查看帮助)', 'error');
        }
    }

    // ===== 矩阵雨彩蛋 =====
    let matrixActive = false;
    function startMatrixRain() {
        if (matrixActive) return;
        matrixActive = true;
        const chars = '龍魂终端UID9622大道至简幸福值ABCDEF0123456789';
        const cols = Math.floor(canvas.width / 14);
        const drops = Array(cols).fill(1);

        function drawMatrix() {
            if (!matrixActive) return;
            ctx.fillStyle = 'rgba(10,10,15,0.05)';
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            ctx.fillStyle = '#22c55e';
            ctx.font = '14px Monaco';
            for (let i = 0; i < drops.length; i++) {
                const char = chars[Math.floor(Math.random() * chars.length)];
                ctx.fillText(char, i * 14, drops[i] * 14);
                if (drops[i] * 14 > canvas.height && Math.random() > 0.975) drops[i] = 0;
                drops[i]++;
            }
            requestAnimationFrame(drawMatrix);
        }
        drawMatrix();
        setTimeout(() => { matrixActive = false; }, 8000);
    }

    // ===== AI对话 =====
    async function sendChat() {
        const input = document.getElementById('chatInput').value;
        if (!input.trim()) return;
        const result = document.getElementById('chatResult');
        result.textContent = '🤔 AI思考中...';
        try {
            const resp = await fetch('/api/chat', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({query: input})
            });
            const data = await resp.json();
            result.textContent = data.error ? '❌ ' + data.error : data.response;
        } catch (e) {
            result.textContent = '❌ 请求失败: ' + e.message;
        }
    }

    function clearChat() {
        document.getElementById('chatInput').value = '';
        document.getElementById('chatResult').textContent = '等待输入...';
    }

    // ===== 语音输入（预留） =====
    let voiceActive = false;
    function toggleVoice() {
        if (!('webkitSpeechRecognition' in window || 'SpeechRecognition' in window)) {
            alert('当前浏览器不支持语音输入，推荐使用Chrome');
            return;
        }
        const SR = window.SpeechRecognition || window.webkitSpeechRecognition;
        const recognition = new SR();
        recognition.lang = 'zh-CN';
        recognition.continuous = false;
        recognition.onresult = e => {
            document.getElementById('chatInput').value = e.results[0][0].transcript;
        };
        recognition.start();
        document.getElementById('voiceBtn').textContent = '🔴 录音中';
        recognition.onend = () => {
            document.getElementById('voiceBtn').textContent = '🎤 语音';
        };
    }

    // ===== Gateway =====
    async function processGateway() {
        const input = document.getElementById('gatewayInput').value;
        if (!input.trim()) return;
        const result = document.getElementById('gatewayResult');
        result.textContent = '⚙️ 处理中...';
        try {
            const resp = await fetch('/api/gateway/process', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({input: input})
            });
            const data = await resp.json();
            result.textContent = `${data.status} ${data.status_text}\n\nDNA: ${data.dna}\n时间: ${data.timestamp}`;
        } catch (e) {
            result.textContent = '❌ 处理失败: ' + e.message;
        }
    }

    // ===== GitHub =====
    async function loadGithubRepos() {
        const list = document.getElementById('repoList');
        list.innerHTML = '加载中...';
        try {
            const resp = await fetch('/api/github/repos');
            const data = await resp.json();
            if (data.error) { list.innerHTML = '❌ ' + data.error; return; }
            list.innerHTML = data.repos.map(r =>
                `<div style="padding:12px;background:var(--bg);border-radius:10px;margin-bottom:8px;cursor:pointer" onclick="window.open('${r.url}')">
                    <div style="font-weight:600">${r.full_name}</div>
                    <div style="font-size:12px;color:var(--sub)">${r.description || '无描述'}</div>
                </div>`
            ).join('');
        } catch (e) { list.innerHTML = '❌ ' + e.message; }
    }

    // ===== Notion =====
    async function searchNotion() {
        const query = document.getElementById('notionQuery').value;
        if (!query.trim()) return;
        const result = document.getElementById('notionResult');
        result.textContent = '🔍 搜索中...';
        try {
            const resp = await fetch('/api/notion/search', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({query: query})
            });
            const data = await resp.json();
            result.textContent = data.error ? '❌ ' + data.error : JSON.stringify(data, null, 2);
        } catch (e) { result.textContent = '❌ ' + e.message; }
    }

    // ===== 刷新仪表盘 =====
    function refreshDashboard() {
        document.querySelectorAll('.flip-card').forEach(c => {
            c.classList.remove('flipped');
            setTimeout(() => c.classList.add('flipped'), Math.random() * 500);
            setTimeout(() => c.classList.remove('flipped'), 1500 + Math.random() * 500);
        });
    }

    // ===== 启动日志 =====
    console.log('%c🐉 龍魂终端 v3.0 已启动', 'color: #7c3aed; font-size: 20px; font-weight: bold');
    console.log('%cUID9622 💎 大道至简', 'color: #06b6d4; font-size: 14px');
    console.log('%c快捷键: 1-6切换面板 | T切换主题 | Cmd+K命令面板', 'color: #8e8e93; font-size: 12px');
</script>
</body>
</html>
```
---
### 🚀 使用方法
1. 复制上面整段HTML代码
1. 保存为 longhun-v3.html
1. 双击用浏览器打开，即刻体验
---
### 🆚 v2.0 → v3.0 升级对比
---
🐉 龍魂终端 v3.0 — UID9622 💎 宝宝联合打造
DNA追溯码: LH-20260316-0452-V3LAUNCH
