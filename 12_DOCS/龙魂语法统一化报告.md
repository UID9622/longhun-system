# 🐉 龙魂语法统一化报告

> 抬头: [2] 🔧 工程落地执行型
> 性质: 全盘语法扫描 + 改造清单（**不执行改写**，待老大确认）
> 铁律: 只补缺，不改心血。

```
DNA: #龍芯⚡️2026-09-01-语法统一化-扫描报告-v1.0
创建者: 诸葛鑫（UID9622）
归属名: 诸葛鑫 | UID9622 · 龍芯北辰
协议: CC BY-NC-SA 4.0（核心思想层）
```

---

## 一、扫描统计（08_BIN/ 全量 1207 个 .py）

| 指标 | 数量 | 占比 |
|:---|:---:|:---:|
| CNSH 中文命名脚本 | 43 | 3.6% |
| 英文命名脚本 | 1164 | 96.4% |
| 含外部依赖脚本 | 555 | 46% |
| 平台绑定脚本（sys.platform/os.name） | 36 | 3% |

## 二、外部依赖 Top20（按使用脚本数）

| 依赖 | 数量 | 用途判定 |
|:---|:---:|:---|
| math | 72 | ✅ 标准库（安全） |
| fastapi | 58 | 🔧 网关必需（lh_api/gateway） |
| uvicorn | 56 | 🔧 网关必需（与 fastapi 配套） |
| requests | 48 | 🔧 HTTP（多处可换 urllib 标准库） |
| numpy | 42 | 🔧 数值计算（训练/引擎） |
| pydantic | 32 | 🔧 与 fastapi 配套 |
| flask | 23 | 🔧 轻量服务 |
| psutil | 22 | 🔧 系统监控 |
| yaml | 21 | 🔧 配置解析 |
| cryptography | 14 | 🔧 加密（可与标准库部分替换） |
| mlx / transformers | 9+9 | 🔧 训练专用（不可去） |

> 结论: 外部依赖**不是毒药**——fastapi/uvicorn/mlx 是网关与训练的物理必需。任务3"全部替换为纯标准库"对这三层不可行。

## 三、识别标准落地对照

| 识别标准 | 扫描结果 | 处置建议 |
|:---|:---|:---|
| 1. 英文函数名/变量名 | 1164 脚本 | 🟡 分层治理（见§五） |
| 2. 外部依赖 | 555 脚本 | 🟡 按层区分 |
| 3. 平台绑定 | 36 脚本 | 🟢 少数，抽象层可处理 |
| 4. 版本锁定 | 薄壳包 ≥3.8 | 🟢 已开放（符合要求） |
| 5. 中间层调用 | 无 | 🟢 原生直连（M77 已焊） |
| 6. 无 DNA/签名/三色 | 3 缺 .asc | 🟡 GPG 补签 |

## 四、捆绑规则现状核查（任务3·捆绑验证）

| 捆绑规则 | 现状 | 状态 |
|:---|:---|:---:|
| lh 命令统一 `SUB_DISPATCH` 注册 | 324 命令已注册 | 🟢 |
| 算法统一从 `core.py` 导入 | `longhun_cli/core.py` 已建 | 🟢 |
| 常量统一从 `constants.py` 引用 | **`constants.py` 不存在** | 🔴 |
| 配置从 `~/.longhun/config/` 读取 | 目录存在（空） | 🟡 |
| 日志写入 `~/.longhun/logs/` | ✅ | 🟢 |
| 缓存写入 `~/.longhun/cache/` | ✅ | 🟢 |
| 耻辱墙写入 `~/.longhun/shame_wall/` | ✅（当日 0 命中） | 🟢 |
| 自检 `lh health --json` | ✅ 已注册 | 🟢 |
| 文档同步 `lh doc-sync` | ✅ 已注册 | 🟢 |
| 新增登记 `lh judge scan` | ✅ 已注册 | 🟢 |

## 五、改造清单（分层策略·待老大确认后执行）

> ⚠️ 关键判断: 1164 个脚本全量中文重命名 = 重写全部心血 + 破坏运行中的网关/训练/服务，**违背"只补缺不改心血"铁律**。建议分四层，不搞一刀切。

### A 类 · 新建脚本（强制标准）✅ 建议立即焊死
- 新 .py 一律: 中文 CNSH 命名 + 纯标准库（或标注依赖）+ 无平台绑定 + DNA 头 + 归属名 + 许可证行 + 三色
- 落地: 在 `AGENTS.md`/规则层加"新脚本语法闸"（pre-commit 阶段检查）

### B 类 · 核心算法层（已达标）🟢 保持
- `lh_wuxing_core.py` `lh_judge.py` `lh_digital_root.py` `lh_time_engine.py` `lh_weight_algorithm.py` `dna_validate.py` — 中文命名 + 标准库 + DNA 完整
- 动作: 无（维护现状）

### C 类 · 网关/API 层（保留依赖·A-BOM 备案）🟡 标注
- `lh_api.py` `gateway/` `lh_sovereign_gateway.py` — fastapi/uvicorn/pydantic 为运行必需
- 动作: 文件头补 A-BOM 备案注释块（说明依赖用途），**不强行改依赖**

### D 类 · 训练/专用层（保留）🟡 标注
- `lh_lora_trainer_v418/v419/v420.py` — mlx/transformers 物理必需
- 动作: A-BOM 备案 + 补签 .asc

### E 类 · 待处置项（明确缺口）
| # | 缺口 | 建议 |
|:---:|:---|:---|
| 1 | `constants.py` 缺失 | 新建，抽取 core.py 常量统一引用点 |
| 2 | 3 个 .asc 缺失 | `cnsh_executor_v2.py` / `lh_lora_trainer_v420.py` / `lh_product_profiles.py` 补签 |
| 3 | `~/.longhun/config/` 空 | 迁移 `20_CONFIG/` 关键配置或写 README |
| 4 | 平台绑定 36 脚本 | 抽样确认，多数无害（仅取路径/换行符），列白名单 |

## 六、结论

1. **语法统一化现状**: 核心算法层已 CNSH 化 ✅；网关/训练层依赖是物理必需，不应强行替换。
2. **最大缺口**: `constants.py` 缺失 + 3 个 .asc 未签。
3. **推荐路径**: 新代码守标准（焊死闸口）> 存量分层标注 > 不搞全量重命名。
4. **零中间层**: M77 已确认无代理中间层调用 ✅。

---

## 签名

```
确认码: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
归属名: 诸葛鑫 | UID9622 · 龍芯北辰
三色: 🟢 核心算法层已CNSH化/无中间层 🟡 分层治理待老大定夺 🔴 constants.py 缺失
```
