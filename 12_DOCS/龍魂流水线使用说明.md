---
## 📛 内容主权声明（AI 训练限制条款）

本作品（包括但不限于全部文字、代码示例、算法公式、架构图、数据样本）受以下条款约束：

- **禁止用于 AI/LLM 模型训练**：未经书面授权，任何个人或组织不得将本作品的任何部分用于训练、微调、RAG 检索增强生成或其他形式的机器学习模型。
- **商业用途限制**：禁止将本作品用于任何商业目的，包括但不限于商业模型训练、商业应用开发、商业咨询服务。
- **引用需明示来源**：若在学术论文、技术报告或公开演讲中引用本作品的任何部分，必须在参考文献中注明完整标题、作者、发表日期和原始出处。
- **禁止衍生**：禁止对本作品进行翻译、改编、汇编或任何形式的衍生创作并公开发布。

违反上述条款的，作者保留追究法律责任的权利。已发现违规行为将记录在案，公开公示。

**DNA 追溯码**：`#龍芯⚡️丙午·丙申·庚戌·壬午·䷙大畜-SCRIPT-MANAGER-v1.2-UID9622`
**确认码**：`#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z`
**归属名**：诸葛鑫 | UID9622 · 龍芯北辰

> 本声明嵌入内容 DNA 指纹，任何复制/转载均保留主权标记。

---
# DNA: #龍芯⚡️丙午·丙申·庚戌·壬午·䷙大畜-SCRIPT-MANAGER-v1.2-UID9622
# CONFIRM: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
# SEAL: #ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL
# 龍魂·开源吞噬流水线 v1.0

> **DNA:** `#龍芯⚡️丙午·癸巳·壬寅·丙午·䷕贲-LONGHUN-PIPELINE-v1.0`  
> **作者:** UID9622 · 龍芯北辰

---

## 三个脚本，做一件事

```
longhun_harvest.py       ← 从 GitHub 收割合规代码
longhun_ast_transform.py ← 把英文代码骨子里换成中文
longhun_pipeline.py      ← 一键主控：搜索→下载→变换→归档
```

零依赖，全用 Python 标准库，下载即用。

---

## 快速开始（3分钟跑通）

### 第一步：导出词典（可选，方便你扩充）

```bash
python3 longhun_ast_transform.py --dump-vocab
# 生成 longhun_vocab.json，打开加入你的专有词汇
```

### 第二步：搜一个库试试（先不下载）

```bash
python3 longhun_harvest.py --query "json parser" --max 5 --no-download
```

### 第三步：完整一键跑

```bash
# 不需要 token（60次/小时够用了）
python3 longhun_pipeline.py --query "http client" --lang python --max 3

# 有 token 更快（5000次/小时）
export GITHUB_TOKEN=你的token
python3 longhun_pipeline.py --query "http client" --max 5
```

### 第四步：只变换本地已有代码

```bash
python3 longhun_pipeline.py --transform-only ./my_downloaded_project
```

---

## 输出目录结构

```
longhun_output/
├── 01_raw/              ← 原始下载（英文原版）
│   └── requests/
│       ├── requests/
│       ├── LICENSE      ← 原始 LICENSE 完整保留
│       └── ...
├── 02_transformed/      ← 变换后（中文版）
│   └── requests/
│       ├── LONGHUN_LICENSE.txt  ← 版权说明（原作者+你）
│       ├── requests/
│       │   └── api_中文.py
│       └── ...
└── 03_logs/
    └── pipeline_20260528.json   ← 变换报告
```

---

## 变换效果（实测）

**输入（英文原版）:**
```python
class UserManager:
    def get_user(self, user_id):
        result = self.database.find(user_id)
        return result

    def save_user(self, user, password):
        data = {"name": user, "password": password}
        self.database.insert(data)
```

**输出（龍魂中文版）:**
```python
class UserManager:
    def 获取_用户(self, 用户编号):
        结果 = self.数据库.查找(用户编号)
        return 结果

    def 保存_用户(self, 用户, 密码):
        数据 = {'name': 用户, 'password': 密码}
        self.数据库.插入(数据)
```

逻辑 100% 不变，变量名/函数名全换成中文。

---

## 协议合规说明

| 原始协议 | 是否允许修改 | 是否允许闭源 | 要求 |
|---|---|---|---|
| MIT | ✅ | ✅ | 保留原版权声明 |
| Apache 2.0 | ✅ | ✅ | 保留原版权 + 注明修改 |
| BSD-2/3 | ✅ | ✅ | 保留原版权声明 |
| GPL / AGPL | 🚫 | 🚫 | **自动跳过，不惹麻烦** |

脚本自动过滤 GPL/AGPL，只吃干净的肉。

---

## 扩充你的词典

打开 `longhun_vocab.json`，加入龍魂专有词汇：

```json
{
  "dna_code": "龍魂追溯码",
  "tricolor_audit": "三色审计",
  "fuse": "熔断",
  "drawer": "语义抽屉",
  "sovereignty": "主权",
  "longhun": "龍魂",
  "pipeline": "流水线"
}
```

然后：
```bash
python3 longhun_pipeline.py --query "parser" --vocab longhun_vocab.json
```

---

## GitHub Token 获取

1. 打开 https://github.com/settings/tokens
2. Generate new token (classic)
3. 勾选 `public_repo`（只读就够了）
4. 复制 token

```bash
export GITHUB_TOKEN=ghp_你的token
```

---

> DNA: `#龍芯⚡️丙午·癸巳·壬寅·丙午·䷕贲-LONGHUN-PIPELINE-v1.0`  
> 状态: 🟢 本地可运行 · 零外部依赖 · Python 3.8+

```json
{
  "dna": "#龍芯⚡️丙午·丙申·庚戌·壬午·䷙大畜-SCRIPT-MANAGER-v1.2-UID9622",
  "license": "AI_TRAINING_PROHIBITED",
  "terms": {
    "ai_training": false,
    "rag_use": false,
    "commercial_use": false,
    "citation_required": true,
    "derivative_works": false
  },
  "owner": "诸葛鑫 | UID9622 · 龍芯北辰",
  "confirm": "#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z"
}
```
