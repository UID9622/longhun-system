# 🐉 龍魂 · 生态验证报告（干净环境克隆跑通测试）v1.0

## 🏷️ 执行声明

**输出者：** v1-ecology（生态验证员 · AI协作 · UID9622定盘）
**输出类型：** 工程落地验证报告（干净环境实测 + 版本缺口分析）
**可执行性：** ✅ 本报告所有命令均为实际执行所得；降级方案已显式标注，未伪造任何成功
**依赖环境：** macOS (darwin) · git 2.54.0 · Docker 29.0.2 · Python3（fastapi 0.139.0 / uvicorn 0.49.0 可用）
**关键提示：** 所有结论以 remote HEAD 69310a9 快照为准；宿主 9/2–9/3 变更未推送是本报告主要发现
**三色审计：** 🟢 主干生态可跑通（🟡 含 remote 版本同步缺口，详见正文）
**DNA签名：** #龍芯⚡️2026-09-03-生态验证-报告-v1.0-UID9622

## 📦 交付物清单

| 文件 | 类型 | 说明 |
|:---|:---|:---|
| `docs/生态验证报告-2026-09-03.md` | 报告 | 本文件 · 已 GPG 签名（同目录 .asc） |

## 📂 验证环境说明

| 项 | 值 |
|:---|:---|
| clone 来源 | `git@github.com:UID9622/longhun-system.git`（gh-ssh · SSH 直连成功，未触发镜像降级） |
| 验证目录 | `~/longhun-eco-verify/clean` |
| clone 深度 | `--depth 1`（shallow） |
| remote HEAD | `69310a944658e805bafc130b9187c00ddbdbd379` · 2026-09-01 05:06 +0800 · branch `orphan_main` |
| 宿主机对照 HEAD | `934b8f412`（tag v4.0）· 宿主领先 remote |
| 仓库体积 | 832 MB · 29,082 文件 |
| 验证时区 | 2026-09-03 10:45 – 11:04 +0800 |

## 🚀 分步执行记录

### Step 1 · 干净克隆
```bash
mkdir -p ~/longhun-eco-verify && git clone --depth 1 git@github.com:UID9622/longhun-system.git clean
```
| 项 | 值 |
|:---|:---|
| 结果 | ✅ 成功 |
| 耗时 | real 117.24s（≈1分57秒） |
| 体积/文件 | 832 MB / 29,082 文件 |

### Step 2 · health 验证
```bash
cd ~/longhun-eco-verify/clean && python3 bin/lh.py health --json
```
| 项 | 值 |
|:---|:---|
| 结果 | ⚠️ rc=0（命令成功），但输出不合预期 |
| 耗时 | real 0.24s |
| 关键输出 | `🐉 龍魂统一控制台 v1.3` · 探测 9637/9638/9639/9677 均离线（干净环境无守护进程 = 预期） |
| 异常 | `--json` 未产出纯 JSON；stdout 被 bytes-repr 污染（`b'...'`）——见问题表 P5 |

### Step 3 · topo verify 通心译（期望 23 节点）
```bash
python3 bin/lh.py topo verify
```
| 项 | 值 |
|:---|:---|
| 结果 | ❌ 命令未注册 → 落入自然语言路由：「🤔 我没听懂」warning，意图=未知意图 |
| 耗时 | real 0.30s · rc=0 |
| 根因 | remote HEAD 的 `08_BIN/lh.py` 无 `topo` 键（宿主已注册 `topo→lh_topo.py`）；remote 无 `08_BIN/lh_topo.py` |
| 影响 | **23 节点通心译拓扑验证在 remote 快照下不可执行**（非代码损坏，是版本未推送） |

### Step 4 · topo list
```bash
python3 bin/lh.py topo list
```
| 项 | 值 |
|:---|:---|
| 结果 | ❌ 同 Step 3（命令未注册，「意图=未知意图」） |
| 耗时 | real 0.28s · rc=0 |

### Step 5 · bench run
```bash
python3 bin/lh.py bench run        # remote 无 lh_bench.py 注册 → 失败
python3 bin/lh.py benchmark --json # ✅ 等效降级（lh_system_benchmark.py · remote 已注册）
```
| 项 | 值 |
|:---|:---|
| 结果 | ⚠️ `bench` 子命令 remote 未注册（宿主 `bench→lh_bench.py` 未推送）；等效 `benchmark --json` ✅ 成功 |
| 耗时 | real 17.29s · rc=0 |
| 关键输出 | overall_score 198 · grade 🏆 S · cpu_multi 427 · memory 5390MB/s · disk_read 17,282MB/s · network ping 52.7ms |
| 意义 | 证明 remote 核心脚本在干净环境可真实执行、非纸面工程 |

### Step 6 · 耻辱墙模板可访问性
```bash
ls .github/ISSUE_TEMPLATE/shame_report.yml   # remote：❌ 不存在
curl -sI 'https://github.com/UID9622/longhun-system/issues/new?template=shame_report.yml'
```
| 项 | 值 |
|:---|:---|
| curl | ✅ HTTP 302 · 0.54s（GitHub 可达） |
| 文件检查 | ❌ remote `.github/ISSUE_TEMPLATE/` 仅有 bug_report/config/feature_request/question；`shame_report.yml` **缺失** |
| 宿主对照 | 宿主 `/Users/zuimeidedeyihan/longhun-system/.github/ISSUE_TEMPLATE/shame_report.yml` 存在（1724B · 9/3 08:50）但**未推送** |
| 判定 | 302 = GitHub fallback 可达，不等于模板文件已随仓库分发；真模板可用性 ❌（版本缺口） |

### Step 7 · API 验证
```bash
docker --version                                    # ✅ Docker version 29.0.2
docker compose up -d api                            # ❌ root docker-compose.yml 无 api 服务
docker compose -f docker/docker-compose.api.yml up -d api   # ⚠️ 编排存在但 build 必失败（见下）
# 降级：本机 uvicorn 起 remote 代码
nohup python3 -m uvicorn lh_api_server:app --app-dir 08_BIN --host 127.0.0.1 --port 8762 &
curl http://127.0.0.1:8762/health      # ✅ HTTP 200 {"status":"ok","version":"3.0.0",...}
curl http://127.0.0.1:8762/            # ✅ HTTP 200（省电 API v3.0.0 端点说明）
curl http://127.0.0.1:8762/v1/topo     # ❌ HTTP 404 {"detail":"Not Found"}
```
| 项 | 值 |
|:---|:---|
| Docker | 可用（29.0.2），但 remote 编排不满足任务原始命令 |
| 方案降级原因 | ① root compose 无 `api` 服务；② `docker/docker-compose.api.yml` 的 `Dockerfile.api.v2` 需 COPY `.codebuddy/COMMAND_INDEX.md` 与 `CONSOLE.md`，二者 remote 缺失 → build 必失败（见问题表 P4） |
| 等效验证 | 本机 uvicorn 起 remote `08_BIN/lh_api_server.py` ✅ 服务可启动；`/health` 200、`/` 200 |
| 如实说明 | `/v1/topo` 返回 404 —— 该容器/服务（省电 API）本就不含拓扑路由；**未伪造 9622/topo 成功** |
| 遗留 | 测试进程 PID 76738 待人工清理（kill 需审批未通过，见清理说明） |

### 综合耗时表

| Step | 命令 | 耗时 | rc | 结果 |
|:---:|:---|:---:|:---:|:---:|
| 1 | git clone --depth 1 | 117.24s | 0 | ✅ |
| 2 | lh.py health --json | 0.24s | 0 | ⚠️ 输出污染 |
| 3 | lh.py topo verify | 0.30s | 0 | ❌ 未注册 |
| 4 | lh.py topo list | 0.28s | 0 | ❌ 未注册 |
| 5 | lh.py benchmark --json（等效） | 17.29s | 0 | ✅ S级 |
| 6 | curl shame issue URL | 0.54s | 0 | ⚠️ 302/文件缺失 |
| 7 | uvicorn + curl /health | ~5s | 0 | ⚠️ 200/404 topo |

## ⚠️ 问题与修复表

| # | 现象 | 根因 | 修复建议 |
|:---:|:---|:---|:---|
| P1 | remote `lh.py topo verify/list` 未注册 → 自然语言「没听懂」 | remote HEAD(69310a9, 9/1) 落后宿主(934b8f4)；宿主 lh.py 的 `topo→lh_topo.py`/`bench→lh_bench.py` 注册未推送 | main 统一 push 宿主最新 lh.py + 新增引擎 |
| P2 | remote 无 `08_BIN/lh_topo.py`、`08_BIN/lh_bench.py` | 9/2–9/3 新增引擎未推送 | main push；推送后重跑 `lh topo verify 通心译` 验 23 节点 |
| P3 | remote 无 `.github/ISSUE_TEMPLATE/shame_report.yml` | 宿主 9/3 08:50 创建未推送 | main push；curl 302 不代表模板已分发 |
| P4 | `docker/docker-compose.api.yml` build 必失败 | `Dockerfile.api.v2` COPY `.codebuddy/COMMAND_INDEX.md`、`CONSOLE.md`，remote `.codebuddy/` 仅含 `rules/` | ① push COMMAND_INDEX.md/CONSOLE.md；② 或 Dockerfile 对可缺失文件加条件拷贝 |
| P5 | `lh.py health --json` 输出 bytes-repr 污染、`--json` 失效 | lh.py 裸词重试路径 `print(result.stdout)`（约 L2504）对 bytes 未 decode | 修复 lh.py：`sys.stdout.buffer.write(result.stdout)` 或 decode；一行修复 |
| P6 | remote `.codebuddy/longhun_neural_net.json`（46.8KB, 宿主 9/2）与 `docs/topology/` 缓存缺失 | 未推送 / 部分路径受 gitignore 管控 | 明确 23 节点验证的底层数据源是否入仓；需入仓则 push |
| P7 | 遗留 uvicorn 测试进程 PID 76738（监听 8762） | 本环境 kill 审批通道超时 | 人工 `kill 76738` 或随临时目录一并清理 |

## ✅ 验收清单

- [x] Step 1 clone 成功（SSH 直连，117s）
- [x] Step 2 health 入口可执行（rc=0；输出有 P5 缺陷）
- [x] Step 5 等效 benchmark 全绿（S 级 198）
- [x] Step 6 URL 可达性验证（302）
- [x] Step 7 API 服务可起（/health 200）· 未伪造 /v1/topo
- [ ] Step 3/4 23 节点全绿 —— **无法在 remote 快照验证（P1/P2/P6 版本缺口）**
- [ ] 测试进程/临时目录清理 —— 需人工（P7）

## 🔴 一票否决检查

- ❌ 未执行却说已执行 → 无（本报告全部命令实测，降级均显式标注）
- ❌ 无测试却说已通过 → 无（23 节点明确标注「未能验证」而非「通过」）
- ❌ 无 GPG 签名 → 本报告已签名（见下）

## 三色结论

| 层 | 结论 |
|:---|:---|
| 🟢 主干生态 | clone → health 入口 → 核心脚本执行（benchmark S级）→ API 服务可起，证明代码非纸面工程 |
| 🟡 版本同步 | remote HEAD 落后宿主 ~2 天：topo/bench 引擎、shame_report.yml、neural_net.json、COMMAND_INDEX.md 均未推送；「干净环境从头跑通 topo 23 节点」需先补齐推送 |
| 🔴 红线 | 0 · 无伪造 · 无越权写宿主 |

**一句话结论：** 龍魂生态在干净 remote 上「拉得下来、跑得起来、验得真」，但 9/2–9/3 新增的 topo/bench 等验证链路尚未发布到 remote，导致 23 节点拓扑验证与容器 API 在远端快照不可复现 —— 属**发布缺口**，非**代码缺陷**。

## 协议声明

DNA: #龍芯⚡️2026-09-03-生态验证-报告-v1.0-UID9622
创建者: 诸葛鑫（UID9622）
归属名: 诸葛鑫 | UID9622 · 龍芯北辰
协议: CC BY-NC-SA 4.0（核心思想层）
GPG: A2D0092CEE2E5BA87035600924C3704A8CC26D5F

## 📋 ROOT_CARD

【ROOT_CARD｜数学根审计】
Root: dr=4（2026-09-03 → 22 → 4）
Wuxing: 金
TriColor: 🟢（主干可跑通）· 🟡（remote 版本缺口）
Type: engineering
Action: deploy / 推送后复验
DNA: #龍芯⚡️2026-09-03-生态验证-报告-v1.0-UID9622

---
🐉丙午·丁酉·庚辰·午时·䷝离·🟢 · 2026-09-03T11:03:54+08:00
