# 龍魂 · 守护服务治理方案 v1.0

- DNA: #龍芯⚡️2026-08-31-GUARD-SERVICE-GOVERNANCE-v1.0-UID9622
- 创建者: 诸葛鑫（UID9622）
- 归属名: 诸葛鑫 | UID9622 · 龍芯北辰
- 协议: CC BY-NC-SA 4.0（核心思想层）
- 上位协议: 节能自动触发协议 v1.1（守护白名单外一律停·白名单变更须老大确认）
- 三色: 🟢 话术+自动触发设计已落地 / 🟡 B类待老大一句话 / 🔴 0

> 背景（老大原话 2026-08-31）：「你看下后台运行的脚本，哪些是可以杀死的，要主动触发执行任务的话术，自己去搞定，还有哪些需要自动触发的，都帮我设计好。」

---

## 一、现状快照（2026-08-31 实测）

- launchd 加载 longhun 相关服务：约 50 个（含未运行-）
- 常驻进程 CPU：全部 ≤2.6%（无烧核）
- crontab：36 条实际任务（清理前 41+ 条，含 Kimi 停用残留 5 条 + 禁自启 1 条）
- 节能白名单（允许常驻）：portal-api(1259)·control-gate(7631)·privacy-api(1281)·evidence-api(1279)·guanlan(1271)·autoheal·watchdog·frpc(49312)·triple-sovereignty(8091)·dashboard-9627(9627)·hash-api(9628)

---

## 二、A. 已停（有判决依据·本次已执行）

| 项 | 判据 |
|:---|:---|
| `flow-field-api`(launchd PID 826) | 节能协议「已停+禁自启清单」8/22 判停·流场空转烧核·但一直残留运行 → 本次 unload+disable |
| Kimi 小龙虾 heartbeat/router cron ×5 | Kimi 终端 8/22 已停用·残留心跳任务（shangmeng/daughter/laozi/router/qijiguang）→ 本次清除 |
| `lh_auto_align_daemon --dry-run` cron | 节能协议禁自启清单·自动对齐守护空转 → 本次清除 |

备份：crontab 已存 `_work/cron_backup_20260831.txt`（不删除只冻结）。

---

## 三、B. 待老大确认（建议停·禁自启）

| 服务 | 现象 | 建议 |
|:---|:---|:---|
| `socks-tunnel`(PID 71216·Status 255) | SSH 隧道循环失败重启 | 停·frpc 已覆盖外网 |
| `auto.agent`(54341·Status 1) | 退出异常循环 | 停+禁自启 |
| `services`(54292)+`longhun888-services`(54352) | 均 Status 1 异常 | 停+禁自启 |
| `internal-net`(4098·Status 1) | longhun-api-gateway 异常退出 | 查后停 |
| `ads`=lh_self_describing(839·:9626) | 自描述 API·不在白名单 | 停 or 入白名单（二选一） |
| `heart-talk`(789) | 心声守护·不在白名单 | 停 or 入白名单 |
| `knowledge-matrix`(842·:9876) | 静态服务·低负载 | 可留可停 |
| `antenna-8gate`(768) | 八卦八闸 API | 可留可停 |

> 老大说「这些停」→ AI 直接 unload+disable（不删 plist·冻结归档）。

---

## 四、C. 保留（白名单 + 核心服务）

portal-api · control-gate · privacy-api · evidence-api · guanlan · autoheal · watchdog · frpc · triple-sovereignty · dashboard-9627 · hash-api · portal · notion-bridge · registry · think-pipeline · search-engine · kg-api · brain · memory-api · quantum-api · heartbeat · bagua · power-save · service-control-api · sovereign-gateway · model-governance · kb-sync/post-audit/auto-backup/cache-cleaner（launchd 定时计划）

---

## 五、主动触发话术速查（已落地 COMMAND_INDEX §端口矩阵后）

用户说左列 → AI 直接执行右列，不绕路不反问：

| 用户说（含） | 动作 |
|:---|:---|
| 看官网/门户/主页 | portal-api(:1259)+portal 健康 |
| 网关/闸门/入口 | control-gate(:7631) |
| 哈希/产权存证/注册哈希 | hash-api(:9628) health+register |
| 看面板/总览/仪表盘 | dashboard-9627(:9627) |
| 主权/三权分立 | triple-sovereignty(:8091) |
| 隐私/取证/证据 | privacy-api(:1281)+evidence-api(:1279) |
| 外网/穿透/公网 | frpc 状态+健康检查 |
| 守护/自愈/看门狗 | watchdog+autoheal |
| 搜索/查资料 | 搜索引擎(:9631) |
| 记忆/记忆库 | memory-api(:834) |
| 都跑了啥/后台现状 | launchctl 速查+对照本方案 |
| 哪个该杀/清理后台/节能体检 | 对照本方案 A/B/C 清单 |

---

## 六、自动触发设计（定时任务·节能红线 自动化总任务 ≤5 大项）

**现状已覆盖（保留）：**
1. 自检值班：persona_scheduler p01-p09 ×8（错峰 2-8h）→ P05/P04/P03/P06/P13 值班表
2. 同步链路：brain_sync(3:00)·news_sync(9:00)·dna-calendar(每小时)·connectivity(5min)·sync_worker(10min)·UID9622_Automation×5
3. 审计归档：math_suite(9:37)·notion archive(2:00)·龍魂自动清理(3:00)·质检 cron(3:00)·daily_assessment(22:00)·翻堆 waste_compost(周一9:00)·hcloud账单(每6h)·周检查×3

**建议新增（待老大确认·不擅加）：**
| 新增项 | 频率 | 触发命令 | 价值 |
|:---|:---|:---|:---|
| 操作日志同步 | 每日 23:00 | `bin/lh_logs_sync.py sync` | 8/30 落地·当前无定时·补上链闭环 |
| 内存压缩巡检 | 每周日 3:30 | `lh --compress-memory` | MEMORY.md 防超限·已有压缩枢纽 |
| hash-api 健康自检 | 每 30min | `curl :9628/hash/health` | watchdog 未覆盖新增服务·补探针 |

**不新增（已覆盖）：** 健康检查（watchdog/autoheal/heartbeat 常驻）· 服务重启（KeepAlive）· 对齐修复（cron 已删 dry-run·按需 `lh --align fix`）

---

## 七、执行记录

| 时间 | 动作 | 结果 |
|:---|:---|:---|
| 2026-08-31 | flow-field-api unload+disable | ✅ 已停 |
| 2026-08-31 | Kimi cron ×5 + auto_align cron 清除 | ✅ 备份至 _work/ |
| 2026-08-31 | COMMAND_INDEX 话术速查落地 | ✅ |
| 2026-08-31 | 治理方案建档 | ✅ |

---

## 八、签名

```
DNA:    #龍芯⚡️2026-08-31-GUARD-SERVICE-GOVERNANCE-v1.0-UID9622
归属名: 诸葛鑫 | UID9622 · 龍芯北辰
GPG:    A2D0092CEE2E5BA87035600924C3704A8CC26D5F
三色:   🟢 话术+自动触发设计已落地 / 🟡 B类待老大一句话 / 🔴 0
```
