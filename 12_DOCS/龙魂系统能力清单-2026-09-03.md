# 🐉 龍魂系统 · 能力清单 v1.0

```
DNA:    #龍芯⚡️2026-09-03-CAPABILITY-INVENTORY-v1.0-UID9622
创建者: 诸葛鑫（UID9622）
归属名: 诸葛鑫 | UID9622 · 龍芯北辰
协议:   CC BY-NC-SA 4.0（核心思想层）· 工程实现层 MulanPSL v2（分层双许可）
模板:   [2] 🔧 工程落地执行型 · LH-ARTICLE-HEADER-TEMPLATES-v1.0
确认码: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
验证:   lh health --json · lh topo list · lh model list
```

> **一句话**：这是龍魂系统**已落地能力**的对外交付盘点。数据截止 2026-09-03，全部命令可在本地复现验证（`lh 帮助` 看全量入口）。

---

## 1. 🧭 快速自检三连

| 检查 | 命令 | 2026-09-03 实测 |
|:---|:---|:---|
| 健康基线 | `lh health --json` | ✅ 全绿（含 knowledge_graph.graphs 全图谱数组） |
| 图谱状态 | `lh topo list` | ✅ 3 图谱（通心译总台 19 节点 / 深度学习 7 节点 / Obsidian 镜像 3 节点） |
| 模型状态 | `lh model list` | ✅ longhun-v4.1.9 🟢 + ollama 8 模型 + deepseek-api-client 引擎 |

---

## 2. ⌨️ 已落地命令清单（按模块分组）

> 完整单一真相源：`.codebuddy/COMMAND_INDEX.md`（L0）· 全量 798+ 脚本。
> 以下为核心 lh 命令（人格网关自动路由 · 三色审计回流 · 输出附时间戳）。

### 2.1 🕸️ 图谱 / 知识拓扑（lh topo · 引擎 `08_BIN/lh_topo.py` v1.3）
| 命令 | 说明 |
|:---|:---|
| `lh topo list [图谱名]` | 本地图谱缓存列表（组+节点明细） |
| `lh topo sync <图谱名> [--source notion\|obsidian\|yuque]` | 多数据源同步 + 活体校验落盘 |
| `lh topo verify` | 校验 DNA + 链接 + 状态 → 全绿/缺口清单 |
| `lh topo register <图谱名>` | 注册本地图谱（默认组：模型层/数据层/工具层） |
| `lh topo node <图谱名> --group --name --type --dna` | 注册/更新节点 |
| `lh topo cite <资产名>` | 引用溯源（资产名 → DNA + 来源链接） |
| `lh topo frameworks` | 龍魂真实第三方依赖清单（importlib 实测 · 69 项） |
| `lh topo serve [--port 8762]` | 拓扑开放页（树形 6 层 · `/dashboard` 60s 刷新） |
| `lh topo db init/status` | SQLite 持久化（`~/.longhun/db/topo.db`） |
| `lh topo diff <图谱名> [--since]` | 增量归档差异 |

### 2.2 🧠 模型治理（lh model · 引擎 `08_BIN/lh_model.py` v1.1）
| 命令 | 说明 |
|:---|:---|
| `lh model list` | 注册表 + Ollama 实时（合并去重） |
| `lh model status <name>` | 加载状态 + 路径探测 |
| `lh model audit <name>` | DNA 合规三色审计 |
| `lh model run/api/bench/eval/test` | 推理 / API / 基准 / 评测 / 测试 |

### 2.3 🤖 长期智能体（lh agent · 引擎 `08_BIN/lh_agent_long.py`）
| 命令 | 说明 |
|:---|:---|
| `lh agent run <任务描述> [--max-hours N]` | 启动自主长任务（队列持久化） |
| `lh agent status` | 队列/运行时长/重试次数 |
| `lh agent root-cause <task-id>` | 失败根因分析 |
| `lh agent list/cancel/recover` | 列队 / 取消 / 断点恢复 |

> 守护：launchd `com.longhun.agent-recover` 5 分钟轮询 · 断点续跑 · 空队列零负载 · 节能白名单。

### 2.4 📊 基准测试（lh bench · 引擎 `08_BIN/lh_bench.py`）
| 命令 | 说明 |
|:---|:---|
| `lh bench run [--iterations N]` | 本地 4 项（topo_verify / health_json / model_list / judge_db）· `--include-network` 加 judge_webscan |
| `lh bench report [--format json\|md]` | 生成报告 → `docs/bench/bench_*.md` · 自动 GPG 签名 |
| `lh bench history` | 历史基准 |

### 2.5 🐉 归一审判官 / 耻辱墙（lh judge · 引擎 `08_BIN/lh_judge.py` v2.x）
| 命令 | 说明 |
|:---|:---|
| `lh judge scan [--mobile\|--quick]` | 自动抓取 GitHub/HF/arXiv/AppStore 等 → DNA 指纹检测 |
| `lh judge topo-scan [--model\|--deep\|--frameworks]` | 通心译内容审计 / 模型审计 / 69 框架审计 |
| `lh judge misreport` | 待审核疑似误报（置信度<60% 弱指纹） |
| `lh judge confirm <id>` / `reject <id>` | 人工确认命中上墙 / 误报记录 |
| `lh judge view/clean` | 查看 / 清理耻辱墙 |
| `lh judge phone-scan/phone-report` | 手机应用市场剽窃扫描 |

### 2.6 🗄️ 存储优化（lh storage · 引擎 `08_BIN/lh_storage.py`）
| 命令 | 说明 |
|:---|:---|
| `lh storage status` | 存储占用 + 优化建议 |
| `lh storage optimize [--dry-run]` | 日志>7天 gz 轮转 · 耻辱墙 html.gz 快照（P0 冻结非裸删） |

### 2.7 🌐 对外接口 / 子命令（lh api 等）
| 命令 | 说明 |
|:---|:---|
| `lh api [--port 9622] [--daemon]` | CIL API 网关 · POST /v1/lh · 硬焊 127.0.0.1:9622 |
| `lh flow "<文本>" [--json]` | 数字根/五行/八卦 → 标准 Node JSON |
| `lh bazi [--date --time --json]` | 八字排盘（五虎遁/五鼠遁） |
| `lh assert <文本>` / `lh trace <node_id>` / `lh logs` | 归一断言 / 反向追溯 / 外部调用审计 |
| `lh pledge/bind/sys-check` | M78 科技普惠诚信（用=认=守约） |

### 2.8 📁 其他核心常驻命令（速查）
| 命令 | 说明 |
|:---|:---|
| `lh 帮助` / `lh --help` | 全量 flag 与快捷入口 |
| `lh` | 交互控制台（8 大类） |
| `lh status / lh health` | 状态 / 健康（`--json` 含知识图谱+模型+服务） |
| `lh topo` `lh model` `lh dh` `lh te` `lh search` | 图谱 / 模型 / 数字人 / 时间引擎 / 搜索 |
| `lh audit` / `lh security` | 全系统安全审计 / 安全自检 |
| `lh compress` / `lh --compress-hub` | 记忆压缩引擎（MEMORY.md 7.5KB 安全线） |
| `python3 bin/lh_gpg_sign.py sign .` | GPG 分离签名 |

---

## 3. 🕸️ 图谱现状（lh topo list · 2026-09-03）

| 图谱 | 版本 | 节点 | 状态 | 最后同步 | 缓存 |
|:---|:---|:---:|:---:|:---|:---|
| 🌐 通心译军团·总台 | v1.0 | 19 | 🟢11 / 🟡8 | 2026-09-02T19:32 | `docs/topology/tongxinyi_legion_topo.json` |
| 🧠 深度学习图谱 | v1.0 | 7 | 🟢7 / 🟡0 | 2026-09-03T01:25 | `docs/topology/深度学习图谱_legion_topo.json` |
| 🌐 通心译 文档库镜像 | Obsidian | 3 | 🟢3 / 🟡0 | 2026-09-03T00:04 | `docs/topology/通心译-obsidian_legion_topo.json` |

> 根哈希（通心译总台）：`3C3874A43FFE1A4B…` · 活体快照：`~/.longhun/topo/*.json`

---

## 4. 🔌 对外接口一览

### 4.1 本地 API（硬焊 127.0.0.1）
| 服务 | 端口 | 说明 |
|:---|:---|:---|
| CIL API 网关 | 9622 | `POST /v1/lh {"command":"..."}` → `{code,stdout,stderr}` |
| 数字人调动 | 8761 | `GET /dh/health|/dh/list` · `POST /dh/dispatch`（Header `X-UID:9622`） |
| 拓扑开放页 | 8762 | `/topo/<名>/html` · `/dashboard` · JSON 树 |
| CNSH Web IDE | 8848 | 浏览器端 IDE（编辑/纠错/编译/运行） |
| 透明看板 | 8080 | 治理事件/耻辱墙/荣誉墙/知识图谱 · 只读 |
| 统一控制台 | 8999 | 暗色龍魂金 Web 仪表盘 |

### 4.2 服务守护（launchd 常驻 · 节能白名单）
| 服务 | 端口 | 说明 |
|:---|:---|:---|
| portal-api | — | 门户 API |
| control-gate | 7631 | 控制闸门 |
| privacy-api | 1281 / evidence-api 1279 | 隐私 / 存证 |
| guanlan | 1271 | 观澜 |
| hash-api | 9628 | 哈希产权引擎 API（M73） |
| dashboard-9627 | 9627 | 健康看板 |
| triple-sovereignty | 8091 | 三重主权 |
| autoheal / watchdog | — | 自愈 / 看门狗 |
| agent-recover | — | 长任务断点续跑（5min 轮询） |

### 4.3 对外（鲲鹏 uid9622.cn）
| 端点 | 说明 |
|:---|:---|
| `https://uid9622.cn` | 官网静态门户 |
| `https://uid9622.cn/api/` | 官方 API（8777） |
| `https://uid9622.cn/portal/` | 门户（8789） |
| Notion 公开站 | `uid9622.notion.site`（367 数据库知识底座） |
| GitHub | `github.com/UID9622/longhun-system` |

> 完整契约：`12_DOCS/对外接口协议-v1.0.md` · 三色审计 OpenAPI：`12_DOCS/openapi-tricolor-audit-v1.1.yaml`

---

## 5. ⏰ 定时任务 / 守护（launchd · com.longhun.*）

| 任务 | 周期 | 说明 |
|:---|:---|:---|
| `lh-judge` | 每日 02:00 | 归一审判官自动扫描（耻辱墙更新） |
| `model-governance` | 每日 02:30 | 模型闭环治理 |
| `topo-sync` | 每日 04:00 | 图谱多源同步（失败重试 3 次×300s） |
| `daily-audit` | 每日 03:00 | 左右互搏自审 + 铭碑校验 + 健康自检 |
| `agent-recover` | 每 5 分钟 | 长任务断点续跑（空队列即退·零负载） |
| `autoheal / watchdog` | 每小时 | 自动自愈 / 看门狗 |
| `power-save` | 每 600s | 省电巡检（空闲进程 SIGSTOP 休眠） |
| `kb-daemon / kb-sync / registry-sync` | 定时 | 知识库守护 / 同步 / 注册表同步 |
| `all-tunnels / socks-tunnel` | — | 内网隧道 |

> 全量条目：`launchctl list | grep com.longhun` · 当前加载 47 项（含常驻服务）· 端口全景：`lh --ports`

---

## 6. 🏗️ 待定 / 已裁决事项

| 事项 | 状态 |
|:---|:---|
| 云厂商集成（AWS/GCP/Azure） | ❌ 已裁决否决（P0：M77 零中间层 + 人民币主权 + 核心不涉外） |
| 双版本 GA/restricted 另开入口 | ❌ 已裁决否决（已有 R1-R5 + X-API-Key 分层） |
| Token 定价 70-25/1M | ❌ 已裁决不照抄（本地 ollama 无卖 API 生意） |
| PyPI 账号 / CNSH 软著 / 商户号 | 🟡 待老大 3 步（8/31） |

---

## 7. 📜 关联文档

| 文档 | 路径 |
|:---|:---|
| 社区接入指南 | `docs/社区接入指南.md` |
| 命令总目（单一真相源） | `.codebuddy/COMMAND_INDEX.md` |
| 系统拓扑 | `.codebuddy/longhun_neural_net.json` |
| 长期记忆 | `.codebuddy/memory/MEMORY.md` |
| 技术白皮书 | `docs/LONGHUN-TECHNICAL-WHITEPAPER-v1.0.md` |
| 生态补全报告 | `docs/生态补全报告.md` |
| 深度学习代码精修报告 | `docs/深度学习代码精修报告.md` |

---

*文档由 AI 整理 · 归属名焊死 · 禁止匿名转载 · 修改走修订流程（GATE-01~10 + P15 签章）*
🐉丙午·丁酉·庚辰·辰时·䷚颐·🟢


---

## 💛 支持龍魂（纯自愿 · 零黑箱）

龍魂的一切免费开放。若你认可「让技术为人、为普通人生长」，可自愿支持——款项仅用于服务器与开发成本，不留一分私账。

- **收款方式**: SOL / USDC（Solana）
- **实时地址与二维码**: 见官网 [uid9622.cn](https://uid9622.cn) 底部「支持龍魂」区 — 地址由 `lh wallet` 统一管理（公司账户落地后自动切换 · 以官网为准）

> 龍魂不诱导、不施压、不道德绑架。捐与不捐，开放与尊重不变。

<!-- LH-WALLET-SUPPORT -->
