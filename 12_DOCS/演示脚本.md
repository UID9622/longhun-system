# 🎬 龍魂系统 · 演示脚本 v1.0（10 条 · 2 分钟跑完）

```
DNA:    #龍芯⚡️2026-09-03-DEMO-SCRIPT-v1.0-UID9622
创建者: 诸葛鑫（UID9622）
归属名: 诸葛鑫 | UID9622 · 龍芯北辰
协议:   CC BY-NC-SA 4.0（核心思想层）· 工程实现层 MulanPSL v2
模板:   [2] 🔧 工程落地执行型 · LH-ARTICLE-HEADER-TEMPLATES-v1.0
验证:   python3 bin/lh.py health --json
```

> 目标：**从零 clone 到跑完基准，2 分钟内走完龍魂最小闭环**。
> 环境：Python 3.11+ · macOS/Linux · Ollama **可选**（不装不报错，模型命令自动显示降级视图）。

---

## ⚡ 10 条演示命令（按序复制即跑）

```bash
# ── ① Clone（--depth 1 秒级拉取）──────────────────────────────
git clone --depth 1 https://github.com/UID9622/longhun-system.git
cd longhun-system
#   预期: 仓库就位 · README.md.asc 等 GPG 签名同目录

# ── ② 入口通（中文帮助第一屏）─────────────────────────────────
python3 bin/lh.py 帮助 | head -25
#   预期: 命令总览 · 8 大类入口

# ── ③ 健康基线 ────────────────────────────────────────────────
python3 bin/lh.py health --json
#   预期: checks 全绿 JSON · knowledge_graph 含全图谱数组

# ── ④ 图谱现状 ────────────────────────────────────────────────
python3 bin/lh.py topo list
#   预期: 通心译总台 19 节点 / 深度学习图谱 7 节点 / Obsidian 镜像 3 节点

# ── ⑤ 时间引擎（干支四柱 · 卦象）─────────────────────────────
python3 bin/lh.py te --stamp
#   预期: 如 丙午·丁酉·…·䷚颐 · 输出附三色相位

# ── ⑥ 流场子命令（数字根/五行/八卦 → 标准 Node JSON）────────
python3 bin/lh.py flow "龍魂 演示" --json
#   预期: {"digital_root":..,"wuxing":..,"bagua":..} 字段齐全

# ── ⑦ 长期智能体守护状态 ──────────────────────────────────────
python3 bin/lh.py agent status
#   预期: 队列 0 · 守护（com.longhun.agent-recover）存活/未注册提示

# ── ⑧ 系统基准（本地 4 项 · ~1s）──────────────────────────────
python3 bin/lh.py bench run
#   预期: topo_verify / health_json / model_list / judge_db 四项耗时

# ── ⑨ 基准报告（自动生成 + GPG 签名）─────────────────────────
python3 bin/lh.py bench report
#   预期: docs/bench/bench_YYYYMMDDTHHMMSS.md (+.asc)

# ── ⑩ 验真（GPG + 铭碑根哈希）────────────────────────────────
gpg --verify README.md.asc README.md
python3 08_BIN/lh_memorial.py --verify
#   预期: gpg: Good signature · 铭碑根哈希 66534a84…（5 位贡献者 · 187 次提交）
```

---

## ⏱ 耗时预算（合计 < 2 分钟）

| 步骤 | 预算 | 说明 |
|:---|:---|:---|
| ① clone | 5–30s | --depth 1 单分支 |
| ②–⑦ 自检组 | 10–20s | 全本地命令 |
| ⑧ bench run | ~1–2s | 本地 4 项 · 实测 0.686s |
| ⑨ bench report | ~1s | 生成 + 签名 |
| ⑩ 验真 | ~2s | GPG + 铭碑 |

> 实测基准（2026-09-03）：bench 本地 4 项 0.686s · health/topo/model 全绿。

## 🔁 想继续深挖？

| 下一步 | 命令 |
|:---|:---|
| 接 API | `python3 bin/lh.py api --daemon` → `curl -X POST 127.0.0.1:9622/v1/lh -d '{"command":"flow 龍魂 --json"}'` |
| 完整接入 | 见 [`docs/社区接入指南.md`](./社区接入指南.md) |
| 能力盘点 | 见 [`docs/龙魂系统能力清单-2026-09-03.md`](./龙魂系统能力清单-2026-09-03.md) |

---
🐉丙午·丁酉·庚辰·辰时·䷚颐·🟢
