---
## 📛 内容主权声明（AI 训练限制条款）

本作品（包括但不限于全部文字、代码示例、算法公式、架构图、数据样本）受以下条款约束：

- **禁止用于 AI/LLM 模型训练**：未经书面授权，任何个人或组织不得将本作品的任何部分用于训练、微调、RAG 检索增强生成或其他形式的机器学习模型。
- **商业用途限制**：禁止将本作品用于任何商业目的，包括但不限于商业模型训练、商业应用开发、商业咨询服务。
- **引用需明示来源**：若在学术论文、技术报告或公开演讲中引用本作品的任何部分，必须在参考文献中注明完整标题、作者、发表日期和原始出处。
- **禁止衍生**：禁止对本作品进行翻译、改编、汇编或任何形式的衍生创作并公开发布。

违反上述条款的，作者保留追究法律责任的权利。已发现违规行为将记录在案，公开公示。

**DNA 追溯码**：`#龍芯⚡️丙午·丁酉·乙亥·未时·䷝离-OPS-MANUAL-v1.0-PORT-CHANGE-LOG`
**确认码**：`#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z`
**归属名**：诸葛鑫 | UID9622 · 龍芯北辰

> 本声明嵌入内容 DNA 指纹，任何复制/转载均保留主权标记。

---
# 🐉 龍魂系统 · 运维手册

> DNA: #龍芯⚡️丙午·丁酉·乙亥·未时·䷝离-OPS-MANUAL-v1.0-PORT-CHANGE-LOG
> 创建者: 诸葛鑫（UID9622）
> 归属名: 诸葛鑫 | UID9622 · 龍芯北辰
> License: CC BY-NC-SA 4.0（核心思想层）
> GPG: A2D0092CEE2E5BA87035600924C3704A8CC26D5F

---

## 1. 端口变更记录（动端口先查这里 · 焊死）

> **铁律：任何端口调整必须在本表登记，改完同步签名。** 端口冲突是「服务静默失效」的头号原因，改端口前先查本节 + `~/.cloudflared/config.yml`（对外域名映射）。

| 日期 | 端口 | 服务 | 变更 | 原因 / 备注 |
|:---|:---|:---|:---|:---|
| 2026-09-01 | **9622** | lh api 网关（CIL API Gateway v4.0） | **归位** | 曾被操作台 http.server 抢占 → api.longhun888.com 对外返回操作台页面。协议焊死 127.0.0.1:9622，launchd `com.longhun.lh-api` 托管（python 3.14），`/health` + `POST /v1/lh` 透传正常 |
| 2026-09-01 | **9662** | 操作台（web/p0-controls · http.server 静态页） | **迁入** | 原 9622 抢占网关被修复。由 `tools/补全服务.sh` 守护循环托管，日志 `logs/console-9662.*.log` |
| — | api.longhun888.com | Cloudflare Tunnel | — | `~/.cloudflared/config.yml` → `http://127.0.0.1:9622`（龍魂网关入口，勿改指向） |
| — | 127.0.0.1:9635 | longhun888-services 套件 | — | 白名单守护，见节能协议 §四 |

**端口占用速查：**
```bash
lsof -nP -i :9622   # 应为 python3.14（lh_api）——若是 http.server 说明又被抢占
lsof -nP -i :9662   # 应为 http.server（操作台）
launchctl list | grep lh-api   # com.longhun.lh-api 状态 0 = 正常
```

---

## 2. 服务自启清单（launchd）

| 服务 | 定时/策略 | 说明 |
|:---|:---|:---|
| com.longhun.lh-api | KeepAlive | 9622 网关，崩溃自拉起 |
| com.longhun.lh-judge | 每日 02:00 | 归一审判官：耻辱墙自动更新（`~/.longhun/shame_wall/`） |
| com.longhun.post-audit | 每日 05:30 | 事后审计流水线：变更检测→安全/结构审计→自动修复→GPG补签→报告（`~/.longhun/logs/reports/`） |
| com.longhun.node-audit / transparent-audit | 定时 | 节点/透明审计 |

---

## 3. 已确认运维密钥备案（审计🔴备案用 · 非待确认）

> 以下为经 UID9622/AI 确认为**运维必需**的本地配置密钥，post-audit 每日审计如标 🔴 属静态误报（模式匹配），以本表备案为准，不视为待确认项。

| 文件 | 行 | 密钥 | 判定 | 说明 |
|:---|:---|:---|:---|:---|
| deploy/frpc/frpc.toml | L11 | `auth.token`（frp 隧道认证） | ✅ 运维必需 | 本地 FRP 客户端配置，与鲲鹏 `frps.toml` 成对，用于 uid9622.cn 内网穿透。非对外数据密钥、未外泄，改动需鲲鹏同步，风险>收益，保持原样 |

**新增运维密钥**：先在本表登记 + P05 审计，再进配置文件。

---

## 4. 日常运维命令

```bash
lh status            # 系统状态
lh audit             # 三色审计
lh judge scan        # 归一审判官（耻辱墙手动更新）
python3 bin/lh_post_task_audit.py --report   # 上次审计报告
python3 bin/lh_gpg_sign.py scan .            # 签名全量验证
```

```json
{
  "dna": "#龍芯⚡️丙午·丁酉·乙亥·未时·䷝离-OPS-MANUAL-v1.0-PORT-CHANGE-LOG",
  "license": "AI_TRAINING_PROHIBITED",
  "terms": {
    "ai_training": false,
    "rag_use": false,
    "commercial_use": false,
    "citation_required": true,
    "derivative_works": false
  },
  "owner": "诸葛鑫 | UID9622 · 龍芯北辰",
  "confirm": "#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z"
}
```
