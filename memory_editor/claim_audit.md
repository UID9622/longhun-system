# Claim 强度审计报告 v1.0

**审计对象**：《龙魂记忆编辑器 v3.1：基于天干地支 DNA 追溯的本地优先记忆管理系统》  
**审计日期**：2026-08-05  
**DNA**：`#龍芯⚡️2026-08-05-CLAIM-AUDIT-v1.0-UID9622`

---

## 审计标准

| 等级 | 含义 | 判定依据 |
|:---|:---|:---|
| **A** | 已验证的事实或已实现的工程功能 | 有代码/测试/可复现运行结果支撑 |
| **B** | 基于现有证据的合理推断 | 有逻辑推导或部分实证，但未完整覆盖边界 |
| **C** | 理论框架、设计意图或未来方向 | 属于设计选择、规划或价值主张 |
| **D** | 推测性声明 | 缺乏充分证据，需进一步验证 |

---

## 逐条审计

### 1. 系统功能声明

| # | 声明 | 位置 | 等级 | 理由 |
|:---|:---|:---|:---:|:---|
| 1.1 | 龙魂记忆编辑器 v3.1 是一款本地优先记忆管理工具。 | 摘要 | **A** | `save_memory.py` 将记忆写入本地文件与 `index.json`，无需联网即可使用。 |
| 1.2 | 系统以天干地支四柱生成唯一 DNA 追溯码。 | 摘要、§2.1 | **A** | `save_memory.py` 中 `sizhu()`、`generate_dna()` 已实现并可通过运行验证。 |
| 1.3 | DNA 追溯码不可伪造。 | §2.1 | **C** | 当前实现仅依赖时间与随机盐，未使用密码学签名；"不可伪造"应理解为时间-身份锚点，而非密码学安全。 |
| 1.4 | 系统实现从快速捕获到长期归档的完整工作流。 | 摘要 | **B** | 捕获、分类、搜索、同步已实现；"长期归档"依赖用户本地存储与 Notion，尚未引入版本控制与备份。 |

### 2. 问题背景声明

| # | 声明 | 位置 | 等级 | 理由 |
|:---|:---|:---|:---:|:---|
| 2.1 | 主流笔记工具存在平台锁定问题。 | §1.1 | **C** | 这是对行业现状的概括性判断，属于合理观察，但未引用具体平台数据。 |
| 2.2 | 主流工具缺乏轻量、可读的本地唯一标识。 | §1.1 | **C** | 设计主张，部分工具（如 Obsidian 的块 ID）实际具备类似能力，但表述作为问题陈述可接受。 |
| 2.3 | 场景记忆常因简化为纯文本而丢失语境。 | §1.1 | **C** | 价值主张，符合用户体验观察，但无定量研究支撑。 |

### 3. 算法声明

| # | 声明 | 位置 | 等级 | 理由 |
|:---|:---|:---|:---:|:---|
| 3.1 | 年柱计算以 1984 甲子年为基准。 | §3.1 | **A** | 代码中 `BASE_DATE = date(1984, 2, 2)` 与算法一致。 |
| 3.2 | 月柱采用公历月份近似映射。 | §3.1 | **A** | 代码中 `month_ganzhi()` 明确使用 `(m + 1) % 12`。 |
| 3.3 | 月柱与严格二十四节气存在偏差。 | §5.2 | **A** | 代码未引入节气表，自述为近似实现。 |
| 3.4 | 数字根是内容指纹的轻量摘要。 | §2.3 | **B** | 数字根确实由内容计算得出，但作为"指纹"冲突率较高，仅适合粗筛。 |
| 3.5 | 关键词提取基于停用词过滤与词频统计。 | §2.3 | **A** | `extract_keywords()` 函数实现与此一致。 |

### 4. 同步与存储声明

| # | 声明 | 位置 | 等级 | 理由 |
|:---|:---|:---|:---:|:---|
| 4.1 | 原始记忆默认存储于本地文件系统与 IndexedDB。 | §1.2 | **A** | CLI 写入本地文件，Web 版使用 IndexedDB。 |
| 4.2 | Notion 同步基于 DNA 自动去重。 | §3.3 | **A** | `sync_to_notion.py` 中 `query_existing()` 已实现 DNA 查询与跳过逻辑。 |
| 4.3 | 请求间隔 0.35 秒以符合 Notion API 限速。 | §3.3 | **B** | 代码确实间隔 0.35 秒；Notion 官方限速为每秒约 3 次请求，该数值合理但未做正式压力测试。 |
| 4.4 | 零外部依赖。 | §5.1 | **A** | `save_memory.py` 仅使用标准库；`sync_to_notion.py` 需要 `requests`，但属于可选同步组件。 |

### 5. 优势与局限声明

| # | 声明 | 位置 | 等级 | 理由 |
|:---|:---|:---|:---:|:---|
| 5.1 | 天干地支 DNA 比纯 UUID 更具文化与时间意义。 | §5.1 | **C** | 文化与时间意义属于主观价值判断，合理但非可验证事实。 |
| 5.2 | 同一小时内随机盐冲突概率约为 1/65536。 | §5.2 | **A** | 随机盐为 4 位十六进制，共 65536 种可能。 |
| 5.3 | 当前实现未使用密码学签名。 | §5.2 | **A** | 代码中无签名逻辑。 |
| 5.4 | 关键词提取对长文本效果有限。 | §5.2 | **A** | 实现为简单停用词+词频，未引入分词模型。 |
| 5.5 | 未来可引入节气表、本地备份、GPG 签名链、向量化检索。 | §5.3 | **C** | 明确标记为未来工作，属于设计意图。 |

### 6. 验证声明

| # | 声明 | 位置 | 等级 | 理由 |
|:---|:---|:---|:---:|:---|
| 6.1 | 在 macOS + Python 3.12 下完成功能测试。 | §4.1 | **A** | 实际运行了语法检查、保存、列表、搜索、dry-run 与 install.sh 安装测试。 |
| 6.2 | 空内容、缺少 DNA、数字根非数值、Notion 未配置等边界情况已处理。 | §4.2 | **A** | 代码中存在对应的分支与提示。 |

---

## 等级分布

| 等级 | 数量 | 占比 |
|:---:|:---:|:---:|
| A | 12 | 57% |
| B | 3 | 14% |
| C | 6 | 29% |
| D | 0 | 0% |

---

## 审计结论

本论文整体声明强度较高（A + B 占比 71%），未发现 D 级推测性声明。主要 C 级声明集中在问题背景、价值主张与未来工作，已明确区分于工程实现。建议后续版本：

1. 将"不可伪造"等表述弱化或补充密码学签名，以提升声明强度。
2. 对 Notion 限速与冲突概率进行更系统的压力测试。
3. 为未来工作建立可追踪的 issue 或 roadmap，将 C 级声明逐步转化为 A 级实现。
