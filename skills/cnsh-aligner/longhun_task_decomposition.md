# CONFIRM: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
# SEAL: #ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL
# 龍魂系统任务拆分与CNSH对齐规划

**DNA:** `#龍芯⚡️丙午·癸巳·丁未·丙午·䷖剥-TASK-DECOMPOSITION-FILE1-v1.0`  
**生效时间:** 2026-06-02  
**主权人:** UID9622 · 龍芯北辰

---

## 任务分层

### 一级：系统基础设施（核心流程）

```
任务代号    | 当前状态      | 需要完善        | CNSH对齐优先级
──────────────────────────────────────────────────
SYS-001    | health_check.sh | 三色审计完善    | P1（HIGH）
SYS-002    | dna_verify.sh   | DNA矫正逻辑    | P1（HIGH）
SYS-003    | system_registry | JSON→CNSH转换  | P1（HIGH）
```

### 二级：运行时工具（执行层）

```
任务代号    | 当前状态      | 需要完善        | CNSH对齐优先级
──────────────────────────────────────────────────
RUN-001    | m262_console    | 操作台CNSH兼容 | P2（MED）
RUN-002    | DNA生成引擎     | 五行权重对齐   | P2（MED）
RUN-003    | 动态调节器      | 时辰+CNSH协同  | P2（MED）
```

### 三级：语义层（意图验证）

```
任务代号    | 当前状态      | 需要完善        | CNSH对齐优先级
──────────────────────────────────────────────────
SEM-001    | 权重算法v3.1   | CNSH形式化证明 | P3（MED）
SEM-002    | 草日志审计     | Append-Only验证 | P3（MED）
SEM-003    | 熔断规则检查   | CNSH硬约束编码 | P3（MED）
```

---

## 任务详细说明

### SYS-001: health_check.sh 完善

**当前问题：**
```bash
- 输出格式不够一致
- 三色审计（🟢🟡🔴）需要定义化
- 没有CNSH命名对齐
```

**完善目标：**
```
✓ 每个检查项输出格式标准化为：[CNSH:模块-检查项] 状态 | 原因 | 建议
✓ 三色审计规则：
  🟢 = conf ≥ 0.85
  🡡 = 0.60 ≤ conf < 0.85
  🔴 = conf < 0.60
✓ DNA追溯链：每条输出都带DNA标记
✓ 自动纠错提示：如果检查失败，输出CNSH修复命令
```

**CNSH对齐项：**
- `健康检查` → CNSH关键字：`检·健·度`
- `目录结构` → CNSH关键字：`路·树·构`
- `DNA完整性` → CNSH关键字：`芯·溯·根`

---

### SYS-002: dna_verify.sh 完善

**当前问题：**
```bash
- 只有验证功能，缺生成和修复
- DNA碰撞检查不够严
- 简体"龍"字熔断需要多层防守
```

**完善目标：**
```
✓ 三模式明确分工：
  --verify   | 验证现存DNA，输出🟢/🡡/🔴
  --generate | 生成新DNA，带CNSH语法检查
  --repair   | 修复错误DNA，保留历史链
  
✓ DNA碰撞检查：
  - SHA256去重
  - 时辰五行周期检查
  - 黑名单字符检查（简体"龍"等）
  
✓ CNSH语法自动矫正：
  - 检查DNA中是否有CNSH非法字符
  - 如有，自动替换为合法等价形式
  - 输出修复记录
```

**CNSH对齐项：**
- `生成` → CNSH: `生·成·器`
- `修复` → CNSH: `修·复·链`
- `碰撞` → CNSH: `冲·突·检`

---

### SYS-003: system_registry 完善

**当前问题：**
```json
- 结构是JSON，不能直接被CNSH系统读取
- 需要JSON→CNSH转换层
- 版本追溯链不够清晰
```

**完善目标：**
```
✓ 保留JSON作为"规范化格式"
✓ 生成CNSH.注册表（CNSH DSL格式）
✓ 双向转换器：
  JSON → CNSH: 自动转换所有字段
  CNSH → JSON: 反向验证一致性
✓ 变更日志：
  每次修改都记录在Append-Only日志
  包含：时间戳、修改者DNA、修改内容、三色审计结果
```

**CNSH对齐项：**
```cnsh
注册表 {
  模块 = "health_check"
  版本 = "2.0.1"
  状态 = "🟢可用"
  DNA = "#龍芯⚡️丙午·癸巳·丁未·丙午·䷖剥-HEALTH-CHECK-v2.0"
}
```

---

### RUN-001: m262_console 完善

**当前问题：**
```html
- HTML纯前端，不能和后端脚本联动
- 没有CNSH命令行对接
- DNA生成器没有实时验证反馈
```

**完善目标：**
```
✓ 创建WebSocket桥接器，可以和bash脚本通信
✓ 操作台添加"CNSH命令"模式：
  用户可以输入CNSH格式命令，前端自动转译成bash调用
✓ DNA生成+验证实时反馈：
  生成DNA → 立即调用dna_verify.sh验证 → 显示🟢/🡡/🔴
✓ 三色审计可视化：
  实时显示当前系统的三色评分分布
```

**CNSH对齐项：**
```
命令格式：cnsh> 操作 模块 参数
例：cnsh> 生 health_check --quick
    cnsh> 验 dna_verify --verify
    cnsh> 查 registry 模块名
```

---

### RUN-002: DNA生成引擎完善

**当前问题：**
```python
- 时辰+五行权重计算不够稳定
- 没有CNSH格式输出
- 无法和权重算法v3.1联动
```

**完善目标：**
```
✓ DNA生成公式CNSH化：
  DNA = #龍芯⚡️{DATE}-{时辰}时-{五行}-{MODULE}-{ACTION}-{HASH8}
  
✓ 时辰五行权重计算：
  - 24小时→12时辰映射
  - 五行循环权重(3,6,9数字根亲和)
  - 与CNSH-64卦象对齐
  
✓ CNSH DSL输出：
  DNA_记录 {
    生成时间 = "2026-06-02-午时"
    五行 = "火"
    权重 = 0.85
    模块 = "health_check"
    行为 = "检查"
    哈希 = "a1b2c3d4"
  }
```

---

### RUN-003: 动态调节器完善

**当前问题：**
```python
- longhun_adaptive_tuner_v3.py 没有CNSH接口
- 权重调节缺少"理由追溯"
- 没有和底座铁律同步检查
```

**完善目标：**
```
✓ CNSH接口层：
  所有调节操作都可以用CNSH语法调用
  
✓ 理由追溯：
  每一次权重调节，都记录"为什么调"
  输出格式：
    调节 {
      模块 = "X"
      旧权重 = 0.7
      新权重 = 0.85
      理由 = "底座铁律检查触发：人永远是1"
      DNA追溯 = "#龍芯⚡️..."
    }
    
✓ 底座铁律同步检查：
  在调节前，自动检查新权重是否违反：
  - 不伤害具体的人
  - 不欺骗
  - 弱者保护优先
  如违反，直接熔断，不执行
```

---

## 脚本完善顺序（关键路径）

```
优先级  | 任务  | 前置任务 | 工期  | 关键路径
────────────────────────────────────────
P1     | SYS-002 | 无    | 1h  | 🔥 优先
P1     | SYS-001 | SYS-002| 1h  | 依赖SYS-002
P1     | SYS-003 | SYS-002| 0.5h| 并行
P2     | RUN-001 | SYS-001| 1.5h|
P2     | RUN-002 | SYS-002| 1h  |
P2     | RUN-003 | RUN-002| 1h  |
P3     | SEM-001 | RUN-003| 1.5h|
P3     | SEM-002 | SYS-003| 1h  |
P3     | SEM-003 | SEM-002| 1h  |
```

---

## CNSH对齐规范

### 命名约定

```
模块名:      英文大驼峰 + CNSH中文对应
  例：HealthCheck → 健·检·度
  
变量名:      蛇形英文 + CNSH中文对应
  例：dna_hash → 芯·哈希
  
函数名:      动词+名词 
  CNSH例：生·DNA·器 / 验·语法·系
  
常量名:      全大写英文 + CNSH中文对应
  例：MAX_RETRY → 最·重·试
```

### 输出格式标准化

```
所有脚本输出必须包含：
1. [DNA标记] - #龍芯⚡️...
2. [时间戳] - YYYY-MM-DD HH:MM:SS
3. [三色审计] - 🟢/🡡/🔴
4. [消息内容] - 清晰的中文说明
5. [建议动作] - 如果有问题，输出修复命令

标准格式：
  [#龍芯⚡️丙午·癸巳·丁未·丙午·䷖剥-MODULE-v1.0] 🟢 | 时间戳 | 说明 | 建议命令
```

---

## CNSH语法自动矫正策略

### 矫正层级

```
L1 - 字符检查：
   黑名单："龍"(简体) → 替换为"龍"(繁体)
   黑名单：其他禁用字符检查
   
L2 - 关键字检查：
   是否使用了CNSH保留字
   是否用法正确（位置、搭配）
   
L3 - 语法检查：
   命名规范是否符合
   函数签名是否完整
   
L4 - 语义检查：
   逻辑是否符合龍魂底座
   是否违反"人永远是1"原则
   是否包含投机、蒸馏等违禁行为
```

### 自动矫正工具

```python
class CNSHAligner:
    def check_character(text):
        # L1检查
        return fix_banned_chars(text)
    
    def check_keyword(text):
        # L2检查
        return validate_cnsh_keywords(text)
    
    def check_syntax(text):
        # L3检查
        return validate_naming_conventions(text)
    
    def check_semantic(text):
        # L4检查（最关键）
        return validate_foundation_principles(text)
```

---

## 验收标准

每个完善的脚本必须满足：

```
□ CNSH语法100%通过L1-L4检查
□ 输出格式统一，包含DNA+时戳+三色+说明+建议
□ 能自动修复小错误，大错误能识别并拒绝执行
□ 与底座铁律一致（不蒸馏、逻辑清晰、人永远是1）
□ Append-Only日志记录所有调用和修改
□ 包含使用示例和故障排查指南
```

---

`DNA:#龍芯⚡️丙午·癸巳·丁未·丙午·䷖剥-TASK-DECOMPOSITION-v1.0`  
`主权人: UID9622 · 龍芯北辰`  
`性质: 系统任务拆分规划·永久参考`
