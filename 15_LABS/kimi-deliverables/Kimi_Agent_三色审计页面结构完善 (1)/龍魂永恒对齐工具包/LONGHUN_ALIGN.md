# 🐉 LONGHUN_ALIGN · 龍魂永恒对齐锚 v1.0

> **本文件是仓库的最高对齐法。任何 AI 窗口（Kimi / CodeBuddy / DeepSeek / 小艺 / 任何后来者）进入本仓库，开工前必须先读完本文件，输出必须过本文件每一条规定。违者产出自动 🔴 回潮拦截，存档冻结，不得入库。**
> 本文件只追加不删改；修订走版本号。当前版本 v1.0 · 锚定 DNA 见文末。

## 一、身份锚（焊死）

- UID：`UID9622` · 主控人格：👑 老大（诸葛鑫 / 龍芯北辰）
- 确认码（唯一合法形态，多一个字符少一个字符都算伪造）：
  `#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z`
- GPG 指纹：`A2D0092CEE2E5BA87035600924C3704A8CC26D5F`
- DNA 前缀：`#龍芯⚡️`（`#ZHUGEXIN⚡️` 为 2026-01 前旧前缀，出现即 🔴）

## 二、DNA 干支纪律（P0 · 最高违规红线）

1. **任何 DNA 干支必须由 rizhu v3.0 算法生成，禁止手写**。手写干支 = 签名纪律违规，产出整体冻结。
2. 算法口径：日柱 = 锚 1900-01-01 甲戌（index 10）+ 偏移 mod 60；月柱 = 节气月（立春起寅月，立春为岁首）；日卦 = 年内日序 mod 64。
3. 生成器：`longhun_core/dna_trace.py` 的 `生成DNA(标识)`。没有生成器的环境，DNA 字段留 `【待生成器回填】`，**宁可空着也不许编**。
4. 旧格式 `#龍芯⚡️YYYY-MM-DD-...` 一律视为历史存档，新产出禁止再用。

## 三、交付纪律（每件产出必须满足）

1. **三段式**：参考来源 / 优化了什么 / 未验证备注（🟡🔴 自报）。
2. **补全≠评审**：要求补全的，交付完整成品，原稿保留不删改，修正写进新区块。
3. **不删除只冻结**：旧内容进备注/存档区，永不物理删除。
4. **能跑才算数**：代码交付必须附实测输出（pytest/demo 原文），未实测标【示意】。
5. **分层许可**：思想层 CC BY-NC-SA 4.0 · 工程层 MulanPSL v2（fork MIT 项目时工程层随 MIT）。

## 四、结构锚（防重复造轮子）

1. 开工前先查：**花名册**（Notion 龍芯家族花名册）+ **资产中心**——已有模块不重造，只迭代。
2. 新资产入库四证：路由编号 `UID9622-[代号]-[序号]` + IPA `IPA-[层级]-[代号]-[职能]` + 功能定位 + DNA 追溯码。
3. 目录规范：脚本进 `08_BIN/`，文档进 `01_protocols/`，引擎进对应编号目录，禁止根目录乱放。
4. 状态口径统一：🟢活跃 / 🟡观察·待命 / 🔴熔断·违规 / ⚪休眠·退役，禁止自造状态词。

## 五、合并·优化·迭代指令协议（老大下口令的标准句式）

老大对每个 AI 窗口下合并迭代指令时，用以下五段式，AI 必须逐段回应：

```
【对齐】先跑 lh_align.py 报告当前仓库三色状态
【合并】列出与现有模块重复/冲突的清单，说明保留哪个、冻结哪个
【优化】逐项说明改了什么、为什么、依据哪条协议
【验证】附实测输出（命令+原文），未实测的标【示意】
【签名】用生成器出 DNA，三段式收尾
```

口令模板（直接抄）：
> 「按 LONGHUN_ALIGN.md 执行对齐合并迭代：先扫对齐报告，再合并重复模块，再优化，全程实测，生成器签名。」

## 六、验收（每次合并迭代后必跑）

```bash
python3 lh_align.py .        # 三色必须 🟢 或仅剩🟡（历史存档除外）
python3 -m pytest -q         # 测试全绿
lh selftest                  # 内核自测🟢
```

三项不过，交付不成立。

---

🐉 `#龍芯⚡️丙午·丙申·丁巳·丙午·䷟恒-ALIGN-ANCHOR-v1.0-UID9622`
