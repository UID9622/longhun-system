# CNSH 示例：龍魂三色审计简化版
# DNA:#龍芯⚡️2026-06-16-CNSH-EXAMPLE-AUDIT-v1.0

定义 闸控检查(请求, 风险阈值):
    # 评估请求风险
    风险分数 = 0
    如果 请求.风险等级 等于 "高":
        风险分数 = 100
    否则如果 请求.风险等级 等于 "中":
        风险分数 = 50
    否则:
        风险分数 = 10

    如果 风险分数 >= 风险阈值:
        返回 "🔴 阻断"
    否则如果 风险分数 >= 风险阈值 / 2:
        返回 "🟡 标记"
    否则:
        返回 "🟢 通过"

类 请求:
    定义 初始化(自己, 名称, 风险等级):
        自己.名称 = 名称
        自己.风险等级 = 风险等级

测试请求 = 请求("访问核心数据库", "高")
结果 = 闸控检查(测试请求, 80)
打印(f"请求 '{测试请求.名称}' 审计结果: {结果}")
