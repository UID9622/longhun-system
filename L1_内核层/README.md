# L1_内核层

> 内核层 · CNSH 运行时、国密、核心类型系统

## 职责

系统底座：CNSH 编译器/运行时、国密算法、核心服务。

## 关联模块

- `cnsh-core/`
- `core/`
- `core-services/`

## 本层入口

- `cnsh-core/` → 符号链接到 `../cnsh-core/`
- `core/` → 符号链接到 `../core/`
- `core-services/` → 符号链接到 `../core-services/`

---

> 本文件为 L 层结构索引，由 Kimi 整理生成。
> 符号链接仅作为视图入口，不移动原始文件。
