#!/bin/sh
# pre-commit 钩子 — 新代码 CNSH 命名闸口（2026-09-01 焊死）
# 新增 .py 必须使用 CNSH 中文命名，否则阻止入库。存量文件不受影响。
# 跳过场景: `git commit --no-verify`（显式绕行，P05 审计留档）
PY=/Users/zuimeidedeyihan/longhun-system/08_BIN/lh_cnsh_gate.py
if [ -f "$PY" ]; then
  python3 "$PY" --pre-commit || exit 1
fi
exit 0
