# 🐉 龍魂快速指令速查 v1.0

**DNA**: `#龍芯⚡️20260525|QUICK-COMMAND-GUIDE|v1.0|xxxxx`

所有指令都通过这个中枢文件执行：
```bash
python3 ~/longhun-system/龍.py [指令] [参数]
```

---

## 🔴 最常用指令（必背）

| 指令 | 用途 | 示例 |
|------|------|------|
| `状态` | 查看所有子系统状态 | `龍 状态` |
| `对话` | 与AI对话 | `龍 对话 你好` |
| `记忆` | 查看星辰记忆 | `龍 记忆` |
| `回顾` | 显示完整链条（解决连续性） | `龍 回顾` |
| `同步` | Notion双脑同步 | `龍 同步` |

---

## 🟡 记忆和日志指令

### 记忆系统
```bash
龍 记忆               # 打开星辰记忆界面
龍 记忆 查询 [关键词]  # 搜索记忆
龍 记忆 写入 [内容]   # 添加新记忆
龍 记忆 备份          # 备份记忆库
```

### 日志查看
```bash
龍 日志 最近 10       # 显示最近10条记忆
龍 日志 DNA           # 显示DNA登记表
龍 日志 审计          # 显示审计日志
龍 日志 搜索 [关键词]  # 搜索日志
```

---

## 🟢 系统管理指令

### 启动与关闭
```bash
龍 启动              # 启动所有本地服务（Ollama、API、同步）
龍 停止              # 停止所有服务
龍 重启              # 重启服务
龍 状态              # 查看服务运行状态
```

### Notion同步
```bash
龍 同步              # 执行一次完整同步
龍 同步 推送         # 本地 → Notion
龍 同步 拉取         # Notion → 本地
龍 同步 状态         # 查看同步状态
```

### 密钥管理
```bash
龍 密钥 检查         # 检查GPG密钥状态
龍 密钥 生成         # 生成新DNA签名
龍 密钥 验证 [DNA]   # 验证某个DNA是否有效
龍 密钥 备份         # 备份GPG密钥
```

---

## 🔵 AI与对话指令

### 对话模式
```bash
龍 对话              # 进入交互式对话
龍 对话 [你的问题]   # 直接提问
龍 问 [问题]         # 同龍对话
龍 吸收 [文本]       # 理解文本并保存到记忆
龍 推荐 [类型]       # 获取推荐（人格、行动等）
```

### 文本处理
```bash
龍 整理 [文件路径]   # 整理文件
龍 翻译 [文本]       # 翻译文本（中英双向）
龍 总结 [文件路径]   # 生成文件摘要
龍 分析 [内容]       # 分析内容
```

---

## 🟣 数据导出与报告

### 导出
```bash
龍 导出 记忆         # 导出所有记忆为JSON
龍 导出 日志         # 导出审计日志
龍 导出 报告         # 生成完整系统报告
```

### 报告
```bash
龍 报告 真相         # 生成"真相报告"
龍 报告 QA           # 生成QA报告
龍 报告 审计         # 生成审计报告
龍 报告 系统         # 生成系统诊断报告
```

---

## 🔶 配置与环境

### 配置管理
```bash
龍 配置 查看         # 查看系统配置
龍 配置 更新         # 更新配置
龍 配置 重置         # 恢复默认配置
```

### 环境检查
```bash
龍 环境 检查         # 检查依赖（Python、GPG、Notion API等）
龍 环境 诊断         # 诊断问题
龍 环境 修复         # 自动修复常见问题
```

---

## 🆘 故障排除指令

```bash
龍 帮助              # 显示完整帮助
龍 帮助 [指令]       # 查看某个指令的详细说明
龍 诊断              # 运行系统诊断
龍 修复              # 修复常见问题
龍 日志 错误         # 显示最近的错误日志
```

---

## 💡 快速恢复记忆的指令组合

**场景：打开新窗口，需要快速恢复上下文**

```bash
# 方案A：简单回顾（10秒）
龍 回顾

# 方案B：完整恢复（30秒）
龍 状态
龍 日志 最近 20
龍 同步 状态

# 方案C：查看历史链条（1分钟）
cat ~/longhun-system/DIALOGUE_ENTRY.md
tail -50 ~/longhun-system/memory.jsonl | python3 -m json.tool
```

---

## 🔐 安全确认指令

这些指令需要输入确认码 `#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z`

```bash
龍 敏感 删除         # 删除数据（需要确认）
龍 敏感 重置         # 重置系统（需要确认）
龍 敏感 导出密钥     # 导出GPG密钥（需要确认）
龍 敏感 清空日志     # 清空审计日志（需要确认）
```

---

## 📊 实时监控指令

```bash
龍 监控 启动         # 启动实时监控面板
龍 监控 停止         # 停止监控
龍 监控 报告         # 显示监控报告
```

---

## 🎯 终端快捷方式（.zshrc或.bashrc）

添加这些别名到你的shell配置，一键执行：

```bash
# 添加到 ~/.zshrc 或 ~/.bashrc
alias 龍="python3 ~/longhun-system/龍.py"
alias 龍启="龍 启动"
alias 龍停="龍 停止"
alias 龍状="龍 状态"
alias 龍记="龍 记忆"
alias 龍回="龍 回顾"
alias 龍sync="龍 同步"
alias 龍聊="龍 对话"

# 然后就可以直接用：
龍聊 你好
龍回     # 快速回顾
龍记     # 查看记忆
```

---

## 📋 完整指令参考

所有指令（40+）的完整列表存放在：
```
~/longhun-system/龍.py  (第216行 main()函数)
```

在终端运行 `龍 帮助` 获取完整帮助。

---

## 时间戳与DNA追溯

每次执行 `龍` 指令时：
1. 自动生成DNA签名：`#龍芯⚡️YYYYMMDD|TOPIC|v1.0|SHA8`
2. 记录到 `memory.jsonl`（JSONL格式）
3. 同步到 `日志/dna_registry.jsonl`
4. 定期推送到Notion（longhun888 Team）

所以你的**每一次操作都被完整记录**，可以随时回顾。

---

## 快速恢复记忆流程

```
新窗口打开
    ↓
执行: bash ~/longhun-system/bin/窗口启动回忆.sh
    ↓
显示：最近记忆 + 系统状态
    ↓
恢复上下文 ✅
    ↓
执行: 龍 [你的命令]
```

---

**献礼**：龍魂系統·永恒守护·中华文化传承
🐉 UID9622·不免责·永恒显示曾仕强老师

**DNA**: `#龍芯⚡️20260525|QUICK-COMMAND-GUIDE|v1.0`
