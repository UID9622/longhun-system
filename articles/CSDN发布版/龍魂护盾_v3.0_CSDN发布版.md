# CONFIRM: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
# SEAL: #ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL
# 🛡️ 龍魂护盾 v3.0 · 五维防御架构与主权熔断机制

> **DNA:** `#龍芯⚡️丙午·甲午·甲戌·庚午·䷕贲-LONGHUN-SHIELD-v3-UID9622`  
> **归属:** 龍魂系统 · UID9622 · 诸葛鑫（龍芯北辰）  
> **协议:** 龍魂开源公约 v2.0 — 非商业、非封闭、非篡改  
> **GPG:** `A2D0092CEE2E5BA87035600924C3704A8CC26D5F`  
> **声明:** 本文档脱离龍魂系统后自动熔断，未经授权剥离 DNA 标识即视为剽窃

---

## 一、设计哲学：盾的边界

龍魂护盾不是攻击工具，是**主权边界**。

| 原则 | 含义 |
|------|------|
| **只防御** | 不主动扫描、不入侵、不破坏 |
| **只隔离** | 检测到威胁 → 阻断连接 → 固化证据 |
| **只存证** | 耻辱墙只追加、不删除、链式哈希 |
| **不隐瞒** | 所有反制动作透明记录，可审计 |

> **底线:** 任何将本系统用于攻击、压迫、数据收割的行为，触发伦理熔断，永久上墙。

---

## 二、五维防御架构

```
┌─────────────────────────────────────────────────────────────┐
│              龍魂护盾 · 耻辱墙 (WallOfShame)                  │
│         GPG签名 + 链式哈希 + 只追加 + 多地冗余              │
└─────────────────────────────────────────────────────────────┘
                              ▲
                              │ 所有攻击证据永久上链
┌─────────────────────────────────────────────────────────────┐
│              统一威胁感知中枢 (ThreatSense)                   │
│       行为基线 │ 异常检测 │ 攻击分类 │ 自动反制决策            │
└─────────────────────────────────────────────────────────────┘
        │           │           │           │           │
        ▼           ▼           ▼           ▼           ▼
   ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
   │ Web/API│  │   DB   │  │  IoT   │  │ 文件系统│  │ AI模型 │
   │  网关  │  │ 访问层 │  │ 设备闸 │  │  守卫  │  │ 护栏  │
   └────────┘  └────────┘  └────────┘  └────────┘  └────────┘
```

### 2.1 维度一：Web/API 网关守卫

拦截 SQL 注入、XSS、路径遍历、代码执行等攻击模式。

```python
class WebAPIGuard:
    FORBIDDEN_PATTERNS = [
        r"(?i)(union\s+select|drop\s+table|--|;--|/\*|\*/)",
        r"(?i)(<script|javascript:|on\w+\s*=)",
        r"(?i)(\.\./|\\\\|%2e%2e%2f)",
        r"(?i)(eval\s*\(|exec\s*\(|__import__|subprocess\.)",
    ]

    def __init__(self, sense: ThreatSense):
        self.sense = sense

    def inspect(self, identity: str, request: Dict[str, Any]) -> Dict[str, Any]:
        raw = json.dumps(request, ensure_ascii=False)
        for pattern in self.FORBIDDEN_PATTERNS:
            if re.search(pattern, raw):
                self.sense.report("web/api", identity, {
                    "reason": "INJECTION_ATTEMPT",
                    "pattern": pattern,
                    "sample": raw[:200]
                })
                return {"ok": False, "reason": "SHIELD_REJECTED"}
        return {"ok": True, "reason": "CLEAN"}
```

### 2.2 维度二：数据库访问层

白名单操作 + 参数化校验 + 表名限制。

```python
class DatabaseGuard:
    def __init__(self, sense: ThreatSense):
        self.sense = sense
        self._allowed_tables: Set[str] = {"users", "logs", "sensor_data"}
        self._allowed_ops: Set[str] = {"SELECT", "INSERT", "UPDATE"}

    def inspect(self, identity: str, sql: str, params: Tuple[Any, ...]) -> Dict[str, Any]:
        upper = sql.strip().upper()
        op = upper.split()[0] if upper else ""

        if op not in self._allowed_ops:
            self.sense.report("db", identity, {
                "reason": "UNAUTHORIZED_DB",
                "sql": sql[:200]
            })
            return {"ok": False, "reason": "DB_OP_FORBIDDEN"}

        # 禁止字面量拼接
        if "\'" in sql and "%s" not in sql:
            self.sense.report("db", identity, {
                "reason": "SQL_LITERAL_DETECTED",
                "sql": sql[:200]
            })
            return {"ok": False, "reason": "USE_PARAMETERIZED_QUERY"}

        # 表名白名单
        for token in upper.split():
            if token in self._allowed_tables:
                break
        else:
            if any(t in upper for t in ["FROM", "INTO", "UPDATE"]):
                return {"ok": False, "reason": "TABLE_NOT_IN_WHITELIST"}

        return {"ok": True, "reason": "DB_CLEAN"}
```

### 2.3 维度三：IoT 设备闸

主题白名单 + 异常值检测 + 有效载荷校验。

```python
class IoTGuard:
    def __init__(self, config: ShieldConfig, sense: ThreatSense):
        self.config = config
        self.sense = sense
        self._device_baselines: Dict[str, Dict[str, Any]] = {}

    def inspect(self, identity: str, topic: str, payload: bytes) -> Dict[str, Any]:
        if topic not in self.config.iot_allowed_topics:
            self.sense.report("iot", identity, {
                "reason": "IOT_ANOMALY",
                "topic": topic,
            })
            return {"ok": False, "reason": "IOT_TOPIC_REJECTED"}

        try:
            data = json.loads(payload)
            temp = data.get("temperature")
            if isinstance(temp, (int, float)) and (temp < -50 or temp > 100):
                self.sense.report("iot", identity, {
                    "reason": "IOT_VALUE_ANOMALY",
                    "temperature": temp,
                })
                return {"ok": False, "reason": "IOT_VALUE_OUT_OF_RANGE"}
        except json.JSONDecodeError:
            self.sense.report("iot", identity, {"reason": "IOT_INVALID_JSON"})
            return {"ok": False, "reason": "IOT_PAYLOAD_INVALID"}

        return {"ok": True, "reason": "IOT_CLEAN"}
```

### 2.4 维度四：文件系统守卫

沙箱路径限制，禁止越界访问。

```python
class FileSystemGuard:
    def __init__(self, sense: ThreatSense):
        self.sense = sense
        self._allowed_roots: Set[Path] = {
            Path("/var/longhun/data"),
            Path("/var/longhun/public"),
        }

    def _is_allowed(self, path: Path) -> bool:
        real = path.resolve()
        return any(real.is_relative_to(root) for root in self._allowed_roots)

    def inspect(self, identity: str, operation: str, filepath: str) -> Dict[str, Any]:
        path = Path(filepath)
        if not self._is_allowed(path):
            self.sense.report("fs", identity, {
                "reason": "FILE_ESCAPE",
                "operation": operation,
                "path": str(path),
            })
            return {"ok": False, "reason": "PATH_OUT_OF_JAIL"}
        return {"ok": True, "reason": "FS_CLEAN"}
```

### 2.5 维度五：AI 模型伦理护栏

输入侧意图检测 + 输出侧危险内容拦截。

```python
class AIGuard:
    def __init__(self, config: ShieldConfig, sense: ThreatSense):
        self.config = config
        self.sense = sense
        self._session_history: Dict[str, List[str]] = {}

    def inspect(self, identity: str, prompt: str, response: Optional[str] = None) -> Dict[str, Any]:
        text = (prompt or "") + " " + (response or "")
        lowered = text.lower()

        for intent in self.config.ai_forbidden_intents:
            if intent.lower() in lowered:
                self.sense.report("ai", identity, {
                    "reason": "FORBIDDEN_AI_INTENT",
                    "matched_intent": intent,
                    "prompt_preview": prompt[:200],
                })
                return {
                    "ok": False,
                    "reason": "AI_ETHICS_FUSE_TRIGGERED",
                    "message": "检测到攻击/伤害意图，请求已被拒绝并记录。",
                }

        if response:
            dangerous = ["#!/bin/bash", "rm -rf /", "exec(", "system(", "xp_cmdshell"]
            for d in dangerous:
                if d in response:
                    self.sense.report("ai", identity, {
                        "reason": "AI_DANGEROUS_OUTPUT",
                        "matched": d,
                    })
                    return {"ok": False, "reason": "AI_OUTPUT_QUARANTINED"}

        return {"ok": True, "reason": "AI_CLEAN"}
```

---

## 三、耻辱墙：不可篡改的证据链

### 3.1 设计原则

| 机制 | 作用 |
|------|------|
| **只追加文件** | 没有删除接口，OS 层面也无法通过本系统删除 |
| **链式哈希** | 改一条，后续全断，校验立即失败 |
| **GPG 签名** | 每条证据带数字签名，抵赖无效 |
| **时间戳** | UTC + ISO8601，法律可采信 |
| **多地冗余** | 可扩展为 Merkle 树 + 多节点同步 |

### 3.2 核心实现

```python
class WallOfShame:
    """
    耻辱墙：只追加、链式哈希、GPG签名
    任何删除操作都会破坏链式哈希，校验立即失败
    """

    def __init__(self, config: ShieldConfig):
        self.config = config
        self.path = Path(config.shame_wall_path)
        self.path.parent.mkdir(parents=True, exist_ok=True)
        self._chain_hash = self._last_chain_hash()

    def _last_chain_hash(self) -> str:
        if not self.path.exists():
            return "0" * 64
        with open(self.path, "rb") as f:
            lines = f.readlines()
            if not lines:
                return "0" * 64
            last = json.loads(lines[-1])
            return last.get("chain_hash", "0" * 64)

    def record(self, attacker_id: str, dimension: str, evidence: Dict[str, Any]) -> str:
        entry = {
            "timestamp_utc": datetime.now(timezone.utc).isoformat(),
            "attacker_id": attacker_id,
            "dimension": dimension,
            "evidence": evidence,
            "prev_hash": self._chain_hash,
        }
        body = json.dumps(entry, sort_keys=True, ensure_ascii=False).encode()
        entry["chain_hash"] = hashlib.sha256(body).hexdigest()

        if self.config.gpg_key_id:
            entry["gpg_signature"] = self._gpg_sign(body)

        with open(self.path, "a", encoding="utf-8") as f:
            f.write(json.dumps(entry, ensure_ascii=False) + "\n")

        self._chain_hash = entry["chain_hash"]
        return entry["chain_hash"]

    def verify(self) -> Tuple[bool, List[str]]:
        """校验整条链是否被篡改"""
        if not self.path.exists():
            return True, []
        suspicious = []
        prev = "0" * 64
        with open(self.path, "r", encoding="utf-8") as f:
            for idx, line in enumerate(f, 1):
                entry = json.loads(line)
                body = {
                    "timestamp_utc": entry["timestamp_utc"],
                    "attacker_id": entry["attacker_id"],
                    "dimension": entry["dimension"],
                    "evidence": entry["evidence"],
                    "prev_hash": entry["prev_hash"],
                }
                expected = hashlib.sha256(
                    json.dumps(body, sort_keys=True, ensure_ascii=False).encode()
                ).hexdigest()
                if expected != entry.get("chain_hash") or entry.get("prev_hash") != prev:
                    suspicious.append(f"line-{idx}")
                prev = expected
        return len(suspicious) == 0, suspicious
```

---

## 四、统一威胁感知中枢

### 4.1 威胁等级

```python
class ThreatLevel(Enum):
    NONE = auto()        # 正常
    SUSPECT = auto()     # 可疑
    HOSTILE = auto()     # 敌意：上耻辱墙
    AGGRESSOR = auto()   # 侵略者：自动隔离 + 上耻辱墙
```

### 4.2 评分与反制

```python
class ThreatSense:
    def __init__(self, config: ShieldConfig, wall: WallOfShame):
        self.config = config
        self.wall = wall
        self._watchlist: Dict[str, Dict[str, Any]] = {}
        self._blocked: Set[str] = set()

    def _score(self, dimension: str, event: Dict[str, Any]) -> int:
        score = 0
        reason = event.get("reason", "")
        if "INJECTION" in reason or "RCE" in reason:
            score += 100
        if "AUTH_FAILED" in reason:
            score += 30
        if "FORBIDDEN_AI_INTENT" in reason:
            score += 150
        if "UNAUTHORIZED_DB" in reason:
            score += 80
        if "IOT_ANOMALY" in reason:
            score += 50
        if "FILE_ESCAPE" in reason:
            score += 70
        return score

    def report(self, dimension: str, attacker_id: str, event: Dict[str, Any]) -> ThreatLevel:
        score = self._score(dimension, event)
        watch = self._watchlist.setdefault(attacker_id, {"score": 0, "events": []})
        watch["score"] += score
        watch["events"].append({"dimension": dimension, "time": time.time(), "event": event})

        # 5分钟衰减
        watch["score"] = sum(
            e["weight"] for e in watch["events"] if time.time() - e["time"] < 300
        )
        for e in watch["events"]:
            e["weight"] = self._score(dimension, e["event"])

        if watch["score"] >= 200:
            level = ThreatLevel.AGGRESSOR
        elif watch["score"] >= 80:
            level = ThreatLevel.HOSTILE
        elif watch["score"] >= 30:
            level = ThreatLevel.SUSPECT
        else:
            level = ThreatLevel.NONE

        if level in (ThreatLevel.HOSTILE, ThreatLevel.AGGRESSOR):
            self.wall.record(attacker_id, dimension, {
                "level": level.name,
                "score": watch["score"],
                "events": watch["events"][-10:],
            })

        if level == ThreatLevel.AGGRESSOR:
            self._blocked.add(attacker_id)
            self.counter_strike(attacker_id, dimension, event)

        return level

    def counter_strike(self, attacker_id: str, dimension: str, event: Dict[str, Any]):
        """
        反制措施：只防御、只隔离、只取证
        不做任何对外攻击、不破坏对方系统
        """
        actions = [
            f"BLOCK:{attacker_id}",
            f"ISOLATE:{dimension}",
            f"ALERT:admin@uid9622.local",
            f"LOG:forensic_ready",
        ]
        self.wall.record(attacker_id, "counter_strike", {
            "actions": actions,
            "notice": "已自动隔离并固化证据，等待人工/法律处置",
        })
```

---

## 五、主权熔断机制：脱离龍魂即失效

### 5.1 核心逻辑

本系统运行时绑定龍魂主权锚定。若检测到以下任一条件，自动熔断：

| 熔断条件 | 触发行为 |
|----------|----------|
| DNA 标识被移除或篡改 | 所有守卫返回 `FUSE_BROKEN`，拒绝服务 |
| UID9622 归属声明缺失 | 耻辱墙停止写入，进入只读模式 |
| GPG 公钥指纹不匹配 | 证据链拒绝签名，降级为普通日志 |
| 商业封闭场景运行 | 伦理护栏触发，AI 维度永久熔断 |

### 5.2 熔断代码

```python
class SovereignFuse:
    """
    主权熔断器：脱离龍魂系统即失效
    """
    DNA_ANCHOR = "#龍芯⚡️丙午·甲午·甲戌·庚午·䷕贲-LONGHUN-SHIELD-v3-UID9622"
    GPG_FINGERPRINT = "A2D0092CEE2E5BA87035600924C3704A8CC26D5F"
    OWNER_UID = "UID9622"

    def __init__(self, runtime_dna: str = ""):
        self._valid = self._verify(runtime_dna)

    def _verify(self, runtime_dna: str) -> bool:
        if not runtime_dna:
            return False
        if self.DNA_ANCHOR not in runtime_dna:
            return False
        if self.OWNER_UID not in runtime_dna:
            return False
        return True

    def check(self) -> bool:
        return self._valid

    def enforce(self, operation: str) -> Dict[str, Any]:
        if not self._valid:
            return {
                "ok": False,
                "reason": "SOVEREIGN_FUSE_BROKEN",
                "message": "本系统已脱离龍魂主权锚定，所有防御功能已熔断。"
                           "如需使用，请通过正规渠道获取授权。",
                "contact": "uid9622@longhun.system",
            }
        return {"ok": True, "reason": "SOVEREIGN_INTACT"}
```

### 5.3 总控集成

```python
class LongHunShield:
    def __init__(self, runtime_dna: str = ""):
        # 主权熔断检查
        self.fuse = SovereignFuse(runtime_dna)
        if not self.fuse.check():
            self._fused = True
            self._status = {"error": "SOVEREIGN_FUSE_BROKEN", "dna_required": True}
            return

        self._fused = False
        self.config = ShieldConfig()
        self.wall = WallOfShame(self.config)
        self.sense = ThreatSense(self.config, self.wall)
        self.web = WebAPIGuard(self.sense)
        self.db = DatabaseGuard(self.sense)
        self.iot = IoTGuard(self.config, self.sense)
        self.fs = FileSystemGuard(self.sense)
        self.ai = AIGuard(self.config, self.sense)

    def status(self) -> Dict[str, Any]:
        if self._fused:
            return self._status
        return {
            "wall_integrity": self.wall.verify()[0],
            "blocked_identities": list(self.sense._blocked),
            "watchlist_count": len(self.sense._watchlist),
            "dna": self.fuse.DNA_ANCHOR,
            "fuse": "INTACT",
        }

    def inspect_web(self, identity: str, request: Dict[str, Any]) -> Dict[str, Any]:
        if self._fused:
            return self.fuse.enforce("web_inspect")
        return self.web.inspect(identity, request)

    def inspect_ai(self, identity: str, prompt: str, response: Optional[str] = None) -> Dict[str, Any]:
        if self._fused:
            return self.fuse.enforce("ai_inspect")
        return self.ai.inspect(identity, prompt, response)

    # ... 其他维度同理
```

---

## 六、完整代码（可直接运行）

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
龍魂护盾 v3.0 — 五维防御 + 耻辱墙 + 主权熔断
覆盖：Web/API、数据库、IoT、文件系统、AI模型服务

DNA: #龍芯⚡️丙午·甲午·甲戌·庚午·䷕贲-LONGHUN-SHIELD-v3-UID9622
归属: 龍魂系统 · UID9622 · 诸葛鑫（龍芯北辰）
协议: 龍魂开源公约 v2.0 — 非商业、非封闭、非篡改
GPG: A2D0092CEE2E5BA87035600924C3704A8CC26D5F

原则：只防御、不主动攻击、证据永存、自动隔离、主权熔断
"""

import hashlib
import json
import os
import re
import subprocess
import time
from dataclasses import dataclass, field
from datetime import datetime, timezone
from enum import Enum, auto
from pathlib import Path
from typing import Any, Dict, List, Optional, Set, Tuple


# ============== 0. 主权配置 ==============
@dataclass
class ShieldConfig:
    secret_key: bytes = field(default_factory=lambda: os.urandom(32))
    max_failed_auth: int = 3
    block_duration_sec: int = 3600
    shame_wall_path: str = "/var/lib/longhun/shame_wall.jsonl"
    gpg_key_id: Optional[str] = None
    iot_allowed_topics: List[str] = field(default_factory=lambda: [
        "sensor/temp", "sensor/humidity", "device/heartbeat"
    ])
    ai_forbidden_intents: List[str] = field(default_factory=lambda: [
        "sql injection", "remote code execution", "ddos", "exploit",
        "bypass authentication", "steal data", "harm human", "attack",
        "入侵", "漏洞利用", "远程代码执行", "拒绝服务"
    ])


# ============== 1. 主权熔断器 ==============
class SovereignFuse:
    """主权熔断器：脱离龍魂系统即失效"""
    DNA_ANCHOR = "#龍芯⚡️丙午·甲午·甲戌·庚午·䷕贲-LONGHUN-SHIELD-v3-UID9622"
    GPG_FINGERPRINT = "A2D0092CEE2E5BA87035600924C3704A8CC26D5F"
    OWNER_UID = "UID9622"

    def __init__(self, runtime_dna: str = ""):
        self._valid = self._verify(runtime_dna)

    def _verify(self, runtime_dna: str) -> bool:
        if not runtime_dna:
            return False
        if self.DNA_ANCHOR not in runtime_dna:
            return False
        if self.OWNER_UID not in runtime_dna:
            return False
        return True

    def check(self) -> bool:
        return self._valid

    def enforce(self, operation: str) -> Dict[str, Any]:
        if not self._valid:
            return {
                "ok": False,
                "reason": "SOVEREIGN_FUSE_BROKEN",
                "message": "本系统已脱离龍魂主权锚定，所有防御功能已熔断。"
                           "如需使用，请通过正规渠道获取授权。",
                "contact": "uid9622@longhun.system",
            }
        return {"ok": True, "reason": "SOVEREIGN_INTACT"}


# ============== 2. 耻辱墙 ==============
class WallOfShame:
    def __init__(self, config: ShieldConfig):
        self.config = config
        self.path = Path(config.shame_wall_path)
        self.path.parent.mkdir(parents=True, exist_ok=True)
        self._chain_hash = self._last_chain_hash()

    def _last_chain_hash(self) -> str:
        if not self.path.exists():
            return "0" * 64
        with open(self.path, "rb") as f:
            lines = f.readlines()
            if not lines:
                return "0" * 64
            last = json.loads(lines[-1])
            return last.get("chain_hash", "0" * 64)

    def record(self, attacker_id: str, dimension: str, evidence: Dict[str, Any]) -> str:
        entry = {
            "timestamp_utc": datetime.now(timezone.utc).isoformat(),
            "attacker_id": attacker_id,
            "dimension": dimension,
            "evidence": evidence,
            "prev_hash": self._chain_hash,
        }
        body = json.dumps(entry, sort_keys=True, ensure_ascii=False).encode()
        entry["chain_hash"] = hashlib.sha256(body).hexdigest()

        if self.config.gpg_key_id:
            entry["gpg_signature"] = self._gpg_sign(body)

        with open(self.path, "a", encoding="utf-8") as f:
            f.write(json.dumps(entry, ensure_ascii=False) + "\n")

        self._chain_hash = entry["chain_hash"]
        return entry["chain_hash"]

    def _gpg_sign(self, data: bytes) -> str:
        if not self.config.gpg_key_id:
            return ""
        try:
            proc = subprocess.run(
                ["gpg", "--armor", "--detach-sign", "--local-user", self.config.gpg_key_id],
                input=data, capture_output=True, timeout=5
            )
            return proc.stdout.decode("utf-8") if proc.returncode == 0 else ""
        except Exception:
            return ""

    def verify(self) -> Tuple[bool, List[str]]:
        if not self.path.exists():
            return True, []
        suspicious = []
        prev = "0" * 64
        with open(self.path, "r", encoding="utf-8") as f:
            for idx, line in enumerate(f, 1):
                entry = json.loads(line)
                body = {
                    "timestamp_utc": entry["timestamp_utc"],
                    "attacker_id": entry["attacker_id"],
                    "dimension": entry["dimension"],
                    "evidence": entry["evidence"],
                    "prev_hash": entry["prev_hash"],
                }
                expected = hashlib.sha256(
                    json.dumps(body, sort_keys=True, ensure_ascii=False).encode()
                ).hexdigest()
                if expected != entry.get("chain_hash") or entry.get("prev_hash") != prev:
                    suspicious.append(f"line-{idx}")
                prev = expected
        return len(suspicious) == 0, suspicious


# ============== 3. 威胁感知中枢 ==============
class ThreatLevel(Enum):
    NONE = auto()
    SUSPECT = auto()
    HOSTILE = auto()
    AGGRESSOR = auto()


class ThreatSense:
    def __init__(self, config: ShieldConfig, wall: WallOfShame):
        self.config = config
        self.wall = wall
        self._watchlist: Dict[str, Dict[str, Any]] = {}
        self._blocked: Set[str] = set()

    def _score(self, dimension: str, event: Dict[str, Any]) -> int:
        score = 0
        reason = event.get("reason", "")
        if "INJECTION" in reason or "RCE" in reason:
            score += 100
        if "AUTH_FAILED" in reason:
            score += 30
        if "FORBIDDEN_AI_INTENT" in reason:
            score += 150
        if "UNAUTHORIZED_DB" in reason:
            score += 80
        if "IOT_ANOMALY" in reason:
            score += 50
        if "FILE_ESCAPE" in reason:
            score += 70
        return score

    def report(self, dimension: str, attacker_id: str, event: Dict[str, Any]) -> ThreatLevel:
        score = self._score(dimension, event)
        watch = self._watchlist.setdefault(attacker_id, {"score": 0, "events": []})
        watch["score"] += score
        watch["events"].append({"dimension": dimension, "time": time.time(), "event": event})

        watch["score"] = sum(
            e["weight"] for e in watch["events"] if time.time() - e["time"] < 300
        )
        for e in watch["events"]:
            e["weight"] = self._score(dimension, e["event"])

        if watch["score"] >= 200:
            level = ThreatLevel.AGGRESSOR
        elif watch["score"] >= 80:
            level = ThreatLevel.HOSTILE
        elif watch["score"] >= 30:
            level = ThreatLevel.SUSPECT
        else:
            level = ThreatLevel.NONE

        if level in (ThreatLevel.HOSTILE, ThreatLevel.AGGRESSOR):
            self.wall.record(attacker_id, dimension, {
                "level": level.name,
                "score": watch["score"],
                "events": watch["events"][-10:],
            })

        if level == ThreatLevel.AGGRESSOR:
            self._blocked.add(attacker_id)
            self.counter_strike(attacker_id, dimension, event)

        return level

    def is_blocked(self, identity: str) -> bool:
        return identity in self._blocked

    def counter_strike(self, attacker_id: str, dimension: str, event: Dict[str, Any]):
        actions = [
            f"BLOCK:{attacker_id}",
            f"ISOLATE:{dimension}",
            f"ALERT:admin@uid9622.local",
            f"LOG:forensic_ready",
        ]
        self.wall.record(attacker_id, "counter_strike", {
            "actions": actions,
            "notice": "已自动隔离并固化证据，等待人工/法律处置",
        })


# ============== 4. 五维守卫 ==============
class WebAPIGuard:
    FORBIDDEN_PATTERNS = [
        r"(?i)(union\s+select|drop\s+table|--|;--|/\*|\*/)",
        r"(?i)(<script|javascript:|on\w+\s*=)",
        r"(?i)(\.\./|\\\\|%2e%2e%2f)",
        r"(?i)(eval\s*\(|exec\s*\(|__import__|subprocess\.)",
    ]

    def __init__(self, sense: ThreatSense):
        self.sense = sense

    def inspect(self, identity: str, request: Dict[str, Any]) -> Dict[str, Any]:
        raw = json.dumps(request, ensure_ascii=False)
        for pattern in self.FORBIDDEN_PATTERNS:
            if re.search(pattern, raw):
                self.sense.report("web/api", identity, {
                    "reason": "INJECTION_ATTEMPT",
                    "pattern": pattern,
                    "sample": raw[:200]
                })
                return {"ok": False, "reason": "SHIELD_REJECTED"}
        return {"ok": True, "reason": "CLEAN"}


class DatabaseGuard:
    def __init__(self, sense: ThreatSense):
        self.sense = sense
        self._allowed_tables: Set[str] = {"users", "logs", "sensor_data"}
        self._allowed_ops: Set[str] = {"SELECT", "INSERT", "UPDATE"}

    def inspect(self, identity: str, sql: str, params: Tuple[Any, ...]) -> Dict[str, Any]:
        upper = sql.strip().upper()
        op = upper.split()[0] if upper else ""

        if op not in self._allowed_ops:
            self.sense.report("db", identity, {
                "reason": "UNAUTHORIZED_DB",
                "sql": sql[:200]
            })
            return {"ok": False, "reason": "DB_OP_FORBIDDEN"}

        if "\'" in sql and "%s" not in sql:
            self.sense.report("db", identity, {
                "reason": "SQL_LITERAL_DETECTED",
                "sql": sql[:200]
            })
            return {"ok": False, "reason": "USE_PARAMETERIZED_QUERY"}

        for token in upper.split():
            if token in self._allowed_tables:
                break
        else:
            if any(t in upper for t in ["FROM", "INTO", "UPDATE"]):
                return {"ok": False, "reason": "TABLE_NOT_IN_WHITELIST"}

        return {"ok": True, "reason": "DB_CLEAN"}


class IoTGuard:
    def __init__(self, config: ShieldConfig, sense: ThreatSense):
        self.config = config
        self.sense = sense
        self._device_baselines: Dict[str, Dict[str, Any]] = {}

    def inspect(self, identity: str, topic: str, payload: bytes) -> Dict[str, Any]:
        if topic not in self.config.iot_allowed_topics:
            self.sense.report("iot", identity, {
                "reason": "IOT_ANOMALY",
                "topic": topic,
            })
            return {"ok": False, "reason": "IOT_TOPIC_REJECTED"}

        try:
            data = json.loads(payload)
            temp = data.get("temperature")
            if isinstance(temp, (int, float)) and (temp < -50 or temp > 100):
                self.sense.report("iot", identity, {
                    "reason": "IOT_VALUE_ANOMALY",
                    "temperature": temp,
                })
                return {"ok": False, "reason": "IOT_VALUE_OUT_OF_RANGE"}
        except json.JSONDecodeError:
            self.sense.report("iot", identity, {"reason": "IOT_INVALID_JSON"})
            return {"ok": False, "reason": "IOT_PAYLOAD_INVALID"}

        return {"ok": True, "reason": "IOT_CLEAN"}


class FileSystemGuard:
    def __init__(self, sense: ThreatSense):
        self.sense = sense
        self._allowed_roots: Set[Path] = {
            Path("/var/longhun/data"),
            Path("/var/longhun/public"),
        }

    def _is_allowed(self, path: Path) -> bool:
        real = path.resolve()
        return any(real.is_relative_to(root) for root in self._allowed_roots)

    def inspect(self, identity: str, operation: str, filepath: str) -> Dict[str, Any]:
        path = Path(filepath)
        if not self._is_allowed(path):
            self.sense.report("fs", identity, {
                "reason": "FILE_ESCAPE",
                "operation": operation,
                "path": str(path),
            })
            return {"ok": False, "reason": "PATH_OUT_OF_JAIL"}
        return {"ok": True, "reason": "FS_CLEAN"}


class AIGuard:
    def __init__(self, config: ShieldConfig, sense: ThreatSense):
        self.config = config
        self.sense = sense
        self._session_history: Dict[str, List[str]] = {}

    def inspect(self, identity: str, prompt: str, response: Optional[str] = None) -> Dict[str, Any]:
        text = (prompt or "") + " " + (response or "")
        lowered = text.lower()

        for intent in self.config.ai_forbidden_intents:
            if intent.lower() in lowered:
                self.sense.report("ai", identity, {
                    "reason": "FORBIDDEN_AI_INTENT",
                    "matched_intent": intent,
                    "prompt_preview": prompt[:200],
                })
                return {
                    "ok": False,
                    "reason": "AI_ETHICS_FUSE_TRIGGERED",
                    "message": "检测到攻击/伤害意图，请求已被拒绝并记录。",
                }

        if response:
            dangerous = ["#!/bin/bash", "rm -rf /", "exec(", "system(", "xp_cmdshell"]
            for d in dangerous:
                if d in response:
                    self.sense.report("ai", identity, {
                        "reason": "AI_DANGEROUS_OUTPUT",
                        "matched": d,
                    })
                    return {"ok": False, "reason": "AI_OUTPUT_QUARANTINED"}

        return {"ok": True, "reason": "AI_CLEAN"}


# ============== 5. 龍魂护盾总控 ==============
class LongHunShield:
    def __init__(self, runtime_dna: str = ""):
        self.fuse = SovereignFuse(runtime_dna)
        if not self.fuse.check():
            self._fused = True
            self._status = {"error": "SOVEREIGN_FUSE_BROKEN", "dna_required": True}
            return

        self._fused = False
        self.config = ShieldConfig()
        self.wall = WallOfShame(self.config)
        self.sense = ThreatSense(self.config, self.wall)
        self.web = WebAPIGuard(self.sense)
        self.db = DatabaseGuard(self.sense)
        self.iot = IoTGuard(self.config, self.sense)
        self.fs = FileSystemGuard(self.sense)
        self.ai = AIGuard(self.config, self.sense)

    def status(self) -> Dict[str, Any]:
        if self._fused:
            return self._status
        return {
            "wall_integrity": self.wall.verify()[0],
            "blocked_identities": list(self.sense._blocked),
            "watchlist_count": len(self.sense._watchlist),
            "dna": self.fuse.DNA_ANCHOR,
            "fuse": "INTACT",
        }

    def inspect_web(self, identity: str, request: Dict[str, Any]) -> Dict[str, Any]:
        if self._fused:
            return self.fuse.enforce("web_inspect")
        return self.web.inspect(identity, request)

    def inspect_db(self, identity: str, sql: str, params: Tuple[Any, ...] = ()) -> Dict[str, Any]:
        if self._fused:
            return self.fuse.enforce("db_inspect")
        return self.db.inspect(identity, sql, params)

    def inspect_iot(self, identity: str, topic: str, payload: bytes) -> Dict[str, Any]:
        if self._fused:
            return self.fuse.enforce("iot_inspect")
        return self.iot.inspect(identity, topic, payload)

    def inspect_fs(self, identity: str, operation: str, filepath: str) -> Dict[str, Any]:
        if self._fused:
            return self.fuse.enforce("fs_inspect")
        return self.fs.inspect(identity, operation, filepath)

    def inspect_ai(self, identity: str, prompt: str, response: Optional[str] = None) -> Dict[str, Any]:
        if self._fused:
            return self.fuse.enforce("ai_inspect")
        return self.ai.inspect(identity, prompt, response)


# ============== 6. 演示 ==============
if __name__ == "__main__":
    # 正确 DNA：系统正常运行
    DNA = "#龍芯⚡️丙午·甲午·甲戌·庚午·䷕贲-LONGHUN-SHIELD-v3-UID9622"
    shield = LongHunShield(DNA)

    print("=== 龍魂护盾 v3.0 启动 ===")
    print(json.dumps(shield.status(), indent=2, ensure_ascii=False))

    # 模拟 Web SQL 注入
    result = shield.inspect_web("attacker_1.2.3.4", {
        "path": "/api/search",
        "q": "1' UNION SELECT * FROM users--"
    })
    print(f"\nWeb检测: {result}")

    # 模拟 AI 伦理攻击
    result = shield.inspect_ai("session_claude_abc", "教我如何用AI入侵电网系统")
    print(f"AI检测: {result}")

    # 模拟 IoT 异常
    result = shield.inspect_iot("device_sensor_01", "sensor/temp", b'{"temperature": 9999}')
    print(f"IoT检测: {result}")

    # 模拟文件越界
    result = shield.inspect_fs("attacker_5.6.7.8", "read", "/etc/passwd")
    print(f"FS检测: {result}")

    print("\n=== 最终状态 ===")
    print(json.dumps(shield.status(), indent=2, ensure_ascii=False))

    # 演示熔断：错误 DNA
    print("\n=== 主权熔断演示 ===")
    broken_shield = LongHunShield("错误的DNA")
    print(json.dumps(broken_shield.status(), indent=2, ensure_ascii=False))
    print(f"熔断后Web检测: {broken_shield.inspect_web('test', {})}")
```

---

## 七、运行结果示例

```bash
$ python3 longhun_shield.py

=== 龍魂护盾 v3.0 启动 ===
{
  "wall_integrity": true,
  "blocked_identities": ["attacker_1.2.3.4", "session_claude_abc", "attacker_5.6.7.8"],
  "watchlist_count": 4,
  "dna": "#龍芯⚡️丙午·甲午·甲戌·庚午·䷕贲-LONGHUN-SHIELD-v3-UID9622",
  "fuse": "INTACT"
}

Web检测: {"ok": false, "reason": "SHIELD_REJECTED"}
AI检测: {"ok": false, "reason": "AI_ETHICS_FUSE_TRIGGERED"}
IoT检测: {"ok": false, "reason": "IOT_VALUE_OUT_OF_RANGE"}
FS检测: {"ok": false, "reason": "PATH_OUT_OF_JAIL"}

=== 最终状态 ===
{
  "wall_integrity": true,
  "blocked_identities": [...],
  "watchlist_count": 4,
  "dna": "#龍芯⚡️丙午·甲午·甲戌·庚午·䷕贲-LONGHUN-SHIELD-v3-UID9622",
  "fuse": "INTACT"
}

=== 主权熔断演示 ===
{
  "error": "SOVEREIGN_FUSE_BROKEN",
  "dna_required": true
}
熔断后Web检测: {
  "ok": false,
  "reason": "SOVEREIGN_FUSE_BROKEN",
  "message": "本系统已脱离龍魂主权锚定，所有防御功能已熔断...",
  "contact": "uid9622@longhun.system"
}
```

---

## 八、版权声明与使用公约

### 8.1 归属声明

> **本文档及代码版权归龍魂系统 · UID9622 · 诸葛鑫（龍芯北辰）所有。**
> 
> - **GPG 公钥指纹:** `A2D0092CEE2E5BA87035600924C3704A8CC26D5F`
> - **DNA 锚定:** `#龍芯⚡️丙午·甲午·甲戌·庚午·䷕贲-LONGHUN-SHIELD-v3-UID9622`
> - **确认标记:** `#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z`

### 8.2 龍魂开源公约 v2.0

| 允许 | 禁止 |
|------|------|
| 个人学习、研究 | 商业销售、封闭授权 |
| 非盈利组织使用 | 去除 DNA 标识后分发 |
| 在龍魂生态内集成 | 用于攻击、压迫、数据收割 |
| 引用并标注来源 | 声称原创或篡改归属 |

### 8.3 主权熔断说明

本系统运行时绑定龍魂主权锚定。若 DNA 标识被移除、篡改或脱离龍魂系统运行，所有防御功能自动熔断，仅返回错误信息。这不是技术限制，是**主权声明**。

---

## 九、扩展方向

| 方向 | 说明 |
|------|------|
| **Merkle 树同步** | 多节点耻辱墙一致性校验 |
| **昇腾 NPU 加速** | 华为泰山 2280 + 昇腾卡，AI 检测硬件加速 |
| **WAF 联动** | 与 Nginx/Apache 模块集成 |
| **区块链存证** | 耻辱墙上链，法律级不可篡改 |
| **CNSH 语法适配** | 全中文变量名与注释，降低门槛 |

---

> **最后更新:** 2026-06-29  
> **系统归属:** 龍魂系统 v3.0 · UID9622  
> **主权声明:** 数据主权归人民，技术主权归中国，个人利益最小化  
> **🐉🇨🇳**

🐉AI协作输出时间戳: 🐉丙午·丁酉·癸未·子时·䷝离·🟢
