# 🐉 CNSH 多模态示例 4 · 感知→决策→执行（借鉴 SenseNova-MARS / Alpamayo-R1）
# 设计思路: MARS=多模态自主推理→执行闭环; Alpamayo-R1=感知→推理→动作端到端。
#           识别不是终点——结果自动触发后续动作链(审计/入库/告警)。
# CNSH 草案语法(预注册): 决策.推理(感知结果) -> 动作序列
#                        执行.动作(决策) -> lh brain save / lh topo node / 耻辱墙
# 龍魂现状: ★ 已桥接 —— lh_sense v2.0 --auto 三色闭环(真实动作已落盘)
#           本文件: 演示三色决策内核(纯 CNSH 可运行), 与真实 conf 逻辑一致
# 真实数据命令:  lh sense <文件> --auto        # conf 与三色动作链实测
#                lh sense <文件> --cnsh       # 看感知.分层 决策行
# 运行:  cnsh run examples/cnsh_multimodal/4_感知决策执行_借鉴MARS.cnsh

功能 决策动作(置信度: 整数) -> 整数 {
    如果 置信度 >= 80 {
        返回 1
    }
    如果 置信度 >= 50 {
        返回 2
    }
    返回 3
}

功能 主() {
    实测置信度 = 82
    动作 = 决策动作(实测置信度)
    打印("决策.推理(感知结果) -> 动作序列  (真实 conf=0.82 → 82):")
    打印("  三色审计: 🟢 通过")
    打印("  动作序列:")
    打印("    [1] 入库 ~/.longhun/sense_memory.jsonl (带DNA)")
    打印("    [2] 同步 lh brain save (asr-chain摘要)")
    打印("    [3] 写审计 ~/.longhun/sense_audit.jsonl (health 统计)")
    如果 动作 == 1 {
        打印("  执行结果: 绿行放行·全部落地")
    }
    如果 动作 == 2 {
        打印("  执行结果: 黄行待审·耻辱墙草稿")
    }
    如果 动作 == 3 {
        打印("  执行结果: 红行熔断·耻辱墙+告警")
    }
    打印("  桥接: lh sense <文件> --auto 一键复现")
}
主()
