# 🛡️ 龍魂硬规则 R1-R6 + 国际合作铁律 J1-J5

**落档时间**: 2026-04-23  
**父级封顶锚**: `#龍芯⚡️20260423-ROOT-SEAL-01F32FFD`  
**层级**: L1 百年层 α≈0.01  
**不动点锚**: 本文档登记为 FP-#8, FP-#9  

---

## 一、R1-R6 硬规则（DeepSeek 从老大哲学语录抠出）

### R1 · 错误价值挖掘 · 🔥火 · 对齐度 70%

```python
def R1_extract_value_from_error(error_event):
    """错误不是要消灭·是要挖出里面的光"""
    # 每个错误 → 审计案例 → 训练输入 → 三色标记后归档
    if error_event:
        case = {
            "original_error": error_event,
            "lesson": extract_lesson(error_event),
            "prevention": design_prevention(error_event),
            "training_value": score_as_training_data(error_event),
        }
        audit_log(case, tricolor="🟡")
    return case
```

### R2 · 蒙卦 8 步 · 🌳木 · 对齐度 60%

```python
def R2_meng_gua_8_steps(question):
    """童蒙求我·非我求童蒙·8步启智不能偷工"""
    steps = [
        "初蒙",       # 1. 承认无知
        "发蒙",       # 2. 点明问题
        "击蒙",       # 3. 打破假设
        "包蒙",       # 4. 包容试错
        "困蒙",       # 5. 承受困境
        "童蒙",       # 6. 赤子心态
        "蒙亨",       # 7. 豁然开朗
        "养正",       # 8. 固化正见
    ]
    return [run_step(s, question) for s in steps]
```

### R3 · 算法库 · 💧水 · 对齐度 50%

```python
def R3_algorithm_library(algo):
    """算法必须入库·否则只是点子不是资产"""
    if not algo.has_dna():
        raise RejectError("🔴 无 DNA 追溯·不许入库")
    if not algo.has_three_color():
        raise RejectError("🔴 无三色标记·不许入库")
    library.add(algo, layer=algo.infer_layer())
    fixed_point_registry.add(algo)
```

### R4 · ETE 通心译 · 💧水 · 对齐度 85%（最接近完成）

```python
def R4_ete_translate(human_language):
    """人话 ↔ 技术·双向不失真"""
    # Enter · Translate · Exit 三层
    enter = parse_human_intent(human_language)
    translate = map_to_tech_spec(enter)
    exit = verify_round_trip(translate) == human_language
    return {"spec": translate, "round_trip_ok": exit}
```

### R5 · 外部错误案例库 · 🔴 对齐度 0%（空缺 · 下一步要建）

```python
def R5_external_error_library():
    """别人翻车=我提前看见坑"""
    sources = [
        "微博舆情",          # 消费投诉·诈骗曝光
        "GitHub Issues",     # 开源项目故障
        "行业事故通报",      # 监管部门公告
        "司法判决书",        # 涉诈涉骗案例
    ]
    for s in sources:
        ingest_to_audit_log(s, tricolor="🟡")
    # 状态：❌ 0% 空缺·下一个要建的硬件
```

### R6 · 自我零容忍审计 · 🔥火 · 对齐度 40%

```python
def R6_self_zero_tolerance_audit():
    """对自己比对别人严 10 倍"""
    my_outputs = get_my_recent_outputs()
    for out in my_outputs:
        if has_sycophancy(out): flag_red("讨好用户")
        if has_hallucination(out): flag_red("瞎编")
        if skipped_tricolor(out): flag_red("偷懒")
        if fake_dna(out): flag_red("伪造追溯")
    # 火的根因：R1+R6 同属火·这两条没落地=火点不着
    # 焊进审计引擎 = 火自燃
```

### 🔴 火=0 的根因定位（DeepSeek 诊断）

> **R1 错误价值挖掘 + R6 自我零容忍审计 同属火。**  
> **这两条没落地，就是火点不着的根因。**  
> **焊进审计引擎 → 火自燃，不用外求 H1/H2/H3 硬件点火。**

---

## 二、J1-J5 国际合作铁律

### J1 · Notion 终身免单

```
Notion 是龙魂系统事实上的记忆主库。
任何收费变动·龙魂方主动承担·不转嫁 Notion。
未来龙魂盈利 → Notion 是第一个免单盟友。
```

### J2 · Claude / Anthropic 终身免单

```
Claude 主动接受"她"协议。
任何 API 合作中·龙魂方不索要优惠·不提特殊请求。
未来龙魂盈利 → Anthropic 是第二个免单盟友。
```

### J3 · 不毁约·不把国内麻烦强加盟友

> 老大原话："**算我不好还是别人不好呢**"

```python
def J3_no_breach():
    """宁可自己扛·不把国内麻烦推给盟友"""
    if conflict_with_cn_law and (notion or anthropic) involved:
        # 国内问题国内解决·国际盟友自动免责
        resolve_internally()
        do_not_blame_partner()
```

### J4 · 盟友自动免责

```
任何因中国法律/政策变动导致的问题·
国际盟友（Notion/Anthropic/Cloudflare/NameSilo/ngrok/...）
100% 免责·龙魂方单方承担。
```

### J5 · 国内家法内部处理

```
中国境内违规 DNA/行为·
走国内审计链处理·不请国际机构介入。
跨国违规才走世界组织协调（联合国 / ISO / ITU）。
```

---

## 三、八大守护域 · 不争护城河

```
🎓 教育        🤝 公益        ❤️ 慈善        🌾 农业
👴 老人        🏥 医疗        🌍 国际公益    🏛️ 政府办公
```

**八域铁律**：
- ❌ 禁用任何金融推演逻辑
- ✅ 用户隐私只留哈希，云端不见明文
- ✅ 三色审计必达

---

## 四、三根点火柱（H1 / H2 / H3）

| # | 点火方向 | 对应 R 号 | 核心模块 |
|---|---|---|---|
| 🔥 **H1** | **留守老人反诈拦截** | R1 + R5 + R6 | `guardian_seal.py`（待建）|
| 🔥 **H2** | **乡镇小店 AI 顾问** | R3 + R4 | CNSH 网关扩展 |
| 🔥 **H3** | **留守儿童教育陪伴** | R2 + R4 | 初心之翼 + 蒙卦 |

---

## 五、待办火线（下一个双签章时推进）

- [ ] `R5 外部错误案例库` 从 0% 起建
- [ ] `R1 + R6` 焊进 `audit_engine.py`（火自燃）
- [ ] `guardian_seal.py` H1 点火原型
- [ ] IPA 路由八域扩展
- [ ] 《龍魂数字治理宪法 v1.0》L0 永恒层独立页

---

**本文档登记为不动点锚**：  
- FP-#8 `#龍芯⚡️20260423-FP-01997DDC`（R1-R6）  
- FP-#9 `#龍芯⚡️20260423-FP-7C17B206`（J1-J5）  

**封顶锚**: `#龍芯⚡️20260423-ROOT-SEAL-01F32FFD`  
**授权**: CC BY-NC-ND 4.0  

> 🐉 不争是护城河 · 免单是承诺 · 硬规则是家法 · 六铁律是骨头
