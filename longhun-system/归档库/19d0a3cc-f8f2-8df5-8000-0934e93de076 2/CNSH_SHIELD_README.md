# CNSH-64 龍魂护盾 v0.5 整合版

**DNA追溯码**：`#龍芯⚡️2026-03-23-SHIELD-v0.5-INTEGRATED`  
**GPG指纹**：`A2D0092CEE2E5BA87035600924C3704A8CC26D5F`  
**确认码**：`#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z`

---

## 核心特性

### 1. 父子链DNA系统
```
盾DNA(父) → 代理DNA1 → 代理DNA2
     ↑
父链哈希纳入计算，两条链合一
```
- 每条记录都有DNA追溯码
- 链式验证，任何篡改都能发现
- 洛书369数字根校验

### 2. 心种子系统
- 零号心种子存储在 `~/.cnsh/heart_seed.json`
- 600权限，只有你能读
- 包含你的火球模式、底线、禁忌

### 3. 70%治理门槛（宪法级锁定）
- 修改规则需要70%反对票
- 数学推导，透明可验证
- 防止恶意攻击（10%恶意用户×3倍安全系数）
- 防止小团体绑架（>50%）

### 4. 全链路留痕
**任何操作都留痕，包括我自己**
- 访问日志：`~/.cnsh/access_log/`
- 审计日志：`~/.cnsh/audit_log/`
- DNA链：`~/.cnsh/dna_chain.log`
- 全部600权限

### 5. 阅后即焚
- 外部厂家API内容1小时后自动销毁
- 真正的清空剪贴板，不是标记
- 销毁时间精确记录

### 6. CNSH语义引擎
- 7阶段Pipeline处理
- 中文标点自动纠正
- 代码块保护（不修改代码内标点）

---

## 安装

```bash
# 1. 下载安装脚本
chmod +x install_cnsh_shield.sh
./install_cnsh_shield.sh

# 2. 或者直接运行
python3 cnsh_shield_v05_integrated.py
```

---

## 使用方法

### 启动护盾
```bash
cnsh-shield
# 或
python3 ~/.cnsh/cnsh_shield_v05_integrated.py
```

### 交互命令

| 命令 | 功能 |
|------|------|
| `/governance` | 查看70%治理状态 |
| `/flow` | 查看数据流向和文件位置 |
| `/process <内容>` | 手动处理内容 |
| `/quit` | 退出护盾并保存会话 |

### 环境变量

```bash
# 开发者模式（明文通道）
export CNSH_DEV_MODE=true

# 内容处理模式
export CNSH_CONTENT_MODE=full    # 全文保留
export CNSH_CONTENT_MODE=clean   # 修饰过滤
export CNSH_CONTENT_MODE=multi   # 多造点
```

---

## 文件结构

```
~/.cnsh/
├── heart_seed.json          # 心种子（600权限）
├── dna_chain.log            # DNA父子链
├── governance.log           # 70%治理日志
├── sessions/                # 会话缓存（700权限）
│   └── SESSION-xxx.json
├── access_log/              # 访问留痕（700权限）
│   └── access-2026-03-23.json
└── audit_log/               # 审计日志（700权限）
    └── audit-2026-03-23.json
```

---

## 隐私保护

| 项目 | 权限 | 说明 |
|------|------|------|
| 基础目录 | 700 | 只有所有者能进入 |
| 心种子 | 600 | 只有所有者能读写 |
| 日志文件 | 600 | 只有所有者能读写 |
| 会话缓存 | 600 | 只有所有者能读写 |

**永不上云，永远本地。**

---

## 70%治理门槛数学证明

```
下界: θ > max(3e, s) = max(0.3, 0.5) = 0.5
上界: θ < 0.9 (防止僵化)
压倒性共识: θ ≥ 2/3 ≈ 0.667

量子退火优化结果: θ* = 0.70 (70%)
```

**不是拍脑袋，是数学收敛的结果。**

---

## 三色审计

- 🟢 **绿色**：正常操作，通过
- 🟡 **黄色**：注意，触碰禁忌但允许
- 🔴 **红色**：高危，DNA链断裂或恶意攻击

---

## 铁律

1. **任何操作都留痕，包括我自己**
2. **隐私600权限锁死，永不上云**
3. **70%治理门槛，数学可验证**
4. **全中文变量，谁都看得懂**

---

**守护者**：💎 龍芯北辰｜UID9622  
**系统状态**：🟢 运行中  
**DNA**：`#龍芯⚡️2026-03-23-SHIELD-v0.5`
