---
description: Behavioral Cryptography full paper — single canonical manuscript (FULL_PAPER_v1.0_Body_Draft.md) with UID9622 CONFIRM/SEAL lock.
globs: "**/BehavCrypto_v1.0/**/*.md"
alwaysApply: true
---

# BehavCrypto · Canonical full paper (locked)

When editing any file under `BehavCrypto_v1.0/` (Markdown):

1. **Single source of truth for full manuscript text:** `FULL_PAPER_v1.0_Body_Draft.md` only. This path is fixed unless **UID9622 personally edits `CANONICAL_LOCK.md`** to change the definition—do not promote any other file to “master.”
2. **`*Clean*` / cleaning drafts:** Files such as `FULL_PAPER_v1.0_Body_Draft_Clean.md` (or similar) are **scratch for scrubbing/formatting only**. When cleaning is done, **merge the final content back into** `FULL_PAPER_v1.0_Body_Draft.md`. Never treat Clean as the long-term canonical file in the repo.
3. **Do not** replace or “sync from” `~/Downloads/`, `~/Documents/`, or other offline copies as authority. If the user supplies an external snippet, merge it **into** `FULL_PAPER_v1.0_Body_Draft.md` via explicit edit, preserving existing audited fixes (WGM wording, §1.2, Appendix A pseudocode, etc.).
4. **`FULL_PAPER_v1.0_TOC.md`:** regenerate or align section tree **from** the body draft; do not invent chapter structure that contradicts the body draft.
5. **Lock tokens** (must remain consistent with `CANONICAL_LOCK.md` and paper front matter):  
   `#CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z#ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL`

After editing `FULL_PAPER_v1.0_Body_Draft.md`, ensure `BehavCrypto_v1.0/CANONICAL_SHA256` matches: from package root run `bash scripts/canonical-sha256/update.sh` and stage the body + checksum (or rely on pre-commit — see `CANONICAL_LOCK.md` for `core.hooksPath`: `scripts/githooks` vs `longhun-system/scripts/githooks`).

Read `BehavCrypto_v1.0/CANONICAL_LOCK.md` before large diffs to this bundle.
