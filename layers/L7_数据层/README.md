# L7_数据层

> 数据表达层 · 公开内容、报告、可视化输出

## 职责

可公开/半公开的数据产物：报告、可视化、发布内容。

## 关联模块

- `public-content/`
- `outputs/`
- `output/`

## 本层入口

- `public-content/` → 符号链接到 `../public-content/`
- `outputs/` → 符号链接到 `../outputs/`
- `output/` → 符号链接到 `../output/`

---

> 本文件为 L 层结构索引，由 Kimi 整理生成。
> 符号链接仅作为视图入口，不移动原始文件。
