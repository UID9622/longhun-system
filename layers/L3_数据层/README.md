# L3_数据层

> 数据层 · 本地数据中台、训练数据、运行时数据

## 职责

数据采集、清洗、存储、训练池。

## 关联模块

- `data-hub/`
- `data/`
- `container_data/`

## 本层入口

- `data-hub/` → 符号链接到 `../data-hub/`
- `data/` → 符号链接到 `../data/`
- `container_data/` → 符号链接到 `../container_data/`

---

> 本文件为 L 层结构索引，由 Kimi 整理生成。
> 符号链接仅作为视图入口，不移动原始文件。
