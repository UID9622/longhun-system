# DNA: #龍芯⚡️丙午·丙申·庚戌·䷙大畜-SCRIPT-MANAGER-v1.2-UID9622
# CONFIRM: #CONFIRM🌌9622-ONLY-ONCE🧬LK9X-772Z
# SEAL: #ZHUGEXIN⚡️2025-🇨🇳🐉⚖️♠️🧚🏼‍♀️❤️♾️-DEVICE-BIND-SOUL
# 🐉 龍魂记错本 · 错误归档

> DNA: `#龍芯⚡️2026-07-06-ERRATA-v1.0-CREATE-F8A3B1C2`
> 原则：同样的错只记一次，记了就永不再犯。
> 每次修复后归类录入，归入对应类别。

---

## 📂 分类索引

| 编号 | 类别 | 说明 |
|:---:|------|------|
| CSS | CSS 兼容性 | 前缀、浏览器兼容、属性缺失 |
| JS-SYNTAX | JavaScript 语法 | 缺括号、拼写、箭头函数 |
| HTML-REF | 资源引用 | 路径错误、文件缺失 |
| DEPLOY | 部署/启动 | launchd、进程管理、端口 |
| TYPE | 类型检查 | basedpyright ERROR/WARNING |
| PROTO | 协议违规 | 焊死规则未遵守、铁律遗漏 |

---

## 已修复记录

### PROTO-001 | 2026-07-08 | DNA 格式用错（v1.0 格里历而非 v∞ 干支卦）

**症状**：AI 生成 DNA `#龍芯⚡️2026-07-08-EXTERNAL-AI-AUDIT-XIAOYI-CB468229`，用了 v1.0 格里历格式，而非 v∞ 干支卦格式。

**根因**：
1. `AGENTS.md` 第 64 行和第 120 行的 DNA 格式定义仍是 v1.0 旧版 `YYYY-MM-DD-模块-动作-哈希8位`
2. `01_技能庫/dna-gen.md` 在 2026-07-08 已升级到 v2.1（推荐 v∞ 干支卦格式），但 AGENTS.md 未同步更新
3. AI 每次读取 AGENTS.md 作为操作规范，看到的就是旧格式，自然不会用新版

**深层根因**：技能文件升级了，但 AI 操作手册没同步。**文档不同步 = 焊死失效**。

**修复方案**：
1. `AGENTS.md` 铁律 #3 更新为 v∞ 格式：`#龍芯⚡️<年干支>·<月干支>·<日干支>·<时辰>·<卦名>-<模块>-<动作>-<哈希8位>`
2. `AGENTS.md` 新增铁律 #6：所有时间戳必须使用农历干支四柱，禁止公历
3. `AGENTS.md` 新增锚点 A-014：农历干支时间戳焊死
4. `AGENTS.md` 操作前自检新增第 9 条：DNA 格式和时间戳格式验证
5. `AGENTS.md` CNSH 规范中 DNA 格式更新为 v∞

**DNA 修复链**：`#龍芯⚡️丙午·丙申·癸丑·午时·需-AGENTS-DNA-FORMAT-LOCKDOWN-BF8BA356`

**改前自查清单**（每次生成 DNA 前必过）：
- [ ] DNA 是 v∞ 干支卦格式还是 v1.0 格里历格式？v1.0 = 熔断
- [ ] 时间戳是农历干支四柱还是公历日期？公历 = 熔断
- [ ] 干支是否通过 `calendar-context-logger/calendar_core.py` 获取？自算 = 熔断
- [ ] 哈希是否通过 `bin/hetu_luoshu_dna.py` 生成？手写 = 熔断
- [ ] **AGENTS.md 中所有相关定义是否与技能文件一致？不同步 = 隐患**

### PROTO-002 | 2026-07-08 | 农历时间戳未焊死到 AGENTS.md

**症状**：AI 使用公历日期 `2026-07-08` 作为时间戳，而非农历干支四柱 `丙午·丙申·癸丑·午时`。

**根因**：`calendar-context-logger/calendar_core.py` 已实现完整的天干地支引擎，但 AGENTS.md 中从未强制要求使用农历时间戳。AI 没有"必须用农历"的焊死指令。

**修复方案**：
1. 新增铁律 #6：所有时间戳必须使用农历干支四柱
2. 新增锚点 A-014：农历干支时间戳
3. 操作前自检新增第 9 条

**改前自查清单**：
- [ ] 任何输出时间戳的地方，是农历干支还是公历？公历 = 立即修正

---

### 联动防护：PROTO 类别自动化检查

> 记错本的每一条，必须对应一条自动化检查。

针对 PROTO-001/002，新增以下自动化规则：

1. **`lh health` 升级**：检查 AGENTS.md 中的 DNA 格式定义是否与 `01_技能庫/dna-gen.md` 一致
2. **DNA 格式验证脚本**：`bin/lh_dna_format_check.py` — 扫描项目中所有 DNA，标记仍在使用 v1.0 格式的
3. **pre-commit 钩子**：提交前检查新增 DNA 是否为 v∞ 格式

### JS-SYNTAX-001 | 2026-07-06 | `escHtml()` 缺收口 `)`

**症状**：20 个 JS 语法错误集中在 `web/symbiote-dashboard.html`

**根因**：
1. `escHtml(\`...\`` 模板字面量开头写 `escHtml(\`)` ，末尾只写 `` ` `` 忘了 `)`——**缺了关闭 `escHtml(` 的 `)`**
2. 箭头函数 `n => {)` —— `{` 和 `)` 挤在同一行，解析器把 `{` 当空块体处理，后面的 `return` 掉到全局产生语法错误
3. `.join(''));` 缺少 `escHtml(` 的关闭 `)`

**根本模式**：多层嵌套函数调用时，忘记收外层函数的口。

**修复方案**：
- 所有 `escHtml(arr.map(fn).join('')) ` 拆为两行：先算结果，再传 `escHtml(result)`
- 模板字面量 `escHtml(\`...\`)` 确保末尾有 `)`

**影响文件**：`web/symbiote-dashboard.html`（6 处函数）

**DNA 修复链**：`#龍芯⚡️2026-07-06-SYMBIOTE-JS-SYNTAX-FIX-ZERO-E1F9C3D2`

**改前自查清单**（下次写相同代码必过）：
- [ ] `escHtml(` 的 `)` 收了吗？
- [ ] 箭头函数 `{` 和 `)` 是不是分在不同行？
- [ ] 嵌套调用是不是拆成两步更清晰？

---

### CSS-001 | 2026-07-06 | `background-clip` 缺标准属性

**症状**：44 个文件出现 `vendorPrefix` 警告

**根因**：写了 `-webkit-background-clip: text` 没写标准属性 `background-clip: text`

**修复方案**：全项目扫描，每处补一行 `background-clip: text;`

**DNA 修复链**：`#龍芯⚡️2026-07-06-CSS-BGCLIP-FIX-GLOBAL-44FILES`

**改前自查清单**：
- [ ] 加了 `-webkit-` 前缀就必须跟标准属性

---

## 统计

| 类别 | 次数 | 最近 |
|------|:---:|------|
| JS-SYNTAX | 1 | 2026-07-06 |
| CSS | 1 | 2026-07-06 |
| HTML-REF | 0 | — |
| DEPLOY | 0 | — |
| TYPE | 0 | — |

---

### JS-SYNTAX-002 | 2026-07-06 | `escHtml()` 同类错误蔓延至 `longhun_hub.html`

**症状**：`longhun_hub.html`（dashboard + portal 两副本）出现 11 个同类 JS 语法错误。

**根因**：与 JS-SYNTAX-001 **完全相同**的书写模式——`escHtml(\`)` 没收 `)`、`escHtml(arr.map(fn => {) ...).join('')` 嵌套混乱。

**追问：为什么记了错还是再犯？**

| 深层根因 | 说明 |
|------|------|
| **记错本没有变成自动检查** | 记了错、写了自查清单，但人不会每次写代码都翻记错本 |
| **lint 规则只在 IDE 里生效** | 没有 `pre-commit` 钩子、没有 CI 检查、`lh health` 的检查只覆盖了 symbiote-dashboard 一个文件 |
| **两个副本没有自动同步** | `dashboard/web/p0-controls/longhun_hub.html` 和 `portal/portal/p0-controls/longhun_hub.html` 是手动 cp 同步的，改一个另一个漏了 |
| **`escHtml` API 设计本身容易出错** | 函数签名 `escHtml(str)` 接收一个参数，但写法习惯 `escHtml(\`...\`)` 让人以为模板字面量末尾就是结束——实际上还需要关 `escHtml(` 的 `)` |

**根本解决方案**（不止于"下次注意"）：

1. **`lh health` 升级**：从只扫 symbiote-dashboard 扩展到全项目所有 HTML
2. **`pre-commit` 钩子**：提交前自动跑 JS 语法检查（已有 `.pre-commit-config.yaml`，加一条 eslint/html-validate）
3. **副本自动同步**：建立符号链接或用脚本自动同步
4. **API 层防御**：在 `lh-utils.js` 里加一个 `safeHTML(el, templateFn)` 包装函数，自动处理 `escHtml` 的收口

**影响文件**：
- `L5_服务层/services/dashboard/web/p0-controls/longhun_hub.html`（8 处）
- `L5_服务层/services/portal/portal/p0-controls/longhun_hub.html`（同步修复）

**DNA 修复链**：`#龍芯⚡️2026-07-06-LONGHUN-HUB-JS-SYNTAX-FIX-11-E2A7D4F1`

**改前自查清单**（升级版 — 强制自动化）：
- [ ] `escHtml(` 的 `)` 收了吗？
- [ ] 箭头函数 `{` 和 `)` 是不是分在不同行？
- [ ] 嵌套调用是不是拆成两步更清晰？
- [ ] **🆕 `lh health` 跑了没？全项目扫描过了没？**
- [ ] **🆕 两个副本同步了没？**

---

## 🔬 追本溯源 · 为什么一再犯错

> 写于 2026-07-06，在第二次修复同一类错误后。

### 错误链追溯

```
AI 生成代码
  └→ 使用了 escHtml(`...`) 模式（看起来像写对了）
      └→ 模板字面量末尾写 ` 后，忘了 escHtml( 还需要 )
          └→ 浏览器解析器不报错（HTML 容错），但 JS 引擎报语法错
              └→ lint 报了 20 个错，修好了 symbiote-dashboard
                  └→ 但同样的模式在 longhun_hub.html 里也存在（没扫到）
                      └→ 又报了 11 个错 ← 就是现在
```

### 五问法追根

1. **为什么 escHtml 没收口？** → 因为多层嵌套时视觉上难以追踪括号配对
2. **为什么记了错还是再犯？** → 因为记错本是被动文档，不会主动阻止错误
3. **为什么第一次修复时没扫全项目？** → 因为 `lh health` 只写了 symbiote-dashboard 的专项检查
4. **为什么没写全项目扫描？** → 因为没有意识到同样的错误模式会在多个文件里复制粘贴
5. **为什么会有复制粘贴？** → 因为 dashboard 的 HTML 文件有多个副本（portal/dashboard 两套），而系统没有副本同步机制

### 根因（根本的根本）

**「记错」不等于「防错」**。记错本是给人类看的，但人类（包括 AI）不会在每次写代码前翻记错本。必须把记错本的内容**编译成自动检查规则**。

### 解决原则（铁律更新）

| 旧规则 | 新规则 |
|------|------|
| 记错本只归档 | 记错本每一条必须对应一条自动化检查 |
| 修复一个文件 | 修复后全项目扫描同类模式 |
| 手动同步副本 | 建立自动同步机制或去副本化 |

---

## 统计

| 类别 | 次数 | 最近 |
|------|:---:|------|
| JS-SYNTAX | **2** | 2026-07-06 |
| CSS | 1 | 2026-07-06 |
| HTML-REF | 0 | — |
| DEPLOY | 0 | — |
| TYPE | 0 | — |
| PROTO | **2** | 2026-07-08 |

---
*此记错本永续更新。每次修复后归类录入，D=A×T（积累 = 每次×永不重复）。*
*铁律：记错本的每一条，必须对应一条自动化检查。*
