# UID9622 完整系统使用指南

## 系统架构

### 核心组件
1. **uid9622_healthcheck_optimized.py** - 健康检查器
2. **uid9622_control_plane.py** - 总控台（24小时强制更新决策）
3. **uid9622_updater.py** - 事件驱动的更新器
4. **uid9622_updater_v3_folder.py** - 三源备份更新器
5. **uid9622_backup_push.py** - 备份推送器
6. **instance_meta.json** - 实例元数据
7. **integrated_health_workflow.sh** - 一键健康检查工作流

## 快速开始

### 1. 基础配置

编辑 `instance_meta.json`：
```json
{
  "instance_id": "instance-001",
  "mother_version": "PUBLIC-LANDING-CONSTITUTION-V1.1",
  "last_sync_time": "2025-12-24T01:00:00+08:00",
  "auto_update": true
}
```

### 2. 目录结构创建

```bash
mkdir -p events mother_applied snapshots backup_repo notron_export dist
```

### 3. 一键健康检查

```bash
./integrated_health_workflow.sh
```

## 三种使用模式

### 模式1: 健康检查模式（推荐日常使用）

```bash
# 运行完整健康检查工作流
./integrated_health_workflow.sh

# 或单独运行健康检查
python3 uid9622_healthcheck_optimized.py --dist ./dist --meta ./instance_meta.json
```

### 模式2: 自动更新模式（无人值守）

```bash
# 步骤1: 生成控制台事件
python3 uid9622_control_plane.py --meta ./instance_meta.json --out ./events

# 步骤2: 执行更新器（基于事件）
python3 uid9622_updater.py --events ./events --meta ./instance_meta.json
```

### 模式3: 三源备份模式（企业级）

```bash
# 使用本地、Git、HTTP三源备份的完整更新器
python3 uid9622_updater_v3_folder.py \
  --meta ./instance_meta.json \
  --apply_dir ./mother_applied \
  --events_out ./events \
  --local_pkg_dir /path/to/local/mother_folder \
  --git_repo https://github.com/xxx/uid9622-mother-package.git \
  --git_branch main \
  --git_pkg_relpath mother_package
```

## 定时任务设置

### macOS/Linux (推荐)

```bash
# 编辑 crontab
crontab -e

# 添加定时任务（每小时执行）
0 * * * * cd /Users/zuimeidedeyihan/LuckyCommandCenter && /usr/bin/python3 uid9622_control_plane.py --meta ./instance_meta.json --out ./events >> ./events/cron.log 2>&1
5 * * * * cd /Users/zuimeidedeyihan/LuckyCommandCenter && /usr/bin/python3 uid9622_updater.py --events ./events --meta ./instance_meta.json >> ./events/updater.log 2>&1
10 * * * * cd /Users/zuimeidedeyihan/LuckyCommandCenter && /usr/bin/python3 uid9622_backup_push.py --apply_dir ./mother_applied --snapshots ./snapshots --git_repo_dir ./backup_repo --git_remote origin --git_branch main >> ./events/backup.log 2>&1
```

### Windows 任务计划程序

1. 打开"任务计划程序"
2. 创建三个基本任务：
   - UID9622 Control Plane Hourly
   - UID9622 Updater Hourly  
   - UID9622 Backup Hourly

## 输出说明

### 健康检查输出

**成功示例**：
```
OK: HEALTHCHECK PASSED
Summary: 15/15 files passed
Instance age: 12.5 hours
Health report saved to: ./dist/health_report.json
```

**失败示例**：
```
FAIL: filename.md
  - PLACEHOLDER_REMAINS
  - ZERO_WIDTH

FAIL: HEALTHCHECK FAILED
```

### 控制台事件输出

```json
{
  "timestamp": "2025-12-24T08:00:00+08:00",
  "instance_id": "instance-001",
  "mother_version": "PUBLIC-LANDING-CONSTITUTION-V1.1",
  "status": "WARN",
  "checks": {
    "age_hours": 27,
    "window_hours": 24,
    "auto_update": true
  },
  "action": "FORCE_UPDATE",
  "deadline_hours": 0
}
```

## 故障处理

### 常见问题及解决方案

1. **占位符残留问题**
   ```bash
   # 重新净化
   python3 uid9622_notion_sanitize.py --in ./notion_export --out ./dist
   python3 uid9622_healthcheck_optimized.py --dist ./dist
   ```

2. **实例超时问题**
   ```bash
   # 检查实例元数据
   cat instance_meta.json
   
   # 手动更新同步时间
   python3 -c "import json; meta=json.load(open('instance_meta.json')); meta['last_sync_time']='2025-12-24T08:00:00+08:00'; json.dump(meta,open('instance_meta.json','w'),indent=2)"
   ```

3. **更新失败问题**
   ```bash
   # 检查事件日志
   cat ./events/event_*.json | tail -1
   
   # 手动强制更新
   python3 uid9622_updater_v3_folder.py --meta ./instance_meta.json --apply_dir ./mother_applied --events_out ./events --local_pkg_dir /your/local/path
   ```

## 监控和维护

### 监控命令

```bash
# 查看最新事件
ls -la ./events/event_*.json | tail -5

# 实时查看日志
tail -f ./events/cron.log

# 检查健康状态
python3 uid9622_healthcheck_optimized.py --dist ./dist --meta ./instance_meta.json
```

### 备份策略

1. **本地快照**: 自动保存在 `./snapshots/` 目录
2. **Git 推送**: 自动提交到指定的备份仓库
3. **事件记录**: 所有操作都有 JSON 事件记录

## 性能优化

### 建议配置

1. ** cron 频率**: 生产环境建议每小时或每4小时执行一次
2. **资源使用**: 脚本轻量级，内存占用 <50MB
3. **网络依赖**: 本地优先模式，网络故障不影响核心功能

### 扩展配置

```bash
# 启用 HTTP 备份源
--http_hook_enabled true

# 设置自定义母本版本
--mother_version "CUSTOM-V1.2"

# 设置自定义事件输出目录
--events_out ./custom_events
```

## 安全考虑

1. **文件权限**: 确保脚本文件有适当权限
2. **网络传输**: Git/HTTP 建议使用 HTTPS + SSH 密钥
3. **数据验证**: 更新器会自动验证文件完整性
4. **回滚机制**: 所有更新都支持自动回滚

---

**版本**: UID9622-SYSTEM-V3.6  
**更新时间**: 2025-12-24  
**支持等级**: P0++ 企业级标准